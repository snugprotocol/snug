# Next tasks

- Budget editing UI (budgets table ships; the editor is minimal) and per-category
  budget suggestions from the agent grounded in trailing medians.
- Net-worth chart annotations: mark the day a plan was saved; show actual-vs-plan
  drift once a scenario is a month old.
- Investment holdings (SimpleFIN carries them for some providers) as a separate,
  clearly-labeled surface.
- Multi-currency display (per-currency subtotals ship; conversion does not).
- Shared connection custody (one SimpleFIN claim serving several apps) — its own ADR;
  per-app custody is deliberate today.
- CSV import for banks SimpleFIN cannot reach.
