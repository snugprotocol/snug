# Next tasks / ideas (not yet built)

- Consider adding stop-limit order type if users ask.
- Consider a portfolio allocation pie/donut chart (Chart.js is in the pinned CDN list).
- Consider surfacing Coinbase order status polling (GET /orders/historical) to reconcile
  `orders_log.status` with what actually filled.
- Consider a "cancel open order" action once open-order listing is added.

