# Done tasks — index

One line per finished task, chronological (append new lines at the bottom on merge). Full text of any entry lives in git history: `git log --all --oneline -- "docs/tasks/**/<slug>*"` then `git show <sha>:<path>`. Policy: [ADR-0027](../../decisions/0027-docs-memory-distilled-not-accumulated.md).

## Tasks

- **TASK-20260731-bootstrap** — Scaffold snug + spec repos with agentic process · no PR (direct-to-main bootstrap exception, `e7fc174`) · ADR-0001, 0002, 0003 · Both repos scaffolded, strategy embedded in the private strategy tree, pushed to private snugprotocol org.
- **TASK-20260731-build-hub** — Snug v1 hub umbrella — all packages, prompt architecture · no PR (pre-PR merge commits, `d5bf830`) · ADR-0004, 0005 · Six children specced, reviewed, merged same day; root 19/19 green, smoke PASS; spec push/eval harness deferred.
- **TASK-20260731-protocol-core** — Envelope schemas, constants, JSON Schema export (child 1) · no PR (merge `f093dc1`) · v0.1 frames/R1–R6/reply-parser/C1 helpers; review blocker (exported schemas contradicted R2) fixed; 74 tests.
- **TASK-20260731-knowledge-store** — Central prompt store, loaders, lint, skill-creator (child 2) · no PR (merge `e30861e`) · ADR-0004 · Store + typed loaders + goldens shipped; 3 review blockers fixed (KB db-field nesting would break persistence); 55 tests.
- **TASK-20260731-runner-sandbox** — Sandboxed iframe host + bridge (child 3) · no PR (merge `58d3efc`) · ADR-0006 · allow-scripts-only host, DOM-parsed CSP, navigation cutoff, browser-CSP suite template; 90 tests, C1/C2 review-verified.
- **TASK-20260731-sdk-db** — In-app hooks + per-app sql.js database (child 4) · no PR (merge `d5bb210`) · Dual-form hooks with KB≡SDK sync test; OPFS/IDB fallback, .sqlite round-trip; fixed usePersistedState key-change clobber; 70 tests.
- **TASK-20260731-server-adapters** — Reference server + provider adapters (child 5) · no PR (merge `298aef5`) · Fastify /invoke SSE + anthropic/openai/mock adapters; blocker fixed: C1 scan missed `responseSchema`; 87 tests, smoke PASS.
- **TASK-20260731-playground-hub** — Playground hub, examples, C2 real-browser gate (child 6) · no PR (merge `5b18db3`) · ADR-0005 · Vite SPA hub/builder/run + Inspector + 3 starters; Playwright C2 gate green; StrictMode blank-iframe fix.
- **TASK-20260803-portable-hub** — Portable user-owned hub: single user DB, serverless, sync, SSO · PR #1 · ADR-0007, ADR-0008, ADR-0009 · Six-child umbrella merged green; review caught dead OPFS sync sidecar (false divergence every login) plus three OPFS data-loss bugs.
- **TASK-20260803-userdb-core** — Single-user-DB core: schema constants, UserDb service, driver face · PR #1 (umbrella) · Protocol DDL constants; UserDb over one sql.js handle — versioning, corrupt-quarantine, secrets-strip export; composes existing driver guardrails.
- **TASK-20260803-serverless-run** — Serverless run, provider+model picker, local LLM · PR #1 (umbrella) · byok/local/subscription modes, Ollama endpoint, state moved into user DB; old IndexedDB/localStorage stores abandoned pre-launch.
- **TASK-20260803-versions-chat** — App versioning UX + persistent chat, client-authoritative · PR #1 (umbrella) · Artifact sink pins app ids; chat persists in user DB; subscription writes client-authoritative; artifact cards not persisted (limitation).
- **TASK-20260803-sync-origins** — SyncProvider, sync loop, Dropbox adapter, export/import UI · PR #1 (umbrella) · Hash-gated pushes, pull-merge preserving secrets, divergence surfaced; new device + non-empty origin requires explicit applyRemote, never auto-pull.
- **TASK-20260803-hub-sso** — Google OIDC, per-user DB provisioning, /userdb, static hosting · PR #1 (umbrella) · Fail-closed CORS/CSRF posture, CAS-revision /userdb endpoints; first login provisions the user row only — never clobbers local.
- **TASK-20260803-spec-v02** — Spec v0.2 draft: Portable User Database Format · PR #1 (umbrella) · Draft prose staged from userdb-schema.ts plus docs/changelog; publication withheld, executed later by AL-13 (PR #8).
- **TASK-20260803-living-apps** — LLM-designed app schemas, app chat, factory pins, wiki · PR #2 · ADR-0010 · Four-child umbrella; adversarial review caught sqlite_sequence id-reuse blocker; schema/doc tools direct-mode only, server twins queued.
- **TASK-20260803-userdb-v2** — Schema v2: native app tables, registry, pins, docs · PR #2 (umbrella) · Verbatim-DDL registry materializer, rename-based write-back, C2 injection negatives; v1→v2 migration structural only — data abandoned (owner-approved).
- **TASK-20260803-schema-doc-tools** — schema_apply + app_doc_write tools, compounding prompts · PR #2 (umbrella) · Store-sourced tool prompts, sink pre-pin, docs-on-every-change doctrine; direct-mode only — subscription twins deferred to next-steps.
- **TASK-20260803-hub-retrofit** — Marketplace dedup, SSO surfacing, design pass · PR #2 (umbrella) · Find-or-open installs end duplicate installs; identity chip on every page; logout now rebuilds sync loop; return path hardened.
- **TASK-20260803-hub-ops** — Long-run builds, observability, app delete, LLM-free apps · no PR — direct merge to main (`f37feff`) · ADR-0011 · Iteration ceiling 6→48 plus 30-min lifetimes fixed silent build deaths; cascade delete; step timeline later retired (PR #4).
- **TASK-20260803-app-context-chat** — App-attached chat, durable pins, factory reset · PR #2 (living-apps umbrella) · Context assembler with caps, durable thread pin (F10), bootstrap = v1-artifact turn (F9), docs panel, factory reset.
- **TASK-20260804-hub-polish** — Identity & brand, watch-it-think rail, build-fork fixes · PR #3 · Five-reviewer adversarial round found six defects behind a green suite (false-green e2e, vacuous AC18 guard); 826 tests.
- **TASK-20260804-observability-caching** — Live LLM observability, prompt caching, brand polish · PR #4 · ADR-0012 · Caching is per-turn, not per-adapter — review caught both inverted scope bugs; inspector memory bound repaired; 906 tests.
- **TASK-20260804-logo-variants** — Logo exploration; owner picked Variant C "The Ember Niche" · PR #3 (shipped via hub-polish) · Design-scratch record; the mark lives in `Logo.tsx`, favicon, and the desktop icon generator (closed 2026-08-15).
- **TASK-20260805-doctrines-devex** — Doctrine ADRs, code-map regenerator, two queued fixes (AL-01) · PR #5 · ADR-0013, ADR-0014 · Zero-backend and local-first-credential doctrines recorded; import tombstone cache-coherence and supportsCaching exact-host fixes; spoof-proof count parser.
- **TASK-20260805-auth-core** — Dynamic Auth part 1: the pure core (AL-02) · PR #6 · New packages/auth — 5-kind schema, custody byte-probes, DI-pure OAuth, userdb v3; review fix: no-write-session migration now persists.
- **TASK-20260805-alpha-umbrella** — Alpha umbrella AL-01…AL-15 · PRs #5–#8, #19–#23, #28–#32 · ADR-0016 · 10/14 children merged; AL-06 delivered later as the Desktop Hub (PR #41). AL-10/11/12/15 HELD for owner manual tests, AL-09 parked — tail extracted to next-steps and closed 2026-08-15.
- **TASK-20260806-auth-wizard** — Dynamic Auth part 3: inferrer, wizard, render directive (AL-04) · PR #22 · Directive is doorbell, never authority; live sweep caught popup-blocker launch blocker; wizard later rebuilt by Dynamic Auth v2 (PR #34).
- **TASK-20260806-auth-kb** — Teach builder LLM auth declaration, connected-API design (AL-05) · PR #23 · Retrieval-tested KB teaching; review caught false security-transparency claim; live build emitted exact directive; post-merge findings folded via PR #24.
- **TASK-20260806-starters-pillars** — Five pillar starter apps (AL-08) · PR #7 · Five keyless starters shipped; found `<form onSubmit>` dead in the C2 sandbox — shipped habit-tracker was broken too, repaired.
- **TASK-20260806-spec-push** — Spec v0.1 + v0.2-draft pushed to snugprotocol/spec (AL-13) · PR #8 · Push executed, clone-verified byte-identical, auth content grep-excluded; stale `HEAD.lock` in spec repo blocked the first commit.
- **TASK-20260806-flip-prep** — Flip-public prep: community files, good-first-issues, staged runbook (AL-14) · PR #19 · Community set placed, real issues #9–#18 opened, breadcrumb sweep done; runbook lives only in untracked the private strategy tree — owner must copy.
- **TASK-20260806-webllm-spike** — WebLLM in-browser brain behind experimental flag (AL-07) · PR #20 · ADR-0015 · `?webllm=1` adapter with demo fallback; default model Llama-3.2-3B — 1B fragments files, Qwen3-1.7B fails the quality gate.
- **TASK-20260806-connected-fetch** — Envelope net capability — Dynamic Auth part 2 (AL-03) · PR #21 · `useConnectedFetch` + confirmed host-ceiling fetch, 1 MiB net-frame class; phantom zod dep made the executor unloadable (BLOCK-MERGE catch).
- **TASK-20260807-al05-review-fold** — Fold post-merge AL-05 adversarial-review findings · PR #24 · Five live KB-truthfulness findings (M62–M66) fixed test-first, two refuted; teaching must stay true about shipped machinery.
- **TASK-20260807-protocol-whitepaper** — The Snug Protocol whitepaper PDF · PR #26 · 22-page A4 PDF + 70-check conformance script, 7 defects fixed; spec-repo push deliberately deferred to an explicit ask.
- **TASK-20260807-connection-reachability** — Chat-less apps gain a path to become connected · PR #30, #32 · ADR-0016 · Install-act declaration resolver + `app_declared` strong-review ladder replaces AL-09's rejected design; MAJOR-1 UX residue queued to AL-10.
- **TASK-20260807-starters-auth-spectrum** — AL-09: five auth-spectrum starters · closed-by-harvest via rewrite P4 (PR #34) · Branch parked at `86a564c` (origin only) as a harvest source, never merges by owner decision Q8; real-provider verification still queued (closed 2026-08-15).
- **TASK-20260810-dynamic-auth-rewrite** — Dynamic Auth v2 umbrella: the v4 requirement/grant world · PR #33 (plan), #34 (P0–P5 chain) · ADR-0017 · Owner redirect Q1–Q9 executed; v3 surface + `snug_auth_specs` deleted (closed 2026-08-15).
- **TASK-20260810-p0-contracts** — v4 contracts, template lint, slot-keyed storage (P0) · PR #34 · spec-v0.3-auth carve-out staged; both review BLOCKERs closed (closed 2026-08-15).
- **TASK-20260810-p1-runtime** — Slot routing, amended gate order (P1) · PR #34 · AC5(b) closed by citation, re-verified in P5 after P3 deleted the cited file (closed 2026-08-15).
- **TASK-20260810-p2-pipeline** — Build-time requirement declaration at the post-turn seam (P2) · PR #34 · Unreachable-pipeline BLOCKER fixed; inferrer's production caller landed in P3 (closed 2026-08-15).
- **TASK-20260810-p3-wizard** — The grandma wizard (P3) · PR #34 · v3 surface deleted, OAuth popup REINSTATED on the v4 slot-keyed row, M9 discard-confirm built (closed 2026-08-15).
- **TASK-20260810-p4-starters** — Registry entries, six manifests, `llmProposalSchema` deleted (P4) · PR #34 · Provider data accuracy later re-verified live by PR #42 (Coinbase CDP, CoinGecko, OpenWeather) (closed 2026-08-15).
- **TASK-20260810-p5-security-close** — Threat-model delta, whole-surface review (P5) · PR #34 · Residual risks documented in ADR-0017 §Residual risk; URL-borne credential decision routed to AL-10/11 (closed 2026-08-15).
- **TASK-20260811-lean-runtime-data-chat** — Lean runtime turns + intent-routed app data chat · PR #35, #36 · ADR-0018, ADR-0019 · Persisted runtime contracts replace authoring-context turns; intent-routed SQL data chat; seven post-merge fresh-review findings fixed after.
- **TASK-20260812-registry-authoritative-auth** — Registry-authoritative auth shapes + connect-error surfacing · PR #37 · Registry kinds/fields authoritative, connect never silent; repro refuted the planned chain — wrong-KIND rows were the live defect; test scripts tsc-gated.
- **TASK-20260812-auth-kind-choice** — Multi-option auth-kind resolution — the user decides · PR #38, #39 · ADR-0020 · Host defaults and discloses provider auth options, user rebinds; Guard 2b variant-aware; fixed Coinbase wrongly triggering OAuth.
- **TASK-20260812-app-reply-parse-failure** — Never cap structured replies; stopReason end-to-end · PR #40 · AC2 delivery loss convicted upstream, client exonerated by `stream-fidelity.test.ts`; retry-policy + AC4 array decisions extracted to next-steps (closed 2026-08-15).
- **TASK-20260812-desktop-hub-scaffold** — Tauri 2 Desktop Hub (roadmap A6) · PR #41 · ADR-0021 · Native fetch, file userdb, loopback OAuth, in-shell C2 gate; Windows WebView2 later ruled structurally blocked (D8, gate RED).
- **TASK-20260812-desktop-auth-awareness** — Desktop-aware dynamic auth: platform truth, provider repairs · PR #42 · ADR-0022, ADR-0023 · Coinbase CDP repair, Spotify opener URL-scope fix, query credentials, Hue LAN connector; three owner hardware verifications left open.
- **TASK-20260813-ui-polish-inspector** — Wordmark, desktop icon, resizable inspector, connection UX · PR #43, #44 · All 11 ACs shipped; merged `--admin` over the documented D8-red Windows baseline; dock-icon eyeball check still queued.
- **TASK-20260814-hue-pairing-e2e** — Hue pairing verifies before claiming connected · PR #46 · ADR-0025 · Wizard now pulls and verifies the bridge key, killing the reopen ghost flow; owner hardware-confirmed on the real bridge.
- **TASK-20260814-hue-starter-real-connection** — Connection-relative addressing; Hue starter drives real bridge · PR #47, #48 · ADR-0026 · Apps address user-supplied hosts via connection-relative URLs (C3 spec-sync); starter drops all mocked rooms/controls.
- **TASK-20260815-docs-context-cleanup** — Distill the agentic memory (compaction policy) · PR #49 · ADR-0027 · docs 2.1MB→0.5MB: lessons −78% / next-steps −84% (all content re-verified), done/→INDEX, 11 stale active tasks closed, 9 ADR statuses amended, auto-memory 18→4; owner confirmed `llmProposalSchema` retained on purpose.

## Handoffs & working artifacts (all spent)

- **AL-04-plan-predraft** — Gate-2 plan pre-draft (v0) for AL-04 auth-wizard · PR #22 (the task it fed) · Predraft written during AL-03; superseded by the v1 plan in TASK-20260806-auth-wizard, which merged via PR #22.
- **AL-04-review-verdict.json** — machine review verdict for AL-04 · working artifact, spent with PR #22.
- **HANDOFF-alpha-umbrella** — Alpha umbrella resume point #1 (TASK-20260805-alpha-umbrella) · PRs #5–#8, #19–#20 status ledger · Written at credit cutoff; AL-03 mid-final-gate chasing an inspector lead; spent, superseded by handoff #2.
- **HANDOFF-alpha-umbrella-2** — Alpha umbrella resume point #2 · PR #21 (AL-03 merged) · AL-04 implemented, FIX_FIRST review pending (1 blocker + 3 majors); corrected #1's child-count off-by-one; superseded by #3.
- **HANDOFF-alpha-umbrella-3** — Alpha umbrella resume point #3 · PR #22 (AL-04 merged) · AL-04 merged after 15/15 verified fixes; AL-05 queued next; spent, superseded by #4.
- **HANDOFF-alpha-umbrella-4** — Alpha umbrella resume point #4 (marked SPENT in-file) · PR #23 (AL-05 merged) · AL-05 merged with marquee KB-taught directive proof; owner amendment "AL-09 then stop"; its AL-09 instruction later reversed (parked).
- **HANDOFF-alpha-umbrella-5** — Alpha umbrella resume point #5 · PR #28 (landed docs-only) · AL-09 parked after seam design failed 3-lens security review; connection-reachability task born; all tail HELD; superseded by #6.
- **HANDOFF-alpha-umbrella-6** — Connection-reachability resume point (handoff #6) · PR #29 (landed docs-only) · Plan v2 ratified: apps never propose connections at runtime (direction C); validate-rule fork landed; skeleton unbuilt; superseded by #7.
- **HANDOFF-alpha-umbrella-7** — Connection-reachability resume point (handoff #7) · PR #30 (eventual task merge) · Walking skeleton complete, T8 e2e proven end-to-end; breadth items unstarted; superseded by #8.
- **HANDOFF-alpha-umbrella-8** — Connection-reachability resume point (handoff #8, final) · PR #30, #31, #32 · ADR-0016 · Feature complete; implementation review's 2 MAJORs fixed (both-versions vouch, untested glob); task merged+closed 2026-08-08.
- **TASK-20260815-spotify-scopes-wizard-links** — Spotify starter 403 fix: registry-pinned scopes + provider-reason banner + pinned-URL console links · PR #51 · ADR-0028, ADR-0029 · Scope-less token was the 403 (owner hardware-verified fixed live); scope changes re-consent set-based with pre-promotion token invalidation (both OAuth kinds); banner quotes the provider's scrubbed reason; console links key on kind-matched registry-pinned bytes, any provenance; closes the Spotify desktop sign-in + real-PKCE verification items.
- **TASK-20260815-coinbase-ed25519** — Coinbase connects with portal-default keys: `cdp_jwt` moves ES256→EdDSA (Ed25519) · PR #53 · ADR-0030 (supersedes ADR-0022 §2) · The registry steered users to the legacy EC key type the portal no longer defaults to, while `es256-key.ts` rejected Ed25519 — the portal default — by design; `ed25519-key.ts` accepts every portal secret shape (armor-first, canonicalized to the seed, total runtime rule), secret field renamed `ed25519_private_key` as the drift-invalidation lever with pre-promotion orphan deletion (posture inversion), probe chain made total (AC5) and "connected" earned by a passing probe (AC6); owner AC8 hardware test (Ed25519 re-credential → portfolio) tracked in next-steps.
- **TASK-20260815-starter-portfolio-revamp** — Umbrella: dynamic provider-API chat + inline cards + starter shelf rebuild · PRs #55-#58 · ADR-0031 · Owner interview reset the read/write posture (crypto read-only rule removed; sandbox+bridge+executor gates ARE the wall); three sequenced children delivered A→B→C as a stacked PR chain; authoring-provenance requirement (mid-task owner ask) shipped gate-enforced.
- **TASK-20260815-provider-chat-lane** — Child A: provider chat lane — LLM-composed API requests through the UNCHANGED ten-gate executor · PR #56 · ADR-0031 §2, spec-changelog INTERNAL-DRAFT · provider_read/provider_write + compile-checked laneForIntent map (silent answer-lane default dead); connection FACTS only in context (public hosts taught, LAN symbolic-only, dotted-decimal RFC-1918 render scrub); confirm FIFO queue, app-shell dialog mount, reference-identity abort-deny (Gate-5 MAJOR); shared session-remember scope pinned (accepted F5); threat delta authored; plan+implementation each fresh-context reviewed with all findings resolved.
- **TASK-20260815-inline-cards** — Child B: inline choice cards + provider write-confirm as a chat card · PR #57 · ADR-0031 §3 · present_card tool (strict bounded schema, one per turn), picks persist + return as plain USER messages (zero new authority); chat-origin confirms render inline via WeakMap origin tagging with modal fallback when no surface is mounted (Gate-5 MAJOR); busy-gated picks + click-time row id (Gate-5 MAJOR); bidi sanitation + "the agent is asking:" provenance line; recorded gap: hook-level pick-to-next-turn e2e (browser-e2e sweep).
- **TASK-20260815-starter-apps-rebuild** — Child C: the gold-standard connected shelf (10 apps) · PR #58 · 9 starters retired; trade-copilot ported byte-verbatim from the owner's DB (independently re-verified) + TWAP smart-order surface (confirmed governed POSTs, deterministic idempotency, no-double-fire probed); Rewind/Moodboard/Should I?/Standup agent-built with honest scope adaptations; AC9 authoring-provenance gate in the validate suite (mutation-checked); real-browser design pass (20 shots, durable at ~/Snug/design-pass-20260815-starter-shelf/) caught the shipped DEFERRABLE reserved-word bug; playground+auth cross-package repoints with OBSOLETE/MIGRATED classifications; owner hardware passes queued in next-steps.
- **TASK-20260816-whatsapp-twin** — Linked-device connections + WhatsApp Twin · PR #60 · ADR-0032/0033, spec-changelog INTERNAL-DRAFT · first `linked_device` auth kind (appended, never inserted) + the derived sidecar route contract; the helper is a CAPABILITY not a host — the Gate-2 loopback draft was withdrawn as unsafe AND unstorable (host-granular ceiling, LDH-only host rule), replaced by a purpose-built Rust command over a unix socket (port squatting unrepresentable) with `/pair/*` wizard-only behind a SEPARATE command (a flag is a claim the caller makes); `StandingApprovalGate` wraps the session gate rather than widening it (a `(appId,host,method)` key cannot tell one thread from another) — built and enforced, arming surface deferred by owner, so Twin ships manual-reply only; AC12 new-reader scrub (pseudonymised JIDs/phone numbers before every LLM turn — third parties never consented) and AC13 export parser (bidi controls, multiline bodies, tombstones); macOS shell gate GREEN incl. `ipc-sidecar-fetch-refused` in a real WKWebView; **owner-verified end to end on hardware, which found EIGHT integration defects past a fully green tree** — every one a seam between independently-tested parts (`docs/solutions/2026-08-17-eight-seam-defects-in-one-feature.md`).
- **TASK-20260817-telepath** — Telepath: the WhatsApp client rebuild · PR #63 · ADR-0034/0035, spec-changelog INTERNAL-DRAFT · the Twin POC became a full client (iOS-style live inbox, images + avatars, unread badges, AI drafts in the user's measured voice/emoji, first-run→incremental analysis on a watermark, six local charts, auto-respond removed). Platform: three app-reachable sidecar routes (`/events` long-poll, media, picture) + **the host live pump** — push forwards INVALIDATIONS not content, because `hostEvent` frames ride the 256 KB class and `post()` drops oversized frames SILENTLY and carry no `instanceId` (Gate-2 blocker, reshaped the design); pump reads through the SAME governed executor, epoch-tokened against StrictMode. Privacy boundary went bidirectional (persisted label map, names restored locally, photos never sent). `installStarterDocs` seeds the app wiki + verbatim build prompt at install (ADR-0035). Closed the next-steps positive-twin item: `ipc-sidecar-fetch-dispatchable` — a negative check had passed while the command was UNREGISTERED. **The owner's hardware walk found three defect waves past a fully green tree** — an invisible rebuild (shelf tiles showed FOLDER names, also hiding four other renamed starters), missing contact names/group rosters/avatars plus **LIDs rendered as phone numbers** (a confident wrong number is worse than a blank), and a wedged session disguised as "still syncing" (the ambiguity WAS the defect; now `needsRelink`, carried on `/chats` because an empty list is where it bites). Merged on LOCAL verification (turbo 23/23, cargo 81, macOS gate green) — GitHub CI was billing-blocked, not failing. Owner action owed: re-pair the linked device. Recorded gap: an installed starter never receives a rebuild (`install_source` identity) — needs a version-upgrade path.
- **TASK-20260818-sidecar-shutdown** — Stop the WhatsApp helper when the shell exits · PR #65 · owner-reported "the app loses connection across hub restarts". **The credential layer was innocent** (approved row + minted token byte-identical to the helper's own copy + `status:connected` all persist correctly): the fault was that `sidecar_ctl('stop')` was implemented, tested and reachable with **zero callers** and no exit hook, so quitting orphaned the child, the next launch spawned a RIVAL against the same Baileys auth store, and two writers of one session directory half-register the link — which reads to a user as "relink me" and is why the earlier wedge kept returning. Confirmed live: two helpers 17 min apart, then a third. Adds `sidecar::shutdown` (infallible, idempotent, recovers a poisoned mutex rather than leaking a process) on `RunEvent::Exit` — chosen over the prevented-and-deadlined `CloseRequested` DB-flush handshake — plus a SOURCE-level pin, because no unit test can drive Tauri's real RunEvent and a hook nobody calls is the untested wire in its purest form. cargo 81→84, desktop 129, turbo 23/23; CI billing-blocked so evidence is local. Recorded gap: `linkVerifiedAt` is written and never read.
- **TASK-20260817-per-app-model-selector** — Per-app LLM model selection + run-header icon buttons · PR #67 · ADR-0036 · each app may PIN its own model and all four app-scoped lanes route there (app-frame turns, builder, app-attached chat, and the two inference call sites that have an app id) through ONE `resolveModelForApp(appId)`; **inheriting is an ABSENCE, not a copy**, so an un-pinned app FOLLOWS later changes to the Settings default and "pinned" stays distinguishable from "inherited"; resolution is PER SEND because RunView/useBuilderChat memoize their transport+agent. **The explorer pass's suggested `RuntimeContract` home was REFUSED** (D1): a host-side user preference there would be version-linked (reverting code would revert the model choice) and would push the change through `packages/protocol` — instead a namespaced `snug_settings['appModel:<appId>']` key means no schema bump, no migration, no spec-changelog (D2), at the price of an explicit `deleteApp` cascade named in the same change. Header cluster became icon buttons (model select → ⚯ connections → ⤓ export): **an emoji is not an icon** (🔌 ignored `currentColor` and read as disabled — caught only by a screenshot, both versions passed every test) and **a glyph is not an accessible name** (two e2e specs locate export by `name: 'export .sqlite'`, kept verbatim as `aria-label`). A pre-existing ordering test was MIGRATED, not deleted. Every guard mutation-checked incl. each routing lane independently; real-browser walk confirmed the pin on the WIRE (pinned app sent `claude-opus-5`, un-pinned sent the default) plus 44×44 icons at 1280px and 420px. Merged on LOCAL verification (turbo 23/23 `Cached: 0 cached`, seven forced root runs) — **GitHub CI was billing-blocked, not failing**: jobs never started, and every recent `main` run failed identically. Owner action owed: restore Actions billing. Recorded residual: the connection-requirement inferrer has no app id (build-time, before one is settled).
- **TASK-20260818-registered-flag** — The wedge detector was destroying working sessions · PR #68 · **my own bug from TASK-20260817, and a destructive one**: `isHalfLinkedStore` read `creds.registered !== true` as "pairing never finished", but in baileys@7.0.0-rc14 that flag is written by EXACTLY ONE site (`Socket/messages-recv.js:940`, the phone-code flow) — the QR flow's `configureSuccessfulPairing` writes `me`/`account`/`signalIdentities`/`platform` and leaves it at its `false` default. Every session this helper creates is QR-paired, so the detector fired on every HEALTHY session; the app said "re-link"; re-linking ran `shouldResetAuthStore`, which DELETED the working session; repeat. The owner went round the loop twice. Now detects the MATERIAL a completed pairing leaves (`account` + non-empty `signalIdentities` beside a saved `me`) — flow-agnostic, and fixtured from the owner's REAL creds.json (redacted) so the regression cannot return silently. sidecar 100→102, turbo 23/23; CI billing-blocked so evidence is local. **Process failures recorded rather than buried**: no task file existed while the work was done (Gate 1 skipped under urgency — written retroactively at Gate 6), and a blanket `git add -A` on a branch cut from a dirty `main` swept the owner's in-flight `TASK-20260817-per-app-model-selector` (31 files, ADR-0036, 36 tests) into the squash — verified intact and complete on `main`, attribution unrewritable without force-pushing; see next-steps.
- **TASK-20260818-ledger-starter** — Ledger: the personal-finance flagship + SimpleFIN token-claim connections + the open-url capability · ADR-0038 · **High tier, full six-gate walk with a fresh-context plan review that returned 3 blockers pre-code** (token-claim mis-homed in protocol → registry union, zero protocol bytes; a two-host ceiling that would have broken symbolic addressing AND the declared probe; the `/simplefin` base path made a checked invariant). Phase A: third `WellKnownPairing` variant `token-claim` (paste one-time token → wizard decodes → frozen-ceiling gates → claim POST → access-URL parse → ADR-0025 verify → write-together with `claimVerifiedAt`); `performTokenClaim` = the third NAMED network seat (allowlist widened deliberately); wizard paste screen + family refusal + no-re-claim gate. Phase B: `examples/ledger` — sample household with PLANTED leaks (radar-provable ≥3 flags), time machine with projection branches, five agent lanes, 15 extracted-core tests, full authoring provenance with both owner prompts verbatim. Phase C: open-url capability (internal-draft frames + published host-ready flag + value-blind runner seam with NAMED refusal + confirm dialog with punycode host and synchronous noopener open — popup escape proven on a REAL tab via its own scoped Playwright project) + cancel playbooks + feed-verified cancellations. **The owner's first real walk found two defects past 23/23-green-twice**: the pinned host was a 302 ALIAS (`bridge.` → `beta-bridge.simplefin.org` — the probes had hit beta all along; fix included host drift joining the migration gate, which was fields/seats/scopes-BLIND, and Ledger going connection-relative because installed starters never receive rebuilds) and the executor's 15 s wall clock killed the first 13-month pull as transport noise ("Request canceled" — now 60 s and self-naming). Lessons: pin the host you PROBED; a bound the code arms must name itself. Residuals in next-steps: owner's full real-data walk, shared connection custody, starters-connect pre-existing reds.
- **TASK-20260818-sidecar-orphan-reap** — The WhatsApp helper is now a singleton: reap orphans, die with the shell · PR #73 · ADR-0037 addendum · **The fix from TASK-20260818-sidecar-shutdown covered only half the exits.** `shutdown()` reaps the helper on a CLEAN quit; a crash, `kill -9`, or a `tauri dev` rebuild runs no hook, so the helper outlived its parent and the next launch's autostart spawned a rival against the same Baileys auth store — and WhatsApp allows ONE live connection per linked device, so the two replaced each other's stream in a permanent `stream:error … conflict type=replaced` loop from launch. Owner-reported with the evidence already in the log: two pids, 60490 at ppid 1 (orphaned 18:23) against the supervised 50057. **Both ends of the lifecycle now answer.** The helper watches its own parent (`parent-watch.ts`) and on reparenting exits through the SAME clean shutdown SIGTERM uses, so the ADR-0037 §1 final flush still runs — keyed on a CHANGED ppid, not `ppid === 1`, because a Linux subreaper adopts orphans to some other pid. The shell records the pid beside the socket AFTER the survival check and reaps a stale one BEFORE spawning; **order is the fix** (reaping after leaves both live long enough to ping-pong), and that half is what retires orphans from OLDER builds, which have no watch of their own. `stale_helper_verdict` is pure and is where the danger concentrates: **pids are recycled**, so a Kill requires the live process's command line to name THIS shell's helper entry — killing by number alone would take out the user's editor, worse than the bug it repairs; every reap path clears the pidfile, since a stale claim is what makes a recycled pid dangerous next time. Tests first: `parent-watch` (6) + cargo orphan-reap (8, incl. a live bystander SURVIVING the real reap and a source-order pin that the reap precedes the spawn); cargo 97 / sidecar 152 / desktop 129, clippy clean, `sidecarContract` untouched. Verified on REAL processes both directions — an orphaned helper self-exited ~1 s after `kill -9` of its parent and removed its socket; a look-alike was reaped (`Terminated: 15`) while a planted-pidfile bystander lived. **CI was billing-blocked again** (jobs never started — 2 s, zero steps, annotation names failed account payments; every recent `main` run fails identically), so the evidence is local; owner action still owed: restore Actions billing. Deploy note carried from lessons 2026-08-18: the helper was rebuilt and reinstalled to `~/Snug/helpers/`, but a running process keeps the code it loaded at spawn — the fix goes live on the next desktop restart. Windows leaves `command_line_of` answering `None` (so it never kills), consistent with ADR-0021 D8.
- **TASK-20260819-desktop-icon-squircle** — Two-master icon pipeline: mac squircle + windows plate, dark niche · PR #75 · The shipped dock icon was wrong three ways and each had a distinct root cause: opaque **white** canvas corners (`omitBackground: false` baked the Chromium page background into the Playwright screenshot), a **white** arched niche (a fill-WINDING bug — the `NICHE` backing path's inner subpath knocked itself out under nonzero, so the page showed through), and zero transparent margin (the deliberate 2026-08-13 full-bleed instruction), so macOS drew an oversized square with white chips instead of a squircle. Key finding: **`tauri icon` only RESIZES its source** — it adds neither margin nor squircle, so both must be baked into the master, and one invocation takes one source ⇒ two masters means two runs with selective keep. Mac master composes on Apple's grid (1024 canvas, artwork 100..924, ~22.4% continuous-corner radius, `omitBackground: true`); windows master stays full-bleed on an opaque `#171310` plate; the six shipped files are copied explicitly from the right run. The niche is now a solid single-subpath layer behind the ember path — winding-proof rather than winding-dependent. Owner decisions: Apple's ~22.4% radius over the brand mark's native ~28.6% (dock consistency wins; the brand shape stays canonical everywhere else); niche `#171310` (the dark-theme `--bg` the web knockout shows); full-bleed superseded for **mac-facing files only**. Tests first per platform (16 assertions, **9 red** against the shipped set — the white niche pinned as `rgba(255,255,255,255)` at the sampled centre, the outcome-not-proxy lesson from 2026-08-04 applied to the same file that earned it). desktop 128/128, macOS gate GREEN, `docs/code-map.md` corrected. **CI billing-blocked** (0 steps executed, annotation names failed account payments) so evidence is local — merged on owner's explicit call. Owner action still owed: the dock eyeball on a real built bundle, which closes the standing 2026-08-14 item "the icon has never been seen on a real dock/taskbar" — the pixel assertions cover AC1–AC5 but no test can cover how it reads beside neighbour apps.
- **TASK-20260819-starter-sample-data** — Sample mode + authoring-doc completion for the four connected starters · PR #76 · ADR-0038 pattern · Rewind, Trade Copilot, Moodboard and Telepath all opened onto empty states (skeletons, "not connected yet" panels, sidecar status screens), so nothing showed a prospective user what the app was FOR before connecting. Each now renders a deterministic, clearly-bannered sample experience. **All four shipped render-only** — no sample row is ever written to a real DB, so there is nothing to evict and the connected paths are untouched. This included a deliberate plan deviation: Trade Copilot's TWAP history was planned as DB-seeded with `sample=1` + wholesale eviction (mirroring Ledger), and shipped as constants instead — a strictly stronger guarantee for the no-impact-on-connected criterion, with zero schema/migration risk and no eviction machinery to get wrong. Telepath never writes sample rows by design (real JID keyspace stays clean; sample content never enters the LLM/analysis path or the pseudonym label map, and its analysis is a canned constant rather than a live run). **The Gate-5 AI review (8 finders) caught four real defects the green suite did not**, all of the same family — *a sample surface shown in a state its author never pictured*: Moodboard rendered the sample home for ALL offline bridge phases and flashed it during `loading` (three predicates had drifted apart; unified on one); Telepath had no real-data guard, so a linked user's helper crash showed fictional chats beside real ones, and `sampleJid` was never reset, so a later helper blip could replace a REAL open thread with the sample one; Trade Copilot's `isSample` misfired for previously-connected installs during outages (now a durable `connected_once` flag in an additive `app_meta` table, not a fetch-error inference). The fourth was in the tests themselves — the banner assertion was a vacuous whole-file `/sample/i` and the render-only scan missed components outside the markers; now proximity-checked per banner plus a line-level scan. Red first: `sample-mode.test.mjs`, 20 red. Final: examples 261/261, playground 1244/1244 (121 files, tsc-gated), starters e2e 5/5. New authoring docs (spotify + hue lessons/next-tasks, whatsapp next-tasks) ingest through the ADR-0035 glob with no code change. **CI billing-blocked** so evidence is local — merged on owner's explicit call. Accepted residuals: already-installed starters never receive rebuilds, so installed copies (including the owner's linked Telepath) report `html_mismatch` against the new factory bytes and that surface still has no renderer — sample mode reaches NEW installs only. Owner action owed: walk the four surfaces on desktop (they are desktop-only/env-gated, so no test covers the visual).
- **TASK-20260819-connection-failure-ux** — Honest Spotify scopes · the failure diagnosis moves into the wizard · a readable Ledger playbook · ADR-0028 amendment · **High tier, full six-gate walk; the fresh-context plan review returned 2 blockers pre-code and both were real.** Three owner-reported defects on one seam — what the user is told when a connection is, or merely looks, unhealthy. **(1)** Rewind raised the red auth-repair alarm on every launch of a *working* Spotify connection: it makes one `GET /v1/me/player/recently-played` per session, the pin omitted `user-read-recently-played`, and the host observer fires on any delivered credentialed 403 with no way to know a refusal was expected. Pinned the scope (ADR-0028 §4 amended, not edited — it is accepted; README's "deliberately omits" paragraph corrected), which also switches on a COMPLETE recently-played lane the app already shipped unreachable. **(2)** The maroon full-bleed banner moved off the app page into the wizard as a **derived attention gate**, with a quiet ember chip left in the run header. The plan had proposed a new `ConnectionWizardStep` member; the review showed `ConnectionWizardSheet.tsx:1842` rejects exactly that in a "WHY NOT NEW STEPS" block, and traced three breakages it would have caused (`showDiff` is step-keyed and would go false; `nextStep` early-returns `'done'` for LAN rows ABOVE its switch, so a LAN row with a live 403 would skip review; three unproven-row catch-alls key on `step !== 'review'`). Owner decisions: the staged diff **outranks** the gate (the diff is the cure — and this task creates that exact collision for every existing Spotify user, so AC12 pins it), and the failure is **copied onto the session** before the store is cleared (clearing alone would leave Step 0 with nothing to read). **This REVERSES TASK-20260813 AC10** — recorded as a decision changing, with `connectionSurfaces.test.tsx` rewritten to assert the new placement rather than silenced. **(3)** Ledger's cancellation playbook was unreadable: `.planbox` was the FOURTH flex child of `.leak`, so the merchant column (`flex:1`, the only shrinkable child) collapsed and the step text overprinted it. The card now stacks, steps are a number/text grid so wrapped lines align under words. **The most valuable finding came from the owner re-reporting (3) as still broken after it was fixed and screenshotted**: installing a starter COPIES `app.html` into the user library (`RunView.tsx:410`) and the run path reads that snapshot forever (`:457`) — the fix could not reach an installed copy, and neither exit works (`delete` cascades irreversibly to the app's data/chat; "reset to factory" restores the pinned v1 of the INSTALL). The owner's own screenshot proved it before any code was read: it showed a `dashed` border, a token the new CSS contains only inside a comment. This generalises the residual TASK-20260819-starter-sample-data logged; queued in next-steps with the mechanism that already exists (`saveAppVersion`). **Also classified the 2026-08-14 playground vitest flake**, whose failing test the note said was never captured: it is FILE PARALLELISM — 1254/1254 serial, versus a different failing set each parallel run (14/15/31 observed, all timeouts in React-rendering files) on clean `main` too. Tests first, each confirmed red for the right reason; AC2's fixture deliberately frozen at the OLD seven-scope consent because the existing scope-drift test would have passed unchanged against the unfixed registry. Final: auth 880/880, playground 1254/1254 (serial), examples 270/270, tsc clean, both restructured starters re-parsed for JSX balance. **Accepted residual, recorded in the ADR amendment**: token invalidation happens on APPROVAL, so a user who dismisses the re-approval diff keeps the old seven-scope token and the chip returns on every launch for them; the app cannot self-gate (C1 — it never sees the token, scopes are host-side).
- **TASK-20260819-gmail-starter** — Inbox Copilot: the Gmail inbox-manager starter · PR #80 · ADR-0039 · **High tier** (the registry pin), full six-gate walk; the fresh-context plan review returned 3 blockers pre-code and all three were real. The 13th shelf app and 8th connection declarer: four lanes over a Gmail account — Pulse (twelve-week trend, top senders, category donut, all hand-drawn SVG), Needs You (senders with 3+ received and zero replies, **excluding transactional senders and anything starred** — that exclusion is the feature, since a naive rule flags exactly the mail a person most needs), Do This (assistant cards that run in-page behind one confirm), Mass Cleanup (multi-select → one governed batch). **Gmail was resolvable but UNCONNECTABLE** — right endpoints, hosts, PKCE and redirect posture, yet no scopes (a token that reads nothing) and no `fields` (the wizard renders `fields ?? []`, so a credential screen with zero inputs); `next-steps` item (7) had recorded the dead-end and nothing failed, because nothing tested that an entry is *sufficient* rather than merely present. Pinned `gmail.modify` + `gmail.settings.basic` + `gmail.send` and **deliberately withheld `https://mail.google.com/`**, the only scope permitting permanent deletion — so "this app never deletes your mail" is a property of the minted token rather than a promise in the code (D3), which is ADR-0028 doctrine extended: a pin can be load-bearing for what an app CANNOT do. The `client_secret` pin came from a **live probe, not the docs** (Google's native-app table calls it "Optional"; the token endpoint refuses a Desktop-client exchange without it) — copying Spotify's honest "PKCE needs no secret" line would have shipped a wizard that completes consent and then dies at the exchange. Unsubscribe is channel-split by what the sandbox can honestly do: `mailto:` → one confirmed `gmail.send`, `https` → the open-url bridge behind an https-only/no-userinfo gate; RFC 8058 POST is structurally excluded by the frozen ceiling. Desktop-only (a Desktop-app OAuth client registers loopback redirects only). **The browser pass found what no suite could**: the readiness check read `app.ready` where the hook returns `isReady` — a typo'd property is `undefined`, not an error, so the app sat on its loading skeleton forever with every gate green (the shape validator checks structure; the extracted-core suite evaluates pure functions and never renders). Guard added and **proved non-vacuous** by reintroducing the bug: a lint deriving the hook's real property set from the SDK. Also fixed there: the trend chart baselines on the quietest week (a zero axis drew a flat line and hid the variation) and the sender bars reserve a slot for a replied-to correspondent, so the green legend describes a colour actually on screen. examples 254, auth 895, playground 1265, turbo 23/23; **CI billing-blocked** (jobs never started — the annotation names failed account payments, identical to #79) so evidence is local, merged on the owner's explicit call. Owner action owed: a live Gmail connect on desktop and one governed cleanup batch on real mail.
- **TASK-20260819-inbox-copilot-fixes** — The verified-connection refresh prompt + three Inbox Copilot defects · PR #80 · Owner testing found four things, one architectural. **(1)** A verified connection left stale sample data on screen: the wizard *proves* a connection works and then discards that knowledge (the probe outcome is local `useState` in `DoneScreen`), while `RunnerHost` lives only in `RunView`'s ref and the wizard is mounted as a SIBLING — so there was no path between them. Added `state/appHosts.ts` as that seam (the wizard should not hold a frame reference; the run view should not know wizards exist), with **token-scoped registration** against the StrictMode mount(A)/mount(B)/unmount(A) ordering, where a naive `delete(appId)` would evict the LIVE view and present as "the prompt does nothing, sometimes". The Done screen now offers "load my real data", **gated on VERIFIED not saved** (reusing `awaitingProbe` — TASK-20260815 AC6's "connected is earned, not declared"; OAuth kinds qualify on arrival since the minted token IS the round trip) and only when a frame is actually mounted. Reuses the **existing** `connection-event` host-event — the `event` field is an open bounded string and ADR-0026 explicitly reserved it for "a future live status event" — so **no protocol change and no spec-sync**; the payload is an INVALIDATION only, never data, because host-event frames carry no `instanceId` to verify a sender and an oversize frame is dropped silently (ADR-0034). **(2)** Unsubscribe emails bounced: the raw message had `To:` and `Subject:` but no `From:`, so Gmail accepted the send and the receiving MTA rejected it — a silent failure with a success message on screen. Now resolved from `users/me/profile` and REQUIRED (the builder returns null rather than producing a header-less message), with header values truncating at the first CRLF since a `List-Unsubscribe` header is attacker-controlled and a break would turn an unsubscribe into a Bcc. **(3)** Refresh was hard-wired to 90 days; now selectable (1 week / 90 days / 6 months / 1 year / everything) where "everything" omits the date clause rather than faking one, an unknown setting NARROWS to the default rather than widening to a full-mailbox scan, and the chart axis follows the window (twelve buckets beside a one-week pull would draw a collapse the data never showed). **(4)** The ask lane returned prose; it now renders verified rows — a table is only promised when every sender the agent names matches the app's own data, so a hallucinated address degrades the whole reply to prose instead of drawing authoritative-looking empty rows. **A fifth defect surfaced while verifying**: bridge errors are OBJECTS, so `result.error || 'fallback'` rendered the literal "[object Object]" at six call sites — replacing the one thing a user needs (what happened, and whether retrying helps) with noise, exactly when they are wondering whether their mail is safe; guarded by a source sweep, since a formatter unit test cannot catch a call site that never uses it. Deferred deliberately and recorded: the `useHostEvents` SDK hook (this is the THIRD hand-rolled consumer — the trigger its own next-steps entry named — but promoting it bumps the byte-locked block in all 13 starters) and adoption in the other five sample-mode starters.
- **TASK-20260819-inbox-copilot-persistence** — Inbox Copilot keeps its synced mail · PR #80 · Owner-reported: connect, sync, close the app, reopen — and it is showing the demo inbox again. **The app had NO persistence at all**: it never called `useAppDB`, so every synced message lived in React state and died with the frame, and the only thing stored was the window preference. That is wrong on Snug's own terms (ADR-0007: the user owns their data in one portable file) and it re-downloaded the whole mailbox on every launch against a quota. Now on Ledger's pattern (ADR-0038): `messages` + `sync_runs` with a `sample` provenance column, bootstrap ordered schema → load → **seed the demo ONLY if the file is empty** so a returning user's own mail appears before anything is fetched, and wholesale eviction of demo rows on the first real sync so a mailbox is never half sample and half real. Cleanup deletes rows locally as well as at Gmail — without it, trashing 400 emails looked right until the next launch brought them all back out of the file. **A zero-result sync is never committed**: a throttled or refused fetch returning nothing would otherwise delete a real mailbox out of the user's file and revert them to sample data permanently, turning a transient failure into data loss. **Two derivations had to move from session-phase to data, and the second was only visible in a browser**: `isSample` (the reported flip) and `connected` — after real rows loaded correctly, a returning user was still told "Sample inbox — connect Gmail", and the header count came from a per-session `syncState.fetched` so it read "0 emails" over a full mailbox. Verified end to end against a stub host with a REAL sql.js database surviving an iframe reload: seed 585 sample rows → sync real ones (585 evicted, run recorded) → relaunch shows "4 emails from the last 90 days" with no banner and "Last read … · last 90 days" → then a FAILING sync, which leaves all four rows and the real view intact. **Process failure recorded rather than buried**: one scripted multi-anchor edit asserted on its second anchor and wrote NOTHING while printing nothing to say so, so I tested a binary without the change in it and spent several probes diagnosing a stub-host bug that did not exist — apply such edits one anchor at a time, or make the failure loud. Owner action owed: connect real Gmail, sync, fully close and reopen, confirm the mailbox is still there.
- **TASK-20260820-threat-model-v1** — The consolidated threat model + three credential-leak fixes · PR #82 · ADR-0021 D8 addendum · **High tier** (the deliverable is a public security claim about C1/C2), full six-gate walk. `SECURITY.md:47` promised a `docs/threat-model.md` that did not exist — the flip-public gate's box, and the first thing a hostile reader opens. **The analysis existed (1,129 lines across eight deltas) and consolidating it was NOT the valuable part: auditing it was.** Five fresh-context adversarial agents, one per surface, checked ~50 claims against the code with instructions to break them. The deltas held up unusually well — most claims are enforced with negatives asserting *zero fetches* rather than a failed result, and every spot-checked file:line citation was accurate — but **three credential-leak paths surfaced that eight deltas and their green suites had all missed**, each fixed test-first in its own commit before a line of the model was written. **(1)** The WhatsApp sidecar send returned 28 lines ABOVE the mutating-confirm gate, under a comment asserting the gate "has answered" — so every send reached the helper with credentials injected and no confirmation, voiding ADR-0033's standing-approval governance for the one connection class that ADR exists to govern. Green because every sidecar test hardcoded `confirm: () => true`, making a gate never consulted indistinguishable from one that granted; the harness now spies and asserts the `slot`/`body` a downstream policy decides on. **(2)** OAuth token-endpoint error bodies reached the iframe AND the LLM unscrubbed — 500 raw chars, on the path where the executor's own refresh POSTs `refresh_token` and `client_secret`, so a provider echoing submitted parameters (ordinary debugging behaviour) leaks both. Green because all fifteen `NET_AUTH_FAILED` assertions read `code` and none read `message`. The scrubber could not have caught it: injection THROWS before a candidate set exists, and the values are POST body params that were never injected headers. **(3)** My own fix for (2) was INCOMPLETE, caught at Gate 5 — an allowlist of error-envelope SHAPES treated as a value guard, when shape recognition picks which FIELD is forwarded and never vouches for its contents (`{"error_description":"bad token rt-…"}` sailed through, which also kills the narrower "only forward recognized fields" fix since that IS the field). Then a second layer: `JSON.parse` decodes `\u` escapes INSIDE the extractor, after the caller's scrub, reconstituting the secret character-for-character — **and the observer seat had the same hole**, reaching the wizard's attention gate. Fixed in the shared module rather than twice at call sites, since the decode is that function's own act. **The document** follows two rules: a promise with no named enforcement is not an invariant (every §5 row names an enforcement file and a regression test; anything that could not carry both moved to residuals rather than being softened), and §6 has equal standing with §5 — 164 lines against 60, because a threat model listing only wins reads as marketing. Residuals the deltas never disclosed, now stated: the desktop main window ships with `csp: null` so host-page XSS is immediate full-IPC (R-1 is worse on desktop than the browser it was written for); macOS-only is enforced by DOCUMENTATION not the build (`targets: "all"` still ships `icon.ico`) while its only regression detector is a CI leg the billing block made unreadable (R-5b); and **WhatsApp pseudonymisation is documented "at the turn's altitude" but implemented inside the sandboxed example app — an app-layer convention, not a boundary** (R-9, the residual a reviewer should weigh most heavily; any other app with an approved sidecar row can forward raw names and numbers to a model). **ADR-0021 D8 addendum**: the macOS-only decision existed only as task-file prose while five documents described D8 as OPEN and one said the trigger resolves to "Electron fallback"; recorded with the owner's scope — through alpha, beta AND 1.0, Windows reconsidered post-1.0 — and all five reconciled. **AC2's checker** hash-pins all eight deltas (a new or EDITED one fails until the model is re-read against it) and proves every cited enforcement/test path exists; mutation-proven three ways. **Its limit is stated against its own interest** after the Gate-5 review found three residuals dropped while it was green: a hash proves a delta has not MOVED, never that its residuals were CARRIED (now R-22..R-25, incl. SimpleFIN wrong-account binding and drift comparing COUNTS not ROWS). AC7 closed one claim that outran the code — the whitepaper's "a frame carrying `allow-same-origin` fails the build" was true only for `packages/runner/src`; `check-sandbox-guard` makes it true workspace-wide, reading the sandbox VALUE rather than banning `setAttribute` so the desktop C2 gate's own probe frames still pass. Both checkers wired into `pnpm test` AND CI — removing R-11's cadence residual for them rather than documenting it. One Gate-5 finding was REJECTED on evidence (a reviewer reported turbo passing 23/23 while auth was red; probing with a deliberately failing test showed it reporting `Failed: @snugprotocol/auth#test` and exiting 1 — it had sampled a mid-edit tree; its other five findings were all real). Code-map regeneration surfaced pre-existing drift far beyond this task (playground recorded at 1028 vs an actual 1265) and the auth cell was stale at 729 vs 915 because its format `(729, tsc-gated)` did not match the generator's `(N total)` marker — rewritten so the number is MAINTAINED rather than hand-copied. Final: root `pnpm test` green uncached — 23/23 turbo tasks + 130 threat-model checks + 4 sandbox-guard checks, auth 915. **CI billing-blocked** (annotation confirmed: jobs never started), merged on local evidence per the standing condition. Follow-ups queued in next-steps, highest-value first: build-enforced macOS-only, and moving pseudonymisation into the host pump.
- **TASK-20260820-desktop-bundle-targets-macos** — Restrict the desktop bundle to macOS targets, pinned by test · PR #84 · ADR-0021 D8 (dated update, no new ADR) · **Medium tier**, the *enforcement half of a decision already made*: D8 settled macOS-only through 1.0 on 2026-08-20, and its own addendum recorded that the decision was **"currently enforced by documentation alone"** — `tauri.conf.json` still carried `"targets": "all"` and shipped `icons/icon.ico`, so a Windows bundle stayed producible (threat-model R-5b). Now `bundle.targets` is `["app","dmg"]`, the `.ico` is deleted, and `bundleTargets.test.ts` (11 tests, modelled on `netTransportCapability.test.ts`) pins **three** surfaces rather than the obvious one: the config, the TREE (config and disk drift apart independently), and the icon GENERATOR — `generate-icons.mjs` clears and rewrites the icons directory, so a generator still emitting `icon.ico` silently restores what the config drops on whatever branch next touches the icons. **The scope claim is deliberately weaker than "the build refuses", and says so in three places.** Probing rather than assuming: `tauri build --bundles nsis` on a macOS host is ACCEPTED and proceeds into the build, so tauri does not reject a foreign target at config-parse time. The shipped config therefore *requests* macOS targets only — an explicit `--bundles` flag overrides it — and R-5b, the ADR update and architecture.md all state that distinction outright rather than letting "enforced by the build" imply a hard stop. Same reasoning kept a host-guard OUT of scope: a second half-mechanism would imply the hard stop neither provides. **Two defects in the plan were caught BY writing the tests first.** (1) The plan asserted the generator's SHIP list *equals* `bundle.icon`'s basenames; it does not and must not — `icon.png` is shipped and read by tauri as the implicit macOS master but is never named in config, so the test pins the exact relationship (config + that one asset), which also catches a sixth file appearing. (2) A first draft asserted "no `icon.ico`" over the WHOLE generator file and failed on the header comment written in the same commit — the comment whose entire job is telling a post-1.0 reader which line to restore; scoped to the `SHIP` block, both survive. Also fixed before it could vouch for nothing: the non-macOS-target assertions used `.not.toContain` against a STRING (`"all"`), a substring check that passed vacuously against exactly the config being removed. **The RED was verified by restoring the three files from `HEAD`** (7 failed / 4 passed) after a first attempt with `git stash` silently did not take and reported the already-fixed tree as green — the reassuring output a RED check exists to rule out; both that and the scoping rule are now lessons. Verified: desktop **139/139** tsc-gated · cargo 97 passed/1 ignored · check-threat-model 130/130 · check-sandbox-guard green · `tauri build` **exit 0, "Finished 2 bundles"** (`Snug.app` + `Snug_0.1.0_aarch64.dmg`, `icon.icns` alone in Resources, zero `.ico` anywhere in the bundle tree). `.github/workflows/ci.yml` deliberately untouched — ADR-0021 requires the red Windows leg to stay exactly as it is, and the new suite rides `pnpm --filter desktop test`, which already runs on both matrix legs. CI still billing-blocked; merged on local evidence per the standing condition. **Still open and NOT this task's to close**: the CI Windows leg is the only detector for an actual R-5 shell-behaviour regression, and remains unreadable until billing is fixed. Post-1.0 Windows is a one-line restore in each of config, generator and test — the win icon master was kept on purpose.
- **TASK-20260820-host-pseudonymisation** — Host-enforced third-party pseudonymisation backstop (R-9) · PR #86 · ADR-0040 · **High tier**, full six-gate walk with BOTH fresh-context reviews (plan + eight-angle diff), each earning its cost with a different defect class. Closes the residual threat-model v1 told reviewers to weigh most heavily: the scrub keeping other people's names/jids/numbers out of LLM payloads lived only in the sandboxed example app — an app-layer convention any other sidecar-connected app simply ignored. Now: **ingress harvest** at the one governed seat every sidecar read crosses (`sidecarAppFetch` → `state/sidecarIdentity.ts`; extraction IS the scrub — names/jids from `/chats` known fields only; memory-synchronous before the body returns so the first wire can't outrun it; ready-gated fire-and-forget persistence because awaiting a db boot at the seat hangs db-less reads), persisted under ONE `snug_settings` key (`sidecar-identity-keys.ts` — a namespaced key, NOT a new table, which would be a spec-normative format change; **no protocol/auth/runner/Rust changes, no spec-sync**); **egress scrub** guarded per send inside BOTH leaf transports (BYOK/local/webllm + `/invoke`) for the sidecar connection **FACT in any status**, plus sidecar-class provider-lane results in `renderProviderResult`. **The owner chose egress-backstop over full host takeover** on coverage-identity grounds (both redact only observed identities + primitives; takeover adds C3 churn, a reverse-map surface and a label store for zero extra coverage), and chose the revoke-wipe lifecycle: the directory is a third-party-PII asset, wiped in-transaction with the last approved sidecar connection (revoke + deleteApp; dirty-marking owned by the wipe), deliberately SURVIVING import. **The plan review (pre-implementation) found two blockers**: `responseSchema` as an in-band smuggling channel (walk widened to the whole envelope) and the ADR-0031 provider lane carrying raw sidecar bodies with no app-message wire (lane brought in scope). **The diff review (eight angles) found three confirmed defects in the first implementation**, each now pinned: (1) a hand-rolled `snug-connection://` regex missed `SNUG-CONNECTION://`/padded spellings the executor happily served — silent no-scrub; now the canonical `parseConnectionUrl` + the executor's own normalization + `isSidecarSlotFact`; (2) the dialable-digit rule rewrote ~35% of real UUIDs inside envelope ids, breaking the model's requestId echo — ids now verbatim, a disclosed ≤128-char channel; (3) an approved-only predicate left every imported `.snug` unscrubbed (import demotes rows to `declared` while the replayable app data rides along) — population widened to `appHasSidecarFact`, pump eligibility stays approved-only, and one review finding ("wipe on import") was consciously REJECTED because its sibling finding proved the import path needs the directory. Also folded: session-scoped memory reset at every store-swap/wipe seam (import/pull/restore/revoke/deleteApp — one user file's contacts can never re-persist into another), `responseSchema` keys/`required` protected from case-folded contact collisions ("Home" vs `home`) via case-sensitive matching there, a commutation-tested raw fallback that unmasks escape-hidden identities without ever corrupting benign `\uXXXX` data, envelope-appId fallback for the uninstalled-starter mode (a wire with no resolvable identity scrubs unconditionally), memoized matcher/directory reads + poll body short-circuit, and a jid-pattern parity pin against `app.html` (the phone pattern deliberately widens to consume a leading `(`). **Honest class, stated in R-9 rather than implied away**: anti-default and anti-naive, not anti-adversarial — obfuscation, numeric smuggling, the id channel, unseen in-body names, and the chat data lane's replay all disclosed as residuals; the app-message guard fails closed, the provider lane surfaces scrub failures as tool errors. 45 new tests (egress 27 / harvest 7 / provider-lane 4 / db wipe 7) incl. C1 negatives; playground 1303/1303, root 23/23 twice, whatsapp-analysis 34/34 (app untouched), threat-model checker 139/139. CI still billing-blocked (jobs failed in 2–3 s with zero steps run); merged on local evidence per the standing condition. The shipped app keeps its P-label scrub as defense in depth — a feature-lane rewrite of the app no longer removes the boundary.
- **TASK-20260820-local-ci-gate** — `gate:local` replaces the dormant CI as the merge gate · PR #88 · ADR-0041 · **High tier** (CI/release config, and the artifact becomes the *sole* merge gate — a false green here is undetectable by construction). GitHub Actions had been billing-blocked since ~2026-08-18; two findings shaped the fix — CI **never actually blocked a merge** (private repo, free org plan: branch protection and rulesets both `403`), and a blocked run is *worse* than no run because it renders as an ordinary red X, which is what trained a dozen merges past reds. `ci.yml` becomes `workflow_dispatch`-only rather than deleted: it stays the contract AC3 grades the local gate against, and restoration is a one-line revert. Six independently selectable legs (`workspace`+`smoke` default ON ~2 min; `e2e`/`rust`/`desktop`/`release` opt-in; `--all` ~15–20 min), chosen per merge at `/close-session` time so the owner holds the time/assurance dial. A deliberate SUPERSET of ci.yml — adds Playwright (CI ran none) and the server smoke leg carrying the byte-exact `RUNNER_CSP` assertion (threat-model R-11). Three states not two (PASS/FAIL/**DESELECTED**); a selected-but-unrunnable leg is a FAILURE never a skip; a partial run enumerates what it did NOT verify and denies ci.yml equivalence, which only `--all` may claim. Narrow purpose-built ci.yml extractor (no `yaml` dependency for one file we own). **Mutation-tested, and it caught a defect in the test itself**: substring matching let a mutant survive, making AC3 decorative until token-set equality replaced it. AC9 — the e2e suite had to be made adoptable first: all 4 undocumented failures were TEST defects (3× tile-click-by-accessible-name after starters gained display names; 1× a ~500 ms connect-CTA race surfacing misleadingly as "wizard not found"), and the 4 documented DEGRADED rows are quarantined with `test.fail()` NOT `test.skip()` so a row that starts passing FAILS the suite and forces the quarantine to shrink. The vitest flake did NOT reproduce (8/8 green, 1303/1303, incl. 3 runs under forced CPU contention) — deliberately no config change, and the 2026-08-19 lesson amended rather than left standing. `/close-session` now pins `model: claude-opus-5`, asks which legs to run, refuses everything on red, and merges automatically on green. Verified `--all` FULL PASS 6/6 on the branch that builds it. **Open:** restoring billing is a hard flip-public blocker — an outside contributor's PR cannot be gated by a command that only runs on the owner's Mac.
- **TASK-20260820-snug-file-and-encryption** — `.snug` as the canonical user file + opt-in passphrase encryption at rest · PR #89 · ADR-0042, ADR-0043 · spec pushed as `snugprotocol/spec@8ea69b8` (§6 naming, §7 the `SNUGENC1` container) · **High tier**, full six-gate walk with BOTH fresh-context reviews. **Part 1** finished a migration ADR-0021 D6 had half-shipped and that was carrying a live bug: web exported `snug-user.sqlite` while desktop exported `.snug`, and the web import picker's `accept` omitted `.snug`, so a desktop export appeared greyed out in a browser. The DB layer was never at risk (sql.js ignores filenames; every validator sniffs content) — the danger was four filename LOOKUPS, so `user.sqlite`, its sync sidecar and the Dropbox path are all READ when the canonical name is absent and adopted forward on the next write, never renamed or deleted. **Part 2** took ADR-0014's explicitly deferred passphrase encryption off the shelf and paid down its named cost with a mandatory Recovery Key: `SNUGENC1` = AES-256-GCM whole-file at the ONE persistence seam, file key wrapped independently per slot (so a passphrase change rewraps 48 bytes and the recovery slot survives), PBKDF2-SHA256 600k (measured 175 ms; whole-file AES at the 64 MiB cap = 88 ms, so no worker), header bound as AAD **plus** an unkeyed checksum separating *damaged* from *wrong secret*. sql.js has no SQLCipher and SEE is paid, so SQLite's own encryption was never available. Protection follows exports and PERSONAL-origin sync; **hub origins keep receiving secrets-stripped plaintext, so `apps/server` and `/userdb` are untouched**. **The plan review found 9 blockers before any code**, three data-loss (sidecar orphaning → a phantom divergence whose obvious resolution discards local work; Dropbox split-brain; second-device unlock impossible as designed); one finding was **tested and REFUTED** (a KDF-param flip breaks the unwrap → DoS, not silent weakening) and AAD adopted for the honest reason instead. **The diff review found no defect in the container** under direct attack (fuzzing, forged slot counts, truncation sweeps, 50-reseal nonce check) **but four wiring defects**, including D-2: the composition root never passed the sealer, so ADR-0043's central promise did not hold in the shipping app. **Self-caught**: the Recovery Key was 127.6 bits against its own 128-bit floor (30-glyph alphabet sized as base-32) with a test hardcoding `log2(32)` that hid it — now 27 symbols = 132.5 bits, entropy derived from the observed alphabet. **The owner then found the feature was UNREACHABLE** — `ProtectSetupFlow` never rendered, 1341 tests green over a screen no user could reach — and after wiring it, a wrong passphrase showed nothing (a transient `'opening'` status unmounted the screen mid-attempt). **Then the e2e leg, run before merge rather than after, failed 24/73**: the offer had shipped as a full-screen gate, so a new user met "protect this file?" and never reached the shelf — now a dismissible in-hub banner. Threat-model v2: **R-3 rewritten, A6 moves from "explicitly not defended" to defended-at-rest, R-14 added as a named unmitigable residual** (lose both secrets, lose the data — no backdoor, by design), §7's custody claim scoped rather than deleted. ~130 new tests; root suite 23/23 (playground 1349 · auth 915 · db 391 · protocol 344); **all six `gate:local` legs green** incl. e2e 72/73; browser-verified end to end (plaintext → protect → `SNUGENC1` on disk → relaunch unlock → wrong-passphrase message → Settings turn-off → plaintext). CI still billing-blocked; merged on local evidence. **Five lessons**, the sharpest being *run the product before claiming a UI feature works* — three defects here were isolated tests passing over a broken composition.
- **TASK-20260820-spec-v03-whitepaper** — Consolidated spec v0.3 draft + whitepaper edition 2 + publication · PR #90 (squash `c16c061`) · ADR-0044 · SPEC-v0.3-draft.md (wire 13 frames + R7 · storage v6 · connected apps · runtime contracts · linked-device) replaces the three staged drafts; whitepaper e2 (33pp/10 figures, checker 103/103, fixture = staged draft + protocol schemas); json-schemas SOURCES → 14 files (strict pairs publish strict, Part III–V contracts prose-only); published as `spec@ea0109d` (e1 whitepaper, rebased pending branch) + `spec@cd011cc` on the owner's explicit ask; evidence local (protocol 345 · auth 915 · db 391 · runner 119; CI billing-blocked). 1.0 promotion + Trivia Night starter removal queued in next-steps.
- **TASK-20260820-starter-updates** — Starter versioning + the in-place update channel · PR #92 · ADR-0045 · **High tier** (touches the two-fact declaration vouch), full six-gate walk with the pre-implementation fresh-context plan review (no blockers; 6 should-fixes, ALL folded, none rejected — incl. the one that matters most: a validator that only checks starter.json's shape cannot enforce "bump on every change", so **`appHash` binds the metadata to the html bytes** and a starter edit shipping without a release is a red test whose message prints the paste-in fix). Closes the owner-hit gap ("an installed starter never receives a rebuild" — the Ledger playbook fix that never reached his installed copy): every starter now declares `starter.json` (integer version + cumulative Tesla-style changelog), the hub tile reports **update · vN** in place of "installed" (own badge class — `dedup.spec.ts` holds a strict single-element selector on the installed badge; hub stays read-only per its own doctrine), and the run header carries the version chip, a formatted release-notes sheet (installed version marked, newer entries tagged "new") and the ONE update write act: one click for unedited copies, one confirm for re-authored ones ("your edits stay in version history" — and they do, revertable). The act lands the bundle as a **new PINNED version via `saveAppVersion`**, which gained opts `{pinned, contract}` so the starter's new runtime contract lands **in the same synchronous call** (plan-review finding: the two-write version left a durable window where updated HTML ran under the old contract; ADR-0018's copy-forward is for user edits, a factory update ships factory contract); idempotent, with the `starterVersion:<appId>` settings row written LAST so a partial failure keeps the update offered and the retry self-heals. Retention is by construction — data tables, `auth:` secrets, connections, chat, docs are all keyed on `app_id`, never version — and asserted anyway (approved connection rows byte-equal across the update; the AC4 never-redeclare lock holds). **Two consistency changes the design forced, both behavior-identical for every pre-existing app**: the vouch's fact 1 generalizes from "v1 == bundle" to "**newest pinned == bundle**" (same forgery strength — an attacker still must control a pinned row AND `current_version`; an EMPTY pinned set refuses; three negative twins pin it, and the in-file doctrine comment was rewritten rather than contradicted), and `resetToFactory` flips MIN→MAX pinned so "factory" means the starter you are on, not install-day bytes (the exact broken exit lessons.md 2026-08-19 named). VersionsPanel's banner/reset deliberately follow the newest pin (was correct only by accident of one pin existing; now test-pinned). Installed version rides a namespaced `snug_settings` row (ADR-0036 D2 precedent) — **no schema bump, no protocol change, no spec-sync**; `deleteApp` equality-sweeps it (mutation-checked); legacy installs derive by byte-comparison, which can only over-offer a no-op, never hide an update. 42 new tests, every guard mutation-checked red (MIN-revert, sweep-removal, v1-vouch-revert each red exactly their owning tests); root `turbo run test --force` 23/23, Cached: 0, exit 0. **Real-browser walk end to end** (dev server + agent-browser, screenshots delivered): install trivia-night at v1 → notes sheet "v1 — Initial release · INSTALLED" → throwaway bump to v2 → hub "UPDATE · V2" → header "update to v2" → one click → the probe marker visibly RUNNING in the reloaded frame → chip v2, two factory pins, v1 revertable → badge cleared. Three lessons recorded (checkout-restore wiping uncommitted work during a mutation check; wait on an act's completion signal, not its first write; a glob literal's star-slash ends a block comment). Stated limitation (ADR-0045): a changed requirement on an APPROVED slot waits for the user's re-review — the never-silently-widen lock stands. CI billing-blocked; merged on local evidence (gate:local workspace+smoke+e2e; rust/desktop/release deselected — no surface touched).
- **TASK-20260821-ui-polish** — App rename + per-version delete + Telepath deep delete + multi-provider BYOK + build-page selector + Settings redesign, one branch (`feat/ui-polish`, owner-named) · ADR-0046 (ADR-0036 amended) · **High tier**, full six-gate walk: fresh-context PLAN review before implementation (16 findings, ALL folded — 4 would have shipped silent defects: the helper's TERM-first exit flush re-writing the thread cache into the just-wiped session store, the route-equivalence test being blind to the hand-written Rust `WIZARD_ROUTES` table, the helper router's `/session/*` branch accepting APP tokens on a destructive route, and provider being CAPTURED at transport construction so a mid-session pin would do nothing). Owner interview decisions: ALL pinned factory versions protected from version-delete (plus the running version); Telepath delete = FULL device unlink; the selector groups BOTH providers' catalogs. Shipped: (1) `deleteAppVersion` (guard order unknown→VERSION_PINNED→VERSION_CURRENT) + VersionsPanel per-row delete with inline confirm on eligible rows only; (2) unique rename (trimmed, case-insensitive, self allowed) with the `appRenamed:` marker guarding the announce-clobber at BOTH altitudes (store merge + async DB re-check for the announce-races-hydration case), hub tile editor, run header preferring the DB-backed name; (3) the deep delete: `POST /session/forget` (wizard-only by prefix, nonce-ONLY at the router, Baileys logout + auth-store erase behind a **persist tombstone** — tested by CALLING the real registered `'exit'` listener after a mutation check exposed the timer-based version as vouching for nothing) + Rust `sidecar_ctl("forget")` disk backstop + the last-sidecar-fact orphanhood trigger in `library.delete` (second-app veto pinned); the Rust wizard table is now pinned equal to the FULL contract (the first equivalence run passed VACUOUSLY against a stale protocol dist — the 2026-08-15 rebuild lesson live); (4) multi-provider BYOK: `providerChoice` (explicit, unconditional) → derived anthropic→openai→demo-brain into the SAME `providerStore` every consumer always read; per-provider default models; legacy `provider`/`model` hydrate-adoption (write-once, never deleted); per-app pins store provider AND model (`appProvider:` + `appModel:`, both deleteApp-swept, clear deletes both), resolved PER SEND at all three adapter-construction sites incl. the inferrer `decideWire(appId)`; (5) the build page carries the selector — attached thread = the app's own control, fresh thread = session pick transferred on install (wiring proven through a real BuilderView build); (6) Settings rebuilt as five labelled inset-group sections with the multi-provider rows, connections rows relaid (all testids + derived copy verbatim), responsive at 390px, every pinned accessible name carried (desktopSettingsView 11/11 untouched; e2e mode-group name verbatim). No schema bump, no spec-sync (spec-changelog INTERNAL-DRAFT entry for the route; staged v0.3 §20.8 route count corrected locally for the next push). ~90 new tests; key guards mutation-checked red (tombstone, pin override, transfer wire). Root `turbo run test --force` 23/23, Cached: 0, exit 0 (playground 1442/1442 in parallel — flake absent); threat-model 139/139; whitepaper 103/103; cargo 100; real-browser pass with screenshots (dark/light/390px + the live key→default→build-selector flow). Two lessons (erasure write-tombstone; per-restatement equivalence pins). Merge gate verdict, verbatim: "PARTIAL PASS — 2/6 legs green (workspace, smoke). NOT VERIFIED: e2e, rust, desktop, release." (owner's leg pick; cargo/desktop/sidecar suites ran green in-session as supplementary evidence). Residuals: helper-cannot-start skips the phone-side unlink (disk still wiped); a keyless pin routes keyless with a "(key missing)" mark. Owner walks queued: Telepath deep delete on hardware; desktop-shell settings eyeball.
- **TASK-20260821-hardening-polish** — Threat model v2 + desktop distribution/update channel + starter wikis + mobile either/or view + trivia removal + header-export hide, one branch, six per-item commits · PR #94 (squash `31350c7`) · ADR-0047 · **High tier** (desktop release config, the threat model, C2-adjacent capability config, e2e-load-bearing strings), full six-gate walk. Owner interview settled four calls up front: GitHub Releases hosting now; Apple Developer ID "planned for, not held" (signing wired + env-gated, unsigned disclosed); a TARGETED threat-model v2 rather than a full re-audit; retrospective Ledger-style docs for the keepers. **The pre-implementation fresh-context plan review (4 lenses + a refuter per finding, 25 agents) confirmed 21 findings with 0 refuted — and 2 were BLOCKERS of one kind: the plan asserted a delivery mechanism the code could not perform.** Item 6 rested on "bump the version and installed copies take the docs through the ADR-0045 channel", and against the real `starterUpdate.ts` that was unreachable twice over — the already-current fast path returns BEFORE `installStarterDocs`, so a copy that took the update received nothing, and `starterUpdateStatus` derived "current" from byte-equality for no-row legacy copies (exactly the owner's pre-seeding installs), so the offer never appeared at all. The module's own doctrine comment ("byte-derivation can only over-offer, never hide") was TRUE for every release that had ever existed and inverted for the first docs-only one; both halves fixed, the comment amended rather than contradicted, and a seed-removal mutant killed. **P5 (ADR-0047)** built the repo's first supply-chain surface: `tauri-plugin-updater` + `-process`, minisign artifact signing (keypair generated this session, custody `~/.tauri`, never committed), `createUpdaterArtifacts`, universal-darwin build, `scripts/release-desktop.mjs` (bumps three version sites together, REFUSES without a matching newest `desktop-releases.json` entry, runs `gate:release`, emits `latest.json`, and prints `gh release create` without ever running it), `/download` (single-homed DMG URL, honest Gatekeeper paragraph while unsigned, macOS-only stated per ADR-0021 D8, bundled notes), and an **offered-never-automatic** update UX: toggleable launch check that is quiet on failure (the pre-flip private repo 404s for everyone, so silence is the designed state and the Settings button is where failure gets NAMED), a non-blocking header chip, a Tesla-style sheet. **The trust split is the design constraint and is stated, not implied** — minisign covers the ARTIFACT, never `latest.json`, so a compromised publishing account cannot install a binary but CAN author the prompt: fetched notes render plain-text with no linkification (pinned), the version is syntax-gated, and the UX offers no button pointing outside the flow (threat-model R-28). Three plan-review security findings shaped it further: the dev-only override became a `--config` overlay with **no code path in the shell at all** (the header-token idea rejected outright — github asset URLs don't header-auth, and a PAT in that transport has the redirect-forwarding failure class lessons 2026-08-12 records), enforced by a new **MUST-APPEAR** scan in `run-release-gate.mjs` that fails any release binary not carrying the production endpoint byte-for-byte (one mechanism covering both "config dropped" and "built with the overlay"); and `relaunch()` **reaps the sidecar FIRST**, because `AppHandle::restart()` skips `RunEvent::Exit` on the main thread — spy-pinned call order, since an orphaned helper wedges the linked-device session. **C2**: three per-command keyless-refusal gate rows (`plugin:updater|check`, `download_and_install`, `plugin:process|restart`) + a positive twin, because capabilities are per-WINDOW and the main window IS the webview holding the sandboxed iframes — placement proves nothing, and the amendment-16 doctrine already said so. **P6 threat model v2.0**: four surfaces folded in, each now owning a delta the model hash-pins — one written RETROACTIVELY for the `.snug` encryption work, which had amended the model in place without a delta, a gap the ledger was structurally blind to (a delta that never existed cannot fail a hash check). New residuals R-28..R-32; **v1's duplicate R-14 resolved** (encryption → R-27, citations swept) and a new **TM7 check makes the class unrepresentable**, with both mutants killed (duplicate restored ⇒ red; loosened definition regex ⇒ red, proving reference-vs-definition is doing real work). Also: trivia-night removed with its three e2e specs CLASSIFIED not deleted (journey → flying-pig, the two deterministic-SQL-write guards → quiz-me); the per-app header export hidden behind one flag with the code path intact and its load-bearing locator claims migrated to Settings; the mobile run view swapped from a bottom-sheet modal to an either/or full-view toggle that always opens on the app; app wikis for every starter (4 retro bundles labeled "Reconstructed retrospectively" and validator-scoped, github/weather gaps closed, a five-doc floor, all 12 bumped to v2). **No `packages/` change at all — no spec-changelog, no spec-sync.** PROCESS.md + the three root mirrors gained the GitHub-Release explicit-ask rule (the standing list predated the repo having anything binary to publish). Verification: **`gate:local` all six legs green** across two invocations (each honestly PARTIAL alone); root `turbo run test --force` 23/23 with **Cached: 0**; playground 1463/1463 · desktop 163/163 · examples 362/362 · threat-model checker 169/169 (was 130) · checker tests 17/17; the three new C2 rows pass in a **real WKWebView** and the MUST-APPEAR scan passes against a real 20.5 MB release binary. Nine mutation checks, each died and was restored by inverse edit (never `git checkout` mid-implementation). Four lessons, the sharpest being that an invariant comment is a claim about a DISTRIBUTION and the first release of a new shape turns it into a bug. **Owner walks owed** (next-steps 2026-08-21): the real update+restart on hardware incl. the single-instance lock race — the reap ORDERING is spy-pinned, the actual restart is the one thing no suite can perform; `/download` + Settings on the shell; the mobile view on a phone; one docs-only starter update. The first `gh release create` and the Apple Developer ID (R-29) stay explicit-ask owner acts.
- **TASK-20260821-launch-security-review** — Final pre-launch security review + threat model v3.0 · PR #96 (squash `2a783bf`) · **no ADR** (owner call: the verdict already lives in the task file, next-steps and threat-model §9; a fourth copy earns nothing) · **High tier**, full six-gate walk. Commissioned as the last gate before the public flip — "air-tight, would pass the HN Show review". **The mandate came from v2's own scope note:** it stated its adversarial pass covered only the four surfaces that landed after v1 and that "the rest of this document is v1's, re-checked mechanically but not re-attacked", so a launch review that simply updated v2 would have shipped v1's audit as current. v3 re-attacked the whole surface across **seven parallel fresh-context lanes** — C1 · C2 + the desktop IPC census + the sidecar's own HTTP surface · the executor's ten gates and the ceiling · prompt injection and LLM egress · spec-vs-code conformance · flip-public hygiene · **the reference server and storage custody, which had never been assigned a reviewer in v1 or v2**. The pre-implementation plan review returned **2 blockers + 4 majors, every one re-verified against primary sources before acceptance**, and two of my own acceptance criteria were unachievable as written: AC6 depended on a scrub-terms file that stage 1.4 *creates* after a destructive step this task never performed (and on scanners that were not installed), and AC3 was unbounded until the claim census was actually counted (73 MUST/MUST NOT + ~200 never/always, with the whitepaper source being **HTML, not markdown**). **Four defects FIXED rather than described, each test-first with the RED recorded and each mutation-checked.** (1) **A live C1 credential leak**: `oauth-service.ts` scrubbed only the DECODED spelling of a submitted secret while `URLSearchParams.toString()` percent-encodes on the wire and `scrubAuthValues` is exact-substring — so a provider echoing back the bytes it received returned a spelling the candidate set could not match, and that message reaches the app iframe, the LLM, and `snug_secrets` (`lastError`, which rides personal-origin sync). Any base64-shaped credential (`+`, `/`, `=`) is affected, i.e. the ordinary shape of an OAuth refresh token; the sibling seat in `connected-fetch.ts` carried this exact defense under a comment explaining why, and this seat never inherited it. **The existing tests were structurally blind**: every fixture token was `[A-Za-z0-9-]`, for which `encodeURIComponent` is the identity, so the failure was *unrepresentable* and the suite passed whether or not the defense existed. (2) **A flip-public BLOCKER, and it was in the guard**: `no-ancestor-tokens.test.ts` hardcoded the real ancestor-system codenames and hook names as a plaintext list *annotated as such* — the test written to prevent that exposure WAS the exposure, in the single most rewarding file in the tree for anyone curious about provenance. Now SHA-256 hashed with substring semantics preserved by hashing candidate windows per published length; the planted-token sample is **recovered by bounded brute force** rather than hardcoded, so no plaintext survives and the scrub gate can reach zero (verified against the private source: 12 real tokens still caught). (3) **R-8's load-bearing sentence was FALSE** — the residual rests on the confirm dialog "naming host, method and URL", and the modal never rendered the URL, so `POST /notes` and `POST /transfer?to=attacker` were indistinguishable to the user; fixed in the dialog rather than by amending the sentence, and the session-remember checkbox now discloses that its grant is keyed `(appId, host, method)` with **no path component**. (4) **R-12's named exception closed** — `sidecar_wizard_fetch` fronts the wizard route table including the token-releasing `GET /pair/status` and auto-injects the spawn nonce, yet its *lower*-privilege sibling was probed and it was not; it now carries its own keyless refusal row on its own callback slot plus the positive twin. **Two of the eight findings were defects in the CHECKS, not the code**, both the same shape — a mechanism credited with a claim it could not make: `check-threat-model.mjs` was cited as the guard against ledger drift while its only count assertion was `actual.size >= 8`, a floor that passes at twelve and never reads the §1 prose (new **TM8** compares prose to ledger; TM3 tightened to exact; mutation-checked by restoring the "eight" and watching it name both numbers). **Spec correction (C3)**: §11.1's `SNUGENC1` layout documented a 13-byte slot entry against the encoder's 61-byte stride, and since §11.2 rule 4 makes the header *through the end of the slot table* the GCM AAD, an implementation written from the published text **cannot open a conforming file at all** — surfacing as a wrong-secret error on a healthy container, precisely what rule 6 forbids. Corrected in the draft (verified empirically against the shipping encoder first: 61-byte stride, 48 zero reserved bytes, 160-byte header) and pinned by two regression tests — **the second was initially written as arithmetic over the encoder's own constants, survived a 61→13 mutation, and was rewritten to MEASURE the padding run in the emitted file.** `packages/protocol` untouched, so no spec-sync push; the changelog entry records that the fix rides the owner's regeneration. Also corrected for public release: SECURITY.md told researchers the connected-fetch runtime "is landing next" (it shipped, and it is the C1 enforcement seat — the highest-value target read as out of scope), README said spec v0.2, called `packages/auth` "in development", and omitted `apps/desktop` + `apps/whatsapp-sidecar`; the flip runbook's npm premise was false (all 13 workspace members are `private: true`) and its the private strategy tree-reference count was 8 against a measured **36**. **Verification: `gate:local --all` FULL PASS 6/6** ("equivalent to ci.yml on macOS", Windows leg named as having no local counterpart) **plus the in-shell gate GREEN in a real WKWebView** where both new IPC rows executed and passed, so R-12's closure is proven behaviourally rather than only unit-pinned; per-package auth 916 · playground 1465 (serial) · desktop 175 · knowledge 185 · db 418 · threat-model 175/175. One red investigated and dismissed with evidence — `starterInstall` timed out under full parallelism, passed in isolation and serially, matching the documented contention flake's varying-set signature; no config touched. **Verdict: PASS WITH CONDITIONS** — C1 and C2 re-attacked and **neither is false**; conditions are owner actions (CI billing, so every green is one machine; no Windows verification, leaving R-5's only detector dark; the SSO-runbook caveat; hardware walks; runbook stage 0 and the 36-file the private strategy tree scrub). Seven non-blocking findings filed in next-steps with evidence, led by the reference server's missing authorization on `/invoke`+`/artifacts` (bounded by ADR-0013 — the shipped hub has no backend) and the breadth of untrusted `.snug` import. **The verdict enumerates what was NOT checked** (two unreviewed packages, sampled rather than traced never/always claims, untouched SHOULD/MAY tokens, unexecuted stage 0) because an unqualified PASS that hides deselected coverage is the failure mode the criterion exists to prevent. **Five lessons**, the two generalizable ones being *a control in one seat and not its sibling is the highest-yield thing to look for — and the codebase usually already explains why the control is needed* (all four security fixes were that single shape), and *a mechanical check is evidence only for the claim its MECHANISM supports; also ask whether the fixture can EXPRESS the failure, because a test whose inputs cannot exhibit the bug is zero coverage wearing a green tick*.
- **TASK-20260821-website-launch** — The public website: marketing landing + docs/spec hub + desktop download, one static site · PR #98 (squash `b218853`) · ADR-0048 · **Medium tier**, full six-gate walk (4-question owner interview settled stack/domains/download/docs-scope; two approval addenda — GitHub org link, local E2E mode). `apps/website` (Astro 5.18 + Starlight 0.35) hosts everything except the playground: a dark-committed ember landing (playground `tokens.css` imported via the `@playground` alias so brand cannot drift; responsive teaser via unit-tested `pickTeaserSource` — 32/38 MB `.mov` masters transcoded to 4.4/4.2 MB MP4s; an animated wire demo spelling real `snug:app-message`/`snug:app-response` frames from the published schemas; SDK snippet verified against `packages/sdk` types after the first draft invented an `ask()` API), `/download` with URLs identity-equal to `releaseChannel.ts` (grep test forbids any second spelling), and `/docs`: 12 authored pages + spec pages **generated verbatim** from `SPEC-v0.3-draft.md` (six Parts + appendices + all-14-schema reference, draft-bannered) + the whitepaper PDF. **The sync doctrine is the durable piece**: `docs-sync.json` maps 24 pages ← 40 source hashes, root `check-website-sync` (in `pnpm test`) reds NAMING the stale pages and the remedy, `/sync-website` is the remedy command, and a Claude auto-memory entry reminds future sessions — mutation-checked live against the REAL manifest (touch product-vision → red naming 4 pages → inverse-edit restore → green). C4 guarded twice (manifest refuses the private strategy tree sources; content grep scoped to shipped files after flagging its own needle — the 2026-08-20 lesson, met in the wild). Local E2E: `PUBLIC_SITE_MODE=local` repoints playground→`localhost:5173` and `/download`→a DMG staged by `stage-local-desktop.mjs` (which learned to skip create-dmg's `rw.*` intermediates). Tests-first: website vitest 29 + checker node:test 7; the suite caught two real defects before any browser did (quickstart hardcoding the playground URL — fixed by MDX importing site config — and the needle self-flag). Build fought two environment classes worth remembering: nanoid/non-secure ESM-interop in Astro SSR (`vite.ssr.noExternal`) and a stale content-layer cache replaying entries without new schema defaults, so `draft === false` dropped every page in build while dev stayed green. AC9 rendered pass at 1440/390px + docs light theme caught a nav-CTA specificity wash-out (`.nav-links > a` outweighing `.btn-primary`). Forced root run 25/25, Cached: 0; merge gate verdict verbatim: "PARTIAL PASS — 2/6 legs green (workspace, smoke). NOT VERIFIED: e2e, rust, desktop, release." — deselected legs cover surfaces the change does not touch. **Deployment deliberately NOT done** (next-steps: Cloudflare Pages + DNS + the launch-time check that `/download` links go live with the first GitHub Release are each explicit asks). Two lessons (stale-cache × schema-default filter; newest-by-mtime picks intermediates).
- **TASK-20260821-site-playground-polish** — Website+playground polish sweep: 8 owner-reported items + 1 walk-found bug · PR #100 (squash `8decc73`) · **no ADR** (owner UI calls recorded in the task file; no architecture decision) · **Medium tier**, full six-gate walk (3 explore agents + live Gmail-CORS probes at Gate 1; 4-question owner interview split the one High-tier item out). Shipped: every website→playground link opens a NEW TAB (`target="_blank"` ×5, siteLinks pins — the `↗` glyph finally kept); a `snugprotocol.org` shell-nav link serving web AND desktop (`WEBSITE_URL` single-homed playground-side per ADR-0048's direction rule; desktop clicks via `oauth.openExternal`); the desktop-only badge as a short `desktop` tag (why in `title`) **plus the walk-found fix — the badge Link had been POINTER-DEAD since ADR-0047**: the tile's flex-stretched card button won hit-testing over the absolutely-positioned tag (both z-auto), invisible to the jsdom href test; `z-index: 1` + `e2e/desktop-badge.spec.ts` (Playwright hit-target click, mutation-verified); the Recovery-Key typed-phrase gate → a mandatory CHECKBOX (blocked-until-checked preserved, re-blocks on uncheck; threat-model wording updated, checker 175/175); the "show"-button overlap root-caused as `className="row"` used in 8 places with NO rule anywhere → all converted to the real `.field-row`, `.field-row > input` flex-fill, dead class banned by source scan; the Starlight `slug "docs" does not exist` error root-caused as an INTERRUPTED `.astro` content sync dropping every `.mdx` entry, healed by a clean rebuild, and the guard gap closed — navIntegrity now checks sidebar slugs against built `dist/` AND refuses a dist older than its sources (the stale-dist mask that kept it green; proven red→green); "no app opens / removed starter still shows" diagnosed ENVIRONMENTAL — a half-written `node_modules/.vite` (`deps_temp_*`, no `deps/`) plus THREE orphaned vite instances, one serving a pre-Trivia-removal module graph — fixed by recycling + cache delete, walk-verified (shelf = exactly the 12 current starters, chess end-to-end), prevented by root **`pnpm dev`** (`scripts/dev.mjs`: build server → run pair, prefixed output, SIGTERM teardown; 5 node:tests via `check-dev` in root `pnpm test`). Gmail dual-mode SPLIT to next-steps with the research pre-loaded (Gmail API fully CORS-open by live probe; Google demands `client_secret` for web clients even with PKCE — the pickup's design decision). Evidence: playground tsc + 1471/1471 serial + badge e2e 1/1 (mutant killed) · desktop 175/175 · website 33/33 + fresh build + sync gate · threat-model 175/175; browser walk with screenshots at 1440/390px. Merge gate verdict verbatim: "PARTIAL PASS — 2/6 legs green (workspace, smoke). NOT VERIFIED: e2e, rust, desktop, release." (owner's leg pick; desktop vitest and the new e2e spec ran green in-session). Three lessons (href≠hit-testable; undefined-className silent jank; interrupted build-cache third-state + stale-dist mask).
- **TASK-20260822-spec-10-final** — Specification 1.0 + whitepaper edition 3 + website 1.0 pages; spec repo PUBLISHED · PR #103 (squash `a40302e`) · ADR-0050 · **High tier**, full six-gate walk. The launch publication set: the v0.3 RC promoted to **`docs/spec-drafts/SPEC-1.0.md` (NORMATIVE)** with the owner's four interview calls (§17 stays marked PROVISIONAL; stage-then-owner-pushes; edition 3; spec-repo SPEC.md becomes THE document, drafts → pointer stubs), pushed to `snugprotocol/spec` as **`3ac7700` on the owner's explicit in-session ask** and verified by fresh clone (schemas `diff -rq` identical, PDF SHA match). The pre-implementation fresh-context plan review earned its keep before a line changed (AC5's planned edit was self-contradictory; the `--spec` mode would have loaded the pointer stub Phase 6 itself creates; "add the forget route" would have duplicated an existing row). A **five-lane spec-vs-code audit at head** then found the task's biggest surprise: **a shipped code defect** — `healMissingTables` counted the v5-dropped `snug_auth_specs` as a permanent miss, so EVERY open of a healthy post-v5 file replayed DDL and persisted spuriously; fixed test-first (red observed → green; expected set now DERIVED from `USERDB_DDL` with a count assert), and the fix unmasked a second finding — the legacy-adoption compose test had been passing FOR THE WRONG REASON, its adoption riding the spurious dirty; given the §10 write it actually needs, claim preserved. Audit folds into the spec (source-of-truth clause: prose is the bug): 2560 = serialized UTF-16 code units; wipe gate = version-advance not "exactly once"; rewrap MAY reuse the slot IV under a new KEK; SNUGENC1 slot count structurally 1–8 (the Gate-5 review caught my own fold over-tightening it to 2–8 — and the changelog "verified" claim being wrong for the lower bound); factory = NEWEST pinned (ADR-0045); CDN_ALLOWLIST + SNUG_APP_REQUEST_TAG published; §12.12 gains the ADR-0049 web-surface seats with the mutual-requirement rule and byte-match endpoint binding. Whitepaper e3: checker AC5 INVERTED (must claim 1.0, must not self-describe draft/RC), `--spec` reads the clone's SPEC.md only, 104/104 in BOTH modes, TOC hints verified programmatically, visual pass. Website: 10 pages regenerated verbatim, ten authored pages walked by agent → 3 pre-existing drifts fixed, 24-page/40-hash sync gate green. Gate-5 diff review: 2 MUST-FIX + 4 NIT, zero blockers, all but one applied (NIT 2 accepted as restoration of intent); editorial correction staged in the clone as `2132692`, UNPUSHED. **Gate verdict verbatim: "PASS 5/6 — workspace · smoke · rust · desktop · release GREEN; e2e leg RED with 6 specs that fail IDENTICALLY on `main` at `f36ba68`"** — verified by running the same files on a main checkout: 4× mobile 375px overflow (real #100 regression) + 2× badge assertions stale against #100's deliberate short-text change; charged to main, filed dated in next-steps (the first `--all` run since those merges rode deselected-leg gates). Root `pnpm test` exit 0 — after the pipe-to-tail lesson fired live TWICE (both background "exit 0" notifications were the pipe's, not the suite's) and became a lessons entry, alongside the permanently-firing-guard lesson. First e2e attempt was an environment verdict: a stale `dist/server.js` held 8787 (zero specs ran) beside six orphaned CPU-hog loops from the 2026-08-20 contention experiment — all verified by command line before killing. Owner acts remaining: push `2132692` (editorial correction), website deploy, first GitHub Release, flip-public.
- **TASK-20260822-mobile-e2e-reds** — The six pre-existing e2e reds (375px header overflow + stale badge assertions) · PR #107 (squash `65c3b6f`) · **no ADR** (a fix, no decision) · **Medium tier**, commissioned in-session by the owner the moment TASK-20260822-spec-10-final's first-ever `gate:local --all` surfaced them (both regressions rode #100's deselected-leg merge gate, exactly the DESELECTED disclosure ADR-0041 warns about). **The real bug:** the header nav's five items (four text links + the theme button, the fifth added by #100's `snugprotocol.org` WebsiteLink) alone outgrow a 375px viewport, so the brand's ellipsis could not absorb it and the page body scrolled ~29px — the header had never owned a narrow compaction. Fixed inside the existing ≤760px media block (`.nav-link` padding space-3→space-2 + text-s→text-xs, nav gap zeroed), **deliberately no wrap** because `.run-layout` hardcodes the single-row header's resolved 61px. **The stale tests:** two AC8/AC9 badge assertions still expected `/desktop app/i` in the badge TEXT after #100 deliberately shortened it to the `desktop` tag; claims MIGRATED per the 2026-08-16 rule — `toHaveText('desktop')` (fails against the pre-#100 text, so it pins the deliberate state) plus a NEW `title`-attribute pin so the why-copy is load-bearing again. Evidence: full e2e leg **75 passed / 1 skipped, exit 0 pipefail-verified** (was 69/6); playground unit **1482/1482** full run (a first run dropped one uncaptured test, unreproducible — the documented contention-flake varying-set signature, recorded rather than classified); 375/1440px screenshots reviewed (mobile one-row, desktop untouched). Environment note for the pattern file: a stale `dist/server.js` held 8787 before BOTH e2e attempts today (third occurrence) — each killed after command-line verification; a `reuseExistingServer`/pre-run-port-check posture is left as a future call, config untouched.
- **TASK-20260822-public-spec-presentation** — Public spec index header + self-identifying whitepaper cover · PR #109 (squash `0444c63`) · ADR-0051 (amends ADR-0048 §3 for the HEADER only) · **Low/Medium tier**, owner-commissioned from live site review ("draft/edit details with Task number… too much for a public web site"). Two findings behind the two complaints: the "unpublished" whitepaper was THREE stacked astro servers (one a `preview` of an old `dist/`; the owner's :4323 tab was the third-ported stale one) while every PDF on disk was byte-identical — all reaped, fresh server on :4321; and the Task-id noise lived ONLY in the index page's verbatim-rendered engineering preamble. `sync-spec.mjs` now builds the index as a PUBLIC header (version/date EXTRACTED from the spec's own header, stability table + conventions still verbatim; process bullets and revision note dropped — website only, `SPEC.md` keeps full traceability) with two generation-time guards: preamble-structure drift throws, and a `TASK-\d{8}` reaching the built index throws. No version switcher (one public version; ADR-0051 names the 1.x seat). **The surprise that earned a lesson**: giving the PDF cover an Edition cell as a FOURTH cover-meta grid item triggered Chrome print's whole-document shrink-to-fit — 33pp→29pp, every font ×0.91, checker green throughout, page count the only tell; isolated by A/B rebuilds from `git show HEAD:` and dominant-span-size comparison (uniform ratio = scale, not reflow), fixed by folding into the Specification cell (`1.0 · edition 3`), trap recorded in the whitepaper README + lessons. Evidence: checker 104/104 both fixture modes; TOC hints re-verified ALL OK; website build 24pp + 33/33 + sync gate 24/40; dist↔public PDF hash identical. Spec-clone PDF refresh staged `adc6901`, UNPUSHED (owner's word).
- **TASK-20260822-whitepaper-mark-niche** — The mark's niche renders ink on the whitepaper cover · PR #111 (squash `a4cb967`) · no ADR · **Low tier**, owner-commissioned ("white inside the ember… keep it consistent"). Root cause is the mark's own design: the niche is a TRUE KNOCKOUT (Logo.tsx: "the page background shows through"), dark on every brand surface and white on the paper's white cover — the one surface where the convention inverts. Paper-only fix: the evenodd path split into ember tile (currentColor) + an explicit `.mark-niche` path filled `var(--ink)`, with a comment recording the deliberate departure from the canonical knockout. 33 pages held (the shrink-to-fit tell from the same-day lesson), checker 104/104, cover crop reviewed, website PDF re-synced hash-identical. Spec-clone staged PDF commit AMENDED to `dcda2c6` — still exactly ONE unpushed commit covering both cover refreshes (edition line + niche), push on the owner's word.
- **TASK-20260822-desktop-app-shell-timeout** — The desktop app-shell test's budget matches its work · PR #115 (`d5baf0e`) · Low · Found gating an unrelated branch and reproduced on clean `main`: `app-shell.test.tsx` did ~3.5s of real work (module-graph reset + whole playground App import + sql.js user DB + React render) against vitest's inherited 5s default, so it timed out only under the full-workspace concurrency the merge gate uses. Explicit 20s per-test budget with a comment stating why the default was wrong. Workspace leg 25/25; gate 6/6.
- **TASK-20260822-playground-flake** — The playground flake was two racy waits, not contention · PR #116 (`a325a88`) · Low · The standing 2026-08-19 "file parallelism" classification was applied and MEASURED ineffective (2-in-6 → 2-in-8) and therefore reverted, not kept. Real cause, found from the failure TEXT: two tests synchronizing on fixed wall-clock waits — `sidecarLive`'s backoff slept 20ms then asserted four recorded sleeps, and `starterInstall`'s `settleUntil` budgeted by attempt count so interleaved `act()` calls shrank the real budget under load. Both now wait for their own condition behind a deadline; no assertion weakened. The first fix attempt waited for three sleeps in a test asserting four and took the rate to 9-in-10 — caught by re-measuring. Final: 0 failures in 10 runs; gate 6/6.
- **TASK-20260822-github-readmes-org-landing** — README front doors for both repos, the GitHub org landing page, and the final one-minute teaser cuts · PR #117 (squash `4b58188`) · **no ADR** (presentation only; the one real decision — creating `snugprotocol/.github` **public pre-launch** — is an owner call recorded in the task, next-steps, and LAUNCH_OPS) · **Low tier**, owner-commissioned. Three surfaces: the snug README rewritten visual-first (teaser GIF hero, three differentiators, app gallery, one-command `pnpm dev` quickstart — which exposed that the website quickstart still taught the pre-dev-runner two-process dance; fixed in the sync walk), the spec README rebuilt as a 1.0-status-led front door (spec@`e39abbd`, presentation only), and the previously nonexistent org profile (`.github`@`90c43da`, verified rendering logged-out; repo links carry a pre-launch note — **flip-day edit owed**, see next-steps). Mid-task the owner delivered final ~64s teaser cuts: landscape re-encoded to 8MB after the 20MB buildOutput guard rightly failed a 38MB lossless remux, and the "mobile" delivery turned out to be portrait content pillarboxed in a landscape canvas — cropdetect-verified crop to true 608×1080, `teaser.ts` dims updated (lesson recorded). Evidence: sync gate red→green cycle proven, website 33/33, `gate:local` PASS workspace+smoke (docs-only change), positioning grep clean, all links verified.
- **TASK-20260822-feedback-loop** — GitHub deep-link feedback channel + structural SSO hide + the mobile either/or band + website new-tab links · PR #119 (squash `079c817`) · **ADR-0052** (hosted receiver REJECTED for launch after the owner's own re-evaluation — ADR-0013's zero-endpoint claim outranks feedback convenience; Worker+D1 design preserved at branch commit `d9240e0`, parked to 1.1 evidence-gated) · **Medium tier**. Shipped: inline "report this" at build/wizard/run/boot error surfaces + header 💬 menu + Settings card, all prefilled GitHub issue-form/Discussions URLs behind a preview-confirm (scrub-before-assembly via the new single-homed `security/credentialShapes.ts`, display/prose modes, llmInspector migrated); `hubAuth` capability gates the auth probe + login() (web opt-in `VITE_SNUG_HUB_AUTH=1`; hub sync origin needs both seats); NEW side-effect-free `platform.openExternalUrl` seat (the OAuth opener BINDS a pending flow's listener — Gate-5 catch); header icon pass (⚙️/💬/`about ↗`); run-view either/or widened to ≤1000px after the owner FOUND the split on real hardware (phone landscape / tablet portrait sat between breakpoints; JS↔CSS literal byte-pinned); website external links swept to zero same-tab (7 anchors + SocialIcons override + rehype-external-links). 8-lane AI review applied 7 finding groups; 6 lessons distilled. Evidence: gate workspace+smoke PASS on the final tree, full e2e leg 76/1-skip, playground 1535, desktop 175, website 35. Flip-day: verify one live prefill; owner phone-landscape feel-check still worthwhile.
- **TASK-20260823-launch-readiness** — Pre-launch hardening audit, branch cleanup, flip checklist + ADR-0053 (retain history) · PR #122 (squash `9873b8b`) · **ADR-0053** (full history + 120 PRs/issues retained at flip, scan-gated + purge-gated) · **High tier** (release-surface classification; diff = docs + dev-script fix). Owner decisions folded: CI billing NOT a flip blocker (supersedes ADR-0041's blocker line; flip on local evidence); instagram-starter withdrawn from origin (LOCAL-only + bundle until post-1.0); AL-09 harvest branch deleted. Executed: branch cleanup to origin=`main`-only (41 deletions, SHA ledger in the task file; `delete_branch_on_merge` ON), full-history gitleaks over ALL refs clean (54 verified-FP → committed `.gitleaksignore`, rescan zero), PR/issue-corpus scan clean (110 bodies), repo+spec+org metadata/topics/Dependabot set, the private flip-public runbook refreshed (stage 4 rewritten for the ADR-0041 world: flip-hour ruleset, 0-approval require-PR; branch protection + private-vuln-reporting probed private-plan-blocked). **Cold-clone AC3 executed for the FIRST time and caught a real breaker**: `pnpm dev` built no workspace deps (green only on dirty trees) — fixed test-first in `scripts/dev.mjs` (`server...` + `playground^...`); post-fix clone→serving 2m37s. **Critical path left standing: the stage-0 purge — pre-scrub SHAs probe-verified STILL FETCHABLE 2026-08-23; GitHub Support ticket is the only path** (re-creation would destroy the retained PRs). Triaged flip checklist in the task file (git history) + runbook. Evidence: root `pnpm test` exit 0; dev.test.mjs 5/5.
- **TASK-20260823-web-deploy** — Website + playground deploy tooling (Cloudflare Pages direct upload) · PR #124 (squash) · **ADR-0054** (tooling lives in the public repo; direct upload from a local script, not Git integration/CI; production only from clean `main == origin/main`, no dirty override; `--deploy` is the distinct act; www via zone Redirect Rule — Pages `_redirects` cannot match a host; pages.dev Bulk-Redirected; preview Access policy; zone script-injectors OFF for ADR-0013) · **High tier**, owner-commissioned after the owner switched wrangler to the zone-holding Cloudflare account (nothing had ever been deployed; the zone was empty; "the existing playground deploy" in next-steps was stale). Shipped: `scripts/deploy-web.mjs` (`init` | `<website|playground|all> [--deploy] [--preview]`; account id from gitignored root `.env`; pre-flight proves wrangler ≥4 + `whoami` on THAT account + project exists; git-gates; pins `VITE_SNUG_HUB_AUTH=''`/`PUBLIC_SITE_MODE=production`, refuses app-level `.env*`, a staged `local-artifacts/`, local-mode HTML; `rm -rf dist` + `turbo --force`; verifies Pages limits + website `404.html` present / playground `404.html` ABSENT + sql.js wasm; argv-only exec seam) + `scripts/deploy-web.test.mjs` (26, root `check-deploy-web`) + `docs/runbooks/deploy-web.md` + `.env.example` + `wrangler@^4` devDependency. Two fresh-context reviews: plan (REVISE → 16 findings folded, 3 blockers: `_redirects` silently skipped by wrangler, `--preview` on main = production, turbo cache defeats the named fix) and diff (APPROVE-WITH-CHANGES → 10 applied, incl. the scrub test that itself carried the account-email domains). Evidence: gate workspace+smoke PASS on the final tree; root `pnpm test` exit 0; real-script smoke reaches the correct "project missing → init" refusal. Two db crypto load-timeout reds in loaded gate runs (0-in-3 isolated) + the gate-vs-root-`pnpm test` seat gap queued in next-steps. **Deploy itself NOT done — separate 🔑 asks per the runbook.**
- **TASK-20260823-legal-terms-privacy-eula** — Terms, Privacy, the DMG EULA clickwrap, and the contextual-consent bands · PR #125 (squash `9d2f56a`) · **ADR-0055** · **High tier**, full six-gate walk with BOTH fresh-context reviews (plan 15 findings / diff 12 findings — every one folded, each spot-checked in the tree first). Posture: `/terms` + `/privacy` do DISCLOSURE, never contract formation (Berman v. Freedom Financial — browsewrap binds nobody, so a gate would cost the first run and buy nothing) — footer-linked, Settings → about, rendered OFFLINE on desktop from plain-data modules (`apps/playground/src/legal/`) and by the website through `@playground`; **exactly one clickwrap, at the DMG**: `bundle.licenseFile` → `EULA.txt` byte-pinned to `legal/eula.ts`, embedded as the classic SLA resource, shape-checked pre-build (`checkEulaText`, ASCII/74-col/60-line — owner's 'under 60 lines' is the hard wall, headroom now 1) and PROVEN post-build by `verifyDmgCarriesEula` over `hdiutil udifderez -xml` with FULL-TEXT decode-compare (first-line-only waved through a stale same-title EULA — Gate-5 F6) against real captured fixtures with/without; `hdiutil attach` showed the Agree screen before mount (udifrez/udifderez are Apple-deprecated — named in the refusal, expiry queued). **The parties**: TechVoyage LLC (CA, ~14 yrs) operates site + hosted Playground (owns the domain, pays Cloudflare); Jeetu Maker personally distributes the app and holds the code copyright — resolved by ONE shared counterparty definition ('We/us/our mean Jeetu Maker and TechVoyage LLC together…') so no cap or disclaimer runs to the wrong party; CA law, CA venue with NO county, hello@ only (owner-verified same day — the publish gate), never a postal address; USD 50 cap with the Civil Code §1668 carve-out so the clause survives with a hole instead of dying whole; a narrow FOUR-item indemnity closed with 'that is the whole of it' (R-9/R-10 are a claim path the user alone creates); NO 18+ gate (contradicts the 11-year-old Beta exit criterion) — parent/guardian paragraph instead. Privacy written FROM the threat model with claim discipline enforced over all three documents (`claimDiscipline.ts` mirrors check-whitepaper AC6 incl. the ADR-0043/0040 bounds; the checker is proven non-decorative by planted violations). **Both reviews earned their cost on truthfulness**: the plan review killed a website-breaking `@snugprotocol/protocol` import (no dependency, alias-only, zod SSR-externalised → restate + pin-equal) and the localhost misclassification (the LAN predicate is IPv4-literal-only BY DESIGN; `security/privateHost.ts` lifts it byte-identical and adds `isLocalEndpointHost`); the diff review caught THREE false sentences in the drafts — 'delete the file and it is gone' (the WhatsApp session + thread cache live BESIDE the file in `~/Snug/whatsapp-session`, survive deletion, skip exports), a promised Settings warning the band deliberately does not give for the user's own LAN, and 'no other automatic outbound' omitting the Dropbox 30-s interval push — the lesson: check a disclosure against the code's automatic EGRESS (launch path + intervals), not its feature list, and never write the rounder claim ('no analytics script', not 'no analytics'). Four `role=note` consent bands extend the lanConsentCopy doctrine (warn, never refuse; key on knowable STATE, name the specific thing): BYOK saved key, local-model remote endpoint (default `localhost:11434` quiet, lookalikes raise), sync origin (ADR-0014 §2's own words + a hub twin wired to the `hubAuth` flag so the disclosure ships WITH the flag), WhatsApp link (R-9 stated where the user acts — it was stated NOWHERE in the UI before). 104 new tests: playground legalContent 29 / legalPages 12 / settingsAbout 3 / privateHost 38 / consentBands 19 / linkedDeviceThirdPartyBand 3 · desktop dmgEula 11 · release-desktop +5 (real fixtures) · website legalPages 7; root `pnpm test` green twice; playground 1637, desktop 186+tsc, website 42 on built output. Entity corrections applied to gitignored the private strategy tree (TRACKER D-02 'no entity' was false; USPTO applicant decision flagged for re-confirmation, not changed). Owed after merge (next-steps): counsel review of /terms pre-flip · first SIGNED release with notarize+staple-with-SLA verification · owner Finder walk of the Agree screen · privacy hub-operator row before any `VITE_SNUG_HUB_AUTH=1` build.
- **TASK-20260824-dependabot-triage** — Disposition all 36 Dependabot alerts on `main` before flip-public · PR #127 (squash `d350506`) · **ADR-0056** (classify by reachability; fix or dismiss with recorded, time-boxed evidence; gate locally) · **Medium tier**, full six-gate walk (4-question owner interview settled the astro path, the react-router posture, tooling targets, and where the gate lives). **Verified outcome: 36 → 0 open — 32 FIXED, 4 DISMISSED.** The 36 were 11 advisories multiply counted per manifest; triage classified each against the surfaces Snug actually ships (a *static* site runs no request-path code; `@vitest/ui`, whose RCE was all three criticals, is installed nowhere). Fixed: vite 5.4.21→6.4.3 (desktop/playground — vite 5 + esbuild 0.21.5 left the tree), vitest 2.1.9→3.2.7 (root/website), nanoid ≥3.3.18 via `pnpm.overrides`, astro 5.18.2→7.2.5 + starlight 0.35→0.41.7 (which also dropped sharp 0.34.5). Dismissed with evidence: 3× react-router `not_used` (declarative router only — no data router, so `deserializeErrors` is unreachable; every `to`/`navigate()` target is an internal literal or `/run/<own-store-id>`) and glib `tolerable_risk` (Tauri gtk-0.18 pins it; Linux is not a shipped target). **Absorbed and closed PR #121**, which bumped `astro` alone and could never merge against Starlight 0.35's `astro ^5.5` peer. **The durable piece is the gate**: `pnpm audit:deps` (`scripts/audit-deps.mjs`, 17 node:test cases written FIRST, red-verified before implementing) reds on un-allowlisted high/critical AND on any acceptance whose `reviewBy` lapsed — so an accepted risk cannot silently become permanent; a malformed entry reds too and does NOT shadow the advisory it failed to excuse; `tool:"cargo"` entries document Rust dismissals the npm gate ignores. Kept OUT of root `pnpm test` (owner call — the root gate stays offline-runnable); added to the flip runbook's pre-flip checks. All four acceptances expire **2026-11-30** (react-router v7 migration; Tauri gtk-0.20 re-check). Three breaking changes fought and fixed: Starlight 0.39's `autogenerate`-without-`label` shape, Astro 7's deprecated `markdown.rehypePlugins` (→ `unified()`), and a build failure where a stray **`~/node_modules/cookie@0.5.0` OUTSIDE the repo** won Node's upward resolution walk over astro's `cookie@2` (fixed with an explicit dep; diagnosed via `createRequire(cwd).resolve()`, which is the only thing that prints the winning PATH). The obsolete `vite.ssr.noExternal:['nanoid']` workaround was removed. **Verification that mattered was the rendered output, not the exit code**: all 26 built pages byte-identical in PROSE against a captured Astro 5 baseline (styles/scripts stripped, style hashes normalized), with `compressHTML:'jsx'`'s newly-adjacent inline elements each confirmed to sit in flex/grid containers spacing via `gap` — a whitespace-normalizing diff would have masked exactly the defect that change can cause. Desktop shell gate GREEN 35/35 (all seven C2 keyless-refusal rows + the C1 wizard secret canaries) once the owner quit the running app that held the single-instance lock. Every suite green under the new toolchain (playground 1639 + e2e 76, auth 939, db 419, examples 362, protocol 346, desktop 186, sidecar 177, adapters 130, server 126, runner 119, website 42, sdk 41). Four lessons (severity ≠ reachability; a `>=` override resolves to the newest MAJOR — `">=3.3.18"` silently installed nanoid **6.0.1**; Node resolution walks above the repo; verify an upgrade by its rendered output). Carried forward in next-steps: the react-router 6→7 migration before the acceptance expires, and the stray `~/node_modules/` (315 packages) that can shadow any workspace package.
- **TASK-20260824-flip-public-scrub** — Stage-1 public scrub: the two codenames, the private-tree references, and the off-tree move · PR (pending) · **ADR-0057** (the codenames stay in git history — accepted; the scrub tooling is gitignored and does not ship) · the runbook's stages 0.5/1.1–1.6 were otherwise executed as written · **High tier** (auto-escalated: 19 of 34 codename lines were in `packages/auth` and `packages/protocol`). **Stage 0 cleared first and it was the flip's #1 blocker since 2026-08-19** — GitHub Support actioned ticket #4692616 and the 0.4 probe now FAILS as required (both pre-scrub SHAs: `not our ref` on fetch, `422` from the commits API, forks_count 0), verified twice. **Scrub:** codenames **34 → 0** across source, tests and ADRs via 31 uniquely-matched edits, each keeping the engineering fact and dropping the provenance label; the *Source systems* paragraph deleted from all three root AI files (preserved privately) and hard constraint C4 rewritten to survive the folder's removal; **43 markdown references to that path** rewritten. `docs/tasks/done/` carried no codenames, so the owner's "scrub done/ too" call cost nothing. **The finding that shaped the task:** of 53 hits on the private strategy path, **10 were the C4 guard machinery itself** (`check-website-sync.mjs` refuses manifest sources under that path; `navIntegrity.test.ts` fails if a website file mentions it) — a mechanical scrub would have disarmed the guard enforcing the constraint the scrub exists to satisfy, and the tell was a PASSING test's expected output in the baseline log. All 43 real targets proved to be markdown, so the reference scrub could not change behaviour; the High-tier weight sat entirely on the codename edits. **AC7 proven, not asserted:** `gen:schemas` + `git diff --exit-code -- packages/protocol/schemas` is empty (14 schemas byte-identical) — the plan's original command was `pnpm run build`, which emits no schemas and would have produced a false green (caught by the pre-implementation plan review). **New gate:** `scripts/check-public-scrub.mjs`, the eighth root `check-*` seat, covering codenames (plaintext, word-bounded, case-sensitive), ancestor identifiers (hashed, reusing the knowledge guard's set — **repo-wide for the first time**; that guard only ever scanned the knowledge store), and references to the private strategy path with a by-file, reason-carrying allowlist. **Perf mattered: 61 s → 5.2 s** (67 M SHA-256 calls over 11.2 MB) via a per-file window cache and a token-alphabet prefilter whose alphabet was *verified by brute-force recovery, not assumed* — a gate that takes a minute is a gate someone deletes. **A mutation ESCAPED and changed the test's shape:** narrowing the prefilter to `/[a-z]/` passed all six tests, because both brute-forceable tokens are pure alpha and the longer ones are unrecoverable by construction — so no planted-sample test can ever reach them. Replaced with a contract test (read `TOKEN_CHAR` from the script's own source, require every published-alphabet character) plus a **differential equivalence test against a naive scan** (1,465,000 cases offline, zero mismatches; an `i = j + 1` off-by-one fails it). **Two gates had to be made reachable before they were worth having:** the flip term list returned 119 hits, so it was split into must-be-zero (13 terms, returns **0**) and review-only (4, never counted) — one ancestor name is the ordinary English word "guardian" that Snug's own legal pages must use, so that hash is excluded from the **tree-wide** scan only (pinned to exactly 1) while the knowledge-store guard keeps all seven; and the first split still returned 9 hits until a bare `sk-ant-api03-` term became a length-anchored `{90,}` pattern matching a real key and none of the 7 synthetic redaction fixtures. `gitleaks` over all refs: **no leaks found** (376 commits). **Stage 1.1 + 0.5:** the private strategy tree moved to a path outside any git work tree (byte-identical, 30/30) and the pre-scrub history bundled and **verified restorable** (clone reaches the known SHA with both purged commits and 76 files) *before* the local branch was deleted. **Gate 5 fresh-context review: no blockers**, and it independently re-ran AC7 and every suite count and differential-tested the skip loop. Its 3 MAJORs were all prose defects the scrub introduced — removing a label removed the referential work it was doing: `the other's` left with no antecedent in two files, and a definite singular in ADR-0017 that asserted there was ONE ancestor system. Four lessons distilled (guards must keep naming what they forbid; an unreachable gate buries real findings; a planted sample cannot prove a matcher's contract; deleting a label can delete a distinction). **Evidence:** protocol 346/346, auth 939/939, playground 1639/1639, knowledge 195/195, `turbo run test --force` **25/25 with 0 timeouts** (an earlier forced run's knowledge failure was chased to a load-timeout flake — 3/3 green isolated, clean `main` also 25/25 — on a test this task never touched), root `pnpm test` green with the guard reporting **OK** once this file retires.
- **TASK-20260824-centralization-lint-false-positive** — Anchor the centralization lint's `You are` marker; retire the stray `~/node_modules` · PR #129 (squash `c7218d1`) · **no ADR** (ADR-0004's doctrine untouched — only the precision of its tripwire moved; rationale lives in the test header, where the next reader looks) · **Low tier**. Two loose ends from the PR #125/#127 sessions. **(1)** The ADR-0004 lint had been RED since PR #125 on `apps/playground/src/legal/eula.ts`, whose DMG license text says "the version **you are** running" — `PROMPT_MARKER` matched `/You are/i`, the one marker in its set that occurs in ordinary English. Fixed the MARKER, not the prose: a path exemption for `src/legal/` buys one fix and creates an unlinted region, and the EULA string is byte-pinned to the shipped DMG SLA resource. `You are` is now anchored to a line/literal start; the other three markers stay unanchored. **The near-miss that became AC2**: the obvious `(?:^|\n)\s*You are` clears the EULA and is WRONG — `TEMPLATE_LITERAL` extracts literals WITH their backticks, so `` `You are Snug…` `` carries its marker at index 1, and that version would have traded a false POSITIVE for a false NEGATIVE on the most natural spelling of a real system prompt while the repo-wide test stayed green. Anchor class is `` [\n`] ``; found by running positive twins against the real extractor's output BEFORE the plan was written. Lint primitives moved to `__tests__/helpers.ts` so the new 10-row `centralization-lint-marker.test.ts` imports the REAL regex, not a copy. Red captured first (`3 failed | 7 passed`, exactly the AC4 prose rows); mutation-checked both ways — old marker reds 4 including the repo-wide lint, dropping the backtick reds **exactly AC2 while the repo-wide lint stays GREEN**, which is why the new spec has to exist. **(2)** Deleted the stray ``~/node_modules/`` (478 MB, 315 packages, 502 `.pnpm` entries) and its orphaned manifests — a tree belonging to no project (four lockfiles, three package managers; no home-tree project declared its deps; all ten sibling JS projects self-contained) that shadowed workspace resolution and had already broken the website build via `cookie@0.5.0`. Inventory preserved in the task journal (git history). `apps/website`'s `cookie: ^2.0.1` **STAYS** — correct regardless of the shadowing, and it protects other machines/CI runners. **(3) The deletion unmasked a real defect (AC9, owner-approved mid-task)**: `apps/playground` imports `zod` and declared it NOWHERE, resolving up into the stray tree; `desktop#build` compiles playground source via the vite alias, so it broke with TS2307. Declared `zod: ^4` (matching protocol/server); the ambient version had been **3.25.76** and the pin is **4.4.3** — a silent major bump under a security boundary (`chatCardSchema` is ADR-0031 §3's strict re-validation), so both APIs used were checked in v4 and the 21 card tests re-run. **The lesson worth the entry**: the first post-deletion root run exited 0 at 25/25 and was MEANINGLESS — turbo's `test` `dependsOn:["build"]` replayed a `desktop#build` cached from before the deletion, because turbo hashes only inputs INSIDE the repo, so a change to module RESOLUTION is invisible to it by construction. Only `turbo run test --force` told the truth. Three lessons distilled (outside-the-repo deletions invalidate in-repo caches; an ambient dependency is invisible until you remove what satisfied it; a matcher's subject is the TOKEN its extractor hands over) plus an **amendment** to the 2026-08-06 entry, which said an undeclared `zod` import is fixed "by removing the import, not adding the dep" — that remedy was scoped to a PHANTOM import, so it was amended in place rather than left to contradict AC9. `code-map.md` counts found stale on three rows (`knowledge` 183 — already stale by 2 before this task, playground 1471, db 416), all corrected by `pnpm run update-code-map`; **that script measures a real run, works, and is wired into no gate** — queued in next-steps as a candidate `--check` seat. Evidence: knowledge 195/195, `turbo run test --force` builds every package clean, root `pnpm test` exit 0 (25/25 + all seven `check-*`, threat-model 175/175, website-sync OK), `pnpm --filter website build` exit 0 (26 pages). Contention timeouts in db and knowledge dismissed with evidence — green serially, and `no-ancestor-tokens.test.ts` (which imports the edited helpers) timed against clean `main` at 822/831/785 ms vs 800 ms on branch.
- **TASK-20260824-first-signed-release** — First signed + notarized universal macOS release (v0.1.0) · PR #134 (squash `36fe45c`) · ADR-0047 §7 amendment · Implemented signing/notarization that §7 only claimed existed; `checkUniversalArchs` gates arm64+x86_64; the DMG needs its OWN notarization submission (a ticket is keyed to one file's hash) and the updater tarball must be rebuilt from the STAPLED app; EULA check moved after stapling (closes review F9). Updater key regenerated — the original was passphrase-protected with no recorded passphrase. R-29 partially resolved; TOFU half retained. Landmine: `release-out/` was unignored, so 35 MB of binaries are in history.
- **TASK-20260825-social-preview-og** — Absolute `og:image` sitewide + the social-preview assets · PR #136 (squash `dc3802d`) · **no ADR** (no decision made: the fix is a bug fix against the Open Graph spec, and `SocialMeta.astro` applies ADR-0048's existing single-homing doctrine rather than establishing new policy) · **Low tier** (`apps/website` only — a leaf in the dependency graph; nothing imports the website). **The bug was live in production**: `MarketingLayout.astro:28` declared `og:image` as the root-relative `/videos/poster-landscape.jpg`, and Open Graph requires an absolute URL — every real scraper DROPS a relative value rather than resolving it against the page URL, so every share of `snugprotocol.org` previewed imageless in the window before launch (verified by `curl` against the live site before the fix, and the site has been live since 2026-08-24). **Scope was 4 marketing pages, not 2** — `/privacy` and `/terms` also use `MarketingLayout` — plus 22 Starlight pages, including a Starlight-generated `404.html`; 26 in total, both shells. **The plan premise the built output corrected:** the plan asserted the docs shell emitted no OG tags at all, which is defensible from source (that one line was the repo's ONLY `og:`-emitting line) and wrong — Starlight generates `og:title`/`og:type`/`og:url`/`og:locale`/`og:description`/`og:site_name` **and** `twitter:card=summary_large_image` from a data array in `utils/head.ts`, omitting exactly one tag, `og:image`. So 22 pages advertised a large-image card pointing at nothing, and the planned fix — a `Head` override emitting the full set — **would have shipped duplicate meta on all 22**; the override adds the image tags only, and a duplicate sweep over all 26 built pages confirms zero. Shipped: `components/SocialMeta.astro` (the whole card set spelled ONCE for both shells, absolute via `Astro.site`, throwing at build time if `site:` is unset rather than silently re-emitting relative URLs), `components/Head.astro` (the Starlight override, same seam as `HeaderSocialIcons.astro`), `config/socialImage.ts` (path + REAL probed dimensions, single-homed beside `site.ts`), `scripts/build-social-previews.mjs` + six assets (two 1280×640 repo cards, the 1280×520 org banner, and their `.svg` sources — colours READ OUT of `tokens.css` so they cannot drift from the site palette; geometry proven from PNG IHDR bytes after writing, never trusted from the converter), and `docs/runbooks/social-preview.md`. **Tests assert over the BUILT `dist/`, not source** (427 new: socialMeta 208 · socialAssets 219), because neither shell's head is visible to a source grep and the two shells emit *different markup for the same tag* (`<meta ...>` vs `<meta .../>`); they pin absolute-and-on-our-origin, that the URL resolves to a file that actually ships, `og:url` matching each page's own route, and the declared dimensions matching the file's real pixels — plus the framework wiring (`site:` and the `Head` registration), whose one-line deletion silently blanks the docs half while every source-level check stays green. **Guards mutation-tested three ways:** reintroducing the relative path reds all 4 marketing pages, removing the `Head` registration reds all 22 Starlight pages, and making `OG_IMAGE.path` relative correctly *passes* (`new URL(path, Astro.site)` normalises it — the implementation being robust, not the test being weak). **Rider, owner-asked mid-session:** the org profile banner replaced a hub screenshot at `snugprotocol/.github@0450950` (a different repo, no branch protection, no task-file convention → committed to `main` without a PR, flagged rather than assumed); verified live at 200. Three lessons (a framework's `<head>` is not in your source — enumerate what it already emits from the BUILT output before overriding; SVG text neither wraps nor shrinks, so an overlong generated line renders off-canvas with every automated check green; a tool's success message is not evidence its output exists — `qlmanage` printed "produced one thumbnail", exited non-zero, and wrote nothing). Evidence: website 469/469 incl. `tsc --noEmit`, root `turbo run test --force` **25/25 with 0 cached**, `check-public-scrub: OK` by hand. **Owed after merge, both 🔑 and neither code:** the two repo uploads, which **must wait for the flip** — GitHub renders the Social preview control only on public repos (its doc: *"…or to a private repository to which you have previously uploaded an image"*), there is no API, and until then a shared link unfurls with GitHub's generic fallback card; and a **deploy**, without which the production card stays broken since the fix ships as HTML.
- **TASK-20260826-ci-restore** — CI back from dormancy and made to ENFORCE · PRs #141 (`c666e2b`) + #142 (`bcf0738`) · **ADR-0058** (supersedes ADR-0041's operative half) · **High tier**, task file written RETROACTIVELY at Gate 6 (it grew from "can we enable CI now?" into two High-tier changes without a moment that felt like a decision — the tell was the first CI run failing). **Billing was never fixed: going public dissolved it** (public repos get unlimited standard-runner minutes), confirmed by dispatching BEFORE editing anything (run `32931999710`) rather than restoring triggers and hoping. That run exposed two real failures, both FIXED not waived. (1) **Timeouts sized from measurement, not guesswork:** `no-ancestor-tokens` (brute-forces a few hundred thousand SHA-256s to *recover* a forbidden token rather than write it down — which is what lets the scrub grep reach zero) and `packages/db`'s crypto suites (`KDF_ITERATIONS = 600_000` PBKDF2 per container open) are **CPU-bound BY DESIGN** and crossed vitest's 5000 ms default: ~550 ms isolated → 3279 ms under a local turbo fan-out → **6501 ms on GitHub's 4-core shared runners, a 12–16× penalty**. Now 30 s. **Nearly fixed wrong:** `apps/playground/vitest.config.ts` documents that raising `testTimeout` was ALREADY tried on the analogous flake, *measured* not to help, and reverted — but that one was wall-clock synchronisation, these are starved CPU work; same symptom, opposite cause, and both configs now cross-reference each other. Mutation-proven still able to fail (planted broken hash set reds in 339 ms). **Also closed the long-standing `@snugprotocol/db` load flake** — same root cause, 3/3 loaded runs green after. (2) **Windows leg PARKED with Windows desktop** (ADR-0021 D8), removed rather than left red because it had **silently stopped being evidence**: since TASK-20260820 deleted `icons/icon.ico` it dies at COMPILE and never reaches the `keyReachable` assertion, so a real R-5 regression and a missing icon are the same colour — the exact "trains you to merge past reds" failure ADR-0041 removed the triggers to prevent, reintroduced by accident. Restore preconditions written into the matrix comment; **do not soften `keyReachable`**. Then `push`+`pull_request` restored and **`workspace` + `desktop-shell (macos-latest)` made REQUIRED**, contexts read from the **live check-runs API** (not typed from the workflow — `desktop-shell (macos-latest)` is *generated* from the matrix and a mismatched context blocks every merge with no diagnostic) and pinned to `integration_id: 15368` so another app cannot spoof a same-named pass. **Enforcement PROVEN**, not assumed: a probe PR was refused with *"the base branch policy prohibits the merge"*, then closed and its branch deleted. **`spec` deliberately got NO required check** — it has no workflows at all, so one would deadlock it permanently, SPEC_SYNC pushes included (symmetry between repos is not a reason to give them the same rule). `gate:local` keeps its job as the pre-push superset; AC3 parity 14/14 (it keys on job *ids*, not the OS matrix). Verified: root `turbo run test --force` 25/25, threat-model 175/175, PR-triggered and push-triggered runs both green (~5–6 min/leg). Open: Node 20 deprecation on three actions (v5 bump), a CONTRIBUTING.md line about the checks, whether `spec` gets minimal CI, and `strict_required_status_checks_policy: false` (right for a solo maintainer).
- **TASK-20260826-website-architecture-page** — Public "How it works" page (`/architecture/`) + fix the mobile nav amputation · PR #145 (squash `e8463be`) · **deployed to production 2026-08-26T07:06Z** · **no ADR** (no policy decided: ADR-0048 already owns the website's shape, and the nav-slot/disclosure calls are owner preferences recorded in the task journal) · **Low tier** (`apps/website` only — a leaf in the dependency graph). **Two independent things in one branch.** **(1) The page:** six hand-authored inline-SVG figures — runtime agent bridge · frozen envelope + swappable adapters · `connect-src 'none'` and the connected-fetch boundary · **Ledger vs. the aggregator model** · private/local-model mode · the portable `.snug` file — plus "Six choices that make it work" and a prominent **"What this doesn't do"** quoting five threat-model residuals (the model DOES see app data; R-5 Windows/WebView2; unrecoverable if both secrets are lost; auth broker unbuilt; packages pre-1.0), pinned by a test so a later copy edit cannot keep the claims and drop the honest half. Written for a cold public reader (Show HN and similar), so **Fig 4 concedes SimpleFIN is itself an aggregator** — the claim is "no intermediary that ACCUMULATES", never "no intermediary" (the most likely objection, conceded before it is raised). Token-only styling: no hex literals, **no font host**, keeping the zero-third-party-request property that makes the privacy page's "no analytics script" claim verifiable in the built output. **(2) The nav bug:** the header hid Docs/Spec/Playground below 560px with **nothing in their place** — unreachable on a phone for the site's whole public life, found by the owner on a phone, not by the suite. Now a zero-JS `<details>` disclosure carrying every destination; breakpoint **560→860px** (six items plus the wordmark do not fit an iPad portrait or a phone in landscape — the band `lessons.md` warns nobody tests), single-homed as a custom property with complementary `min-width:860px`/`max-width:859.98px` rules. Header `Download` REMOVED (owner call; landing keeps three other paths, footer keeps one everywhere; `/docs/*` and the legal pages accept no above-footer affordance). **Verified in a real browser — and again against production** — at 375/560/768/850/**859/860**/1024/1280px: exactly one nav at every width, **zero horizontal scroll everywhere**, disclosure operable by click and keyboard with a visible focus ring. **Two findings worth the entry.** **(a) A third recurrence became a test:** Astro trims the newline between running text and an inline tag, so `…has\n<em>no network…` ships as "hasno network" — three shipped into the first build, written by an author who had read the existing 2026-08-24 lesson. That entry was UPDATED rather than duplicated, and the rule is now a **test over built html** (`architecturePage.test.ts`), because three strikes proves prose cannot hold the line; it is also invisible to a screenshot review, which is how it survived one. **(b) AC11 was WITHDRAWN after being implemented:** the plan called for dropping `cookie` from `apps/website` as unused; `next-steps.md` (2026-08-24) records an owner decision that it **stays** — Astro imports `parseCookie` from `cookie@2` and the local declaration is a shim that wins Node resolution against a hoisted stray. Removed mid-session, caught, and reverted before commit. The trap: the local build is green **either way** (the stray tree was deleted 2026-08-24), so "it builds" was never evidence — **read `next-steps.md` for the artefact you intend to delete, not just the code around it**. Also fixed a pre-existing hard-coded `#1d1207` in `.local-banner` (byte-identical to `--ember-ink`; now the token, so it follows the light theme too), surfaced by the new no-hex guard. Evidence: website **520/520** incl. `tsc --noEmit`, root `pnpm test` **exit 0**, 27 pages built, CI green on both required checks, live-apex verification post-deploy with **0 `cdn-cgi` hits** (ADR-0013 re-confirmed).
- **TASK-20260826-demo-brain-clarity** — Make the demo brain unmistakable + invite BYOK/local honestly · PRs #147 (squash `3da8ef6`) + #148 (`1703674`, release-guard fix) + #149 (ADR-0047 credential amendment) · **ADR-0059** (ambient disclosure is permanent, derived from the routing decision, and scripted output is provenance-tagged at the turn) · **Medium tier** (`apps/playground` only — the desktop shell compiles the same source, so the work shipped to both) · **playground deployed to production + desktop v0.1.1 released 2026-08-26**, signed/notarized/stapled on both the DMG and the inner `.app`, universal, **auto-update proven end-to-end and the update walk owner-verified** — closing the two follow-ups the v0.1.0 release left open (the walk needed a SECOND release; `release-desktop.mjs` had never run start-to-finish). Its first true run found a real bug: a `TAURI_SIGNING_PRIVATE_KEY || ..._PATH` guard accepting a spelling `tauri build` cannot read, so `_PATH` alone passed in 3s and died after a full universal compile (fixed, lesson recorded). Post-merge design items stay QUEUED in next-steps, owner-prioritized.
- **TASK-20260826-backlog-hygiene-stale-findings** — Strike two already-fixed 2026-08-21 review findings (`healMissingTables`, `SNUGENC1` §11.1 stride — both closed by `a40302e`) + dated errata on threat-model R-11 and two sibling "CI billing-blocked" passages (ADR-0058) · PR #150 · **no ADR** · **Low tier**, docs only; root `pnpm test` green; task moved to `done/` in the same commit at the owner's ask so the change CIs once.
- **TASK-20260826-helper-bundle-update-sheet** — On-demand helper distribution + update-sheet fixes · PR #151 (squash `f1f2d58`) + PR #152 (release, `7ea419f`) · **ADR-0060**, ADR-0047 amendments · Helpers are separately released, pre-release-tagged, self-contained (own Node) GitHub artifacts, **content-pinned** in the shell and downloaded on one click — signature AND pinned sha256, admission-checked unpack, two-rename swap; `helper-whatsapp-sidecar-v0.1.0` published. Sheet shows only newer-than-installed notes; the "chopped off" dialog was `.shell-header`'s `backdrop-filter` making it the containing block for a fixed overlay → `ui/ConfirmOverlay` portals to body. Shipped in **desktop v0.1.2**. Plan review (8 blocking) + `/code-review` (40) folded; four release-script defects fixed with tests after they cost real builds.
