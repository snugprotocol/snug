# Done tasks — index

One line per finished task, chronological (append new lines at the bottom on merge). Full text of any entry lives in git history: `git log --all --oneline -- "docs/tasks/**/<slug>*"` then `git show <sha>:<path>`. Policy: [ADR-0027](../../decisions/0027-docs-memory-distilled-not-accumulated.md).

## Tasks

- **TASK-20260731-bootstrap** — Scaffold snug + spec repos with agentic process · no PR (direct-to-main bootstrap exception, `e7fc174`) · ADR-0001, 0002, 0003 · Both repos scaffolded, strategy embedded in internal/, pushed to private snugprotocol org.
- **TASK-20260731-build-hub** — Snug v1 hub umbrella — all packages, prompt architecture · no PR (pre-PR merge commits, `d5bf830`) · ADR-0004, 0005 · Six children specced, reviewed, merged same day; root 19/19 green, smoke PASS; spec push/eval harness deferred.
- **TASK-20260731-protocol-core** — Envelope schemas, constants, JSON Schema export (child 1) · no PR (merge `f093dc1`) · v0.1 frames/R1–R6/reply-parser/C1 helpers; review blocker (exported schemas contradicted R2) fixed; 74 tests.
- **TASK-20260731-knowledge-store** — Central prompt store, loaders, lint, skill-creator (child 2) · no PR (merge `e30861e`) · ADR-0004 · Store + typed loaders + goldens shipped; 3 review blockers fixed (KB db-field nesting would break persistence); 55 tests.
- **TASK-20260731-runner-sandbox** — Sandboxed iframe host + bridge (child 3) · no PR (merge `58d3efc`) · ADR-0006 · allow-scripts-only host, DOM-parsed CSP, navigation cutoff, browser-CSP suite template; 90 tests, C1/C2 review-verified.
- **TASK-20260731-sdk-db** — In-app hooks + per-app sql.js database (child 4) · no PR (merge `d5bb210`) · Dual-form hooks with KB≡SDK sync test; OPFS/IDB fallback, .sqlite round-trip; fixed usePersistedState key-change clobber; 70 tests.
- **TASK-20260731-server-adapters** — Reference server + provider adapters (child 5) · no PR (merge `298aef5`) · Fastify /invoke SSE + anthropic/openai/mock adapters; blocker fixed: C1 scan missed `responseSchema`; 87 tests, smoke PASS.
- **TASK-20260731-playground-hub** — Playground hub, examples, C2 real-browser gate (child 6) · no PR (merge `5b18db3`) · ADR-0005 · Vite SPA hub/builder/run + Inspector + 3 starters; Playwright C2 gate green; StrictMode blank-iframe fix.
- **TASK-20260803-portable-hub** — Portable user-owned hub: single user DB, serverless, sync, SSO · PR #1 · ADR-0007, ADR-0008, ADR-0009 · Six-child umbrella merged green; review caught dead OPFS sync sidecar (false divergence every login) plus three OPFS data-loss bugs.
- **TASK-20260803-userdb-core** — Single-user-DB core: schema constants, UserDb service, driver face · PR #1 (umbrella) · Protocol DDL constants; UserDb over one sql.js handle — versioning, corrupt-quarantine, secrets-strip export; composes existing driver guardrails.
- **TASK-20260803-serverless-run** — Serverless run, provider+model picker, local LLM · PR #1 (umbrella) · byok/local/subscription modes, Ollama endpoint, state moved into user DB; old IndexedDB/localStorage stores abandoned pre-launch.
- **TASK-20260803-versions-chat** — App versioning UX + persistent chat, client-authoritative · PR #1 (umbrella) · Artifact sink pins app ids; chat persists in user DB; subscription writes client-authoritative; artifact cards not persisted (limitation).
- **TASK-20260803-sync-origins** — SyncProvider, sync loop, Dropbox adapter, export/import UI · PR #1 (umbrella) · Hash-gated pushes, pull-merge preserving secrets, divergence surfaced; new device + non-empty origin requires explicit applyRemote, never auto-pull.
- **TASK-20260803-hub-sso** — Google OIDC, per-user DB provisioning, /userdb, static hosting · PR #1 (umbrella) · Fail-closed CORS/CSRF posture, CAS-revision /userdb endpoints; first login provisions the user row only — never clobbers local.
- **TASK-20260803-spec-v02** — Spec v0.2 draft: Portable User Database Format · PR #1 (umbrella) · Draft prose staged from userdb-schema.ts plus docs/changelog; publication withheld, executed later by AL-13 (PR #8).
- **TASK-20260803-living-apps** — LLM-designed app schemas, app chat, factory pins, wiki · PR #2 · ADR-0010 · Four-child umbrella; adversarial review caught sqlite_sequence id-reuse blocker; schema/doc tools direct-mode only, server twins queued.
- **TASK-20260803-userdb-v2** — Schema v2: native app tables, registry, pins, docs · PR #2 (umbrella) · Verbatim-DDL registry materializer, rename-based write-back, C2 injection negatives; v1→v2 migration structural only — data abandoned (owner-approved).
- **TASK-20260803-schema-doc-tools** — schema_apply + app_doc_write tools, compounding prompts · PR #2 (umbrella) · Store-sourced tool prompts, sink pre-pin, docs-on-every-change doctrine; direct-mode only — subscription twins deferred to next-steps.
- **TASK-20260803-hub-retrofit** — Marketplace dedup, SSO surfacing, design pass · PR #2 (umbrella) · Find-or-open installs end duplicate installs; identity chip on every page; logout now rebuilds sync loop; return path hardened.
- **TASK-20260803-hub-ops** — Long-run builds, observability, app delete, LLM-free apps · no PR — direct merge to main (`f37feff`) · ADR-0011 · Iteration ceiling 6→48 plus 30-min lifetimes fixed silent build deaths; cascade delete; step timeline later retired (PR #4).
- **TASK-20260803-app-context-chat** — App-attached chat, durable pins, factory reset · PR #2 (living-apps umbrella) · Context assembler with caps, durable thread pin (F10), bootstrap = v1-artifact turn (F9), docs panel, factory reset.
- **TASK-20260804-hub-polish** — Identity & brand, watch-it-think rail, build-fork fixes · PR #3 · Five-reviewer adversarial round found six defects behind a green suite (false-green e2e, vacuous AC18 guard); 826 tests.
- **TASK-20260804-observability-caching** — Live LLM observability, prompt caching, brand polish · PR #4 · ADR-0012 · Caching is per-turn, not per-adapter — review caught both inverted scope bugs; inspector memory bound repaired; 906 tests.
- **TASK-20260804-logo-variants** — Logo exploration; owner picked Variant C "The Ember Niche" · PR #3 (shipped via hub-polish) · Design-scratch record; the mark lives in `Logo.tsx`, favicon, and the desktop icon generator (closed 2026-08-15).
- **TASK-20260805-doctrines-devex** — Doctrine ADRs, code-map regenerator, two queued fixes (AL-01) · PR #5 · ADR-0013, ADR-0014 · Zero-backend and local-first-credential doctrines recorded; import tombstone cache-coherence and supportsCaching exact-host fixes; spoof-proof count parser.
- **TASK-20260805-auth-core** — Dynamic Auth part 1: the pure core (AL-02) · PR #6 · New packages/auth — 5-kind schema, custody byte-probes, DI-pure OAuth, userdb v3; review fix: no-write-session migration now persists.
- **TASK-20260805-alpha-umbrella** — Alpha umbrella AL-01…AL-15 · PRs #5–#8, #19–#23, #28–#32 · ADR-0016 · 10/14 children merged; AL-06 delivered later as the Desktop Hub (PR #41). AL-10/11/12/15 HELD for owner manual tests, AL-09 parked — tail extracted to next-steps and closed 2026-08-15.
- **TASK-20260806-auth-wizard** — Dynamic Auth part 3: inferrer, wizard, render directive (AL-04) · PR #22 · Directive is doorbell, never authority; live sweep caught popup-blocker launch blocker; wizard later rebuilt by Dynamic Auth v2 (PR #34).
- **TASK-20260806-auth-kb** — Teach builder LLM auth declaration, connected-API design (AL-05) · PR #23 · Retrieval-tested KB teaching; review caught false security-transparency claim; live build emitted exact directive; post-merge findings folded via PR #24.
- **TASK-20260806-starters-pillars** — Five pillar starter apps (AL-08) · PR #7 · Five keyless starters shipped; found `<form onSubmit>` dead in the C2 sandbox — shipped habit-tracker was broken too, repaired.
- **TASK-20260806-spec-push** — Spec v0.1 + v0.2-draft pushed to snugprotocol/spec (AL-13) · PR #8 · Push executed, clone-verified byte-identical, auth content grep-excluded; stale `HEAD.lock` in spec repo blocked the first commit.
- **TASK-20260806-flip-prep** — Flip-public prep: community files, good-first-issues, staged runbook (AL-14) · PR #19 · Community set placed, real issues #9–#18 opened, breadcrumb sweep done; runbook lives only in untracked `internal/` — owner must copy.
- **TASK-20260806-webllm-spike** — WebLLM in-browser brain behind experimental flag (AL-07) · PR #20 · ADR-0015 · `?webllm=1` adapter with demo fallback; default model Llama-3.2-3B — 1B fragments files, Qwen3-1.7B fails the quality gate.
- **TASK-20260806-connected-fetch** — Envelope net capability — Dynamic Auth part 2 (AL-03) · PR #21 · `useConnectedFetch` + confirmed host-ceiling fetch, 1 MiB net-frame class; phantom zod dep made the executor unloadable (BLOCK-MERGE catch).
- **TASK-20260807-al05-review-fold** — Fold post-merge AL-05 adversarial-review findings · PR #24 · Five live KB-truthfulness findings (M62–M66) fixed test-first, two refuted; teaching must stay true about shipped machinery.
- **TASK-20260807-protocol-whitepaper** — The Snug Protocol whitepaper PDF · PR #26 · 22-page A4 PDF + 70-check conformance script, 7 defects fixed; spec-repo push deliberately deferred to an explicit ask.
- **TASK-20260807-connection-reachability** — Chat-less apps gain a path to become connected · PR #30, #32 · ADR-0016 · Install-act declaration resolver + `app_declared` strong-review ladder replaces AL-09's rejected design; MAJOR-1 UX residue queued to AL-10.
- **TASK-20260807-starters-auth-spectrum** — AL-09: five auth-spectrum starters · closed-by-harvest via rewrite P4 (PR #34) · Branch parked at `86a564c` (origin only) as a harvest source, never merges by owner decision Q8; real-provider verification still queued (closed 2026-08-15).
- **TASK-20260810-dynamic-auth-rewrite** — Dynamic Auth v2 umbrella: the v4 requirement/grant world · PR #33 (plan), #34 (P0–P5 chain) · ADR-0017 · Owner redirect Q1–Q9 executed; v3 surface + `snug_auth_specs` deleted (closed 2026-08-15).
- **TASK-20260810-p0-contracts** — v4 contracts, template lint, slot-keyed storage (P0) · PR #34 · spec-v0.3-auth carve-out staged; both review BLOCKERs closed (closed 2026-08-15).
- **TASK-20260810-p1-runtime** — Slot routing, amended gate order (P1) · PR #34 · AC5(b) closed by citation, re-verified in P5 after P3 deleted the cited file (closed 2026-08-15).
- **TASK-20260810-p2-pipeline** — Build-time requirement declaration at the post-turn seam (P2) · PR #34 · Unreachable-pipeline BLOCKER fixed; inferrer's production caller landed in P3 (closed 2026-08-15).
- **TASK-20260810-p3-wizard** — The grandma wizard (P3) · PR #34 · v3 surface deleted, OAuth popup REINSTATED on the v4 slot-keyed row, M9 discard-confirm built (closed 2026-08-15).
- **TASK-20260810-p4-starters** — Registry entries, six manifests, `llmProposalSchema` deleted (P4) · PR #34 · Provider data accuracy later re-verified live by PR #42 (Coinbase CDP, CoinGecko, OpenWeather) (closed 2026-08-15).
- **TASK-20260810-p5-security-close** — Threat-model delta, whole-surface review (P5) · PR #34 · Residual risks documented in ADR-0017 §Residual risk; URL-borne credential decision routed to AL-10/11 (closed 2026-08-15).
- **TASK-20260811-lean-runtime-data-chat** — Lean runtime turns + intent-routed app data chat · PR #35, #36 · ADR-0018, ADR-0019 · Persisted runtime contracts replace authoring-context turns; intent-routed SQL data chat; seven post-merge fresh-review findings fixed after.
- **TASK-20260812-registry-authoritative-auth** — Registry-authoritative auth shapes + connect-error surfacing · PR #37 · Registry kinds/fields authoritative, connect never silent; repro refuted the planned chain — wrong-KIND rows were the live defect; test scripts tsc-gated.
- **TASK-20260812-auth-kind-choice** — Multi-option auth-kind resolution — the user decides · PR #38, #39 · ADR-0020 · Host defaults and discloses provider auth options, user rebinds; Guard 2b variant-aware; fixed Coinbase wrongly triggering OAuth.
- **TASK-20260812-app-reply-parse-failure** — Never cap structured replies; stopReason end-to-end · PR #40 · AC2 delivery loss convicted upstream, client exonerated by `stream-fidelity.test.ts`; retry-policy + AC4 array decisions extracted to next-steps (closed 2026-08-15).
- **TASK-20260812-desktop-hub-scaffold** — Tauri 2 Desktop Hub (roadmap A6) · PR #41 · ADR-0021 · Native fetch, file userdb, loopback OAuth, in-shell C2 gate; Windows WebView2 later ruled structurally blocked (D8, gate RED).
- **TASK-20260812-desktop-auth-awareness** — Desktop-aware dynamic auth: platform truth, provider repairs · PR #42 · ADR-0022, ADR-0023 · Coinbase CDP repair, Spotify opener URL-scope fix, query credentials, Hue LAN connector; three owner hardware verifications left open.
- **TASK-20260813-ui-polish-inspector** — Wordmark, desktop icon, resizable inspector, connection UX · PR #43, #44 · All 11 ACs shipped; merged `--admin` over the documented D8-red Windows baseline; dock-icon eyeball check still queued.
- **TASK-20260814-hue-pairing-e2e** — Hue pairing verifies before claiming connected · PR #46 · ADR-0025 · Wizard now pulls and verifies the bridge key, killing the reopen ghost flow; owner hardware-confirmed on the real bridge.
- **TASK-20260814-hue-starter-real-connection** — Connection-relative addressing; Hue starter drives real bridge · PR #47, #48 · ADR-0026 · Apps address user-supplied hosts via connection-relative URLs (C3 spec-sync); starter drops all mocked rooms/controls.
- **TASK-20260815-docs-context-cleanup** — Distill the agentic memory (compaction policy) · PR #49 · ADR-0027 · docs 2.1MB→0.5MB: lessons −78% / next-steps −84% (all content re-verified), done/→INDEX, 11 stale active tasks closed, 9 ADR statuses amended, auto-memory 18→4; owner confirmed `llmProposalSchema` retained on purpose.

## Handoffs & working artifacts (all spent)

- **AL-04-plan-predraft** — Gate-2 plan pre-draft (v0) for AL-04 auth-wizard · PR #22 (the task it fed) · Predraft written during AL-03; superseded by the v1 plan in TASK-20260806-auth-wizard, which merged via PR #22.
- **AL-04-review-verdict.json** — machine review verdict for AL-04 · working artifact, spent with PR #22.
- **HANDOFF-alpha-umbrella** — Alpha umbrella resume point #1 (TASK-20260805-alpha-umbrella) · PRs #5–#8, #19–#20 status ledger · Written at credit cutoff; AL-03 mid-final-gate chasing an inspector lead; spent, superseded by handoff #2.
- **HANDOFF-alpha-umbrella-2** — Alpha umbrella resume point #2 · PR #21 (AL-03 merged) · AL-04 implemented, FIX_FIRST review pending (1 blocker + 3 majors); corrected #1's child-count off-by-one; superseded by #3.
- **HANDOFF-alpha-umbrella-3** — Alpha umbrella resume point #3 · PR #22 (AL-04 merged) · AL-04 merged after 15/15 verified fixes; AL-05 queued next; spent, superseded by #4.
- **HANDOFF-alpha-umbrella-4** — Alpha umbrella resume point #4 (marked SPENT in-file) · PR #23 (AL-05 merged) · AL-05 merged with marquee KB-taught directive proof; owner amendment "AL-09 then stop"; its AL-09 instruction later reversed (parked).
- **HANDOFF-alpha-umbrella-5** — Alpha umbrella resume point #5 · PR #28 (landed docs-only) · AL-09 parked after seam design failed 3-lens security review; connection-reachability task born; all tail HELD; superseded by #6.
- **HANDOFF-alpha-umbrella-6** — Connection-reachability resume point (handoff #6) · PR #29 (landed docs-only) · Plan v2 ratified: apps never propose connections at runtime (direction C); validate-rule fork landed; skeleton unbuilt; superseded by #7.
- **HANDOFF-alpha-umbrella-7** — Connection-reachability resume point (handoff #7) · PR #30 (eventual task merge) · Walking skeleton complete, T8 e2e proven end-to-end; breadth items unstarted; superseded by #8.
- **HANDOFF-alpha-umbrella-8** — Connection-reachability resume point (handoff #8, final) · PR #30, #31, #32 · ADR-0016 · Feature complete; implementation review's 2 MAJORs fixed (both-versions vouch, untested glob); task merged+closed 2026-08-08.
- **TASK-20260815-spotify-scopes-wizard-links** — Spotify starter 403 fix: registry-pinned scopes + provider-reason banner + pinned-URL console links · PR #51 · ADR-0028, ADR-0029 · Scope-less token was the 403 (owner hardware-verified fixed live); scope changes re-consent set-based with pre-promotion token invalidation (both OAuth kinds); banner quotes the provider's scrubbed reason; console links key on kind-matched registry-pinned bytes, any provenance; closes the Spotify desktop sign-in + real-PKCE verification items.
- **TASK-20260815-coinbase-ed25519** — Coinbase connects with portal-default keys: `cdp_jwt` moves ES256→EdDSA (Ed25519) · PR #53 · ADR-0030 (supersedes ADR-0022 §2) · The registry steered users to the legacy EC key type the portal no longer defaults to, while `es256-key.ts` rejected Ed25519 — the portal default — by design; `ed25519-key.ts` accepts every portal secret shape (armor-first, canonicalized to the seed, total runtime rule), secret field renamed `ed25519_private_key` as the drift-invalidation lever with pre-promotion orphan deletion (posture inversion), probe chain made total (AC5) and "connected" earned by a passing probe (AC6); owner AC8 hardware test (Ed25519 re-credential → portfolio) tracked in next-steps.
- **TASK-20260815-starter-portfolio-revamp** — Umbrella: dynamic provider-API chat + inline cards + starter shelf rebuild · PRs #55-#58 · ADR-0031 · Owner interview reset the read/write posture (crypto read-only rule removed; sandbox+bridge+executor gates ARE the wall); three sequenced children delivered A→B→C as a stacked PR chain; authoring-provenance requirement (mid-task owner ask) shipped gate-enforced.
- **TASK-20260815-provider-chat-lane** — Child A: provider chat lane — LLM-composed API requests through the UNCHANGED ten-gate executor · PR #56 · ADR-0031 §2, spec-changelog INTERNAL-DRAFT · provider_read/provider_write + compile-checked laneForIntent map (silent answer-lane default dead); connection FACTS only in context (public hosts taught, LAN symbolic-only, dotted-decimal RFC-1918 render scrub); confirm FIFO queue, app-shell dialog mount, reference-identity abort-deny (Gate-5 MAJOR); shared session-remember scope pinned (accepted F5); threat delta authored; plan+implementation each fresh-context reviewed with all findings resolved.
- **TASK-20260815-inline-cards** — Child B: inline choice cards + provider write-confirm as a chat card · PR #57 · ADR-0031 §3 · present_card tool (strict bounded schema, one per turn), picks persist + return as plain USER messages (zero new authority); chat-origin confirms render inline via WeakMap origin tagging with modal fallback when no surface is mounted (Gate-5 MAJOR); busy-gated picks + click-time row id (Gate-5 MAJOR); bidi sanitation + "the agent is asking:" provenance line; recorded gap: hook-level pick-to-next-turn e2e (browser-e2e sweep).
- **TASK-20260815-starter-apps-rebuild** — Child C: the gold-standard connected shelf (10 apps) · PR #58 · 9 starters retired; trade-copilot ported byte-verbatim from the owner's DB (independently re-verified) + TWAP smart-order surface (confirmed governed POSTs, deterministic idempotency, no-double-fire probed); Rewind/Moodboard/Should I?/Standup agent-built with honest scope adaptations; AC9 authoring-provenance gate in the validate suite (mutation-checked); real-browser design pass (20 shots, durable at ~/Snug/design-pass-20260815-starter-shelf/) caught the shipped DEFERRABLE reserved-word bug; playground+auth cross-package repoints with OBSOLETE/MIGRATED classifications; owner hardware passes queued in next-steps.
- **TASK-20260816-whatsapp-twin** — Linked-device connections + WhatsApp Twin · PR #60 · ADR-0032/0033, spec-changelog INTERNAL-DRAFT · first `linked_device` auth kind (appended, never inserted) + the derived sidecar route contract; the helper is a CAPABILITY not a host — the Gate-2 loopback draft was withdrawn as unsafe AND unstorable (host-granular ceiling, LDH-only host rule), replaced by a purpose-built Rust command over a unix socket (port squatting unrepresentable) with `/pair/*` wizard-only behind a SEPARATE command (a flag is a claim the caller makes); `StandingApprovalGate` wraps the session gate rather than widening it (a `(appId,host,method)` key cannot tell one thread from another) — built and enforced, arming surface deferred by owner, so Twin ships manual-reply only; AC12 new-reader scrub (pseudonymised JIDs/phone numbers before every LLM turn — third parties never consented) and AC13 export parser (bidi controls, multiline bodies, tombstones); macOS shell gate GREEN incl. `ipc-sidecar-fetch-refused` in a real WKWebView; **owner-verified end to end on hardware, which found EIGHT integration defects past a fully green tree** — every one a seam between independently-tested parts (`docs/solutions/2026-08-17-eight-seam-defects-in-one-feature.md`).
- **TASK-20260817-telepath** — Telepath: the WhatsApp client rebuild · PR #63 · ADR-0034/0035, spec-changelog INTERNAL-DRAFT · the Twin POC became a full client (iOS-style live inbox, images + avatars, unread badges, AI drafts in the user's measured voice/emoji, first-run→incremental analysis on a watermark, six local charts, auto-respond removed). Platform: three app-reachable sidecar routes (`/events` long-poll, media, picture) + **the host live pump** — push forwards INVALIDATIONS not content, because `hostEvent` frames ride the 256 KB class and `post()` drops oversized frames SILENTLY and carry no `instanceId` (Gate-2 blocker, reshaped the design); pump reads through the SAME governed executor, epoch-tokened against StrictMode. Privacy boundary went bidirectional (persisted label map, names restored locally, photos never sent). `installStarterDocs` seeds the app wiki + verbatim build prompt at install (ADR-0035). Closed the next-steps positive-twin item: `ipc-sidecar-fetch-dispatchable` — a negative check had passed while the command was UNREGISTERED. **The owner's hardware walk found three defect waves past a fully green tree** — an invisible rebuild (shelf tiles showed FOLDER names, also hiding four other renamed starters), missing contact names/group rosters/avatars plus **LIDs rendered as phone numbers** (a confident wrong number is worse than a blank), and a wedged session disguised as "still syncing" (the ambiguity WAS the defect; now `needsRelink`, carried on `/chats` because an empty list is where it bites). Merged on LOCAL verification (turbo 23/23, cargo 81, macOS gate green) — GitHub CI was billing-blocked, not failing. Owner action owed: re-pair the linked device. Recorded gap: an installed starter never receives a rebuild (`install_source` identity) — needs a version-upgrade path.
- **TASK-20260818-sidecar-shutdown** — Stop the WhatsApp helper when the shell exits · PR #65 · owner-reported "the app loses connection across hub restarts". **The credential layer was innocent** (approved row + minted token byte-identical to the helper's own copy + `status:connected` all persist correctly): the fault was that `sidecar_ctl('stop')` was implemented, tested and reachable with **zero callers** and no exit hook, so quitting orphaned the child, the next launch spawned a RIVAL against the same Baileys auth store, and two writers of one session directory half-register the link — which reads to a user as "relink me" and is why the earlier wedge kept returning. Confirmed live: two helpers 17 min apart, then a third. Adds `sidecar::shutdown` (infallible, idempotent, recovers a poisoned mutex rather than leaking a process) on `RunEvent::Exit` — chosen over the prevented-and-deadlined `CloseRequested` DB-flush handshake — plus a SOURCE-level pin, because no unit test can drive Tauri's real RunEvent and a hook nobody calls is the untested wire in its purest form. cargo 81→84, desktop 129, turbo 23/23; CI billing-blocked so evidence is local. Recorded gap: `linkVerifiedAt` is written and never read.
- **TASK-20260817-per-app-model-selector** — Per-app LLM model selection + run-header icon buttons · PR #67 · ADR-0036 · each app may PIN its own model and all four app-scoped lanes route there (app-frame turns, builder, app-attached chat, and the two inference call sites that have an app id) through ONE `resolveModelForApp(appId)`; **inheriting is an ABSENCE, not a copy**, so an un-pinned app FOLLOWS later changes to the Settings default and "pinned" stays distinguishable from "inherited"; resolution is PER SEND because RunView/useBuilderChat memoize their transport+agent. **The explorer pass's suggested `RuntimeContract` home was REFUSED** (D1): a host-side user preference there would be version-linked (reverting code would revert the model choice) and would push the change through `packages/protocol` — instead a namespaced `snug_settings['appModel:<appId>']` key means no schema bump, no migration, no spec-changelog (D2), at the price of an explicit `deleteApp` cascade named in the same change. Header cluster became icon buttons (model select → ⚯ connections → ⤓ export): **an emoji is not an icon** (🔌 ignored `currentColor` and read as disabled — caught only by a screenshot, both versions passed every test) and **a glyph is not an accessible name** (two e2e specs locate export by `name: 'export .sqlite'`, kept verbatim as `aria-label`). A pre-existing ordering test was MIGRATED, not deleted. Every guard mutation-checked incl. each routing lane independently; real-browser walk confirmed the pin on the WIRE (pinned app sent `claude-opus-5`, un-pinned sent the default) plus 44×44 icons at 1280px and 420px. Merged on LOCAL verification (turbo 23/23 `Cached: 0 cached`, seven forced root runs) — **GitHub CI was billing-blocked, not failing**: jobs never started, and every recent `main` run failed identically. Owner action owed: restore Actions billing. Recorded residual: the connection-requirement inferrer has no app id (build-time, before one is settled).
- **TASK-20260818-registered-flag** — The wedge detector was destroying working sessions · PR #68 · **my own bug from TASK-20260817, and a destructive one**: `isHalfLinkedStore` read `creds.registered !== true` as "pairing never finished", but in baileys@7.0.0-rc14 that flag is written by EXACTLY ONE site (`Socket/messages-recv.js:940`, the phone-code flow) — the QR flow's `configureSuccessfulPairing` writes `me`/`account`/`signalIdentities`/`platform` and leaves it at its `false` default. Every session this helper creates is QR-paired, so the detector fired on every HEALTHY session; the app said "re-link"; re-linking ran `shouldResetAuthStore`, which DELETED the working session; repeat. The owner went round the loop twice. Now detects the MATERIAL a completed pairing leaves (`account` + non-empty `signalIdentities` beside a saved `me`) — flow-agnostic, and fixtured from the owner's REAL creds.json (redacted) so the regression cannot return silently. sidecar 100→102, turbo 23/23; CI billing-blocked so evidence is local. **Process failures recorded rather than buried**: no task file existed while the work was done (Gate 1 skipped under urgency — written retroactively at Gate 6), and a blanket `git add -A` on a branch cut from a dirty `main` swept the owner's in-flight `TASK-20260817-per-app-model-selector` (31 files, ADR-0036, 36 tests) into the squash — verified intact and complete on `main`, attribution unrewritable without force-pushing; see next-steps.
- **TASK-20260818-ledger-starter** — Ledger: the personal-finance flagship + SimpleFIN token-claim connections + the open-url capability · ADR-0038 · **High tier, full six-gate walk with a fresh-context plan review that returned 3 blockers pre-code** (token-claim mis-homed in protocol → registry union, zero protocol bytes; a two-host ceiling that would have broken symbolic addressing AND the declared probe; the `/simplefin` base path made a checked invariant). Phase A: third `WellKnownPairing` variant `token-claim` (paste one-time token → wizard decodes → frozen-ceiling gates → claim POST → access-URL parse → ADR-0025 verify → write-together with `claimVerifiedAt`); `performTokenClaim` = the third NAMED network seat (allowlist widened deliberately); wizard paste screen + family refusal + no-re-claim gate. Phase B: `examples/ledger` — sample household with PLANTED leaks (radar-provable ≥3 flags), time machine with projection branches, five agent lanes, 15 extracted-core tests, full authoring provenance with both owner prompts verbatim. Phase C: open-url capability (internal-draft frames + published host-ready flag + value-blind runner seam with NAMED refusal + confirm dialog with punycode host and synchronous noopener open — popup escape proven on a REAL tab via its own scoped Playwright project) + cancel playbooks + feed-verified cancellations. **The owner's first real walk found two defects past 23/23-green-twice**: the pinned host was a 302 ALIAS (`bridge.` → `beta-bridge.simplefin.org` — the probes had hit beta all along; fix included host drift joining the migration gate, which was fields/seats/scopes-BLIND, and Ledger going connection-relative because installed starters never receive rebuilds) and the executor's 15 s wall clock killed the first 13-month pull as transport noise ("Request canceled" — now 60 s and self-naming). Lessons: pin the host you PROBED; a bound the code arms must name itself. Residuals in next-steps: owner's full real-data walk, shared connection custody, starters-connect pre-existing reds.
- **TASK-20260818-sidecar-orphan-reap** — The WhatsApp helper is now a singleton: reap orphans, die with the shell · PR #73 · ADR-0037 addendum · **The fix from TASK-20260818-sidecar-shutdown covered only half the exits.** `shutdown()` reaps the helper on a CLEAN quit; a crash, `kill -9`, or a `tauri dev` rebuild runs no hook, so the helper outlived its parent and the next launch's autostart spawned a rival against the same Baileys auth store — and WhatsApp allows ONE live connection per linked device, so the two replaced each other's stream in a permanent `stream:error … conflict type=replaced` loop from launch. Owner-reported with the evidence already in the log: two pids, 60490 at ppid 1 (orphaned 18:23) against the supervised 50057. **Both ends of the lifecycle now answer.** The helper watches its own parent (`parent-watch.ts`) and on reparenting exits through the SAME clean shutdown SIGTERM uses, so the ADR-0037 §1 final flush still runs — keyed on a CHANGED ppid, not `ppid === 1`, because a Linux subreaper adopts orphans to some other pid. The shell records the pid beside the socket AFTER the survival check and reaps a stale one BEFORE spawning; **order is the fix** (reaping after leaves both live long enough to ping-pong), and that half is what retires orphans from OLDER builds, which have no watch of their own. `stale_helper_verdict` is pure and is where the danger concentrates: **pids are recycled**, so a Kill requires the live process's command line to name THIS shell's helper entry — killing by number alone would take out the user's editor, worse than the bug it repairs; every reap path clears the pidfile, since a stale claim is what makes a recycled pid dangerous next time. Tests first: `parent-watch` (6) + cargo orphan-reap (8, incl. a live bystander SURVIVING the real reap and a source-order pin that the reap precedes the spawn); cargo 97 / sidecar 152 / desktop 129, clippy clean, `sidecarContract` untouched. Verified on REAL processes both directions — an orphaned helper self-exited ~1 s after `kill -9` of its parent and removed its socket; a look-alike was reaped (`Terminated: 15`) while a planted-pidfile bystander lived. **CI was billing-blocked again** (jobs never started — 2 s, zero steps, annotation names failed account payments; every recent `main` run fails identically), so the evidence is local; owner action still owed: restore Actions billing. Deploy note carried from lessons 2026-08-18: the helper was rebuilt and reinstalled to `~/Snug/helpers/`, but a running process keeps the code it loaded at spawn — the fix goes live on the next desktop restart. Windows leaves `command_line_of` answering `None` (so it never kills), consistent with ADR-0021 D8.
- **TASK-20260819-desktop-icon-squircle** — Two-master icon pipeline: mac squircle + windows plate, dark niche · PR #75 · The shipped dock icon was wrong three ways and each had a distinct root cause: opaque **white** canvas corners (`omitBackground: false` baked the Chromium page background into the Playwright screenshot), a **white** arched niche (a fill-WINDING bug — the `NICHE` backing path's inner subpath knocked itself out under nonzero, so the page showed through), and zero transparent margin (the deliberate 2026-08-13 full-bleed instruction), so macOS drew an oversized square with white chips instead of a squircle. Key finding: **`tauri icon` only RESIZES its source** — it adds neither margin nor squircle, so both must be baked into the master, and one invocation takes one source ⇒ two masters means two runs with selective keep. Mac master composes on Apple's grid (1024 canvas, artwork 100..924, ~22.4% continuous-corner radius, `omitBackground: true`); windows master stays full-bleed on an opaque `#171310` plate; the six shipped files are copied explicitly from the right run. The niche is now a solid single-subpath layer behind the ember path — winding-proof rather than winding-dependent. Owner decisions: Apple's ~22.4% radius over the brand mark's native ~28.6% (dock consistency wins; the brand shape stays canonical everywhere else); niche `#171310` (the dark-theme `--bg` the web knockout shows); full-bleed superseded for **mac-facing files only**. Tests first per platform (16 assertions, **9 red** against the shipped set — the white niche pinned as `rgba(255,255,255,255)` at the sampled centre, the outcome-not-proxy lesson from 2026-08-04 applied to the same file that earned it). desktop 128/128, macOS gate GREEN, `docs/code-map.md` corrected. **CI billing-blocked** (0 steps executed, annotation names failed account payments) so evidence is local — merged on owner's explicit call. Owner action still owed: the dock eyeball on a real built bundle, which closes the standing 2026-08-14 item "the icon has never been seen on a real dock/taskbar" — the pixel assertions cover AC1–AC5 but no test can cover how it reads beside neighbour apps.
- **TASK-20260819-starter-sample-data** — Sample mode + authoring-doc completion for the four connected starters · PR #76 · ADR-0038 pattern · Rewind, Trade Copilot, Moodboard and Telepath all opened onto empty states (skeletons, "not connected yet" panels, sidecar status screens), so nothing showed a prospective user what the app was FOR before connecting. Each now renders a deterministic, clearly-bannered sample experience. **All four shipped render-only** — no sample row is ever written to a real DB, so there is nothing to evict and the connected paths are untouched. This included a deliberate plan deviation: Trade Copilot's TWAP history was planned as DB-seeded with `sample=1` + wholesale eviction (mirroring Ledger), and shipped as constants instead — a strictly stronger guarantee for the no-impact-on-connected criterion, with zero schema/migration risk and no eviction machinery to get wrong. Telepath never writes sample rows by design (real JID keyspace stays clean; sample content never enters the LLM/analysis path or the pseudonym label map, and its analysis is a canned constant rather than a live run). **The Gate-5 AI review (8 finders) caught four real defects the green suite did not**, all of the same family — *a sample surface shown in a state its author never pictured*: Moodboard rendered the sample home for ALL offline bridge phases and flashed it during `loading` (three predicates had drifted apart; unified on one); Telepath had no real-data guard, so a linked user's helper crash showed fictional chats beside real ones, and `sampleJid` was never reset, so a later helper blip could replace a REAL open thread with the sample one; Trade Copilot's `isSample` misfired for previously-connected installs during outages (now a durable `connected_once` flag in an additive `app_meta` table, not a fetch-error inference). The fourth was in the tests themselves — the banner assertion was a vacuous whole-file `/sample/i` and the render-only scan missed components outside the markers; now proximity-checked per banner plus a line-level scan. Red first: `sample-mode.test.mjs`, 20 red. Final: examples 261/261, playground 1244/1244 (121 files, tsc-gated), starters e2e 5/5. New authoring docs (spotify + hue lessons/next-tasks, whatsapp next-tasks) ingest through the ADR-0035 glob with no code change. **CI billing-blocked** so evidence is local — merged on owner's explicit call. Accepted residuals: already-installed starters never receive rebuilds, so installed copies (including the owner's linked Telepath) report `html_mismatch` against the new factory bytes and that surface still has no renderer — sample mode reaches NEW installs only. Owner action owed: walk the four surfaces on desktop (they are desktop-only/env-gated, so no test covers the visual).
- **TASK-20260819-connection-failure-ux** — Honest Spotify scopes · the failure diagnosis moves into the wizard · a readable Ledger playbook · ADR-0028 amendment · **High tier, full six-gate walk; the fresh-context plan review returned 2 blockers pre-code and both were real.** Three owner-reported defects on one seam — what the user is told when a connection is, or merely looks, unhealthy. **(1)** Rewind raised the red auth-repair alarm on every launch of a *working* Spotify connection: it makes one `GET /v1/me/player/recently-played` per session, the pin omitted `user-read-recently-played`, and the host observer fires on any delivered credentialed 403 with no way to know a refusal was expected. Pinned the scope (ADR-0028 §4 amended, not edited — it is accepted; README's "deliberately omits" paragraph corrected), which also switches on a COMPLETE recently-played lane the app already shipped unreachable. **(2)** The maroon full-bleed banner moved off the app page into the wizard as a **derived attention gate**, with a quiet ember chip left in the run header. The plan had proposed a new `ConnectionWizardStep` member; the review showed `ConnectionWizardSheet.tsx:1842` rejects exactly that in a "WHY NOT NEW STEPS" block, and traced three breakages it would have caused (`showDiff` is step-keyed and would go false; `nextStep` early-returns `'done'` for LAN rows ABOVE its switch, so a LAN row with a live 403 would skip review; three unproven-row catch-alls key on `step !== 'review'`). Owner decisions: the staged diff **outranks** the gate (the diff is the cure — and this task creates that exact collision for every existing Spotify user, so AC12 pins it), and the failure is **copied onto the session** before the store is cleared (clearing alone would leave Step 0 with nothing to read). **This REVERSES TASK-20260813 AC10** — recorded as a decision changing, with `connectionSurfaces.test.tsx` rewritten to assert the new placement rather than silenced. **(3)** Ledger's cancellation playbook was unreadable: `.planbox` was the FOURTH flex child of `.leak`, so the merchant column (`flex:1`, the only shrinkable child) collapsed and the step text overprinted it. The card now stacks, steps are a number/text grid so wrapped lines align under words. **The most valuable finding came from the owner re-reporting (3) as still broken after it was fixed and screenshotted**: installing a starter COPIES `app.html` into the user library (`RunView.tsx:410`) and the run path reads that snapshot forever (`:457`) — the fix could not reach an installed copy, and neither exit works (`delete` cascades irreversibly to the app's data/chat; "reset to factory" restores the pinned v1 of the INSTALL). The owner's own screenshot proved it before any code was read: it showed a `dashed` border, a token the new CSS contains only inside a comment. This generalises the residual TASK-20260819-starter-sample-data logged; queued in next-steps with the mechanism that already exists (`saveAppVersion`). **Also classified the 2026-08-14 playground vitest flake**, whose failing test the note said was never captured: it is FILE PARALLELISM — 1254/1254 serial, versus a different failing set each parallel run (14/15/31 observed, all timeouts in React-rendering files) on clean `main` too. Tests first, each confirmed red for the right reason; AC2's fixture deliberately frozen at the OLD seven-scope consent because the existing scope-drift test would have passed unchanged against the unfixed registry. Final: auth 880/880, playground 1254/1254 (serial), examples 270/270, tsc clean, both restructured starters re-parsed for JSX balance. **Accepted residual, recorded in the ADR amendment**: token invalidation happens on APPROVAL, so a user who dismisses the re-approval diff keeps the old seven-scope token and the chip returns on every launch for them; the app cannot self-gate (C1 — it never sees the token, scopes are host-side).
- **TASK-20260819-gmail-starter** — Inbox Copilot: the Gmail inbox-manager starter · PR #80 · ADR-0039 · **High tier** (the registry pin), full six-gate walk; the fresh-context plan review returned 3 blockers pre-code and all three were real. The 13th shelf app and 8th connection declarer: four lanes over a Gmail account — Pulse (twelve-week trend, top senders, category donut, all hand-drawn SVG), Needs You (senders with 3+ received and zero replies, **excluding transactional senders and anything starred** — that exclusion is the feature, since a naive rule flags exactly the mail a person most needs), Do This (assistant cards that run in-page behind one confirm), Mass Cleanup (multi-select → one governed batch). **Gmail was resolvable but UNCONNECTABLE** — right endpoints, hosts, PKCE and redirect posture, yet no scopes (a token that reads nothing) and no `fields` (the wizard renders `fields ?? []`, so a credential screen with zero inputs); `next-steps` item (7) had recorded the dead-end and nothing failed, because nothing tested that an entry is *sufficient* rather than merely present. Pinned `gmail.modify` + `gmail.settings.basic` + `gmail.send` and **deliberately withheld `https://mail.google.com/`**, the only scope permitting permanent deletion — so "this app never deletes your mail" is a property of the minted token rather than a promise in the code (D3), which is ADR-0028 doctrine extended: a pin can be load-bearing for what an app CANNOT do. The `client_secret` pin came from a **live probe, not the docs** (Google's native-app table calls it "Optional"; the token endpoint refuses a Desktop-client exchange without it) — copying Spotify's honest "PKCE needs no secret" line would have shipped a wizard that completes consent and then dies at the exchange. Unsubscribe is channel-split by what the sandbox can honestly do: `mailto:` → one confirmed `gmail.send`, `https` → the open-url bridge behind an https-only/no-userinfo gate; RFC 8058 POST is structurally excluded by the frozen ceiling. Desktop-only (a Desktop-app OAuth client registers loopback redirects only). **The browser pass found what no suite could**: the readiness check read `app.ready` where the hook returns `isReady` — a typo'd property is `undefined`, not an error, so the app sat on its loading skeleton forever with every gate green (the shape validator checks structure; the extracted-core suite evaluates pure functions and never renders). Guard added and **proved non-vacuous** by reintroducing the bug: a lint deriving the hook's real property set from the SDK. Also fixed there: the trend chart baselines on the quietest week (a zero axis drew a flat line and hid the variation) and the sender bars reserve a slot for a replied-to correspondent, so the green legend describes a colour actually on screen. examples 254, auth 895, playground 1265, turbo 23/23; **CI billing-blocked** (jobs never started — the annotation names failed account payments, identical to #79) so evidence is local, merged on the owner's explicit call. Owner action owed: a live Gmail connect on desktop and one governed cleanup batch on real mail.
- **TASK-20260819-inbox-copilot-fixes** — The verified-connection refresh prompt + three Inbox Copilot defects · PR #80 · Owner testing found four things, one architectural. **(1)** A verified connection left stale sample data on screen: the wizard *proves* a connection works and then discards that knowledge (the probe outcome is local `useState` in `DoneScreen`), while `RunnerHost` lives only in `RunView`'s ref and the wizard is mounted as a SIBLING — so there was no path between them. Added `state/appHosts.ts` as that seam (the wizard should not hold a frame reference; the run view should not know wizards exist), with **token-scoped registration** against the StrictMode mount(A)/mount(B)/unmount(A) ordering, where a naive `delete(appId)` would evict the LIVE view and present as "the prompt does nothing, sometimes". The Done screen now offers "load my real data", **gated on VERIFIED not saved** (reusing `awaitingProbe` — TASK-20260815 AC6's "connected is earned, not declared"; OAuth kinds qualify on arrival since the minted token IS the round trip) and only when a frame is actually mounted. Reuses the **existing** `connection-event` host-event — the `event` field is an open bounded string and ADR-0026 explicitly reserved it for "a future live status event" — so **no protocol change and no spec-sync**; the payload is an INVALIDATION only, never data, because host-event frames carry no `instanceId` to verify a sender and an oversize frame is dropped silently (ADR-0034). **(2)** Unsubscribe emails bounced: the raw message had `To:` and `Subject:` but no `From:`, so Gmail accepted the send and the receiving MTA rejected it — a silent failure with a success message on screen. Now resolved from `users/me/profile` and REQUIRED (the builder returns null rather than producing a header-less message), with header values truncating at the first CRLF since a `List-Unsubscribe` header is attacker-controlled and a break would turn an unsubscribe into a Bcc. **(3)** Refresh was hard-wired to 90 days; now selectable (1 week / 90 days / 6 months / 1 year / everything) where "everything" omits the date clause rather than faking one, an unknown setting NARROWS to the default rather than widening to a full-mailbox scan, and the chart axis follows the window (twelve buckets beside a one-week pull would draw a collapse the data never showed). **(4)** The ask lane returned prose; it now renders verified rows — a table is only promised when every sender the agent names matches the app's own data, so a hallucinated address degrades the whole reply to prose instead of drawing authoritative-looking empty rows. **A fifth defect surfaced while verifying**: bridge errors are OBJECTS, so `result.error || 'fallback'` rendered the literal "[object Object]" at six call sites — replacing the one thing a user needs (what happened, and whether retrying helps) with noise, exactly when they are wondering whether their mail is safe; guarded by a source sweep, since a formatter unit test cannot catch a call site that never uses it. Deferred deliberately and recorded: the `useHostEvents` SDK hook (this is the THIRD hand-rolled consumer — the trigger its own next-steps entry named — but promoting it bumps the byte-locked block in all 13 starters) and adoption in the other five sample-mode starters.
- **TASK-20260819-inbox-copilot-persistence** — Inbox Copilot keeps its synced mail · PR #80 · Owner-reported: connect, sync, close the app, reopen — and it is showing the demo inbox again. **The app had NO persistence at all**: it never called `useAppDB`, so every synced message lived in React state and died with the frame, and the only thing stored was the window preference. That is wrong on Snug's own terms (ADR-0007: the user owns their data in one portable file) and it re-downloaded the whole mailbox on every launch against a quota. Now on Ledger's pattern (ADR-0038): `messages` + `sync_runs` with a `sample` provenance column, bootstrap ordered schema → load → **seed the demo ONLY if the file is empty** so a returning user's own mail appears before anything is fetched, and wholesale eviction of demo rows on the first real sync so a mailbox is never half sample and half real. Cleanup deletes rows locally as well as at Gmail — without it, trashing 400 emails looked right until the next launch brought them all back out of the file. **A zero-result sync is never committed**: a throttled or refused fetch returning nothing would otherwise delete a real mailbox out of the user's file and revert them to sample data permanently, turning a transient failure into data loss. **Two derivations had to move from session-phase to data, and the second was only visible in a browser**: `isSample` (the reported flip) and `connected` — after real rows loaded correctly, a returning user was still told "Sample inbox — connect Gmail", and the header count came from a per-session `syncState.fetched` so it read "0 emails" over a full mailbox. Verified end to end against a stub host with a REAL sql.js database surviving an iframe reload: seed 585 sample rows → sync real ones (585 evicted, run recorded) → relaunch shows "4 emails from the last 90 days" with no banner and "Last read … · last 90 days" → then a FAILING sync, which leaves all four rows and the real view intact. **Process failure recorded rather than buried**: one scripted multi-anchor edit asserted on its second anchor and wrote NOTHING while printing nothing to say so, so I tested a binary without the change in it and spent several probes diagnosing a stub-host bug that did not exist — apply such edits one anchor at a time, or make the failure loud. Owner action owed: connect real Gmail, sync, fully close and reopen, confirm the mailbox is still there.
- **TASK-20260820-threat-model-v1** — The consolidated threat model + three credential-leak fixes · PR #82 · ADR-0021 D8 addendum · **High tier** (the deliverable is a public security claim about C1/C2), full six-gate walk. `SECURITY.md:47` promised a `docs/threat-model.md` that did not exist — the flip-public gate's box, and the first thing a hostile reader opens. **The analysis existed (1,129 lines across eight deltas) and consolidating it was NOT the valuable part: auditing it was.** Five fresh-context adversarial agents, one per surface, checked ~50 claims against the code with instructions to break them. The deltas held up unusually well — most claims are enforced with negatives asserting *zero fetches* rather than a failed result, and every spot-checked file:line citation was accurate — but **three credential-leak paths surfaced that eight deltas and their green suites had all missed**, each fixed test-first in its own commit before a line of the model was written. **(1)** The WhatsApp sidecar send returned 28 lines ABOVE the mutating-confirm gate, under a comment asserting the gate "has answered" — so every send reached the helper with credentials injected and no confirmation, voiding ADR-0033's standing-approval governance for the one connection class that ADR exists to govern. Green because every sidecar test hardcoded `confirm: () => true`, making a gate never consulted indistinguishable from one that granted; the harness now spies and asserts the `slot`/`body` a downstream policy decides on. **(2)** OAuth token-endpoint error bodies reached the iframe AND the LLM unscrubbed — 500 raw chars, on the path where the executor's own refresh POSTs `refresh_token` and `client_secret`, so a provider echoing submitted parameters (ordinary debugging behaviour) leaks both. Green because all fifteen `NET_AUTH_FAILED` assertions read `code` and none read `message`. The scrubber could not have caught it: injection THROWS before a candidate set exists, and the values are POST body params that were never injected headers. **(3)** My own fix for (2) was INCOMPLETE, caught at Gate 5 — an allowlist of error-envelope SHAPES treated as a value guard, when shape recognition picks which FIELD is forwarded and never vouches for its contents (`{"error_description":"bad token rt-…"}` sailed through, which also kills the narrower "only forward recognized fields" fix since that IS the field). Then a second layer: `JSON.parse` decodes `\u` escapes INSIDE the extractor, after the caller's scrub, reconstituting the secret character-for-character — **and the observer seat had the same hole**, reaching the wizard's attention gate. Fixed in the shared module rather than twice at call sites, since the decode is that function's own act. **The document** follows two rules: a promise with no named enforcement is not an invariant (every §5 row names an enforcement file and a regression test; anything that could not carry both moved to residuals rather than being softened), and §6 has equal standing with §5 — 164 lines against 60, because a threat model listing only wins reads as marketing. Residuals the deltas never disclosed, now stated: the desktop main window ships with `csp: null` so host-page XSS is immediate full-IPC (R-1 is worse on desktop than the browser it was written for); macOS-only is enforced by DOCUMENTATION not the build (`targets: "all"` still ships `icon.ico`) while its only regression detector is a CI leg the billing block made unreadable (R-5b); and **WhatsApp pseudonymisation is documented "at the turn's altitude" but implemented inside the sandboxed example app — an app-layer convention, not a boundary** (R-9, the residual a reviewer should weigh most heavily; any other app with an approved sidecar row can forward raw names and numbers to a model). **ADR-0021 D8 addendum**: the macOS-only decision existed only as task-file prose while five documents described D8 as OPEN and one said the trigger resolves to "Electron fallback"; recorded with the owner's scope — through alpha, beta AND 1.0, Windows reconsidered post-1.0 — and all five reconciled. **AC2's checker** hash-pins all eight deltas (a new or EDITED one fails until the model is re-read against it) and proves every cited enforcement/test path exists; mutation-proven three ways. **Its limit is stated against its own interest** after the Gate-5 review found three residuals dropped while it was green: a hash proves a delta has not MOVED, never that its residuals were CARRIED (now R-22..R-25, incl. SimpleFIN wrong-account binding and drift comparing COUNTS not ROWS). AC7 closed one claim that outran the code — the whitepaper's "a frame carrying `allow-same-origin` fails the build" was true only for `packages/runner/src`; `check-sandbox-guard` makes it true workspace-wide, reading the sandbox VALUE rather than banning `setAttribute` so the desktop C2 gate's own probe frames still pass. Both checkers wired into `pnpm test` AND CI — removing R-11's cadence residual for them rather than documenting it. One Gate-5 finding was REJECTED on evidence (a reviewer reported turbo passing 23/23 while auth was red; probing with a deliberately failing test showed it reporting `Failed: @snugprotocol/auth#test` and exiting 1 — it had sampled a mid-edit tree; its other five findings were all real). Code-map regeneration surfaced pre-existing drift far beyond this task (playground recorded at 1028 vs an actual 1265) and the auth cell was stale at 729 vs 915 because its format `(729, tsc-gated)` did not match the generator's `(N total)` marker — rewritten so the number is MAINTAINED rather than hand-copied. Final: root `pnpm test` green uncached — 23/23 turbo tasks + 130 threat-model checks + 4 sandbox-guard checks, auth 915. **CI billing-blocked** (annotation confirmed: jobs never started), merged on local evidence per the standing condition. Follow-ups queued in next-steps, highest-value first: build-enforced macOS-only, and moving pseudonymisation into the host pump.
