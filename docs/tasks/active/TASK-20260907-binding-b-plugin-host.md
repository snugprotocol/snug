# TASK-20260907-binding-b-plugin-host: Binding B as a plugin-bundled local process — the network executor for the host kit

- **Status**: in-progress — **plan APPROVED by the owner 2026-09-07 ("yes go ahead"; Q1–Q5 = the recommended defaults)**; plan review before Gate 4, then Gate 3
- **Owner**: Jeetu
- **Risk tier**: **high** — threads a new `fetchImpl`, `oauth` and `userdbBackend` into the platform seam `connectedFetchDepsFor` consumes (C1 paths); a loopback data plane in front of the credential executor; a spawned child that runs the user's own `claude` CLI. Auto-escalated per PROCESS.md; plan review with finder angles before Gate 4; explicit self-sign-off at Gate 5.
- **Branch**: `feat/TASK-20260907-binding-b-plugin-host` off `main` `6d9feda`
- **Packages touched**: `apps/host-mcp` (NEW — the process), `apps/host` (second build: `snug-host-local.html` + `src/local/*`), `apps/playground` (`platform/copy.ts` local-host custody arm; `platform/platform.ts` one additive `CustodyState.heldBy?` seat), `packages/auth` (**imports only** — `isForbiddenNetHost`, `scrubAuthValues`, no source change; **D-B14 dropped the OAuth-transport imports** — the web popup path needs none), **not** `packages/knowledge` (see D-B12 — the `instructions` text lives at `apps/host-mcp/src/instructions.md`, because `gen-content.mjs` would compile a prompt-store copy into the committed `content.ts` and thence into every bundle), `scripts/` (`build-plugin.mjs`, `check-host-mcp.mjs`, `gate-local.mjs` e2e leg), root manifests' SOURCE (`scripts/lib/plugin-manifests.mjs`), `docs/`
- **Spec impact**: none — wire protocol v1, `net-request`/`net-response`, `snug-app-bundle/1` and the userdb schema unchanged. `PersistenceKind` unchanged (the page uses the existing `'file'` kind over an HTTP `FileBackendFs`).
- **Related**: parent program `TASK-20260904-skill-only-snug` (this task **re-scopes its T3 row**; D4, D5, D6, D7, D12, D15); ADR-0065 §2 B (amended by this task at Gate 6), §3, §4, §8, §10 and its "MCP server as the host" alternative; ADR-0066 A9 (page tools, not a remote server — the same reasoning forbids a data-plane tool here); ADR-0061 (never "built on MCP"); ADR-0021 (the desktop's native side is the structural precedent — this process is that side, in Node, on loopback); ADR-0032/0033 (a helper reached over a unix socket, every route credentialed); ADR-0045 (hand-in rules); ADR-0059 (disclosure chips); ADR-0063 (`snug-app-bundle/1`); `docs/security/threat-model-delta-desktop-shell.md` (the 41420 listener row + residuals 1 and 3), `…-whatsapp-sidecar.md` (unix socket, 1 MiB cap while reading, spawn nonce); the parked sibling `TASK-20260906-host-brain-page-tools` (its branch carries only its task file — nothing to rebase on); the not-started `TASK-20260904-claude-brain` (Option C).

## Spec (what & why)

**What.** Binding B (ADR-0065 §2 B — the local host, the ONLY binding with connected apps) is delivered as a **plugin-bundled local MCP server** rather than a launch script the user runs by hand. When the `snug` plugin is installed in Claude Code, Cowork (desktop) or Codex, the host spawns `scripts/snug-mcp.mjs` at session start over stdio. The process has exactly three jobs: (1) **serve the kit on loopback** — a second build of `apps/host`, `snug-host-local.html`, with connections compiled in, opened in the user's browser, the user's file at `~/Snug/user.snug` on disk; (2) **be the network side of the page's executor** — the page's `connected-fetch.ts` stays THE seat that reads a credential and calls fetch with all ten gates unchanged; what crosses to the process is the platform's raw `fetchImpl` (the seam Snug Desktop fills with `plugin-http`), the `oauth` loopback transport (the seam Snug Desktop fills with `tauri-plugin-oauth`) and the `userdbBackend` file seam (the seam Snug Desktop fills with `read_user_file`/`write_user_file`) — the process is Snug Desktop's Rust side written in Node and reached over `127.0.0.1`, nothing more; (3) **expose a four-tool control plane to the agent** — `snug_status`, `snug_open`, `snug_hand_in`, `snug_list_apps`. No data-plane tool, ever. The `claude -p` brain shim (program D5) lands in the same process as a module, reached by the page through the T2 brain seat.

**Why.** Apps built in the kit get connected fetch, with or without credentials (API keys, OAuth, signed requests), through one proxy the plugin ships, and the browser's CORS ceiling disappears exactly as it does on desktop. Spawn-and-connect is a channel every target host already offers plugins; nothing here makes Snug "built on MCP" (ADR-0061) — the proxy and the file are plumbing, and product copy stays "Snug apps run inside Claude Code / Cowork / Codex". Product copy never says "Snug MCP server".

**Hard rules (each becomes a test or a lint — the brief's list, restated as the contract):**

- **C1 stays where it is.** The executor is neither moved into Node nor duplicated. `/fetch` receives the request AFTER the page's gates and injection and is value-blind: it never logs, persists or echoes a URL, header or body; every error message passes `scrubAuthValues` (imported from `@snugprotocol/auth`) against the injected header values it saw before it returns. Defense in depth on the process side because anything on the machine can open a loopback socket: https-only (no LAN rung in this task — below), `isForbiddenNetHost` (**imported, never a second copy**), no redirect followed (`node:https` never follows one; the 3xx is returned as data), the 1 MiB cap **while reading**, the 60 s wall clock that names itself. The executor's own gates run twice — not a new policy (so NO resolved-address check: the executor's gate 5 is literal-only, and a resolved-IP rule would be a new policy the desktop does not have either; the DNS-rebinding residual stays where the threat model already records it).
- **The LLM is never a network principal.** `tools/list` returns exactly `snug_status`, `snug_open`, `snug_hand_in`, `snug_list_apps` — an allowlist test fails on any addition. The data-plane routes (`/fetch`, `/oauth/*`, `/userdb/*`, `/v1/*`, `/events`) live ONLY on the TCP loopback listener and require the page's bearer + a same-origin proof; the control plane (attached sessions, the human CLI) lives ONLY on a `0600` unix socket and carries no bearer. **The bearer appears in no MCP message, no tool result, no lock file and no log** — `snug_open` makes the PROCESS open the browser; the URL a tool result shows never carries the fragment.
- **Loopback only, authenticated, origin-bound.** Bind `127.0.0.1` on the **fixed port 43127** with an ephemeral fallback (D-B13: the OAuth redirect URI is `${origin}/oauth/callback` and a user registers that string in a provider dashboard, so the origin must survive a restart; a busy 43127 is a named condition that falls back to an ephemeral port and DISABLES the OAuth rows with a reason, never silently changing a registered URI). A per-launch 256-bit bearer rides to the page in the launch URL **fragment** (never the query), is stripped from `location` before the router boots, and must be present on every data-plane call (`Authorization: Bearer …` — the SSE stream is read with `fetch`, never `EventSource`, so the header rule has no exception). `Origin`, when present, must equal the served origin; **when absent the bearer alone admits the call** (review F2: Safari implements no `Sec-Fetch-*`, and `Origin` is omitted on same-origin GET, so an `Origin`-or-`Sec-Fetch-Site` rule would 403 every GET — `/events`, the userdb read leg, `/status` — on the default browser of a Mac). `Sec-Fetch-Site`, when present, must be `same-origin`. The bearer is the guard; the origin rule is the second proof where the browser offers one. **`Host` MUST equal `127.0.0.1:<port>` — reversed by review F1 (D-B18).** Measured this session with headless Chromium under `--host-resolver-rules=MAP evil.test 127.0.0.1`: after a rebind the attacker's page is same-origin in the browser's eyes, its GET carries **no `Origin` and no `Sec-Fetch-Site` at all**, and the ONLY header naming the attacker is `Host: evil.test:59901`. A browser always sends the name from the URL bar and a page cannot forge it, so the `Host` check is the one thing that refuses a rebind — and "never consulted" would have left the data plane open to any domain the attacker points at 127.0.0.1. No CORS headers are sent at all (the page is same-origin; a foreign page's response stays unreadable AND refused). **Every data-plane route MUST require a header or content-type that forces a CORS preflight** (the bearer alone does it) — this is load-bearing, not incidental: a route reachable by a *simple* request would let a foreign page fire the side effect blind, unable to read the answer but able to cause it. `Sec-Fetch-Site`, when sent, must be `same-origin` and never merely `same-site` — measured: a different port on 127.0.0.1 reads as `same-site`, so accepting that would open every other loopback service on the machine as a launcher. Missing bearer → 401, wrong `Host`/origin → 403, both bodies empty. Never `0.0.0.0`.
- **One process, one file.** `~/Snug/host/lock.json` `{port, pid, tokenHash, startedAt, socket}`; a second session attaches over the unix socket instead of spawning; the primary lives while ANY attached session lives and exits within the stdin-close grace once the last one is gone, releasing the lock and the socket. The user file follows `createFileBackend`'s discipline over a `FileBackendFs` the process implements with the desktop's rules (bare-name charset, temp + fsync + rename + dir fsync, absence = ENOENT only, the one-byte-discriminant read); if Snug Desktop is running, the process opens **read-only and says so** in the custody chip. Never a byte of state under `${CLAUDE_PLUGIN_ROOT}`.
- **OAuth = the WEB popup path over the process's own origin (D-B14, changed by review F1 — NOT the desktop transport).** The page leaves `platform.oauth` **undefined**, so the wizard takes its web branch: the blank-popup gesture escape (`ConnectionWizardSheet.tsx:1289`) is preserved, the popup-blocked and popup-closed backstops stay live, and the redirect URI is the existing `${window.location.origin}/oauth/callback` — a route the app already routes (`App.tsx:275`) and the PROCESS serves, delivering over `BroadcastChannel` same-origin exactly as on the hosted playground. **Why the plan's original (install `platform.oauth` + `createDesktopOAuthTransport` + a 41420 listener) is wrong:** `getPlatform().oauth !== undefined` is the wizard's ONE discriminator for "not a browser" and flips three behaviours at once — it skips the pre-opened popup, installs a handle-less pseudo-popup with no null check and no closed-poll (`connectionWizard.ts:2610-2648`), and swaps the register copy. On a real browser page `openExternal` can only be `window.open`, which by then has lost transient activation (the click awaits `crypto.subtle` for PKCE + the state HMAC), returns `null`, and the wizard parks on `awaiting_callback` forever with no error — the precise failure the web branch's `popup === null` arm exists to prevent. So `SNUG_DESKTOP_OAUTH_PORT`/41420 and `createDesktopOAuthTransport` are **out of this task**; the process needs no OAuth listener at all, and `desktopRedirectPosture` is not consulted (the web `webRedirectPosture: 'origin-callback'` seat is). The registered URI is `http://127.0.0.1:43127/#/oauth/callback`'s non-hash twin — pinned by D-B13's fixed port and shown on the register screen as today.
- **D4 holds.** `snug-host.html` and (later) `snug-host-micro.html` still ship `connections:false`, so no door renders and `host-ready.net` is false STRUCTURALLY (D-B33: the executor's bytes are in every build — the exclusion is runtime, not build-time; D4 governs what a binding claims and does). Connected apps exist ONLY in the `local-host` build, which only this process ever serves.
- **Owner acts stay owner acts.** No `npm publish`, no `@snugprotocol` scope registration, no marketplace submission, no push to the distribution repo, no `gh release`, no deploy. Prepare and print; never perform.

**Acceptance criteria** (each becomes at least one test; Gate-2 additions marked ⊕):

1. **Spawn and connect.** With the plugin installed from a local marketplace path (`claude plugin marketplace add <dir>` → `claude plugin install snug@snug-local`), a fresh Claude Code session shows the server connected (`/mcp`); `tools/list` returns exactly the four names; `initialize` carries `instructions` = the launch-protocol text, byte-identical to `apps/host-mcp/src/instructions.md` (D-B12; `check-host-mcp` owns the compare, and T6's skill build reads the same file). Note: claude.ai chat drops `instructions`; Claude Code and Codex read it.
2. **Serve.** `snug_open` opens `snug-host-local.html`; the probe reports `binding:'local-host'`; `connections` allowed; the brain chip says "Claude · your CLI" (or "demo brain — no host brain found" when no `claude` binary is on PATH); the custody chip says "on this Mac — ~/Snug/user.snug".
3. **Connected fetch, no credential.** The `weather` starter's (or the e2e stub's) `net-request` reaches a stub HTTPS server THROUGH the process; the page's executor gates and the process's re-checks are both observed to run (the process exposes a per-request check ledger to the e2e through its stderr-free status counters — never a URL); response headers obey the protocol whitelist; the 1 MiB cap trips on an oversize body with a named code.
4. **Connected fetch, with credential.** An API-key starter (Ledger's shape via the e2e stub requirement) completes the wizard, stores the credential in the page's `auth:` secrets, and its injected request reaches the stub through the process. A canary credential value appears in exactly one place on the wire — the outbound request from the process — and in no log, lock file, MCP message or tool result (grep-tested: the process's stdout/stderr capture, `~/Snug/host/*`, the MCP transcript, every tool result).
5. **OAuth.** The wizard's OAuth journey completes through the WEB popup path against `fake-idp.mjs`, with the process serving `/oauth/callback` on its own origin and the delivery riding `BroadcastChannel` (D-B14). Assert on the WIZARD's connected state, never the popup's navigation (the journey-4 flake, next-steps 2026-08-26/2026-09-03). A 401 on a connected call triggers the executor's single refresh-retry through the same `/fetch` seam — the refresh POST (a `URLSearchParams` body, review F6) and the retried call both observed at the stub. Negative twin: `platform.oauth` is asserted UNDEFINED on the local page, so the pseudo-popup branch is unreachable.
6. **Negative security.** No bearer → 401 (empty body); wrong bearer → 401; foreign `Origin` → 403; no `Origin` and no `Sec-Fetch-Site` → 403; a private/loopback/link-local/`.local` target → refused with `NET_INVALID_REQUEST`-class code before any socket opens; `http://` → refused; a 3xx is returned as data, never followed; `tools/list` allowlist; **no debug/log-everything env var exists** (a test enumerates every `process.env` read in the release bundle against an allowlist of exactly `HOME`, `PATH`, `SNUG_HOME` (test isolation of `~/Snug`) and `NODE_EXTRA_CA_CERTS` is Node's own — and asserts the test-hook env name is ABSENT from the release bundle's bytes); the control socket refuses a data-plane path; the TCP listener refuses a control-plane path.
7. **Single instance + custody.** A second spawn attaches (same port, no second listener, `snug_status` from both reports the same `startedAt`); the primary survives the first session's stdin close while the second is attached and exits within the grace after the last; a stale lock (dead pid, or a live pid whose socket does not answer with the lock's `tokenHash`) is taken over; with Snug Desktop running (`snug-desktop` on the process table — the only holder signal that exists today), the page REFUSES TO OPEN the db and renders a surface naming the holder (D-B24 — an opened-but-unsaveable page loses the user's work silently); killing the host closes the process within the stdin-close grace and releases lock + socket.
8. **Hand-in.** `snug_hand_in({bundle})` delivers a `snug-app-bundle/1` to the open page over `/events`; the page applies it under ADR-0045 exactly as Binding A does (unedited-only auto-apply; edited copy offered; tombstone honoured; a bundle carrying connections is a hard refusal — pre-checked in the process for a clear tool error, decided by the page); with no page connected the bundle is queued (bounded) and the tool result says so.
9. **Brain.** The `claude` shim (`/v1/chat/completions`, bearer-gated, same origin) spawns the user's own `claude -p` with D5's flags — never `--bare`; child env stripped of `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` (⊕ and `ANTHROPIC_BASE_URL`, `CLAUDE_CODE_*` overrides — enumerated in a constant, pinned by test); reached through the T2 brain seat with `localAdapter({ baseUrl, apiKey: bearer })`. ⊕ **No `local`-mode change is needed** (see D-B7) — Option C has nothing to rebase on from this task.
10. **Codex parity, documented.** `.codex-plugin/plugin.json` + `.mcp.json` emitted from the one constants module in the `npx -y @snugprotocol/host-mcp` form with the absolute-path interim documented; Cowork desktop walked once by the owner (where the process runs, whether `open` works) and journaled.
11. ⊕ **Release inertness.** The shipped `dist/snug-mcp.mjs` contains no test hook: the string sweep for the test-resolver env name and the `lookup` injection symbol is a gate (`check-host-mcp`), the desktop's `gate:release` transposed.
12. ⊕ **Kit twins stay honest.** `check-host-kit` is unchanged and green (the local build lives in `apps/host/dist-local/`); a new structural check on `snug-host-local.html` proves connections ARE compiled in (positive twin: the wizard door renders under `local-host`) while the artifact kit's absence test stays green.

**Out of scope:** the `codex`/`hermes`/`openclaw`/`ollama` shims (T3 remainder); Binding C; ChatGPT; Windows; any wire-protocol, frame or userdb-schema change; a remote/hosted MCP server; any identity provider; moving the credential store out of the page; a raw-fetch or SQL tool for the agent (a rule); **the LAN rungs** (`lanFetch` pinned-TLS and the `lanHttpPrivate` plain-http rung — the page carries neither seat, so the executor's own named refusal answers a LAN row; the desktop's http rung is itself broken (next-steps 2026-09-05) and a Node pinned-TLS transport is its own High task); `share` links from the local page (the relay is reachable from a browser, but no AC covers it — left `false`, queued); streaming from the `claude` shim (D5 pins `--output-format json`; `streaming:false` on the seat is the truth); `probeOllama` on the local page (with the ollama shim).

## Decisions taken at Gate 2 (the brief's defaults, each kept or overturned with the reason)

- **D-B1 — Which page is served: a second entry of `apps/host`, `snug-host-local.html`. KEPT, with the mechanism corrected.** `apps/host/vite.config.ts` cannot take a second input: `inlineDynamicImports` refuses multiple inputs, `inline-single-file` throws on more than one page (`src/plugins/inline-single-file.ts:96-99`), and `check-host-kit` requires `dist/` to hold exactly `snug-host.html` (`scripts/check-host-kit.mjs:87`). So the local page is a **second Vite config** (`apps/host/vite.local.config.ts`, the precedent the kit's own header names for T5's micro kit) with its own entry `local.html` → `src/local/main.tsx`, output `dist-local/snug-host-local.html`, the same `inline-single-file` + `swap-resolved` + `build-stamp` plugins, the starter swap and the WebLLM stub identical. The artifact kit's config, output and gate are untouched.
- **D-B2 — Where the code lives: `apps/host-mcp/`, one `dist/snug-mcp.mjs`. KEPT — with one question for the owner (D-B2′).** Layout: `src/main.ts` (entry: stdio MCP when spawned; `open`/`status` subcommands for a human terminal over the control socket), `src/mcp/jsonrpc.ts` + `src/mcp/server.ts` (the stdio framing and the five methods), `src/tools.ts` (the four tools + `instructions`), `src/loopback.ts` (`node:http` on `127.0.0.1:0`; bearer + origin gates; static page; route table), `src/control-socket.ts` (`node:net` unix socket, `0600`, the ADR-0032 shape), `src/fetch-proxy.ts` (`node:https` request with the re-checks, the envelope, the cap while reading, the self-naming wall clock), `src/userdb-fs.ts` (the desktop's `userfile.rs` rules in Node), `src/lock.ts` (attach / take over / release), `src/events.ts` (the SSE ring: `hand-in`, `status`, `oauth-callback`, `shutdown`), `src/brain-claude.ts` (the shim), `src/holder.ts` (the Snug Desktop process-table check), `src/env.ts` (the enumerated env reads). **Bundler: `vite build` in SSR/library mode** (`build.ssr`, `ssr.noExternal: true`, target `node20`, `inlineDynamicImports`) — the repo's one bundling tool; no esbuild/tsup direct dependency is introduced. The plugin ships `dist/snug-mcp.mjs` + `dist-local/snug-host-local.html` (resolved relative to `import.meta.url`), never source.
- **D-B2′ — MCP framing: hand-rolled JSON-RPC over stdio, with the official SDK's CLIENT as a dev-only interop test. RECOMMENDED over the brief's default (SDK in the shipped bundle) — owner decides.** Measured this session in a scratch bundle: `@modelcontextprotocol/sdk` 1.30.0 declares 17 runtime dependencies (express, hono, jose, ajv, cors, eventsource…); a minimal stdio server bundles to **796 KB (372 KB minified)** of which zod is 974 KB source and ajv 206 KB — express/hono tree-shake out. The server subset this process needs is `initialize`, `notifications/initialized`, `ping`, `tools/list`, `tools/call` (+ `-32601` for anything else and silence for unknown notifications), newline-delimited JSON — a few hundred stdlib lines. Reasons to overturn: `docs/conventions.md` ("a new dependency requires justification in an ADR"; prefer fewer), the plugin is a marketplace-scrutiny target (ADR-0065 consequences) and a readable ~30 KB file beats a 372 KB blob, and the SDK's runtime half (HTTP transports, auth) is dead weight for a stdio-only server. The SDK stays as a **devDependency** so the interop test drives the hand-rolled server with the reference client (`Client` + `StdioClientTransport`): `initialize` → `tools/list` → `snug_status` → stdin close. Either way the decision and the dependency are recorded in ADR-0068 (draft on this branch).
- **D-B3 — Executor placement: in the page, `fetchImpl` over loopback. KEPT.** Read against the code: `connectedFetchDepsFor` (`apps/playground/src/state/net.ts:309`) threads `getPlatform().fetchImpl` unchanged; the executor's gates run on a `Response` object, so the page-side `fetchImpl` reconstructs one from the proxy's envelope and every gate (status check after `redirect:'manual'`, the whitelist, the cap while reading, the scrub) runs exactly as on desktop. Nothing found that the hop breaks. The envelope: proxy HTTP 200 + JSON `{ok:true, status, statusText, headers:[[k,v]…], bodyBase64}`; proxy-level refusals HTTP 4xx + `{ok:false, code, message}` with `message` already scrubbed — the page maps `ok:false` to a thrown typed error so the executor names it as it names a transport failure today. `Set-Cookie` is dropped on the process side (the executor drops it again).
- **D-B4 — Page↔process channel: `/events` SSE, bearer-gated, read with `fetch` streaming. KEPT.** Events: `hand-in {bundle}`, `status {…}`, `shutdown` (no `oauth-callback` — D-B14 delivers over `BroadcastChannel`). One subscriber per page tab; a reconnect replays nothing older than the bounded ring.
- **D-B5 — Plugin layout. KEPT.** `.claude-plugin/plugin.json` + `.mcp.json` `{"mcpServers":{"snug":{"command":"node","args":["${CLAUDE_PLUGIN_ROOT}/scripts/snug-mcp.mjs"]}}}` (shape confirmed against installed marketplace plugins: `external_plugins/discord/.mcp.json` uses `${CLAUDE_PLUGIN_ROOT}` exactly so); scoped names `mcp__plugin_snug_snug__*`; `.codex-plugin/plugin.json` + its `.mcp.json` from the same constants module. **Since T6's generator does not exist yet**, this task adds `scripts/lib/plugin-manifests.mjs` (the ONE constants module: names, description, the mcp entries, both host forms) and `scripts/build-plugin.mjs`, which assembles a **local plugin directory** `dist/plugin/snug/` (`.claude-plugin/plugin.json`, `.mcp.json`, `.codex-plugin/plugin.json`, `scripts/snug-mcp.mjs`, `scripts/snug-host-local.html`, `README.md`) plus a one-entry marketplace `dist/plugin/.claude-plugin/marketplace.json` (`source: "./snug"`) — what AC1 installs from. T6 consumes the module and emits into the distribution repo; both output dirs gitignored in the same commit.
- **D-B6 — S12 spike (the agent as brain over tool calls). DEFERRED — not in this task.** The tool surface is frozen by the allowlist test; a `snug_reply` tool would be a fifth name and a data-plane-adjacent channel that needs its own threat-model row. Recorded in next-steps as a candidate with the 2026-09-04 memo's verdict.
- **D-B7 (new) — No `local`-mode bearer change.** The program's D5/T3 text ("the playground's `local` mode gains a bearer field + presets + a boot config") predates T2's brain seat. `localAdapter` already accepts `apiKey` (`packages/adapters/src/local.ts:41`, `options.apiKey ?? 'local'`), and `createTurnAdapter`'s `'host'` arm hands back the platform's adapter with no key read and no F15 (`agent/transport.ts:116-137`). So the local page pins `brain: {kind:'host', label:'Claude · your CLI', adapter: localAdapter({baseUrl: origin + '/v1', apiKey: bearer, model:'claude'}), streaming:false, tools:false}` and touches no mode, setting, secret or preset. The "no bearer path for `local` mode" item in the program record is struck for Binding B (annotated, not deleted) and stays Option C's if Option C wants it.
- **D-B8 (new) — Token custody: memory only.** The bearer lives in the primary's memory and the page's `sessionStorage` (origin `http://127.0.0.1:<port>`, per tab). It is never written to disk: attached sessions and the human CLI reach the primary over the `0600` unix socket, and `snug_open`/`snug-mcp open` ask the PRIMARY to build the URL and run `open`. The lock's `tokenHash` (SHA-256) is the attach handshake's identity check, nothing more. Fallback when `open` fails (a sandboxed host): the tool result says "run `node <plugin>/scripts/snug-mcp.mjs open` in a terminal" — the CLI prints the URL (with the fragment) into the USER's terminal over the socket; it never rides an MCP message.
- **D-B9 (new) — Lifetime = the union of attached sessions.** The primary counts attached control-socket clients; its own stdin close and parent-watch (ppid change, `apps/whatsapp-sidecar/src/parent-watch.ts` imported by path) start the grace only when no client remains. Two Claude Code windows = one Snug that outlives either window.
- **D-B10 (new) — The holder signal is the process table.** No lock, pidfile or holder marker exists for `~/Snug/user.snug` on the desktop (only `tauri-plugin-single-instance`, invisible from outside). The process checks for a running `snug-desktop` (`ps`/`pgrep -x`, the exact binary name from `Cargo.toml`, matched by command identity per lesson 2026-08-19) at boot and every 5 s, flips the page read-only (the `PUT /userdb/*` route answers `423` with a named code; `CustodyState.readOnly` + `heldBy:'Snug for Mac'`), and flips back when it exits. A shared holder marker written by both products is queued in next-steps as the durable answer (a desktop change, outside this task).
- **D-B11 (new) — Test hooks live in a second build, never in the release.** The proxy takes `lookup?` (the `node:https` request option) as a constructor parameter; the release entry never passes it; a test entry `src/main.test-hooks.ts` → `dist/snug-mcp.test.mjs` reads `SNUG_MCP_TEST_RESOLVE=host=ip[,…]` and passes a resolver, and the stub's self-signed CA rides Node's own `NODE_EXTRA_CA_CERTS`. AC11's sweep proves the release bundle carries neither the env name nor the symbol. Interop and stdin-close tests run the RELEASE bundle; only the connected-fetch e2e runs the test bundle.
- **D-B12 (new) — the `instructions` text lives at `apps/host-mcp/src/instructions.md`, not the prompt store.** `gen-content.mjs` compiles every `prompts/**.md` into the committed `content.ts` that the playground, the desktop and the host kit all import, so a prompt-store copy would inflate every bundle with process-only text — what ADR-0065 D6 forbids. One source, byte-compared by `check-host-mcp`, read by T6's skill build later. (Full reasoning in Decisions & surprises.)
- **D-B13 (new, review F1/F2) — fixed port 43127, bearer-only for GET.** The origin is a registered OAuth redirect URI, so it must survive a restart: bind 43127, fall back to an ephemeral port only when it is busy and then disable the OAuth rows with a named reason (never silently re-register the user's URI). `Origin` is checked where the browser sends it; where it does not (same-origin GET; Safari sends no `Sec-Fetch-*` at all) the bearer is the guard.
- **D-B14 (new, review F1) — the web OAuth path, not the desktop transport.** See the hard rule above: `platform.oauth` stays undefined, the process serves `/oauth/callback`, and 41420 / `createDesktopOAuthTransport` leave this task's scope entirely. This also deletes the `oauth-loopback.ts` module and the `oauth-callback` SSE event from D-B2's layout and D-B4's event list.
- **D-B15 (new, review F3) — the brain shim MUST speak SSE.** `openaiAdapter` writes `stream: true` unconditionally (`packages/adapters/src/openai.ts:78`) and parses the reply with `parseSse`; a plain-JSON answer yields no events and every turn returns `streamDroppedResult()` — a permanently broken brain. So `/v1/chat/completions` answers `text/event-stream` with at least one `choices[0].delta.content` chunk, a `choices[0].finish_reason`, and `data: [DONE]`, even though `claude -p --output-format json` is non-streaming underneath (the shim buffers the child, then emits). `brain.streaming` stays **false** — it is an app-facing declaration read only by `host-ready` (`state/webllm.ts:112`), never a transport switch. Pinned by a test that drives the REAL `localAdapter` against the real shim.
- **D-B16 (new, review F4/F6) — the client's `Response` and request serialization are their own contract.** Reconstruction passes `null` as the body for the null-body statuses 204/205/304 (a `new Response(bytes, {status:204})` THROWS, and the throw surfaces as `NET_FETCH_FAILED` on a successful DELETE), clamps `status` to 200–599, and drops a `statusText` outside the reason-phrase grammar. The body crosses as base64 of the RAW bytes, never a UTF-8 round trip. On the request side the page-side `fetchImpl` serializes `init.body` for both shapes the callers use — a `string` (the executor, `frames.ts:144`) and a **`URLSearchParams`** (`oauth-service.ts:655`, every token exchange/refresh/revoke; `JSON.stringify` of one yields `"{}"`), and never honours `redirect:'error'` toward the PROXY (`token-claim.ts:126` sets it for the provider hop; a proxy 3xx must return as data, not throw).
- **D-B17 (new, review F5) — the page-side cap is decorative; the process's is load-bearing.** `readBodyCapped` streams and cancels, but a reconstructed `Response` has already fully materialised, so `reader.cancel()` drops nothing. The guard lives in `fetch-proxy.ts`, which must trip at the SAME `LIMITS.MAX_NET_RESPONSE_BODY_BYTES` (`packages/protocol/src/constants.ts:87`) so both sides name one code. D-B3's "exactly as on desktop" is corrected to "the same gates, with the cap's teardown effect on the process side only" — stated in the ADR so no later reader treats the page-side cap as the wall.
- **D-B18 (new, angle-2 F1/F2/F3) — `Host` is the rebinding wall; preflight-forcing is an invariant.** See the hard rule. Three measured facts behind it: a rebound page sends no `Sec-Fetch-*` and an attacker-named `Host`; a different loopback PORT reads as `same-site`, so only the literal `same-origin` may pass; and every data-plane route must force a preflight (the bearer header does), or a foreign page can cause a side effect it cannot read. Non-browser same-user processes bypass all of it — the bearer is the only guard there, disclosed in the ADR.
- **D-B19 (new, angle-2 F4) — the inbound rebind is a NEW residual, not the desktop's inherited one.** The desktop's DNS-rebinding residual is about OUTBOUND fetches to public names; the desktop serves its UI from `tauri://localhost` and has no inbound loopback listener at all. Two protections this process lacks are also recorded: Tauri bakes an HTTP capability allowlist at build time (a ceiling no runtime bug can widen — the Node process has only `fetch-proxy.ts`'s gates), and the desktop page is not on an http origin. The threat-model delta writes its own inbound row rather than citing the desktop's.
- **D-B20 (new, angle-2 F6, CORRECTS the plan's number) — the process's wall clock is 75 s, strictly greater than the executor's 60 s.** At an equal 60 s whichever timer wins is a scheduling race, and when the process aborts first `timeoutSignal.aborted` is false in the page, so the executor's self-naming sentence never runs and the user reads the transport's spelling — the exact defect `next-steps.md` already logs for the LAN leg, reproduced in the other direction. The ordering is an invariant with a test, not just a number. The process's overflow must also map to the executor's `NET_SIZE_EXCEEDED` code, not a generic `ok:false`.
- **D-B21 (new, angle-2 F7 + the earlier env measurement) — the child's environment is built by ALLOWLIST, from scratch.** A denylist against an undocumented, version-varying `CLAUDE_*` namespace drifts silently the next time the CLI adds a variable. The child gets `{HOME, PATH}` plus terminal basics and nothing else, so it falls back to the user's own keychain/OAuth — which is what the brain chip's "your CLI" actually promises. Measured: the inherited env carries `CLAUDE_CODE_MESSAGING_TOKEN` and `CLAUDE_CODE_MESSAGING_SOCKET`, a live IPC channel back into the running session.
- **D-B22 (new, angle-2 F5) — `parent-watch.ts` is NOT imported across app boundaries.** `apps/whatsapp-sidecar` is `private`, declares no `exports` map, sets `rootDir: src`, and its `index.ts` re-export pulls in `baileys` — which `ssr.noExternal` would inline into the release bundle, contradicting the dependency claim and failing the inertness sweep. The 80 lines are copied into `apps/host-mcp/src/parent-watch.ts` **with an equivalence test against the sidecar's copy** (the D-B3 precedent), or moved to a shared package if Gate 4 finds that cheap. Either way one behaviour, pinned.
- **D-B23 (new, angle-2 F8) — the lock is created with `O_EXCL`, and take-over verifies command identity.** `fs.open(path,'wx')` decides the winner BEFORE any bind, closing the simultaneous-spawn race (two ephemeral binds never collide, so both would otherwise succeed and one becomes an unreapable orphan holding the user file). The socket is never `rmSync`'d blindly — that would unlink a live primary's socket out from under it, freezing the client count; a socket that answers belongs to a live primary, and only a proven-dead one is removed. A stale lock's pid is matched by COMMAND LINE before any signal (the desktop's `stale_helper_verdict` shape — pids recycle, and killing a stranger is worse than the conflict it repairs). A new attach during the exit grace CANCELS the grace, or is refused so the newcomer takes over cleanly.
- **D-B24 (new, angle-4 B1 — a data-loss blocker) — the holder check REFUSES TO OPEN, it does not open read-only.** `userdb.ts:1475` and `driver.ts:315` both swallow a failed `backend.save` with a bare `catch { dirty = true }` — no rethrow, no callback, no store, no log — and there is no persist-failure seam anywhere between the backend and the custody chip. So the plan's "answer 423 and set `readOnly`" would let the user install apps, complete a wizard, store credentials and type for an hour while every save silently 423'd, losing all of it on tab close, with the only hint buried in a popover they must click. **Therefore:** when the holder is present the local page does NOT open the db at all — it renders a refusal surface naming Snug for Mac and offering to retry once the desktop is closed. This is what "read-only and says so" honestly means, and it needs no `packages/db` change (which the plan's scope excludes). AC7 is restated accordingly. Adding an `onPersistError` seam to `packages/db` is queued in next-steps as the general fix, since the artifact binding has the same blind spot.
- **D-B25 (new, angle-4 B2) — the custody copy CHANGES an existing pinned assertion.** `copy.ts:88-97` falls `local-host` through to the shared default whose label is hardcoded `'your file: in this browser'` — a lie on this binding — and `hostCopy.test.ts:51` currently pins that string green. `readOnly` IS already read (`copy.ts:56`), refuting the plan's implication that it is ignored; what is missing is a holder NAME, which needs a change to the status ternary, not merely an additive `heldBy` field. There is no desktop twin to pin against: the desktop carries no custody seat at all. So step 4 edits an existing test rather than only adding one, and says so.
- **D-B26 (new, angle-4 B3 — an AC contradiction) — the hand-in's D4 refusal needs a binding-aware arm, and the hub needs a refresh.** Three separate breakages, all confirmed: (a) `handin.ts:91-97` refuses ANY bundle carrying connections with a message reading "not available inside an artifact" — but Binding B is defined as the ONE binding with connected apps, so AC8 and AC4 cannot both hold until that refusal takes the binding into account (`apps/host/src/handin.ts` joins the file list, and the artifact arm's message and behaviour stay byte-identical, pinned); (b) `Composition` (`compose.ts:48`) exposes no post-boot re-entry — `handIn` is called once from `main.tsx:53` and `pendingByApp`/`publishPending` are private closures, so an SSE hand-in needs a new exported seam; (c) `HubView.tsx:186` lists the library once on mount and `refreshAppMeta` only repopulates tile metadata, so a hand-in delivered while the user sits on the hub is invisible until a reload — and an auto-applied update to the app the user is INSIDE swaps v2 bytes under v1 with no `contentEpoch` bump. The SSE path must refresh the hub list and bump the run view's epoch, or say plainly that it did nothing. The `pending`/offered path is already reactive (`useSyncExternalStore`) and needs no work.
- **D-B27 (new, angle-4 F4/F5/F6) — the userdb HTTP client's own contract.** Absence is **404 and only 404**; every other status and every transport error REJECTS, because a client that maps any non-200 to `undefined` opens a pristine empty database over the user's real file and the next write destroys it (lesson 2026-08-03, cited in the backend's own header). The process's `valid_name` copies the desktop's charset verbatim so it admits the quarantine names too (`user.snug.corrupt-<base36>.bak`, `userdb.ts:1366`) and the legacy sidecar — a hand-narrowed two-name allowlist would throw INSIDE the corruption handler and turn a recoverable quarantine into an unrecoverable crash. The `/userdb/*` routes carry their own much larger body limit, never the proxy's 1 MiB (a user file trivially exceeds it; sharing the cap would silently 413 into the swallow above). Save cadence is fine: a 250 ms debounce, single-flighted, plus `pagehide`.
- **D-B28 (new, angle-3 F1 — a false-green gate risk) — `dist-local/**` joins `turbo.json`'s build outputs, and the e2e precondition checks it.** Proven: `turbo.json:4` declares only `outputs: ["dist/**"]`, so a cache hit restores `dist/snug-host.html` and leaves `dist-local/` MISSING — `FULL TURBO` in 26 ms with no local page on disk. The existing e2e precondition (`gate-local.mjs:108`) only stats the artifact kit, so the leg would run against a missing or stale local page rather than reporting CANNOT RUN. Both the turbo output glob and the precondition (plus `apps/host-mcp/dist/`) land in the same commit as the second build.
- **D-B29 (new, angle-3 F2 — blocks AC3/AC4/AC5) — the e2e stubs must EXPORT their CA cert; `NODE_EXTRA_CA_CERTS` is unsatisfiable as planned.** Both fixtures mint a self-signed cert into an ephemeral `mkdtemp` dir and never write it anywhere a caller can find (`net-stub.mjs:79-99`, `fake-idp.mjs:29-43`); the browser only works because Playwright passes `--ignore-certificate-errors`, which `node:https` has no equivalent for (measured: `DEPTH_ZERO_SELF_SIGNED_CERT`). So both fixtures gain an opt-in `SNUG_E2E_CERT_OUT=<path>` that writes the generated cert, and `apps/playground/e2e/fixtures/{net-stub,fake-idp}.mjs` join the files-to-touch list — the plan's "reuse BY PATH, unchanged" was wrong. (Both are otherwise genuinely portable: only `node:` builtins and `os.tmpdir()`, ports fully parameterised, verified running from a foreign cwd. They do shell out to `openssl`.)
- **D-B30 (new, angle-3 F3) — the process DEEP-IMPORTS the two guards; the barrel drags in sql.js.** Measured on the plan's exact Vite SSR config: the two pure functions cost **4,368 B** deep-imported and **329,902 B** through `@snugprotocol/auth`'s barrel, because the barrel reaches `@snugprotocol/db`, whose barrel exports `driver.js`, which imports sql.js (~2,100 lines of Emscripten shim in the output) — plus the whole OAuth provider registry. A 75× amplification of a WASM SQL driver and a provider list the process never uses, on a marketplace-scrutiny target whose D-B2′ argument was minimalism. The bundle imports `@snugprotocol/auth/dist/net-guards.js` and `.../scrub.js`; the identity test imports the BARREL (the `toBe` still holds — same binding) so the deep path can never drift from the public export. `packages/auth` declares no `exports` map today, so the deep path is reachable; a test pins that too. Zod (~168 KB) stays irreducible for `parseAppBundle`.
- **D-B31 (new, angle-3 F4) — the manifests module emits `owner`, `description`, `author` and `version`.** Measured with the real validator: omitting marketplace `owner` is a HARD error (`owner: Invalid input: expected object, received undefined`, exit 1), and `--strict` additionally demands a marketplace `description` and a plugin `author`. Step 5's "green" means `claude plugin validate dist/plugin --strict` exit 0. Marketplace `name` need not match the directory (verified).
- **D-B32 (new, angle-3 F5 — a false-green in the owner's walk) — an installed plugin is a version-keyed COPY, so the dev loop is uninstall → rebuild → install.** Measured: the install copies into `~/.claude/plugins/cache/<marketplace>/<plugin>/<version>/` (not a symlink); after editing the source, BOTH `claude plugin install` ("already installed") and `claude plugin update` ("already at the latest version") leave the stale copy in place. Without this step AC1 and the Gate-5 walk silently exercise an old bundle — the exact false-green class the gates exist to prevent. AC1 and the walk instructions name the loop; the alternative is bumping `version` per build.
- **D-B33 (new, angle-3 F6 — CORRECTS AC12 and the D4 wording) — the wizard, executor and registry are ALREADY in `snug-host.html`; `connections:false` is a RUNTIME flag, not a build-time exclusion.** Measured in the shipped artifact page: `NET_AMBIGUOUS_CONNECTION` ×3, the wizard markers ×8, `accounts.spotify.com` ×1. Building the local page with a second config produced **2,274,084 B — the same size as the artifact page**, so the local build needs no larger ceiling and the existing 2,750,000 (475,916 B of headroom) stands. The hard rule's phrase "without the wizard, executor or registry **reachable**" is true only in the runtime-gated sense and is restated that way; AC12's "proves connections ARE compiled in" becomes a RUNTIME assertion (the wizard door renders under `local-host`, `host-ready.net` is true) with the artifact kit's absence twin unchanged. Also: `check-host-kit`'s cited 2,219,519 B is stale — the current build is 2,274,084 B.

## Plan

### Order (tests first at every step; one PR)

0. **Gate 2 → owner approval; plan review (finder angles) before Gate 4.** Reviewers verify the mechanism claims against the named files (lesson 2026-08-21), in particular: the `Origin`/`Sec-Fetch-Site` rule, the envelope's `redirect` semantics on the page-side `Response`, the attach handshake, the read-only flip, and that `check-host-kit` stays untouched.
1. **`apps/host-mcp` scaffold + the pure modules with their tests (Gate 3 red → Gate 4 green):** `jsonrpc.ts` (framing: partial lines, oversized line refused at 4 MiB, `-32700`/`-32600`/`-32601`), `server.ts` (`initialize` echoes the client's protocol version when supported, `instructions`, `capabilities:{tools:{}}`; `tools/list` allowlist; `tools/call` dispatch; unknown notification ignored), `env.ts` (the enumerated reads), `lock.ts` (attach/take-over/release with an injected fs + `kill(pid,0)` + socket probe), `holder.ts` (injected `ps` output), `userdb-fs.ts` (`valid_name`, temp+fsync+rename, absence=ENOENT, the discriminant), `fetch-proxy.ts` (re-checks with the REAL `isForbiddenNetHost`; https-only; `Set-Cookie` dropped; the cap trips at 1 MiB + 1 while streaming; the wall clock names itself; error text scrubbed with `scrubAuthValues`; an injected `request` so no socket opens in unit tests), `events.ts` (ring, replay bound), `brain-claude.ts` (argv pinned by test, env strip pinned by test, `stop_reason`/`result` mapping from a fake CLI, the timeout names itself, a spawned child is reaped on shutdown and on client abort), `tools.ts` (four tools; `snug_hand_in` validates `snug-app-bundle/1` with `@snugprotocol/protocol`'s parser and refuses connections by name; results never carry the token — a canary test).
2. **`loopback.ts` + `control-socket.ts` + `main.ts`** with the integration tests: real listeners on ephemeral ports/paths under `SNUG_HOME=<tmp>`; bearer/origin matrix (AC6); route-class separation (a data-plane path on the socket → 404, a control path on TCP → 404); the SDK-client interop test against the RELEASE bundle (`initialize` → `tools/list` → `snug_status`, stdin close → exit within the grace, lock + socket gone); the two-process attach test (spawn twice, same port, first stdin closed → still serving, second closed → exit).
3. **`apps/host` local build:** `vite.local.config.ts`, `local.html`, `src/local/main.tsx` (fragment → `sessionStorage` → `history.replaceState('#/')` BEFORE `HashRouter`), `src/local/client.ts` (the typed process client: `fetch`, `fs`, `oauthListener`, `events`, `status`, `reportApps`), `src/local/compose-local.ts` (pure: the platform with `fetchImpl`, `oauth` = `createDesktopOAuthTransport` over the client's listener + the desktop's `pendingFlow` discipline lifted into a shared helper if the copy is byte-identical — else a documented twin with an equivalence test, `userdbBackend = createFileBackend(client.fs, 'Snug')`, `brain` per D-B7 or demo, `custody` with `heldBy`, `agentHandIns` fed by `/events`, capabilities `{connections:true, brainSettings:false, account:false, sync:false, share:false, appExport:true, lanHttpPrivate:false, hubAuth:false, subscriptionMode:false, hubSyncOrigin:false}`), `src/local/handinEvents.ts` (SSE bundles → `handInFromPage`'s block shape). Tests: compose matrix with fakes; the fragment strip (a `#token` never reaches the router; a reload keeps working from `sessionStorage`; a tab opened without a token renders the "open Snug from your agent" notice, not a dead page); the client's `Response` reconstruction from the envelope incl. 3xx-as-data and `ok:false` → typed throw; the `probe.ts` `local` leg becomes `'resolved' | 'absent'` from the client's status.
4. **Playground:** `platform/copy.ts` gains the `local-host` × `'file'` arm ("your file: on this Mac" / "~/Snug/user.snug" / read-only + holder line) — pinned byte-for-byte with its web/desktop positive twins; `CustodyState.heldBy?: string` (additive). `hostSettings`/`hostCapabilities` suites gain the local twin (connections door present under `local-host`, absent under `artifact`).
5. **Manifests + plugin assembly + gates:** `scripts/lib/plugin-manifests.mjs`, `scripts/build-plugin.mjs` (+ `.test.mjs`), `scripts/check-host-mcp.mjs` (release-inertness sweep; `instructions` byte-compare against `packages/knowledge/prompts/skills/snug/launch-protocol.md`; exactly one file in `dist/`; the local page passes `checkHostKitPage`'s structural rules with the connections positive twin) wired into root `test` and gate-local's workspace leg + ci.yml (AC3 token-set mapping kept exact); `.gitignore` for `apps/host/dist-local/` and `dist/plugin/`; `claude plugin validate dist/plugin` green.
6. **e2e (`apps/host/e2e/local.spec.ts`, its own Playwright project with `--host-resolver-rules=MAP stub.snug.test 127.0.0.1,MAP idp.snug.test 127.0.0.1` + `--ignore-certificate-errors`, reusing `apps/playground/e2e/fixtures/net-stub.mjs` and `fake-idp.mjs` by path):** `globalSetup` spawns `dist/snug-mcp.test.mjs` under `SNUG_HOME=<tmp>` with `SNUG_MCP_TEST_RESOLVE` + `NODE_EXTRA_CA_CERTS`, reads the port from the lock, obtains the launch URL over the control socket; AC2 (probe, chips), AC3 (weather-shaped stub request through the process; oversize body refused), AC4 (API-key wizard → injected request at the stub; the canary grep over the process's captured stdio, `~/Snug/host/*`, every tool result), AC5 (OAuth via 41420 + the refresh-retry), AC7 (read-only when a fake `snug-desktop` is on the injected process table — the test hook build takes `SNUG_MCP_TEST_HOLDER=1`; also swept from the release bundle), AC8 (hand-in over `/events` against a real user db). Capture the failing run's text before diagnosing any red; flake claims as rates over ≥ 6 runs.
7. **Gate 5:** suites of every package touched + dependents (`host`, `playground`, `desktop`, `host-mcp`; root `pnpm test` with `turbo --force`); gate-local legs named PASS/FAIL/DESELECTED; diff review with finder angles; **the owner's walk**: install from `dist/plugin`, `/mcp`, `snug_open`, Ledger through the wizard, one OAuth flow, Cowork (AC10), Codex when installed.
8. **Gate 6:** docs owed (below), the program record's T3 row, `next-steps.md`, lessons, `/close-session`.

### Files to touch (new unless marked)

- `apps/host-mcp/{package.json,tsconfig.json,tsconfig.test.json,vite.config.ts,src/**,src/__tests__/**}` — `tsc -p` typecheck + `vite build` (two entries: release, test-hooks) + vitest; devDeps `@types/node`, `vite`, `@modelcontextprotocol/sdk` (interop client only, D-B2′); runtime deps `@snugprotocol/{auth,protocol}` (workspace) — nothing else.
- `apps/host/src/handin.ts` (edit: the D4 refusal gains a binding-aware arm — D-B26a), `apps/host/src/compose.ts` (edit: a post-boot hand-in seam — D-B26b), `apps/host/{vite.local.config.ts,local.html,src/local/**,src/__tests__/local*.test.ts,e2e/local.spec.ts,e2e/local-helpers.ts,playwright.config.ts (edit: second project + globalSetup),package.json (edit: build:local, test:e2e covers both)}`; `src/probe.ts` (edit: the `local` leg).
- `apps/playground/src/platform/{copy.ts,platform.ts}` (edit, additive) + tests.
- `apps/playground/e2e/fixtures/{net-stub,fake-idp}.mjs` (edit: an opt-in `SNUG_E2E_CERT_OUT` so the process can trust the stub — D-B29), `turbo.json` (edit: `dist-local/**` — D-B28).
- `apps/host-mcp/src/instructions.md` (the `instructions` text — D-B12; ONE source, byte-compared by `check-host-mcp`, read by T6's skill build later).
- `scripts/{lib/plugin-manifests.mjs,build-plugin.mjs,build-plugin.test.mjs,check-host-mcp.mjs,check-host-mcp.test.mjs,gate-local.mjs (edit)}`, root `package.json` (edit: `check-host-mcp`), `.github/workflows/ci.yml` (edit: the step), `.gitignore` (edit).
- `docs/`: this file; `decisions/0068-*.md` (draft now); ADR-0065 status/§2 B amendment (Gate 6); `security/threat-model-delta-local-host-process.md` + `threat-model.md` ledger row (hash) + prose count + R-40…; `architecture.md` ("Host kit" section + dependency graph: `host-mcp` consumes `auth`+`protocol`; `host` local build); `code-map.md` rows; `glossary.md` ("local host process"); the program record (T3 row, D5/D12 annotations, the struck bearer item); `next-steps.md`; `lessons.md`.

### Cross-package impact (dependency graph)

- `apps/host-mcp` (new) → depends on `packages/auth`, `packages/protocol`; imports `apps/whatsapp-sidecar/src/parent-watch.ts` by path (one source, no copy). Any change to those → run `host-mcp`.
- `apps/host` (second build) → the playground source + all seven packages, as today; the e2e runs against the real process → `host-mcp` build precedes `host test:e2e`.
- `apps/playground` (`copy.ts`, `platform.ts`, additive) → run `playground`, `desktop`, `host`.
- `packages/auth` — no source change; the process is a NEW READER of its guards (import identity pinned by test: the proxy's `isForbiddenNetHost` `toBe` the package export).
- Root `test` chain gains `check-host-mcp`; gate-local's workspace leg + ci.yml gain the same step in the same change (lesson 2026-08-24); the e2e leg gains `pnpm --filter host test:e2e` preconditions for `dist-local/` and `apps/host-mcp/dist/`.

### Spec-sync impact

None. No `packages/protocol` byte changes; `snug-app-bundle/1` is consumed through the existing parser; the `local-host` binding literal already exists on the platform seat (T2). If Gate 4 finds the hand-in needs a new provenance literal it is an append + spec-changelog line — not expected (Binding A's `agent:<lineage>` chain is reused).

### Threat model (delta rows to write; ledger + count + R-40+)

Loopback data plane (bearer-in-fragment, bearer-as-the-guard with `Origin` checked where sent, no CORS, the FIXED port 43127 and why a registered redirect URI forces it, `Host` ignored); the unix control socket (0600; no bearer; command identity, not a flag); the lock + attach handshake (`tokenHash`, stale-lock takeover, pid recycling → command-line match); read-only custody by process-table signal (an availability/consistency residual: a holder that starts AFTER the page opened is caught by the 5 s poll, a write in flight before the flip is the desktop's temp+rename vs ours — both atomic, last writer wins on separate saves; disclosed); the `claude -p` child (env strip; argv visible in `ps`; subscription usage on the user's own CLI); LLM-not-a-principal (allowlisted tools; the bearer never in a message); the `/fetch` proxy as a second reader of the executor's output (scrub at the new reader's altitude — lesson 2026-08-15); the OAuth popup path on a loopback origin (the callback rides `BroadcastChannel` same-origin; a squatter on 43127 before us would receive callbacks — hence the fixed port is claimed at boot and the OAuth rows disable when it is lost); DNS rebinding not claimed.

### Docs owed (Gate 5/6)

ADR-0065 amendment (§2 B → "delivered as the plugin-bundled process; the process is the desktop's native side on loopback; the `Alternatives` line about an MCP server stands for the HOST and the BRAIN and is annotated for the spawn channel"; D4 restated); ADR-0068 accepted; threat-model delta + ledger; `architecture.md`; `code-map.md`; `glossary.md`; the program record's D12 table gains the row "Claude Code / Cowork desktop / Codex with the plugin installed → `snug_status` → `snug_open`" (T6 renders it into `SKILL.md`); `next-steps.md` items: T3 remainder shims, Codex `npx` cut-over, the shared holder marker, `share` on the local page, S12, `probeOllama` on the local page.

### Open questions for the owner (answer with the approval; defaults in bold)

- **Q1 — D-B2′:** **hand-rolled JSON-RPC + SDK client dev-only** vs the SDK in the shipped bundle (796 KB / 372 KB minified measured).
- **Q2 — D-B10:** the process-table holder signal now, shared marker queued — **yes**; or block read-only custody on a desktop change first.
- **Q3 — the LAN rungs out of scope** (no `lanFetch`/`lanHttpPrivate` on the local page in this task) — **yes**.
- **Q4 — `share` off on the local page** for this task — **yes**.
- **Q5 — `--max-turns`:** not listed in `claude --help` 2.1.211 (the CLI on this machine); Gate 3 verifies acceptance with a no-cost invocation; if gone, the pinned argv becomes `--tools ""` + `--disallowedTools "*"` (no tools → one turn by construction) — **proceed on that rule**.

## Decisions & surprises

- **2026-09-07 — T6 has not started:** no `skills/`, no `.claude-plugin/`, no `check-skill-sync`, no generator. This task authors the launch-protocol text in the process's own source and the ONE manifests constants module; the byte-compare lives in `check-host-mcp` until T6 folds it into `check-skill-sync`.
- **2026-09-07 — the MCP SDK measured** (scratch bundle, Node 22, esbuild): 17 declared runtime deps; 796 KB unminified / 372 KB minified; express/hono shaken out; zod + ajv dominate; `initialize` answers `2025-06-18` with `instructions`. → D-B2′.
- **2026-09-07 — `apps/host` cannot take a second input** (three independent refusals + the kit gate) → a second Vite config and `dist-local/`.
- **2026-09-07 — the repo has no Node bundler;** every bundling build is `vite build` → SSR/lib mode, no new bundler dependency.
- **2026-09-07 — `localAdapter` already accepts `apiKey`; the T2 brain seat routes around `local` mode entirely** → D-B7; the program's "bearer for `local` mode" first commit is unnecessary for Binding B.
- **2026-09-07 — no holder/lock exists for the desktop's user file** (single-instance plugin only; `user.sqlite` in Rust tests is the LEGACY name — `USERDB_FILE` is `user.snug`, adopted forward by `openUserDb`) → D-B10; the process must accept the legacy name too (`createFileBackend` asks for it when the canonical is absent).
- **2026-09-07 — the desktop's http capability admits only two loopback ports** (`threat-model-delta-desktop-shell.md:16`); irrelevant here because the page calls the process with the browser's own same-origin `fetch`, but it rules out ever pointing Snug Desktop's `tauriFetch` at this process.
- **2026-09-07 — `claude --help` (2.1.211) lists no `--max-turns`** → Q5.
- **2026-09-07 — Gate-3/5 facts verified first-hand (so no step re-derives them):** the executor's wall clock is `REQUEST_TIMEOUT_MS = 60_000` and it names itself at `packages/auth/src/connected-fetch.ts:1280` — **the process's own clock must sit ABOVE it** (75 s) or the proxy's abort wins the race and the user reads the transport's spelling instead of the executor's sentence (the standing LAN-leg defect in next-steps, same shape); gate 9 refuses a redirect on `response.type === 'opaqueredirect' || (status >= 300 && < 400)` (`:1298`), so the page-side reconstruction must make a 3xx take that branch; `readBodyCapped` (`:364`) STREAMS with `reader.cancel()` and falls back to `response.text()` when `body` is null, so the process must cap while reading (the bytes must not have crossed already); the response header whitelist is seven names + `x-ratelimit-*` (`packages/protocol/src/constants.ts:162`), none of them forbidden-response-header names, so a browser `Headers` reconstruction carries all of them; `RunView.tsx:352` already keys the net handler on `allows('connections')`, so `connections:true` mounts the executor and makes `host-ready.net` true STRUCTURALLY — no new wiring; `parseAppBundle` (`packages/protocol/src/app-bundle.ts:205`) is the hand-in tool's validator; `packages/auth` builds clean to `dist/` (1.5 MB) for the process to import.
- **2026-09-07 — `claude -p` flag acceptance MEASURED on 2.1.211** (`--version` probe, no model call): `--output-format json`, `--system-prompt`, `--disallowedTools`, `--no-session-persistence`, `--tools` and **`--max-turns` are ALL accepted** — `--max-turns` is absent from `--help` but the parser takes it, so Q5's fallback is the pin and the flag rides as belt-and-braces. `--tools ""` is documented as "disable all tools".
- **2026-09-07 — 🔴 a Claude Code session exports twelve `CLAUDE_*` variables that an MCP server inherits** and would hand to a spawned child: `CLAUDECODE`, `CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_SESSION_ID`, `CLAUDE_CODE_CHILD_SESSION`, `CLAUDE_CODE_EXECPATH`, `CLAUDE_CODE_MESSAGING_SOCKET`, **`CLAUDE_CODE_MESSAGING_TOKEN`**, `CLAUDE_CODE_ENABLE_TASKS`, `CLAUDE_CODE_ENABLE_SDK_FILE_CHECKPOINTING`, `CLAUDE_AGENT_SDK_VERSION`, `CLAUDE_PID`, `CLAUDE_EFFORT`. The messaging socket + token are a live control channel into the PARENT session. AC9's env strip therefore is not a three-name list: the child env is built by **ALLOWLIST** (`HOME`, `PATH`, `SHELL`, `USER`, `LANG`, `TMPDIR`, `TERM`) — every `CLAUDE_*`, `CLAUDECODE`, `ANTHROPIC_*` and `AWS_*`/`GOOGLE_*` vertex/bedrock override dropped by construction — pinned by a test that asserts the child sees NONE of the twelve measured names and no `ANTHROPIC_API_KEY`.
- **2026-09-07 — the desktop's `subtle-fallback.ts` is for the `tauri://` scheme, not for loopback:** `http://127.0.0.1` is a secure context by spec, so the local page needs no fallback (`crypto.subtle` is required by PKCE at `packages/auth/src/oauth-service.ts:180` and the signing template engine). Confirmed by the plan review's angle 1; if a target browser disagrees the page must refuse the OAuth row by name rather than degrade.
- **2026-09-07 — 🔴 the prompt store COMPILES every `.md` into a committed `content.ts`.** `packages/knowledge/scripts/gen-content.mjs:20,43` sweeps `prompts/**` for `.md/.txt/.py/.html/.json` and writes each file's full text into `src/generated/content.ts` (419,768 B, committed, imported by `layers.ts`). So authoring `prompts/skills/snug/launch-protocol.md` would inflate `content.ts` — and therefore the playground, the desktop AND `snug-host.html` — with text only the MCP process needs, which is exactly what D6 forbids ("`gen-content.mjs` must not compile the kits into `content.ts`"). **Decision (D-B12):** the `instructions` text lives at **`apps/host-mcp/src/instructions.md`**, imported by the process's build with Vite's `?raw`, and `check-host-mcp` byte-compares the built bundle's copy against that one file. When T6 builds the skill it reads the SAME file (one source, byte-compared — lesson 2026-07-31), rather than a prompt-store copy. ADR-0004 is not breached: this is a tool's own instruction string on a control plane the LLM reads, not an LLM prompt the product assembles — and the alternative breaks D6.
- **2026-09-07 — the `/etc/hosts`-free way to reach an HTTPS stub from the process is a `lookup` seam in a test-only build** (the desktop's `cfg(debug_assertions)` remap transposed) → D-B11; a resolved-address re-check would have refused that very stub and is a new policy besides → not done.

## Session journal (append-only, newest last)

### 2026-09-07 06:10 UTC — Jeetu (via Claude Code) — session (Gates 1–2)
- Done: read INDEX (C1–C5), PROCESS, the program record (D0–D15, T3 row, §T3, the lessons appendix), ADR-0065 (§2 B, §3, §4, §8, §10, Alternatives), ADR-0066 A9, the code-map executor + platform-seam rows, architecture "Host kit"; read the seam code myself (`platform.ts`, `connectedFetchDepsFor`, `platform-desktop.ts`, `desktop-transport.ts`, `net-guards.ts`, `scrub.ts`, `fs.ts`/`oauth.ts`, `file-backend.ts`, `compose.ts`/`platform-host.ts`/`probe.ts`/`main.tsx`, `copy.ts`); two exploration reports (lessons/next-steps/threat-model checker; build/gates/deps/e2e/sidecar/userfile/local-mode); verified toolchain facts (claude 2.1.211, `plugin validate`, no SDK in repo, Node 22, plugin/marketplace manifest shapes from an installed marketplace); measured the SDK bundle. Wrote this task file and the ADR-0068 draft; branched.
- State: **Gate 2 — plan printed, awaiting approval + Q1–Q5.** No implementation code.
- Next step: on approval → plan review (finder angles), then Gate 3 red tests in `apps/host-mcp` (step 1).
- Open questions: Q1–Q5 above.

### 2026-09-07 06:40 UTC — Jeetu — plan approval
- Done: owner answered **"yes go ahead"** — the plan stands as written and Q1–Q5 take the recommended defaults: Q1 hand-rolled JSON-RPC over stdio with the SDK as a dev-only interop client; Q2 process-table holder signal now, shared marker queued; Q3 LAN rungs out of scope; Q4 `share` off on the local page; Q5 `--max-turns` verified at Gate 3, else `--tools ""` + `--disallowedTools "*"`.
- State: Gate 2 passed. ADR-0068 moves to `accepted` at Gate 6 (its status line says so). Next: the plan review (finder angles) before any implementation, then Gate 3.
- Next step: plan review → Gate 3 red tests in `apps/host-mcp` (step 1 of the order).
- Open questions: none.

### 2026-09-07 07:30 UTC — Jeetu (via Claude Code) — plan review (four fresh-context finder angles), folded
- Done: four independent reviewers ran against the code, not the prose (lesson 2026-08-21). **Six blocking findings, each verified by me before folding**, and three of them reverse a decision the approved plan had made:
  1. **DNS rebinding (angle 2, reproduced by me with headless Chromium under `--host-resolver-rules=MAP evil.test 127.0.0.1`)** — after a rebind the attacker's page is same-origin to the browser: its GET carried NO `Origin` and NO `Sec-Fetch-Site`, and the only header naming it was `Host: evil.test:59901`. The plan's "the `Host` header is never consulted" is REVERSED (D-B18). Two more from the same run: a different loopback PORT reads as `same-site` (so only the literal `same-origin` may pass), and preflight-forcing is load-bearing rather than incidental.
  2. **Silent data loss (angle 4)** — `userdb.ts:1475` and `driver.ts:315` both swallow a failed `backend.save` with a bare `catch { dirty = true }`, and no persist-failure seam exists. "Open read-only and say so" would have lost an hour of work with the only hint inside a popover. The holder check now REFUSES TO OPEN (D-B24).
  3. **An OAuth flow that could never complete (angle 1)** — `getPlatform().oauth !== undefined` is the wizard's ONE not-a-browser discriminator; installing the seat disables the popup-blocker escape and installs a handle-less pseudo-popup with no null check, so `window.open` (already past its activation, after the PKCE awaits) returns null and the wizard parks on `awaiting_callback` forever. The web popup path + a fixed port replace the desktop transport (D-B13/D-B14).
  4. **Two false-green gates (angle 3, measured)** — `turbo.json` declares only `dist/**`, so a cache hit restores the artifact page and leaves `dist-local/` MISSING (`FULL TURBO`, 26 ms); and an installed plugin is a version-keyed COPY that neither `install` nor `update` refreshes, so the owner's walk would exercise a stale bundle (D-B28, D-B32).
  5. **AC3/AC4/AC5 were unsatisfiable (angle 3)** — both e2e stubs mint their self-signed CA into an ephemeral dir and never expose it; the browser only works because Playwright ignores cert errors, which `node:https` cannot (measured `DEPTH_ZERO_SELF_SIGNED_CERT`). The fixtures gain `SNUG_E2E_CERT_OUT` (D-B29).
  6. **Two AC contradictions (angle 4 + angle 3)** — `handin.ts:91` refuses ANY connected bundle with a message reading "inside an artifact", in the one binding that HAS connections; and AC12's premise was wrong, since the executor and wizard are already in the artifact bundle (measured: same 2,274,084 B for both pages) — `connections:false` is a RUNTIME flag, so D4 governs what a binding claims, never what the bundle contains (D-B26, D-B33).
- Also corrected: the process wall clock 60 → **75 s** (at a tie the executor's self-naming sentence never runs — the defect `next-steps.md` already logs for the LAN leg, reproduced in the other direction); the child env built by ALLOWLIST after measuring `CLAUDE_CODE_MESSAGING_TOKEN` + `_SOCKET` in the inherited environment; `parent-watch.ts` copied with an equivalence test rather than cross-imported (the sidecar's barrel pulls in `baileys`, which `ssr.noExternal` would inline); the guards deep-imported (4,368 B vs 329,902 B — the auth barrel drags in sql.js); `O_EXCL` on the lock with command-identity take-over; and **"value-blind" corrected to "transit-only"** in both docs, because the repo's own lint defines value-blindness as never importing the credential layer at all.
- Rejected nothing outright; every finding either landed as a decision (D-B13–D-B33) or was already sound. Claims the reviewers CONFIRMED: the Vite SSR bundling mechanism works (one file, runs standalone, `import.meta.url` survives); `connections:true` alone mounts the executor and makes `host-ready.net` true structurally; the plugin install flow and `${CLAUDE_PLUGIN_ROOT}` are correct; the fixtures are portable; 43127 is free; `checkHostKitPage` is reusable for the local page; `tokenHash` in the lock is not a leak.
- State: **Gate 2 complete and reviewed; 33 decisions recorded. Starting Gate 3** (red tests first, `apps/host-mcp` step 1).
- Next step: scaffold `apps/host-mcp` and write the failing unit tests for the pure modules.
- Open questions: none blocking. Queued for the owner at Gate 5: whether `packages/db` should gain an `onPersistError` seam (the artifact binding has the same blind spot) — recorded in next-steps rather than done here.

### 2026-09-07 08:15 UTC — Jeetu (via Claude Code) — Gates 3–4, steps 1–2
- Done: **Gate 3 red then Gate 4 green — 122 tests over 9 suites in the new `apps/host-mcp`.** Step 1, the pure modules: `jsonrpc` (framing; a UTF-8 sequence split across chunks; an over-long line dropped and resynced rather than truncated, since a truncated line parses as a DIFFERENT message), `tools` (the four-name allowlist, plus a lint that no tool NAME reads as a data-plane capability, and that no description or schema mentions the bearer), `fetch-proxy` (the executor's gates run twice — the REAL `isForbiddenNetHost` asserted by identity `toBe` against the barrel export; https-only; a 3xx returned as data; `Set-Cookie` dropped; the cap tripped WHILE reading; 75 s so the executor's own 60 s still names the failure; the canary absent from every error, header and body, and no `console.*` called at all), `loopback-gates` (the `Host` wall, with the measured rebind shape as its own case), `brain-claude` (the child env allowlist tested against the twelve measured `CLAUDE_*` names; the pinned argv; SSE), `userdb-fs` (absence is ENOENT only; the quarantine names admitted; atomicity with a crash seam), `lock` (`O_EXCL`; a live stranger by command line is REFUSED, never signalled; release only if still ours). Step 2: `mcp/server.ts` (five methods; an unknown protocol version answered with ours rather than echoed; notifications never answered; a throwing tool becomes an `isError` result), `main.ts`, and the Vite SSR build.
- **The interop test is the D-B2′ evidence:** the official `@modelcontextprotocol/sdk` client drives the SHIPPED `dist/snug-mcp.mjs` over a spawned process — handshake, `instructions`, the four tools with SDK-validated schemas, a call, the allowlist refusal, exit on stdin close, and silence on stdout before it is spoken to. The hand-rolled framing is proven against the reference implementation, not against our own reading of the spec.
- **Measured:** `dist/snug-mcp.mjs` is **8,737 bytes** and contains no sql.js and no provider registry — D-B30's deep import held through the real build (the barrel would have brought 329,902 B). `pnpm exec turbo run build --filter=host-mcp` passes; root `check-host-kit` still ok at 2,274,084 bytes, so the artifact kit is untouched.
- One thing the build taught: the `?raw` import of `instructions.md` needs an ambient declaration (`src/env.d.ts`) because this package deliberately does not take `vite/client`'s whole browser type surface.
- State: **Gate 4 in progress.** Remaining: the loopback listener + control socket + lock wired into `main.ts` (step 2's other half), the `apps/host` local build and its platform (step 3), the playground custody copy (step 4), the manifests + `check-host-mcp` + gate wiring (step 5), the e2e (step 6).
- Next step: the listener and control socket, with their integration tests on real ephemeral ports under a temp `SNUG_HOME`.
- Open questions: none.
