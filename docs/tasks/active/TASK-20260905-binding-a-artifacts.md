# TASK-20260905-binding-a-artifacts: Binding A — Snug apps inside Claude artifacts (T4 of the skill-only Snug program)

- **Status**: in-review — **plan approved as amended 2026-09-05; Gate 3 complete 2026-09-06; Gate 5 diff review DONE + folded 2026-09-06; the child `TASK-20260906-tool-free-kb-inlining` (the build-leg defect the walk found) is FIXED, reviewed and fast-forwarded INTO this branch 2026-09-06 (host 165 + e2e 15, playground 1903, knowledge 222, root `pnpm test` exit 0 at `db6f890`); OWED: the owner's re-walk (AC7 of the child = AC13's build leg: rebuild, republish to the SAME artifact, build an app, journal the version id) + the remaining AC13 legs (save/export/hand-in/Safari)**; then Gate 6 + ONE PR carrying both task files' done-moves
- **Owner**: Jeetu
- **Risk tier**: **high** — the app sandbox runs nested inside a foreign (Anthropic) sandbox (C2 negatives owed); user-file custody moves into page storage (a data-loss class); `packages/db` gains persisted-enum literals and a new file kind at the import boundary; viewer-billed inference (a `sample` call on load would spend a stranger's usage — lesson 2026-09-04); a page-embedded bundle installs code at boot (threat-model R-39 delta)
- **Branch**: `feat/TASK-20260905-binding-a-artifacts` (off `main` `6514f52`)
- **Packages touched**: `apps/host` (the bulk: two brains, two storage kinds, the hand-in, the artifact export seat, e2e harnesses), `packages/db` (`PersistenceKind` + `DbPersistence` append; the `snug-user-file/1` wrapper + `sniffSnugFile`; an `agent` provenance arm on the bundle install/update acts + the delete tombstone), `apps/playground` (custody chip + disclosure copy; the per-purpose adapter seat; the prompt budget refusal; wrapper-aware import; the export act under a new flag; the classifier skip under host; two T2 riders), `packages/knowledge` (two KB rules), `scripts/` (`snug-embed.mjs`, `lib/page-blocks.mjs` + `.d.mts`; `check-host-kit` script line), `docs/`
- **Spec impact**: **none to schemas or the wire protocol.** Two prose-only notes are prepared for T7's spec push (never pushed here): `snug_apps.install_source` gains a third documented value `agent:<lineage>` (SPEC-1.0 §storage row + a spec-changelog advisory line, no version bump — the column is free TEXT with a partial unique index, `userdb-schema.ts:230,339`, same as `share:<lineage>` in ADR-0063; verified: no zod enum lists the prefixes), and the export wrapper `snug-user-file/1` is named beside `.snug` (a `packages/db` sniff concern). → [SPEC_SYNC.md](../../engineering/SPEC_SYNC.md) followed for the changelog line only.
- **Related**: parent record [TASK-20260904-skill-only-snug](TASK-20260904-skill-only-snug.md) (D2, D3, D4, D8, D10, D12, D15; the T4 row; the lessons appendix) · ADR-0065 §2/§5/§6 (proposed) · ADR-0063 (`snug-app-bundle/1`, `installAppFromBundle`/`updateAppFromBundle`, `sniffSnugFile`, §4/§8 consent posture) · ADR-0045 (update act, data kept, §7 edited-copy confirm) · ADR-0059 (disclosure derives from the routing decision) · ADR-0018 D3 (system-slot text) · ADR-0006 (`RUNNER_CSP`) · ADR-0043 (`SNUGENC1`) · threat-model R-21, R-24, R-37, R-39 · T1's numbers (S1, S2, S3, S4b, S10 — `git show deffb88:docs/tasks/active/TASK-20260905-host-bindings-spikes.md`) · T2's seams (`git show 4348f56:docs/tasks/active/TASK-20260905-host-kit.md`; `docs/architecture.md` "Host kit — the third shell") · the `artifact-capabilities` skill (runtime contract **0.2.41** — `sample.d.ts`, `artifact.d.ts`, `downloads.d.ts`, `claude.d.ts` are the authority) · `docs/next-steps.md` 2026-09-05 host-kit entry (the two riders; the owner acts) · lessons 2026-08-03 (A/B slots, empty-read = corrupt), 2026-08-05 (re-derive every bound), 2026-08-20 (count `backend.save` calls), 2026-08-22 (`catch → null` hides corruption), 2026-09-04 (a preview must not spend the viewer's usage on load; enum widening = append)

## Spec (what & why)

**What.** The host kit (`snug-host.html`, T2) learns to live inside a Claude artifact. Today it boots on the demo brain with the browser bucket; after this task, when `window.claude.use` exists and `sample` resolves (A1 — Claude Code / Cowork hosted artifacts) the probe pins **`sample`** as the brain and the page's own html republish as the durable record of the user's file; when `window.claude.complete` exists (A2 — claude.ai / Claude Desktop chat artifacts) it pins **`claude-complete`** as the brain and **`window.storage`** as the file's home. Export leaves through the artifact `downloads` seat as **`snug-user.snug.json`** (a wrapper: the `downloads` allowlist has no `.snug`), and the playground learns to import that wrapper everywhere. A **"your file" chip** beside the brain chip says where the file lives, in which host, and what a republish does to it. Apps are **handed in** by the agent as `snug-app-bundle/1` blocks embedded in the page: boot installs a new lineage as an owned, versioned, editable app and applies a changed html as a new version with data kept (never over a copy the user re-authored — that one is offered); `scripts/snug-embed.mjs` merges bundles into a live page mechanically so an edit is a republish, never a second runner. The nested runner stays exactly as it is (S1: `RUNNER_CSP` is enforced inside the artifact's nested frame); the embedder's narrower CDN policy becomes a knowledge-base rule and an embed-time lint (a loading aid, never a security control — R-21).

**Why.** This is the first demonstrable milestone of the program (T2 → **T4** → T6-Claude → owner E2E): a user with only the `snug` skill builds an app and sees it running inside the Claude they already pay for, with their file under their control (export any time) and every invariant where it was — the sandbox (nested, CSP enforced), the credential boundary (no keys exist in the kit), the one file. Nothing here is an MCP server (ADR-0061).

**Measured facts this design stands on (T1, 2026-09-05):** `sample` on `quick`: median 1.2 s, 48/48 legal chess moves, consent exactly once, every `quick` reply code-fenced (so `sample()` TEXT + Snug's graduated parser is the one parse path; `sample.json` unfences but reports neither `truncated` nor `modelTierApplied`); one concatenated user turn ≡ two turns; `limits().maxPromptBytes` = 65,536; the files form of `artifact.publish` is `capability_disabled` in the owner's own view; html republish of 2 MiB = 1.8 s + a view reload, byte-identical after; `localStorage` 5 ms with a 10 GiB quota at the artifact origin; `window.storage` persists across reloads but is **per view** (in-chat and published link hold separate stores); in chat `<a download>` is inert, `navigator.clipboard` blocked, `execCommand('copy')` works; both viewers block jsDelivr `/gh/` and unpkg, allow jsDelivr `/npm/` and cdnjs, and the hosted viewer blocks every CDN stylesheet/font. **S11 (2026-09-05, this task's Gate 2):** the builder's whole-app turn works on `sample` — 8 of 8 calls that reached Claude returned a COMPLETE fenced document (33 KB chess edit ≈ 94 s on `default`, 107 s on `quick`; a 51 KB app came back as a complete 54 KB document in 223 s with a 70 s first token; a fresh 10 KB build in 40 s), `truncated: false` on all eight, every tier honoured, the input cap inclusive at exactly 65,536 UTF-8 bytes, over-cap refused in 0 ms with `prompt_too_large` and no usage. **Unmeasured, owed here:** `window.claude.complete` (latency, JSON, consent, truncation — only detected); `RUNNER_CSP` enforcement inside the CHAT nested runner; the hosted `downloads.save` path; whether a session-side html republish preserves the page's `snug-db` block; **what the hosted viewer injects into the document before the page's script runs** (the shape of the injected runtime — decides nothing, but the first walk records it).

**Acceptance criteria** (each becomes at least one test; the real-runtime rows are journaled walks with the artifact URL):

1. **`sample` brain (A1).** When `window.claude.use` exists, the probe resolves `sample`, `artifact` and `downloads` in one `Promise.all` behind one named guard (sized from the contract's 10 s; the walk journals the real answer time); `sample` resolved pins `{ kind: 'host', label: 'Claude · this artifact’s viewer', streaming: true, tools: false, maxPromptBytes }` (`limits().maxPromptBytes`, falling back to 65,536 when `limits()` rejects), and `createTurnAdapter`'s `'host'` arm hands back the adapter **for the purpose it was asked for** (the seat gains `chatAdapter?`; absent → `adapter`): the app adapter uses `modelTier: 'quick'`, the builder/inferrer adapter `'default'` (a per-purpose host decision, D15 — never a control; S11 confirmed it: the same 33 KB rewrite took 94 s on `default` and 107 s on `quick` — output dominates, so `quick` buys nothing on builder turns while `default` thinks first; the builder surface shows its thinking state from the call until the first token, which S11 put at 5 s for a 33 KB app and 70 s for a 51 KB one). Both adapters concatenate `system` + the messages into **one user turn** through ONE shaper (`brains/prompt.ts`, also the budget's measuring stick — AC3); call `sample()` TEXT with `cache: false`; forward `onText` as deltas to `onDelta`; map `truncated: true` → `stopReason: 'max_tokens'` (never a parse strike — lesson 2026-08-12); map every `SampleError` to a named `AdapterError` (`not_granted` / `sampling_disabled` → `CONSENT_DENIED`, retryable false; `rate_limited` → retryable **false** with "back off" copy — the runner must never loop; `prompt_too_large` → a named refusal that names the byte count; `cancelled` → `ABORTED`; `empty_completion` / `invalid_json` / `refused` / `upstream_error` named); **never call `sample` on load** — the first think is the first call. The runtime contract rides inside that user turn exactly as `buildHostSystemPrompt({appRuntime})` assembles it today, after the existing `runtimeContractSchema` validation and size caps (unchanged, pinned by the test that the bytes reaching the fake `sample` equal the playground's assembled system text + separator + wire); the authority downgrade (system slot → user turn, ADR-0018 D3) is disclosed in ADR-0065 and gets its own threat-model delta row in T7 (R-24/R-37 were written for other channels). Negative (C1): the adapters read no `snug_secrets`, no key, no URL, and are consumed ONLY through `createDirectAppTransport` / `createDirectBuilder` (never a parallel path), so `guardWireForApp` (the R-9 egress scrub) runs ahead of them by construction — pinned by the existing `hostBrain.test.ts` shape plus a byte scan of what reaches the fake `sample`. **Under the host brain the builder's router classifier is skipped** (the tool-free builder has no data lanes to route to — `useBuilderChat.ts:604-613`), so an app-attached message costs exactly ONE `sample` call, and an artifact landing costs one more (contract synthesis) — both counts pinned.
2. **`claude-complete` brain (A2).** With only `window.claude.complete`, the probe pins `{ label: 'Claude · this chat', streaming: false, tools: false }`; the adapter sends the same one-turn prompt through `complete(prompt)` and parses the text through the same path; `host-ready.capabilities.streaming` reads `false` in the nested frame (the T2 forward at `RunView.tsx:166`, now exercised); a rejection or a non-string reply is a named error, never a hang. **Scope note (review A11):** the kit is ONE html page and a chat bootstrap cannot iframe it (opaque origins cannot reach `parent.claude.complete` — the very property AC10 relies on), so the chat DELIVERY (a script build `snug-host.js` that executes in the bootstrap's own window, its gate, the npm layout, the ≤ 60-line bootstrap) is **its own follow-up task** listed in next-steps and the parent record, ahead of T6's A2 recipe. T4 proves the `complete` adapter, the `window-storage` backend and the chip under the chat harness (in-page fakes on the built html) and leaves the real chat walk owed.
3. **Prompt budget — budget or refuse, never a cut app.** The cap is inclusive at exactly `maxPromptBytes` UTF-8 bytes (S11 arm F: 65,536 accepted; arm G: 65,537 → `prompt_too_large` in 0 ms, no consent, no usage). The measurement is taken on **the identical string the adapter sends** (`brains/prompt.ts` exposes `measurePrompt(system, messages)`; the builder's host arm calls it before `runAgentTurn` — lesson 2026-08-05: a bound re-derived upstream of the sent string is a second bound). Under a pinned `maxPromptBytes`: history is dropped oldest-first, then the docs and schema sections truncate with the existing marker (they are read, not rewritten) — but **the current app html either rides WHOLE or the edit turn is refused** with a named message ("this app is 117 KB; editing inside this artifact fits apps up to ~58 KB — export your file and edit it in the playground"), because S11 arm F showed the failure mode: a chess context cut mid-file plus "write the ENTIRE file" returned a plausible full-length app (ratio 1.01) whose tail the model invented — a silent rewrite of code it never saw, landing as the next version. `appContext.ts` gains only the "html whole or absent" switch; the byte arithmetic lives in the shaper. A 51 KB app fits whole (arm C: 56,446 B in, a complete 54 KB document out). Pinned by tests: a 51 KB app under the cap produces a prompt the fake `sample` accepts with the html intact and `bytes(sent) ≤ maxPromptBytes` asserted on that string; a 117 KB app is refused with zero adapter calls; with no cap the bytes are unchanged (web positive twin, the 140,000-char rule untouched).
4. **`window-storage` backend (A2).** `PersistenceKind` gains `'window-storage'` (append; `DbPersistence` in `driver.ts:24` and both `persistence` getters widen with it; `storageDisclosure`'s switch becomes exhaustive with a `never` arm). The backend stores base64 chunks under **generation-numbered** keys `snug-user/<file>/<gen>/<n>` plus a manifest `snug-user/<file>` carrying the magic, the live generation, the chunk count and sha-256; `save` writes the NEW generation's chunks, flips the manifest, then deletes the old generation (a torn write leaves the previous generation complete — lesson 2026-08-03's A/B discipline); `load` reads the manifest's generation (no fallback to an older generation — the manifest names the ONE live file, and the old chunks are deleted after the flip); `load` returns `undefined` **only** when `list()` proves the manifest key absent (a `list()` that itself throws is CORRUPT, not absent) (S10: `get` of a missing key throws a generic message — the message alone is never "fresh"); a manifest whose live generation is incomplete **throws** (`corrupt`, never a pristine file — lesson 2026-08-03/22); `backend.save` call counts are asserted through a real `openUserDb` round trip; the 20 MB text cap refuses with a named error and offers export. Torn-write fixture: a fake `set` that throws on chunk k → the previous bytes still load.
5. **`artifact-html` durable record (A1).** `PersistenceKind` gains `'artifact-html'`. The working copy stays in the probed browser bucket (OPFS → IDB → memory, unchanged); the record wraps it: `load(USERDB_FILE)` is bucket-first and, when the bucket has no file and the page carries a `<script type="text/plain" id="snug-db">` block (manifest line: magic, byte length, sha-256, **a monotonic save counter and an ISO instant**; then the base64), the block's bytes seed the bucket with ONE explicit, counted `bucket.save` at load (only for `USERDB_FILE`, never the legacy name — `userdb.ts:1299`); a block that fails its own sha is `corrupt` and says so. If the bucket has a file AND the block differs, nothing is overwritten: the chip shows the divergence **with direction** ("this browser’s copy is newer than the page’s" / "older", from the counter) and two acts — "keep this browser’s copy" (marks dirty) and "load the page’s copy" (first copies the bucket file to `user.snug.before-load`, then seeds). A bucket file with NO block on the page is simply dirty, never divergence. **"save to this artifact"** is an explicit act on the chip: it fetches the page's **canonical source** with `fetch(location.href, { cache: 'no-store' })` (the hosted viewer's `connect-src 'self'` admits it; the contract forbids serializing the live DOM — `artifact.d.ts:155-159`: `outerHTML` carries the viewer's injected runtime), verifies the fetched text with the shared top-level tokenizer (exactly one inline module script, the `snug-host-build` stamp equal to the DOM's, only `snug-db` / `snug-app-bundle+json` data blocks otherwise) and **refuses the save with a named reason if that check fails — never falls back to `outerHTML`** (**amended 2026-09-06, measured on the real artifact:** the fetched text is the VIEWER-WRAPPED page — the viewer's own skeleton and two injected classic scripts ahead of the kit's whole document — so the act first lifts the kit document out with `unwrapViewerPage` (the one grammar; any other wrapper shape is a named refusal), verifies THAT, and publishes the bare kit page, which the viewer wraps again; `scripts/fixtures/viewer-wrapper.mjs` is the measured shape and the e2e serves it); then splices the current bytes as base64 into the `snug-db` block (inserting the block before `</body>` when the page has none — the build ships NO placeholder, so `check-host-kit`'s "exactly one inline module script" rule stays true of `dist/`), refuses when the **projected page** (`pageBytes + ⌈userBytes·4/3⌉ + bundleBytes`) would exceed 16 MiB minus a margin (the message names the three parts; export offered), stashes the conflict/result note in `sessionStorage`, and calls `artifact.publish(html)`. `conflict` → the view reloads per contract; the bucket keeps the working copy, and the stashed note renders after reload: "someone published first — your copy is safe here; save again". `too_large` from the runtime is the same named refusal. `not_writer` / `not_granted` / `artifact === null` → read-only: the act is **present until the first refusal** (the contract gives no writability signal before a publish, and Q3 forbids publishing at boot), then hidden, and the chip says "read-only view — export to keep a copy". **The files form is NOT in T4** (review A2: a second durable location boot never reads; every measurement so far says `capability_disabled`) — recorded as a residual in ADR-0065 §2's amendment with the S4(b) re-run as its precondition. Never on a timer, never on `pagehide`. **`artifact-static`:** when `window.claude.use` exists but `sample` AND `artifact` both resolve `null` (the page served top-level on the artifact host — `claude.d.ts:17-20`), the binding is `'artifact-static'`: plain bucket, demo brain, no save act, chip "a copy of the artifact page — nothing saves here; export to keep".
6. **Export.** Where the platform carries the kit's `saveFile` seat, `downloadBlob` (unchanged) hands bytes to it; the kit's seat wraps a user file (SQLite or `SNUGENC1`) as `{"format":"snug-user-file/1","sha256":…,"bytesBase64":…}` — **`format` is the first key by contract**, so the sniff is a byte-prefix check, never a `JSON.parse` of an 85 MB string — named `snug-user.snug.json`, and calls `downloads.save`; an app bundle (already JSON) is saved as `<stem>.snug.json`; the seat **owns its outcome**: `declined` is silent, a second click while a prompt is open is debounced (`rate_limited`), every other code is a named note in `custodyStore` rendered by the chip (the playground's `downloadBlob` fires the seat with `void`). In chat (no `downloads`) the seat copies the wrapper text with `execCommand('copy')` after the two awaits the export path already has (pinned in the chat harness) and the note says so, including the size. `sniffSnugFile` learns `'user-file-wrapper'` (prefix `{"format":"snug-user-file/1"`); `unwrapUserFile` verifies the sha (mismatch → `corrupt`) **and the unwrapped bytes must sniff `'user-file'` before they reach any confirm or import** (review S4: a wrapper's sha proves only self-consistency — a wrapper around a bundle or around arbitrary bytes is refused by name); `looksLikeUserFilePath` and BOTH Settings pickers' `accept` lists admit `.snug.json`; `dispatchOpenedSnugFile` and the Settings picker unwrap before the existing user-file path — so the playground and the desktop import a file the kit exported (program AC1's last clause). **The bundle export is reachable in the kit**: the share sheet's download-only mode (a build with no link acts) is gated by a new surface flag `appExport` (absence = enabled) while `share` (links) stays `false` under host (review A5 — without it the user cannot hand a kit-edited app back to the agent).
7. **"Your file" chip (D8).** Beside the brain chip (`App.tsx` `shell-nav`, every route), disclosure only, copy pinned per `binding × kind × state`: `artifact`+`artifact-html`: "in this artifact — Anthropic-hosted; saved when you save it; a republish from Claude Code rewrites it unless the agent merges it; export any time" (+ dirty / divergence / read-only arms) · `artifact-static`: as in AC5 · `artifact-chat`+`window-storage`: "in this chat’s page storage — this view only; the published link keeps its own; copy to move or keep it" · `file`/`local-host`+`opfs|idb`: the T2 sentence · `memory`: the T2 sentence. Web and desktop render nothing new (positive twin). The Safari case (third-party-frame storage denied → `memory`) is named in the copy and walked.
8. **Hand-in.** The page may carry `<script type="application/snug-app-bundle+json" data-lineage="<uuid>">` blocks (JSON with every `<` written `<`, so `</script` cannot occur). Boot (after the user db opens) parses each with `parseAppBundle` (strict; a bad block is a named note, never a crash). **Resolution order per block:** (1) an app with `install_source = 'agent:<lineage>'` → an update candidate; (2) else an app whose OWN id equals the lineage (the app the bundle was lifted from — a kit-built app or an installed starter, `buildAppBundle` sets `lineage = appId`) → an update candidate for THAT app (its `install_source` is left as is; the starter vouch withdrawing on new pinned bytes is the expected ADR-0045 consequence, journaled); (3) else a **new install** as an OWNED app (`install_source = 'agent:<lineage>'`, find-or-create; note "installed by your agent"; editable, versioned, no "unowned" branch). **Update rule:** an update candidate whose recorded bundle id differs applies **only when the installed copy is unedited** (`current == newest pinned`, computed exactly as `installedCopyForBundle` does); then the html lands as a new pinned version through the bundle update act (note "updated by your agent · vN"; data, chat, docs kept; the previous version revertable; a hub note names it). **An edited copy is never superseded silently**: the bundle is recorded as pending and the run header offers "update from your agent · keeps your data" through ADR-0045 §7's confirm. **A bundle carrying a non-empty `connections[]` is a hard refusal** (named note, nothing installed or updated — D4; the ONE rule, pinned by mutation). A bundle `contract` is admitted as the app's contract (under this binding the agent is the author, exactly as the builder writes contracts today). Idempotent on bundle id via the existing `sharedBundle:<appId>` key (safe: `installShared.ts:47` filters on `share:`). **Delete is honoured:** `deleteApp` on an `agent:` source writes a tombstone `agentDismissed:<lineage>` = the bundle id before cascading, and boot skips a block whose bundle id matches it (a NEW bundle id from the agent installs again); `snug-embed --remove <lineage>` drops the block on the agent side. **Trust boundary, stated:** under Binding A the page is the publisher's output — whoever can republish the artifact can replace the kit's own JavaScript, so a consent gate on bundle blocks would defend nothing against a page writer; the boundary is the artifact's write permission (private by default; writers are the owner's grant), and the guards that remain are the ones that hold *within* it: no connections, no silent supersession of user edits, every version revertable, the hub note, the tombstone. ADR-0065 §6 gets this paragraph; T7's threat-model delta gets an R-39 row for Binding A. Hostile fixtures: a bundle whose html contains `</script>` and `<!--`; two lineages; a stale `snug-db` block beside them; a lineage that spells `starter:`/`share:` (refused by the UUID rule); a bundle with connections; a bundle for a deleted app (tombstoned); an edited copy (zero `saveAppVersion` calls).
9. **`snug-embed.mjs`.** `node scripts/snug-embed.mjs <live.html> --bundle app.json [--bundle …] [--remove <lineage>] [--out file]` merges bundles into a live page by lineage (replace same-lineage, append new, remove on request), **keeps the `snug-db` block and every block it does not own byte-for-byte**, inserts before `</body>` when the page has no blocks, escapes `<`, refuses a page the tokenizer cannot parse or a bundle over the schema caps, and **lints the bundle html against the Binding-A CDN rule** (scripts only from `https://cdn.jsdelivr.net/npm/` or `https://cdnjs.cloudflare.com/`; no unpkg, no `/gh/`; no external stylesheets or fonts) — a named warning by default, a refusal with `--strict`; the lint's own message says it is a loading aid ("will this app load in the artifact viewer"), never a safety check (R-21). The block grammar AND the top-level tokenizer live in ONE module (`scripts/lib/page-blocks.mjs` + a hand-kept `page-blocks.d.mts`, the `release-desktop.d.mts` precedent) imported by the script, by the kit (`artifactHtml.ts`, `handin.ts`), by `check-host-kit.mjs` and by the starters-index plugin — whose `escapeForInlineScript` twin is deleted (`starters-index.ts:19-21`). Imported, not restated; no byte-compare needed. T6 note: the skill's `scripts/` must ship `lib/`.
10. **C2 negatives inside the artifact.** On the built page under the fake hosted harness AND on the real artifact: the app frame keeps `sandbox="allow-scripts"` and `RUNNER_CSP` first in head; a probe app that reaches for `parent.claude`, `top.claude` and `parent.parent` gets a `SecurityError` (opaque origin), and its `fetch` raises the `securitypolicyviolation` (the enforcement signal, not a return value); `host-ready` reports `net: false`, `auth: false`; the app-message envelope carries nothing the transport did not already have (C1 byte scan). **T4 adds NO `message` listener to the host page** — the hand-in reads DOM blocks, the save is a UI act, the only router stays `packages/runner`'s (routed by `contentWindow` identity) — pinned by a source guard over `apps/host/src` (no `addEventListener('message'`); positive twin: the bridge frames still flow (chess plays a move). **The CHAT nested-runner enforcement is OWED** (T1 left it unmeasured; the chat walk is blocked on the npm publish) and the A2 chip copy claims no enforced sandbox until it is measured.
11. **KB rules (the skill's `references/` are the KB).** `80-cdn-compatibility.md` gains "inside a Claude artifact" (the narrowed allowlist, inline CSS and fonts; "this is about loading, not safety"); `10-overview-and-contract.md` gains "never think on a timer — every agent call is a user act" with the reason (viewer-billed usage). Both pinned by the knowledge suite's text tests; `pnpm --filter @snugprotocol/knowledge gen:content` regenerated with its snapshots and the drift test in the same commit.
12. **Honesty.** No dead control: in a read-only artifact the save act disappears on the first refusal and the chip says so; in chat the export button reads "copy"; the brain chip names `sample` / `complete` (ADR-0059 rule 2 — from `resolveActiveBrainFrom`, never parallel state); the inspector's empty state under the DEMO brain in the host kit stops saying "your browser calls the model directly in byok mode" (the T2 rider — the `'host'` arm exists; the demo-in-host arm is the gap); `starterUpdateStatus` no longer fetches a wrapper from the CDN at hub paint (the T2 rider — answered from the baked index).
13. **Real runtime walks (journaled with URLs).** Hosted: the built kit published PRIVATE as an artifact titled `Snug` with `capabilities: {sample: {}, artifact: {}, downloads: true}`; the owner opens it (Chrome AND Safari — the Safari third-party-storage rung is the point), installs Chess, takes one move (consent once, `quick`), saves to the artifact (a version + reload, bytes identical; the boot log records `document.scripts` so the injected-runtime shape is a fact), reloads, exports (`downloads.save` → the file the playground imports), and a session-side `snug-embed` + Artifact republish hands a built app in while preserving the db block. Chat: blocked on the `@snugprotocol/host` npm publish and the chat-delivery task (AC2) — the paste walk is prepared and listed as owed.

**Out of scope**: `artifact-db` chunks (org-internal shared apps — its own task when a shared-app ask exists); the files form of `artifact.publish` (review A2; residual + S4(b) precondition in ADR-0065); the chat DELIVERY build (`snug-host.js` + bootstrap + gate — its own task, AC2); `SKILL.md` text and the A1/A2 recipes' wording (T6 — this task proves the mechanics and writes the recipe as a journal-tested command sequence); `npm publish` of `@snugprotocol/host` / `@snugprotocol/starters`, the kits' GitHub Release (owner acts; listed in next-steps); the micro kit (T5); connected apps in any artifact (D4); the chess `responseGuidance` mismatch (queued in next-steps, a starter release); the starters version-pin gate (T6/T8); any change to `RUNNER_CSP` or the runner's sandbox attributes (a lint on the app side, never a CSP widening); a brain/model/provider control of any kind (D15).

## Plan

### Owner decisions (answered 2026-09-05 — all four defaults; two mechanics amended by the plan review, flagged under "For the owner")

- **Q1 — D2's third path.** ✅ **Confirmed:** keep the nested runner (S1 clause 1 passed in both runtimes) and carry the embedder's narrower CDN allowlist as a KB rule + the `snug-embed` lint — the in-page fallback would run under the same embedder policy and cure nothing; `RUNNER_CSP` itself is not touched.
- **Q2 — Hand-in provenance and auto-apply.** ✅ **Confirmed:** `install_source = 'agent:<lineage>'`, installed as an OWNED app, a changed html applied at boot as a new pinned version. **Review amendment (A3/A4/A6/S1):** auto-apply only when the installed copy is unedited; an edited copy gets ADR-0045 §7's offered update instead; the bundle's lineage is also matched against the app it was lifted from; a deleted app is tombstoned; connections are a hard refusal; the trust boundary is written down.
- **Q3 — The hosted durable save.** ✅ **Confirmed:** explicit act only, no timer, no `pagehide` publish; the browser bucket is the survival copy. **Review amendment (A2/A1):** the files form is dropped from T4 entirely (a second durable location boot would never read; still `capability_disabled` everywhere it was measured), and the page source for the republish is fetched (`fetch(location.href)`), verified, and refused on mismatch — never serialized from the live DOM (the contract forbids it: the viewer injects its runtime into the document).
- **Q4 — The `sample` whole-app spike.** ✅ **Run before approval** — S11, numbers under "Decisions & surprises"; folded into AC1/AC3.

### Design notes (what the code reading and the review settled)

- **Where the brains live.** `apps/host/src/brains/{prompt,sample,complete,errors}.ts`. `prompt.ts` is the one turn-shaper both use and the budget's ruler: `shapeTurn(system, messages)` → `[{role:'user', content}]`, `measurePrompt(...)` → UTF-8 bytes of that exact string. Tool turns cannot occur (`tools: false` → the builder's tool-free arm, T2). `maxOutputTokens` cannot be forwarded (no seat in the contract) — a residual; `truncated` is the signal.
- **Per-purpose adapters.** `PlatformBrain`'s host arm gains `chatAdapter?: AgentAdapter`; `createTurnAdapter`'s `'host'` case returns `purpose === 'chat' ? pinned.chatAdapter ?? pinned.adapter : pinned.adapter` (`adapter.ts:139-148`; the builder passes `'chat'`, the app transport `'app'`, the inferrer `'chat'`). The kit pins `adapter = sample@quick`, `chatAdapter = sample@default`.
- **The probe becomes async only where a host answers.** `probeBrain` calls `claude.use('sample' | 'artifact' | 'downloads')` in one `Promise.all` ONLY when `window.claude.use` is a function, behind one named guard (`HOST_ANSWER_MS`, sized from the contract's 10 s and re-sized from the walk's journaled number). Legs recorded per capability (`detected` / `resolved` / `null`). `sample` null + `artifact` null → binding `'artifact-static'`; `sample` null alone → the demo brain with the legs recorded, and the chip says so.
- **Storage is two seams, not one.** A2's `window-storage` IS a `PersistenceBackend` (the file's only home). A1's `artifact-html` is a **record over the probed bucket**: `createArtifactRecord({ bucket, block, canonicalSource, publish })` → a `PersistenceBackend` whose `kind` is `'artifact-html'` (the disclosure), whose `load` is bucket-first with block seeding (one counted `bucket.save` at load, `USERDB_FILE` only), whose `save` is the bucket's save plus a dirty flag; the **publish** is a separate explicit act the chip calls. `openUserDb`'s debounced `save` count therefore stays the bucket's plus the named seed (asserted).
- **Canonical page source.** `fetch(location.href, { cache: 'no-store' })` at save time; verified by the shared tokenizer; refused by name on mismatch. The e2e round trip (route-fulfilled spliced page → boot → save → captured html → served again → same stamp, same blocks) proves it; the fake harness serves the spliced page through `page.route(KIT_URL, …)` — **never by writing into `dist/`** (`check-host-kit` and the static server both refuse a second file).
- **Export rides the existing `saveFile` seat** (`downloadBlob` prefers it — W2b item 6; exactly two callers: `SettingsView.tsx:756` and `exportShare.ts:126`). The kit's seat decides wrapper vs bundle by `sniffSnugFile`, owns every `DownloadsError`, and writes notes to `custodyStore`. The bundle download is reached through the share sheet's download-only mode under the new `appExport` flag.
- **Hand-in reuses ADR-0063's install/update acts** with a `provenance: 'agent'` option on `installAppFromBundle` / `updateAppFromBundle` (`agentInstallSource(lineage)`; the lineage check at `app-bundle.ts:285` takes the provenance; `declareSharedConnections` is never reached because the `agent` arm refuses connections at the boundary), so DDL replay, docs absent-only, contract-in-the-same-call and the half-made-app rollback are inherited. The `edited` predicate is `installedCopyForBundle`'s, lifted into `packages/db` so boot and the run header share it. The tombstone is written in `deleteApp`'s cascade (an `agent:` source only).
- **One block grammar + one tokenizer.** `scripts/lib/page-blocks.mjs` (pure string functions: tokenizeTopLevel — moved from `check-host-kit.mjs` — find/replace/append/remove the `snug-db` block and the bundle blocks, escape/unescape, verifyKitPage) + `page-blocks.d.mts`. Verified feasible under the host's tsconfig (`moduleResolution: Bundler`; TS7016 without the `.d.mts`, green with it); Vite and vitest resolve the relative path outside the package root.
- **Chip copy is derived, never parallel** (ADR-0059): `custodyDisclosure(platform.binding, platform.userdbBackend?.kind, state)` in `platform/copy.ts`, exhaustive `never` arms; the chip reads the platform seats and a small `custodyStore` (dirty / read-only / divergence direction / note) the record and the export seat update.
- **Test wiring.** The two new node:test files are appended to the root `check-host-kit` script (rides `ci.yml:107` and the gate-local workspace leg unchanged; `UNMAPPED_CI_STEPS` untouched) — never a new root script (a script missing from `ci.yml` would silently not run in CI).

### Files to touch (in order; tests FIRST in each step — [TDD.md](../../engineering/TDD.md))

1. **`packages/db`** — `persistence.ts` (`PersistenceKind` + `'window-storage' | 'artifact-html'`), `driver.ts:24` (`DbPersistence` widened; both `persistence` getters), new `userdb/user-file-wrapper.ts` (`USER_FILE_WRAPPER_FORMAT`, `USER_FILE_WRAPPER_PREFIX`, `wrapUserFile`, `unwrapUserFile` → re-sniffed, size cap) + `sniffSnugFile` → `'user-file-wrapper'`; `userdb/app-bundle.ts` (`agentInstallSource`, `provenance` option, `agent` refuses connections, `isEditedCopy`, agent notes); `userdb.ts` `deleteApp` tombstone + `agentDismissedSettingKey`; `index.ts` exports. Tests: `__tests__/user-file-wrapper.test.ts` (round trip; sha mismatch → corrupt; wrapper-around-bundle and wrapper-around-garbage refused; BOM/whitespace; prefix sniff on a 64 MiB-shaped head), `app-bundle` tests extended (agent install owned; update keeps rows; connections refused under agent; lineage rule; edited-copy predicate; tombstone written on delete).
2. **`apps/host` brains** — `src/brains/*.ts` + `__tests__/brains.test.ts` (fake `sample` recording input/options; every error code; `truncated`; deltas; tier per adapter; `measurePrompt` equals the sent bytes; `complete` non-string reply; C1 byte scan; `limits()` rejection → 65,536).
3. **`apps/playground` seat + budget + classifier** — `platform/platform.ts` (`chatAdapter?`, `appExport` flag, `custody` seat type), `agent/adapter.ts` (`'host'` arm by purpose), `agent/builder.ts` (host-arm refusal via `measurePrompt` before `runAgentTurn`), `agent/appContext.ts` (html whole-or-absent switch), `agent/useBuilderChat.ts` (classifier skipped under host; call counts), `agent/inferrerAdapter.ts` (purpose). Tests: `hostBrain.test.ts` extended (two adapters by purpose), `appContext.test.ts`, a builder test (51 KB rides whole; 117 KB refused with zero adapter calls; history dropped before docs/schema; no cap = unchanged bytes), `useBuilderChat` count pins. Then `desktop` + `host` suites (dependents rule).
4. **`apps/host` probe + platform** — `probe.ts` (async arm, `Promise.all`, guard, `'artifact-static'`, legs), `platform-host.ts` (`saveFile` seat, `custody` seat, `appExport: true`, `share: false`), `main.tsx` (hand-in after db ready; boot log of `document.scripts`); `__tests__/probe.test.ts` + `platform-host.test.ts` extended (a `use` that resolves `null` per capability; a `use` that never resolves trips the guard and boots on demo; static binding).
5. **`apps/host` storage** — `src/storage/windowStorage.ts`, `src/storage/artifactHtml.ts`, `src/storage/custodyStore.ts`; `__tests__/windowStorage.test.ts` (generations, torn write, absent-only-after-list, corrupt cases, cap), `__tests__/artifactHtml.test.ts` (seed counted, divergence with direction, before-load copy, save act, projected-page refusal, canonical-source verification refusal, conflict/not_writer/null copy, `backend.save` counts through `openUserDb`), a relaunch harness (open → write → "reload" → open again → rows present) per lesson 2026-08-19.
6. **`apps/playground` chip + import + export + riders** — `platform/copy.ts` (`storageDisclosure` exhaustive + `custodyDisclosure`), new `views/YourFileChip.tsx` mounted beside `BrainChip` (`App.tsx` `shell-nav`), `platform/openFile.ts` (`.snug.json` path rule; unwrap + re-sniff before the confirm), `views/SettingsView.tsx` (both `accept` lists; unwrap in the picker), `share/ShareSheet.tsx` + `run/RunHeaderActions.tsx` (`appExport` gate for the download-only mode), `run/LlmInspectorPanel.tsx` (demo-in-host copy), `starter/starterUpdate.ts` (no CDN fetch at paint). Tests: `copy.test.ts` arms, `YourFileChip.test.tsx`, `openFile.test.ts` (`.snug.json` fixture through `dispatchOpenedSnugFile`; wrapper-around-bundle refused), share-sheet gate test, inspector copy pin, `starterUpdate` no-fetch pin.
7. **Hand-in + embed** — `scripts/lib/page-blocks.mjs` + `.d.mts` + `page-blocks.test.mjs` (tokenizer moved here; `check-host-kit.mjs` and `starters-index.ts` import it; the twin deleted); `scripts/snug-embed.mjs` + `snug-embed.test.mjs` (hostile fixtures under `scripts/fixtures/`; `--remove`; `--strict`); root `package.json` `check-host-kit` line; `apps/host/src/handin.ts` + `__tests__/handin.test.ts` (resolution order, unedited auto-apply, edited → pending + offered, connections refused, tombstone, idempotence).
8. **KB** — the two rules; `gen:content` + snapshots + drift test; `sdk` KB≡SDK suite.
9. **e2e** — `apps/host/e2e/artifact.spec.ts` (hosted harness via `addInitScript`: `window.claude.use` fake — `sample` callable with `.limits()`/`.json`, scripted replies; `artifact.publish` + `downloads.save` recorders; `null` arms; `not_writer` arm; spliced pages served by `page.route`) and the chat harness (`window.claude.complete` + `window.storage` fakes with S10's envelope shapes) — per AC1/2/5/6/7/8/10/12; `check-host-kit` re-measured (ceiling headroom ≈ 529 KB today).
10. **Docs** — `architecture.md` (host kit section: bindings A1/A2 wiring, the two storage seams, the hand-in and its trust boundary), `code-map.md` rows, `glossary.md` (your-file chip, hand-in, `snug-user-file/1`, `artifact-static`), parent record T4 row + D2/D3 amendments + the chat-delivery follow-up child, ADR-0065 §2/§5/§6 dated amendments (files form residual; `agent:` provenance + trust boundary; contract-in-user-turn residual softened to "output ceiling not hit up to 54 KB"), `lessons.md`, `next-steps.md` (owner acts; the chat walk + chat delivery owed), the two spec prose notes and the two threat-model delta rows for T7.

### Test plan summary (Gate 3; one per AC, negatives for C1/C2)

| AC | Test | Kind |
|---|---|---|
| 1 | `brains.test.ts`: one-turn shape; tier by adapter; `cache:false`; deltas; `truncated`→`max_tokens`; every `SampleErrorCode` → named `AdapterError`, `retryable:false`; no call on construction; C1 scan; `limits()` fallback. `hostBrain.test.ts`: purpose routing. `useBuilderChat`: one call per attached message, one more per landing | vitest |
| 2 | `brains.test.ts` (`complete`): prompt equality with the `sample` shape; non-string → named error; `streaming:false` reaches `host-ready` (e2e chat harness) | vitest + e2e |
| 3 | builder test + `appContext.test.ts`: 51 KB html rides whole and `bytes(sent) ≤ cap` on the identical string; 117 KB → named refusal, zero adapter calls; history dropped before docs/schema; no cap = unchanged bytes | vitest |
| 4 | `windowStorage.test.ts` (generations, torn write keeps the previous bytes, absent-only-after-list, corrupt, cap) + a real `openUserDb` round trip with a fake `window.storage` (S10 shapes verbatim); relaunch harness | vitest |
| 5 | `artifactHtml.test.ts`: seed counted; divergence direction; before-load copy; canonical-source verification refusal (an injected script in the fetched page → named refusal, zero publishes); projected-page refusal; conflict note survives reload; `not_writer`/`null` → read-only after the first refusal; `backend.save` counts; e2e round trip through `page.route` | vitest + e2e |
| 6 | `user-file-wrapper.test.ts` (incl. wrapper-around-bundle refused); kit `saveFile` seat tests (wrapper vs bundle; declined silent; debounce; notes); `openFile.test.ts` (`.snug.json` route; re-sniff); share-sheet `appExport` gate | vitest |
| 7 | `copy.test.ts` (every arm, `never`), `YourFileChip.test.tsx`; e2e presence on every route; web/desktop twin renders nothing | vitest + e2e |
| 8 | `handin.test.ts` with the hostile fixtures; resolution order (agent source → lifted-from app → install); unedited auto-apply; edited → zero `saveAppVersion` + pending offer; connections refused; tombstone; idempotence | vitest |
| 9 | `page-blocks.test.mjs` (tokenizer + blocks; insert-when-absent); `snug-embed.test.mjs`: merge/keep/replace/remove; `</script` fixture; lint warnings + `--strict`; `check-host-kit` still green on `dist/` | node:test |
| 10 | e2e: `parent.claude` / `top.claude` / `parent.parent` → SecurityError; `securitypolicyviolation` on fetch; sandbox attr + CSP first; C1 envelope scan; source guard: no `message` listener in `apps/host/src` | vitest + e2e + real artifact walk |
| 11 | knowledge text tests + snapshot/drift | vitest |
| 12 | e2e read-only harness: save act gone after the first `not_writer`; chat: export reads "copy"; inspector demo-in-host copy; `starterUpdate` no request at paint (route policy `all`) | vitest + e2e |
| 13 | journaled walk with URLs (hosted, Chrome + Safari); the chat walk owed | walk |

Mutation checks (each guard flipped once, red at its pin): absent-only-after-`list()`; the sha check AND the re-sniff in `unwrapUserFile`; the projected-page refusal; the canonical-source verification (skip it → the injected-script fixture publishes → red); the `agent` connections refusal; the UUID lineage rule; the edited-copy guard (remove it → `saveAppVersion` called → red); the tombstone; the no-publish-at-boot rule (a boot with a stale block records zero `publish` calls); the `<` escape in `page-blocks`; the classifier skip (remove it → two calls → red); `measurePrompt` on the sent string (a 1-byte drift → red).

### Cross-package impact (dependency graph in [architecture.md](../../architecture.md))

`packages/db` → run `sdk`, `playground`, `auth`, `host`, `desktop`. `apps/playground` source → `desktop` + `host` (both alias it; the kit e2e opens the rebuilt page). `packages/knowledge` → `server`, `playground`, `desktop`, `sdk` (KB≡SDK). `scripts/` → root `check-host-kit` script (two new node:test files; `ci.yml` and `gate-local` unchanged). `packages/protocol`, `packages/runner`: **untouched** (if a step finds it needs either, stop and re-plan — High-tier area, spec-sync).

### Verification standard

Root `pnpm test` exit 0 with `turbo run test --force`; `pnpm --filter host build && pnpm --filter host test:e2e` green (served + `file://`); `pnpm run check-host-kit` green with the ceiling re-measured; `gate:local` legs named PASS / FAIL / DESELECTED in the PR; the hosted walk performed by the owner in Chrome and Safari and journaled with the artifact URL, version ids and the boot's `document.scripts` log; a diff review after (the plan review is done).

### Hard stops (owner acts — prepare, print, journal, STOP)

Publishing anything to npm (the chat binding's prerequisite); creating or editing a GitHub Release; deploying the playground; pushing to `snugprotocol/spec` (T7); the owner's opens of the private kit artifact (the D14 loop — the session publishes, the owner opens).

### For the owner (approval message)

Three things the review changed after your Q1–Q4 answers, none of them widening scope: (1) **Q3's files-form arm is dropped from T4** — it would have created a second durable location boot never reads, and it is still `capability_disabled` everywhere it was measured; the page republish is the one durable act. (2) **Q2's auto-apply keeps the user's edits safe** — an unedited copy updates at boot as you chose; a copy you re-authored in the kit's builder is offered the update through the existing confirm instead of being superseded silently (ADR-0045 §7), and a deleted app stays deleted. (3) **The chat DELIVERY (a script build the bootstrap can execute in its own window) becomes its own follow-up task** — the kit is one html page and a chat bootstrap cannot iframe it; T4 still ships and tests the chat brain and storage. Approve, or amend.

### Plan review (2026-09-05, fresh context, three lenses — every finding dispositioned; the fold is in place above)

**Security lens** — S1 (blocker: auto-install as owned skips ADR-0063's consent; two contradicting connections sentences) → folded into AC8: ONE hard-refusal rule for connections; edited copies never superseded; the trust boundary written down; R-39 delta row for T7. S2 (contract in the user turn) → AC1: validation and caps unchanged, bytes pinned, ADR + T7 delta row. S3 (host-side message routing for new seams) → AC10: no new `message` listener, source guard, `top`/`parent.parent` probes, positive twin. S4 (wrapper sha proves only self-consistency) → AC6: re-sniff after unwrap; hostile fixtures. S5 (lint is not a security control) → AC9/AC11 copy.
**Adversarial lens** — A1 (blocker: `outerHTML` capture contradicts the contract; the viewer injects its runtime) → AC5: `fetch(location.href)` + tokenizer verification + refusal, never a DOM fallback; the walk logs `document.scripts`. A2 (files form = a second durable location) → dropped from T4; residual in ADR-0065. A3 (edited copy superseded) → AC8 unedited-only + offered update. A4 (lineage ≠ install_source for kit/starter apps → duplicate) → AC8 resolution order. A5 (bundle export unreachable under `share:false`) → AC6 `appExport` flag. A6 (delete → reload re-installs) → AC8 tombstone + `--remove`. A7 (read-only/static undecidable at boot) → AC1/AC5 `Promise.all`, `artifact-static`, present-until-refused. A8 (window-storage torn write destroys the last good copy) → AC4 generations. A9 (`.snug.json` fails the path rule and pickers) → AC6. A10 (budget computed upstream of the sent string) → AC3 `measurePrompt`. A11 (A2 delivery undefined) → AC2 scope note + follow-up task. A12 (divergence without direction; conflict note lost; absent block undefined) → AC5. A13 (single-homing was two copies) → AC9 imports, twin deleted. A14 (8 MiB measures the wrong quantity) → AC5 projected page. A15 (chat C2 row cannot run here) → AC10 OWED. A16 (limits fallback, seat notes, async copy, export serialization) → AC1/AC6. A17 (Safari third-party storage) → AC7/AC13.
**Feasibility lens** — F1 (no per-purpose seat) → design note + step 3. F2 (`DbPersistence` breaks) → step 1. F3 (classifier + synthesis calls under host) → AC1 skip + counts. F4 (root test chain is a list) → design note + step 7. F5 (`check-host-kit` refuses non-module scripts and a second dist file) → no placeholder; insert-when-absent; `page.route`. F6 (capture timing) → superseded by A1. F7 = A14. F8 (double `load`, seed = a save) → AC5 `USERDB_FILE` only, counted. F9 (seat is `Promise<void>`, fire-and-forget) → AC6 seat owns notes. F10 = A7. F11 (`gen:content` + snapshots) → AC11. F12 (inspector rider targets the wrong arm) → AC12. F13 (idempotence key + note strings) → AC8.
**Confirmed sound by the reviewers (not re-checked in Gate 3):** `install_source` is free TEXT with a partial unique index and no enum; `agent:`/`share:` cannot collide; every prefix reader falls through safely; the artifact origin is per artifact; `claude.use` + `limits()` at boot spend nothing; the `'host'` arm receives `purpose`; F15 and key reads skipped under host; `<` escaping makes `</script`/`<!--` unspellable; `parseAppBundle` refuses non-UUID lineages; `updateAppFromBundle` is DDL-first and reusable with a provenance parameter; `extractAppHtml` tolerates prose before the fence; `storageDisclosure` has one consumer; `RUNNER_CSP` frozen; `guardWireForApp` runs ahead of every direct transport arm; default export strips secrets and `secretsUsable()` gates import stripping by capability; `downloads` allows `json`; `.mjs` + `.d.mts` imports work under the host's tsconfig; `addInitScript` runs before page scripts on http and `file://`; `hostReadyStreaming` is forwarded; 529 KB of ceiling headroom.

## Decisions & surprises

- **2026-09-05 — the files form is out of T4 (deviates from the parent's D3 "retried once at boot" and from Q3's "tried on the first save").** The contract forbids publishing on load, and a files-form success would create a second durable record the html-block boot never reads (review A2). It stays a residual in ADR-0065 §2 with the S4(b) re-run as its precondition.
- **2026-09-05 — `artifact-html` is a record over the bucket, not a replacement backend.** S4b measured the bucket at 5 ms and the republish at 1.8 s + a reload; a backend whose every debounced `save` republished would reload the view on every keystroke. The bucket is the working copy; the republish is the durable act (Q3).
- **2026-09-05 — the page source for a republish is fetched, never serialized.** `artifact.d.ts:155-159` says so outright (`outerHTML` carries the viewer's injected runtime), and T1's S4b note ("the viewer injects its runtime into a page the page itself published") is the measurement behind it. Verification by the shared tokenizer, refusal on mismatch; the first walk records what is injected.
- **2026-09-05 — S11 measured (probe `38974070-6821-43fd-b0a4-7a98f956aa00`, `results/s11`, run 21:51–22:06Z, 10 calls, 0 db failures).** `limits()` = `{maxPromptBytes: 65536, tools: {maxCount: 16}}` (no `images`). Consent dialog seen on call #1 only (call took 93.4 s including the dialog; first token 4.1 s). Inputs: system (tool-free builder layers + `WEBLLM_BUILD_SUFFIX`) 4,651 B; chess turn 37,736 B; flying-pig turn 56,446 B; trade-copilot turn 122,116 B; fresh build 4,814 B. **A** chess edit, `default` ×2: 93.4 s / 94.6 s, first token 4.1 / 5.8 s, 165 `onText` calls, both complete fenced documents (33,011 / 32,842 B, ratio 1.01), `truncated: false`, tier honoured, a one-line explanation after the fence. **B** same on `quick` ×2: 107.2 / 107.1 s, first token 1.1 / 0.9 s, complete, tier honoured (one reply opened with a sentence before the fence — the extractor's fence rule handles it). **C** flying-pig, `default`: 223.1 s, first token 69.6 s, complete 54,067 B (ratio 1.05), not truncated. **D** fresh pomodoro build: 39.9 s, complete 10,599 B. **E** 122,116 B: `prompt_too_large` "the prompt exceeds the 64 KiB limit", 0 ms, no `onText`, nothing spent. **F** exactly 65,536 B (chess cut with the marker + space padding): accepted, 91.1 s, complete 32,848 B (ratio 1.01 — the cut tail was re-invented, see AC3). **G** 65,537 B: `prompt_too_large`, 0 ms. **H** chess on `complex`: 114.7 s, first token 3.7 s, complete, tier honoured. **Consequences:** builder tier `default` (AC1); budget-or-refuse for the app html (AC3); the output ceiling was not reached at 54 KB and stays an undocumented residual (the ADR keeps the sentence, softened to "not hit up to 54 KB"); every reply is code-fenced, so `extractAppHtml`'s fence path is the normal path; the builder needs its thinking state for 5–70 s before the first token.
- **2026-09-05 — `agent:<lineage>` is prose, not schema.** `install_source` is free TEXT in `snug_apps` (SPEC-1.0 §storage; `userdb-schema.ts:230`); `share:<lineage>` landed the same way (ADR-0063 §5). No `USERDB_SCHEMA_VERSION` bump, no protocol change; a spec-changelog advisory line rides with T7.
- **2026-09-05 — the hand-in's trust boundary is the artifact's write permission, not a consent gate on blocks.** A page writer can replace the kit's own JavaScript; a gate on blocks would defend nothing against them. What holds inside the boundary: no connections, no silent supersession of user edits, revertable versions, the hub note, the tombstone (review S1, written into AC8 and owed to ADR-0065 §6 + T7's R-39 row).

## Session journal (append-only, newest last)

### 2026-09-05 — Jeetu (via Claude Code) — session (Gates 1–2)
- Done: task file drafted from the parent record's T4 row, T1's numbers, T2's seams and the code (`apps/host/src/*`, `packages/db` persistence + app-bundle + sniff, the playground's `'host'` arms, `exportDb`/`saveFile`, `sharedInbox`/`installShared`, the runtime contract 0.2.41 type definitions). Four owner questions (Q1–Q4) with defaults; the plan, test plan and file order written above. Branch `feat/TASK-20260905-binding-a-artifacts` created off `main` `6514f52`.
- Then: **the owner answered Q1–Q4 with all four defaults** (nested runner + KB rule/lint; `agent:<lineage>` owned + auto-apply; explicit save act, files form on first save; spike now). **S11 probe authored and published PRIVATE**: `scripts/spikes/s11-gen.mjs` emits `s11-sample-whole-app.html` (451,562 B; system 4,651 B = the tool-free builder layers + `WEBLLM_BUILD_SUFFIX`; chess block 32,947 B, flying-pig block 51,641 B, trade-copilot block 117,359 B; 8 arms / 10 calls: A chess×2 `default`, B chess×2 `quick`, C pig `default`, D fresh build `default`, E over-cap copilot, F exactly `maxPromptBytes`, G cap+1, H chess `complex`) → **https://claude.ai/code/artifact/38974070-6821-43fd-b0a4-7a98f956aa00** (`capabilities {sample, db}`, contract 0.2.41; results land in `results/s11`, read back with `read_db`).
- State: stopped for the owner's open of S11.

### 2026-09-05 22:10 UTC — Jeetu (via Claude Code) — S11 folded in
- Done: the owner ran S11 (10/10 calls, consent once, ~15 min); results read through `read_db` and journaled under "Decisions & surprises". Both open questions answered: `default` stays the builder tier (`quick` is no faster on whole-app output and thinks less); no truncation up to a 54 KB reply. AC1 and AC3 amended in place — AC3 became **budget-or-refuse** after arm F showed a cut context yields a plausible re-invented app.
- State: plan complete; the fresh-context plan review spawned.

### 2026-09-05 22:45 UTC — Jeetu (via Claude Code) — plan review folded (Gate 2 complete)
- Done: three fresh-context reviewers (security lens, adversarial document reviewer, feasibility reviewer) read the plan, the parent record, the ADRs, the runtime contract's type definitions and the code; 35 findings (2 blockers, 18 majors, the rest minor/advisory), every one dispositioned in the "Plan review" section and folded into the ACs, design notes, file order and test plan. The two blockers: the live-DOM capture (the contract forbids it — now a fetched, verified, refusable canonical source) and the consent-free hand-in (now: connections a hard refusal, edits never superseded silently, delete honoured, the trust boundary stated). Three items are flagged to the owner in "For the owner" because they touch the mechanics of Q2/Q3 and the A2 scope. The reviewers' citations were spot-checked against the contract and the code before folding (artifact.d.ts:155-159; claude.d.ts:17-20; openFile.ts:51; SettingsView.tsx:928/939; the two `downloadBlob` callers; the `starters-index.ts` twin; `app-bundle.ts:285`).
- State: **STOPPED for the owner's plan approval.**
- Next step: on approval → Gate 3 step 1 (`packages/db`: enum + `DbPersistence`, the wrapper with re-sniff, the `agent` provenance arm, the tombstone — tests first) → step 2 brains → step 3 the playground seat + budget + classifier skip → … → the hosted walk. Before the walk: build, `check-host-kit`, e2e. Owner acts still queued: npm scope + `@snugprotocol/host` / `@snugprotocol/starters` publish (the chat walk's prerequisite), the kits' GitHub Release; new follow-up child: the chat delivery build.
- Open questions: the three "For the owner" items (approve or amend).

### 2026-09-05 23:00 UTC — Jeetu — plan approval
- **Approved as amended** ("Approve as amended (Recommended)"): the files-form arm out of T4, unedited-only auto-apply with the offered update for edited copies, the chat delivery as its own follow-up task. Gate 3 begins.

### 2026-09-05 23:50 UTC — Jeetu (via Claude Code) — Gate 3 steps 1–3
- Done, tests first, each guard mutation-checked: **step 1 `packages/db`** (`c5d68e2`) — `PersistenceKind` + `DbPersistence` append (`window-storage`, `artifact-html`); `userdb/user-file-wrapper.ts` (`snug-user-file/1`: `format` first, prefix sniff `'user-file-wrapper'`, sha + payload re-sniff on unwrap, 64 MiB-shaped cap before parse); the `agent` provenance arm on `installAppFromBundle` / `updateAppFromBundle` (`agent:<lineage>`, connections = hard refusal `USERDB_AGENT_CONNECTIONS_REFUSED` before any DDL, the lifted-from app as an update target, a `share:` copy never, notes "installed/updated by your agent"), `isEditedCopy` (the one predicate), the `agentDismissed:<lineage>` tombstone written inside `deleteApp`'s transaction; db 475/475 (25 new), every dependent type-checks. **Step 2 `apps/host/src/brains`** — `prompt.ts` (one shaper: a single user message → ONE string `system + '\n\n' + content`; history → a leading user turn + turns; `measurePrompt` = the bytes of what is sent), `errors.ts` (the SampleErrorCode table → named codes, `retryable` only on `upstream_error`, CANCELLED with partial text, HOST_ERROR for the unknown), `sample.ts` (text verb, `cache:false`, tier per adapter, whole-text → deltas, `truncated` → `max_tokens`, `model: 'claude (<tier>)'`, tool traffic refused), `complete.ts` (one string, one delta, non-string reply named, abort discards); 28 tests. **Step 3 `apps/playground`** — the seat gains `chatAdapter?` + `promptBytes?` and `HostSurface` gains `appExport`; `createTurnAdapter`'s host arm routes by purpose; `agent/promptBudget.ts` (`PROMPT_TOO_LARGE_CODE` — imported by the kit's table, one string; `HOST_CONTEXT_CAPS` html unbounded / schema 4,000 / docs 6,000 / history 4,000; `fitHostTurn` drops history oldest-first then refuses; `promptTooLargeMessage` names the bytes and the cap); `builder.ts` measures the identical string before `runAgentTurn` and refuses with zero adapter calls; `buildAppTurnContext` takes per-call caps; `classifierApplies` (router skipped under host); playground 1871/1871, host 86/86, desktop + host type-check.
- State: steps 1–3 committed on the branch; next is step 4 (probe async arm, `artifact-static`, the platform seats) then 5 (storage).
- Next step: `probe.ts` — `Promise.all` over `sample`/`artifact`/`downloads` behind one guard, the legs record, `'artifact-static'`, the two sample adapters + ruler pinned on the platform; `platform-host.ts` — `appExport: true`, `saveFile`, `custody` seats.
- Open questions: none.

### 2026-09-06 — Jeetu (via Claude Code) — Gate 3 steps 4–7
- Done, tests first, every guard mutation-checked: **step 4 probe** (`6ae8ac4`) — `resolveHostNamespaces` asks `sample`/`artifact`/`downloads` together behind one guard (`HOST_ANSWER_GUARD_MS` 15 s, above the contract's own 10 s null); `'artifact-static'` when `use` exists but sample AND artifact are null; the pinned brains (`sample`: `adapter`=quick, `chatAdapter`=default, `promptBytes`=the shaper, the cap from `limits()` with the 65,536 fallback; `complete`: the chat adapter, `streaming:false`); a tripped guard keeps `artifact` (never static). **Step 5 storage** (`2963acc`) — `scripts/lib/page-blocks.mjs` (+`.d.mts`): the ONE tokenizer (moved out of `check-host-kit.mjs`, which imports it) + the `snug-db` / bundle-block grammar + `verifyKitPage`; `escapeForInlineScript` now has one home (the starters plugin's twin deleted, the drift pin rewritten to "imports, never restates"); `windowStorage.ts` (generation-numbered chunks, manifest flipped LAST, absence only by `list()`, corrupt never fresh, the 20 MB cap); `artifactHtml.ts` (the record over the bucket: seed-on-empty with one counted save + the magic-prefixed custody sidecar, divergence with direction, `fetch(location.href)` + `verifyKitPage` + refusal, projected-page refusal naming the three parts, every runtime code mapped, the conflict note stashed in sessionStorage); `custodyStore.ts`; `sha256.ts` (WebCrypto or pure, FIPS vectors pinned). **Step 6 playground** (`1c05291`) — `CustodyState`/`CustodySeat` on the platform (one home; the kit imports the type), `custodyDisclosure` + the two storage arms with a `never`, `YourFileChip` beside the brain chip (acts only from the seat; nothing on web/desktop), `.snug.json` through `dispatchOpenedSnugFile` (unwrap → re-sniff → the confirm) and both Settings pickers, `appExport` (the share door stays as the download-only sheet; links still off), the inspector's demo-in-host copy, `starterUpdateStatus` answered from the catalogue (two offline tests moved their html-read intent to the update act). **Step 7 hand-in + embed + composition** — `handin.ts` (the resolution order, unedited auto-apply, edited → pending, tombstone, connections refused, hostile blocks named, `describeHandIn`), `scripts/snug-embed.mjs` + test (in the root `check-host-kit` chain), `AgentUpdateControls` in the run header behind one confirm, `exportSeat.ts` (wrapper/bundle by sniff, `downloads` codes owned, one prompt at a time, the chat copy path), `compose.ts` (each binding → its seam and seats; the save act only where `artifact` resolved; the stashed note rendered once), `main.tsx` (probe → compose → render → hand-in after the db opens; the boot logs `document.scripts` for the first walk). Verdicts: db 475, host 152, playground 1892, node 14 (+ the existing 29), desktop type-checks.
- State: steps 1–7 done and committed; next is step 8 (the two KB rules), step 9 (the e2e harnesses on the built page), then docs and the hosted walk.
- Next step: `80-cdn-compatibility.md` + `10-overview-and-contract.md`, `gen:content`, snapshots; then `apps/host/e2e/artifact.spec.ts` (hosted + chat harnesses via `addInitScript`; spliced pages via `page.route`).
- Open questions: none.

### 2026-09-06 — Jeetu (via Claude Code) — Gate 3 steps 8–10 (Gate 3 complete; Gate 5 open)
- Done: **step 8** the two KB rules (`80-cdn-compatibility.md`: "Inside a Claude Artifact: a Narrower Allowlist" + "Never Think on a Timer" — the frame literal through `{{frameType:appMessage}}`; the overview points at it), `gen:content`, the heading-tree snapshot, `binding-a-rules.test.ts`; knowledge 202/202. **Step 9** `apps/host/e2e/artifact-helpers.ts` (the hosted fake: use-only `window.claude` with recording `sample`/`artifact`/`downloads`; the chat fake: flat `complete` + a `window.storage` backed by localStorage so it persists like the real one) + `artifact.spec.ts` (8 cases: no call on load / one `quick` call per move with `cache:false` and the C1 scan; chess answered by the fake; the save round trip — canonical source fetched from the served page, verified, spliced, published once, the stash — and the SEED in a fresh browser; `not_writer` → read-only after the first refusal; the wrapper export through `downloads.save`; the hand-in install → idempotent reload → update to v2; `artifact-static`; the chat brain + `streaming:false` + storage across a reload + the copy export) + the caps probe's `parent`/`top`/`parent.parent` reach test (all `SecurityError`). Three things the real page taught: (1) the chip must keep its menu open on a refused save (the read-only status was never read); (2) the hub reads its list once at mount, so the hand-in now runs BEFORE the first paint behind a 4 s bound (`main.tsx`); (3) a regex "strip the block" in a test started at the kit's own `<script type="text/plain" id="snug-db">` literal (compose.ts) — blocks are located with the tokenizer, never a regex (lesson). Host e2e 14/14 (kit 6 + artifact 8); root `pnpm test` exit 0 (turbo + every `check-*`, `check-host-kit` 43 node tests + the gate at 2,257,3xx B under the 2,750,000 ceiling). **Step 10** docs: `architecture.md` (the probe's host arm, the composed platform + seats, "Binding A — two storage seams, one file", "The hand-in", the gates), `code-map.md` (a Binding A row; the gate row's tokenizer note), `glossary.md` (Binding + `artifact-static`, Probe, Your file chip, Artifact record, Hand-in, `snug-user-file/1`), the parent record (T4 row + the amendments paragraph incl. the chat-delivery child), ADR-0065 amendments (§2 files form residual + canonical source + `artifact-static` + budget/classifier; §5; §6 unedited-only + tombstone + the trust boundary; the A2 delivery split; two consequences), `lessons.md` (+6), `next-steps.md` (the owner's hosted walk, the owner acts, the chat-delivery child, residuals, two follow-ups).
- State: **Gate 3 complete on the branch. Gate 5 open:** the diff review (fresh-context, High) and the owner's hosted walk (AC13 — the artifact URL, version ids and the boot log's `document.scripts` line to journal); then Gate 6 (journal, done-move) and the PR.
- Next step: spawn the diff reviewers; fold findings; hand the owner the walk (the printed command sequence is in next-steps); PR on the owner's ask.
- Open questions: none.

### 2026-09-06 — Jeetu (via Claude Code) — Gate 5 fold (the three-lens diff review)
- Done: three fresh-context reviewers (correctness, security, maintainability) on `git diff main...HEAD` → 44 findings, every one dispositioned and folded in four rounds, tests first, the new guards mutation-checked (five mutations, five caught by exactly their tests):
  - **Round A (db / page-blocks / embed):** `unwrapUserFile` re-sniffs the payload and imports the ONE `sha256Hex` (the host's twin deleted); `wrapperHeadAt` tolerates a pretty-printed wrapper; `isEditedCopy` is the ONE edited-copy predicate (`installShared.ts` folded onto it); the update target rule excludes `share:` copies from lifted-from; the tombstone lineage covers a lifted-from app; `parseDbBlockBody` validates bytes/sha/saved/savedAt; `verifyKitPage` documented as a shape check.
  - **Round B (host kit):** `probeBrain` is async — `limits()` answers BEFORE the adapters are built (one number, one time; the `ready` promise pattern is gone); the runtime is invoked as a METHOD (`claude.use(name)` — "Illegal invocation" otherwise) and a `use` that REJECTS keeps the artifact binding (`rejected` on the seat; only null answers make `artifact-static`); the record's `loadPageCopy` is TERMINAL (stash + `onReload`, else the open db flushed the browser copy back); the sidecar takes the projected counter before the publish and is restored on a rejection; a sha match re-syncs a stale sidecar; a fetched block NEWER than boot's is a conflict; the stash is dropped on every non-conflict rejection; a corrupt block over an EMPTY bucket starts a fresh dirty file with the note (never a boot that throws forever); `window-storage` treats a throwing `list()` as CORRUPT; `handInBeforePaint` names the 4 s bound; the hand-in refuses connections at the boundary before any pending offer, and checks the tombstone only when no target exists; a memory working copy is named on the custody state.
  - **Round C (playground):** `classifierApplies` is the positive rule `brain !== 'host'` (webllm/demo classify like settings — test reworded); `AgentUpdateControls` uses `ConfirmOverlay`; `unwrapOpenedWrapper` is shared by the open-file route and the settings importer; `YourFileChip` narrows its acts once (no `!`); `custodyDisclosure` carries the S2 memory sentence; one `fixtures/hostPlatform.ts` replaces eight local host-platform builders.
  - **Round D (e2e / docs):** the fakes install in the TOP frame only and `use`/`complete` are this-dependent; the caps probe posts a runtime-shaped message to `top`/`parent` and asserts NO reply (`#bridge`); the timed waits became state signals (the chip's "unsaved changes"; a poll on the fake's storage); ADR-0065 §6 + architecture.md carry the three review sentences (seed trusts the block; `verifyKitPage` is a shape check; the viewer-bridge residual); AC4's "other complete generation" fallback wording removed (there is none by design).
- Not folded (stated): the security lens's finding 1 (does the VIEWER answer a nested frame's runtime-shaped postMessage?) is unverifiable from the contract or this repo — it is the hosted walk's question, journaled as the residual above.
- State: **Gate 5 review closed; the hosted walk (AC13) is the one open Gate-5 item and is the owner's.** Then Gate 6 (journal, done-move) and the PR on the owner's ask.
- Next step: the owner runs the walk (`docs/next-steps.md` T4 entry has the command sequence: build, publish `apps/host/dist/snug-host.html` private with `{sample, artifact, downloads}`, Chrome + Safari) and journals the artifact URL, version ids and the boot log's `document.scripts` line.
- Open questions: none.

### 2026-09-06 — Jeetu (owner walk, AC13 part 1) — 🔴 THE HOSTED WALK FOUND A REAL DEFECT: the tool-free host builder tells the model to call a tool it does not have

**The walk so far.** The built page (`ac792c3`) was published PRIVATE through the Artifact tool as `Snug` with `{sample: {}, artifact: {}, downloads: true}` → **https://claude.ai/code/artifact/d2013d26-fa05-42c9-a573-2f05182acdd3**. **The hub RENDERS** — the kit boots inside a real hosted artifact, which is AC13's first and largest question answered YES. (A pre-publish worry that the publisher's `<!doctype>…<body>` wrapper would nest our complete document and strip the module script proved UNFOUNDED — the page boots.)

**What the owner then hit, building an app from the hub:** the reply said *"I wasn't able to pull the Snug knowledge-base template in this session, so I built this as a fully self-contained app (client-side AI logic + localStorage persistence, no host round-trips needed)"* — and the app it wrote ran as a WHITE PAGE.

**Root cause (confirmed from the LLM inspector + the assembled prompt, not inferred).** The inspector shows the leading user turn carries the KB HEADER (`You are the assistant behind a Snug reference host …`) but NOT the authoring rules — no `snug:app-message`, no `app-announce`, no "Never Think on a Timer". That is by design of the *tool* path and fatal on the *tool-free* path:

- `buildHostSystemPrompt({ appBuilder: true, artifacts: false, platform: 'host' })` is **4,101 bytes** and instructs, verbatim: *"Before writing ANY Snug app — even one you feel sure about — call the `snug_app_builder` tool to retrieve the authoring knowledge base: the mandatory HTML template, the copy-exactly bridge hooks, the reply contract, and the pinned CDN table. … **Never write an app from memory**."*
- The host brain is **tool-free** (`builder.ts:227` — `toolFree` is set for a pinned host brain with `tools: false`, because `sample` cannot call tools). So the model is told to fetch the rules, has NO way to fetch them, and is forbidden to proceed from memory. It resolved the bind by building without the contract and saying so. **The model's message was literally true — not a confabulation.**
- The white page is the same defect downstream: an app written without the bridge hooks never posts `snug:app-announce`, so the frame has nothing to render. ONE root cause, both symptoms.

**Why no test caught it.** The webllm arm carries the identical tool-free constraint and answers it with `WEBLLM_BUILD_SUFFIX`, which replaces the *artifact-write* mechanism — NOT the *knowledge-base consult*. T4 generalised `toolFree` to the host brain (T2 P2/A5) and inherited that suffix and its blind spot. S11 measured whole-app builds on `sample` with a prompt assembled BY HAND for the spike, so it never exercised this assembly. The host tests assert which adapter a purpose routes to, never that the resulting prompt is SELF-SUFFICIENT. **The lesson, generalised: a prompt assembled for a tool-free brain must be checked for instructions that presuppose tools — "which adapter" and "what did it actually say" are different questions.**

**Sizes measured for the fix (the reason this is a task, not a patch).** The full KB is **84,237 B across 10 files** — it does NOT fit the host's 65,536 B cap, so the fix must SELECT layers, and that is a decision:

| file | bytes | carries |
|---|---:|---|
| `10-overview-and-contract.md` | 5,117 | announce · app-message · timer rule · cdn |
| `20-html-template.md` | 15,626 | announce · app-message · cdn |
| `30-bridge-protocol.md` | 5,609 | announce · app-message |
| `40-persistence-and-db.md` | 5,476 | the db (vs `localStorage`) |
| `80-cdn-compatibility.md` | 5,609 | app-message · timer rule · cdn (incl. T4's artifact rules) |
| — essential core subtotal — | **37,437** | fits with room for the request + history |
| `50-app-catalog.md` / `60-design-quality.md` / `70-defensive-coding.md` / `95-runtime-contract.md` | 19,136 | optional |
| `90-auth-and-connected-apis.md` | 21,446 | MUST stay out (no connected apps under Binding A anyway — D4) |

- **Status:** AC13 is PART DONE — the boot, the hub and the brain wiring are proven on real glass; **the BUILD path is broken under the host brain** and is now its own child task (below). The remaining AC13 legs (save to this artifact → version + reload, export → `snug-user.snug.json` → import in the playground, the `snug-embed` hand-in, Safari's memory rung) are INDEPENDENT of the builder and still owed.
- **Also observed, correct behaviour, no action:** clicking a starter says *"starters load from the network — this page is offline or the starters package is unreachable"* — `@snugprotocol/starters` is not published yet (a queued owner act), and the named refusal instead of a dead control is exactly T2's intent.
- **Next step:** the child task `TASK-20260906-tool-free-kb-inlining`; then the owner resumes the AC13 legs above.
- **→ 2026-09-06, the child is FIXED in code (branch `fix/TASK-20260906-tool-free-kb-inlining`, off this branch's tip; ADR-0066). NOTE for AC3's "a 51 KB app fits whole": no longer true under the host brain — the inline core takes ~41.9 KB of the cap, so the html room is ~23 KB (fixtures retuned: 18 KB fits, 51 KB is the refused example; ADR-0066 A8 records the starter-edit consequence as an open owner decision).** the tool-free host builder now carries the five-file KB core inline (`knowledge: 'inline'`, ~41 KB under the cap, 44 KiB ceiling pinned on the kit's ruler), names no tool in either slot, and the AC5 e2e proves an app built from the template in the prompt announces and renders on the built page (host e2e 15/15). **AC13 resumes here:** rebuild, republish to the SAME artifact, build an app from the hub, journal the version id — then the save/export/hand-in/Safari legs.
- **Open questions:** which layers inline by default, and whether the selection is static or request-shaped — the child task's interview.

### 2026-09-06 — Jeetu (via Claude Code) — /pickup: state verified, the fixed kit REPUBLISHED to the same artifact (AC13 build leg, session half)
- Verified: branch tip `6b3b903` = the child's fast-forward (both branches at the same commit); working tree clean; **the branch IS pushed** (`origin/feat/TASK-20260905-binding-a-artifacts` updated by push 2026-09-06 13:43 PDT — the two close-out entries above and the child's say "NOT pushed"; they were written before the push). No PR. Every diff file is explained by the two journals (T4 + the child).
- Tests: db 478/478 · knowledge 222/222 · host 165/165 · playground 1903/1903 · `pnpm --filter host build` → `dist/snug-host.html` 2,269,526 B, stamp `0.1.0 6b3b903` · host e2e 15/15 (kit 6 + artifact 9, incl. the child's AC5) · root `check-host-kit` 47/47, ceiling 2,750,000.
- Done: read the live artifact back through the Artifact tool first — the stored version was the bare pre-fix build (stamp `0.1.0 f6fe820-dirty`), **no `snug-db` block and no bundle blocks** (nothing to carry over), so the fresh `6b3b903` page was published straight over it to the SAME URL **https://claude.ai/code/artifact/d2013d26-fa05-42c9-a573-2f05182acdd3** (version label "T4 re-walk: 6b3b903 (KB inline fix)"; capabilities `sample; artifact; downloads` carried forward by the runtime, contract 0.2.41, private).
- **A fact for the record (the "what does the viewer inject" question, AC13):** the read-back of the STORED artifact carries the viewer's wrapper AROUND our whole document — `<!doctype html><html><head><!-- frame-runtime --><script>window.__FRAME_PREAMBLE={…capabilities: artifact/assets/comments/db/downloads/embed/endpoints/mcp/network/permissions/room/sample/self/user…}</script><script>(frame runtime, ~20 KB minified: RTC lockdown, `window.claude` defined non-writable with a `use()` that resolves each capability lazily from `/_runtime/<cap>.js`, the parent `postMessage` bridge to the shell origins, patch/theme/scroll handling)</script><!-- /frame-runtime --><meta charset><meta viewport><style>reset</style></head><body>` + OUR complete `<!doctype html><html>…</html>` + `</body></html>`. So the injected runtime is two classic scripts in the wrapper's head, ahead of our nested document, and our module script boots unchanged inside it (which is why the hub rendered). Consequence for the save act: `fetch(location.href)` returns THIS wrapped form — `verifyKitPage`'s tokenizer must tolerate two non-module scripts before the kit's own inline module, or the first hosted save refuses by name. **This is the first thing the save leg tests** (the e2e's fake serves the bare page).
- State: **STOPPED for the owner's open (D14: the session publishes, the owner opens).**
- Next step (owner, AC13 build leg = the child's AC7): open the artifact in Chrome, build an app from the hub with a one-line ask, confirm it announces and renders (no white page), then journal the version id. Then the save leg (the your-file chip → "save to this artifact"; if it refuses with a verification reason, that is the wrapped-source finding above — journal the exact message), export (`snug-user.snug.json` → playground import), the `snug-embed` hand-in over the Artifact read, and Safari's memory rung.
- Open questions: whether `verifyKitPage` accepts the viewer-wrapped canonical source (decided by the save leg, not by reading).

### 2026-09-06 — Jeetu (via Claude Code) — 🔴→✅ the save leg would have refused on the real artifact: the canonical source is viewer-wrapped (found from the read-back, fixed test-first before the owner's walk)
- Found: running `verifyKitPage` over the Artifact tool's read-back of the live page (the exact bytes the viewer stores — 14,261 B of wrapper ahead of the kit document, `\n</body></html>` after) returned two problems — "an unknown inline `<script>` is on the page" ×2 — the viewer's `__FRAME_PREAMBLE` script and its frame runtime. The e2e fake served `dist/` BARE, so the AC5 round trip was green on a shape the real viewer never serves; the owner's first "save to this artifact" would have refused by name. `snug-embed` had the same blind spot from the other side: its insert-before-`</body>` would have landed a bundle in the WRAPPER's body and the republish would have nested a stale frame runtime inside the page.
- Fixed, tests first (RED seen for each, then GREEN; two shape guards mutation-checked): **`unwrapViewerPage`** in `scripts/lib/page-blocks.mjs` (+ `.d.mts`) — a bare page passes through; one wrapper → the kit document lifted out byte-for-byte (its own trailing newline kept; the wrapper's is the one before `</body>`); a wrapper inside a wrapper, content after the kit's `</html>`, or a wrapper body that does not open with the kit's doctype → a named `problem`, no html. **`artifactHtml.ts`** unwraps the fetched source before `verifyKitPage`, reads the live block through the unwrapped document, and publishes the BARE page (the viewer wraps it again); an unrecognised wrapper is `not-the-kit-page` with zero publishes. **`snug-embed.mjs`** unwraps its input (and `--list`), merges into the kit body, writes the bare page, and says so on stdout ("publish this bare page as it is"). **`scripts/fixtures/viewer-wrapper.mjs`** (+ `.d.mts`) pins the measured shape with placeholder script bodies; the e2e's `servePage` now wraps everything it serves (the save round trip and the hand-in run on the real shape; the published page is asserted bare through `unwrapViewerPage` — a `<html` count was wrong: the kit's own script carries twelve). The unwrap was run over the REAL read-back in-session: `wrapped: true`, no problem, inner 2,255,136 B, `verifyKitPage(inner) → []`.
- Verdicts: page-blocks 14/14, snug-embed 8/8 (check-host-kit 51/51, dist 2,270,793 B under 2,750,000), host 167/167 (tsc + vitest), host e2e 15/15 on the wrapped shape.
- Docs: AC5 amended in place (above); `architecture.md` (the Binding A seam), ADR-0065 §2 amendment, glossary (Artifact record), code-map (the Binding A row), `lessons.md` (+1: make the fake serve the runtime's REAL output), `next-steps.md` (the hand-in recipe: the read-back is wrapped, publish the bare output).
- State: committed on the branch; the fixed kit is REBUILT and REPUBLISHED to the same artifact (the entry below has the stamp). The owner's walk now starts on a page whose save leg has been proven against the real shape. **STOPPED for the owner's open.**
- Next step (owner): the AC13 legs — build an app from the hub (announces + renders), "save to this artifact" (a version + reload; the note after reload says "saved to this artifact (save #1)"), reload and see the file seed, export → `snug-user.snug.json` → import in the playground, the `snug-embed` hand-in over the read-back, Safari's memory rung — each journaled with the version id.
- Open questions: none new. The security lens's residual (does the viewer answer a nested frame's runtime-shaped postMessage?) stays the walk's.

### 2026-09-06 — Jeetu (via Claude Code) — the FIXED kit republished; the stored version verified against the build
- Done: `8ee3b36` built (stamp `0.1.0 8ee3b36`, 2,270,787 B) and published to the SAME artifact **https://claude.ai/code/artifact/d2013d26-fa05-42c9-a573-2f05182acdd3** (version label "T4 re-walk: 8ee3b36 (wrapped-source save fix)"; capabilities `sample; artifact; downloads` carried forward; contract 0.2.41; private). The artifact now holds three versions from today: `f6fe820-dirty` (the owner's first walk), `6b3b903` (the KB-inline fix, save leg latent), `8ee3b36` (current).
- Verified on the read-back of THIS version: stored 2,285,063 B; `unwrapViewerPage` → wrapped, no problem; **the lifted kit document is byte-identical to `dist/snug-host.html`** (sha `0f4d8587…` both sides, 2,270,787 B); `verifyKitPage(inner, '0.1.0 8ee3b36')` → no problems. So the store keeps the published bytes verbatim inside a 14,261 B wrapper head + `\n</body></html>`, and the save act's unwrap → verify → splice → publish-bare chain is exact on the real store. What is NOT yet measured: whether `fetch(location.href)` from inside the frame returns this same stored form (the wrapper's frame runtime could also be injected at serve time with different bytes) — the unwrap accepts any wrapper of the same SHAPE, and the walk's first save decides it.
- State: **STOPPED for the owner's open.** The branch is 2 commits ahead of `origin` (`41cf0f7`, `8ee3b36`) — not pushed this session (the push is the owner's call before the PR).
- Next step (owner): open the artifact (Chrome, then Safari) and run the AC13 legs in this order, journaling each with the version id: (1) build an app from the hub with a one-line ask → it announces and renders; (2) install Chess, one move (consent once); (3) the your-file chip → "save to this artifact" → a version + reload → the note reads "saved to this artifact (save #1)" (a refusal naming "wrapper" or "unknown inline script" is the fetch-vs-store question above — paste the exact message); (4) reload → the file seeds from the block; (5) export → `snug-user.snug.json` → import it in the playground; (6) the session hands an app in: Artifact read → `node scripts/snug-embed.mjs <read-back.html> --bundle app.json --out live.html` → publish `live.html` (bare) → the hub shows it and the db block survived; (7) Safari: the chip names the memory rung. Then Gate 6 + ONE PR carrying both task files' done-moves.
