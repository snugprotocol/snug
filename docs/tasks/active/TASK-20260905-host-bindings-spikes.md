# TASK-20260905-host-bindings-spikes: go/no-go spikes for the host bindings (T1 of TASK-20260904-skill-only-snug)

- **Status**: in-review — Gate 6 done 2026-09-05 (scratch deleted, lessons + parent + ADR + next-steps updated); PR open; S9 owed by the owner outside this task's PR
- **Owner**: Jeetu
- **Risk tier**: low — scratch probes under `scripts/spikes/` (deleted at Gate 6), no product code, no protocol, no sandbox change; the only durable output is numbers and decisions in this journal and in the parent record
- **Branch**: `feat/TASK-20260905-host-bindings-spikes` — stacked on `feat/TASK-20260904-skill-only-snug` (PR #171); rebases onto `main` when #171 merges
- **Packages touched**: none (scratch only); `docs/` (this file, the parent record, `lessons.md` at Gate 6)
- **Spec impact**: none
- **Related**: parent [TASK-20260904-skill-only-snug](TASK-20260904-skill-only-snug.md) (D2, D3, D11, D14 depend on these answers); ADR-0065 (proposed); the `artifact-capabilities` skill (runtime contract 0.2.41 — `sample.d.ts`, `artifact.d.ts`, `downloads.d.ts`, `db.d.ts`); `packages/runner/src/csp.ts` (`RUNNER_CSP`, "srcdoc documents also inherit the EMBEDDER's CSP"); `examples/chess/` (the envelope + runtime contract the probes use)

## Spec (what & why)

The program's design rests on facts about foreign hosts that no test in this repo can establish: whether a sandboxed srcdoc runner survives inside a Claude artifact, which CDNs a chat artifact may load from, how `sample.json` behaves as the envelope brain, whether the artifact's files-form publish holds a binary user file across the Artifact tool's own republish, and whether a Babel-pre-compiled React starter fits OpenClaw's 256 KB widget. Each spike is a **go/no-go with a pre-declared fallback** (never a third path invented on the spot). Spikes that need Hermes, OpenClaw or Codex installed (S5, S6, S8) are deferred to the start of T3, T5 and T9 — those tools are not on this machine and installing them is an owner act.

**Acceptance criteria** (one per spike; "passes" = the number or observation is journaled with a date and the decision it drives is written into the parent's D-list):

1. **S1 — nesting.** A hosted artifact (published from a file) whose page nests `<iframe sandbox="allow-scripts" srcdoc="…">` with an inline script that `postMessage`s to its parent, plus a `securitypolicyviolation` listener in both frames, and the same page pasted as a chat artifact. Pass: the child loads and the message arrives in both runtimes; the violation log names only what `RUNNER_CSP` itself forbids. Fail → D2's in-page mode for that runtime (`sandbox:'host'` disclosure).
2. **S2 — chat-artifact CDN.** From a chat artifact, load one script each from `cdn.jsdelivr.net/npm/…`, `cdn.jsdelivr.net/gh/…` and `cdnjs.cloudflare.com/…` and report which executed. Pass: at least one jsDelivr path. Fail → the chat bootstrap must come from cdnjs (owner act to get a listing) or A2 ships later.
3. **S3 — `sample` as the envelope.** A hosted artifact page that sends the Chess runtime contract + a real Chess envelope to `sample.json` as (a) one concatenated user turn and (b) two user turns, on `quick` and `default` tiers, 10 calls each, `cache:false`; records latency per call, first-try JSON validity, the consent prompt's position (first call only?), and cache behaviour when `state` changes. Pass: median `quick` ≤ 3 s and ≥ 9/10 first-try parses on at least one turn shape. Fail → `sample()` text + Snug's graduated parser; if still poor, A ships turn-based starters only.
4. **S4 — custody in a hosted artifact.** (a) files-form `artifact.publish({"data/user.snug": Blob})` of ~2 MB binary with an explicit content type (try `application/octet-stream`, then `application/vnd.sqlite3`, then a base64 `.txt`/`.json` wrapper if binary is refused); reload; a same-origin `fetch('data/user.snug')` restores byte-identical content. (b) The Artifact TOOL then republishes the page html — is `data/user.snug` still served? (c) `downloads.save({filename:'snug-user.snug.json', data})` is offered and accepted. (d) publish latency ≤ 5 s; the version history is readable. Pass: a–d. Fail on (a) or (b) → `artifact-html` (embedded base64, html republish); fail again → `artifact-db` chunks; last resort `localStorage` + export.
5. **S7 — widget size.** Babel-transform `examples/chess/app.html` host-side (`@babel/standalone` in Node, `presets: ['react']`), inline React + ReactDOM UMD, and measure the single file plus a stub micro-runner. Pass: ≤ 256 KB total. Fail → Binding C limited to starters authored without JSX/React.
6. **S9 — Cowork.** In Cowork: does the Artifact tool exist with `capabilities`, and can the sandbox download a GitHub Release asset? Pass: both. Fail → D11 flips to committed kits; A1 on Cowork rides the committed copy.
7. **S10 — chat-artifact storage and export.** In the same pasted page as S2: `window.storage` before and after publishing; whether `<a download>` or the clipboard works as an export path. Pass: a documented story. Fail → A2 ships as "in memory until published, export by copy" and says so.
8. **Deferred, recorded here for the ledger:** S5 (Hermes `:8642` + a `claude -p` shim through the playground's `local` mode — T3), S6 (OpenClaw `show_widget` round trip + approval count — T5), S8 (`codex exec --output-schema` shim — T9).

**Out of scope:** any product code; any change under `packages/` or `apps/`; publishing anything public (probe artifacts stay private and are unpublished at Gate 6); driving the owner's browser; installing Hermes / OpenClaw / Codex.

## Plan

### How results come back without copy-paste (D14)

Every hosted probe page declares `capabilities: {db: {}, …}` and writes its measurements to `results/<spike>` with `db.doc(...).set(...)`; the session reads them with the Artifact tool's `read_db` (`db_op: get`). The owner's part is: open the artifact once, click "run" where a viewer gesture is required (`sample` consent, `downloads.save`), and say "done". Chat probes (S2, S10) show their result on screen and the owner pastes the one-line JSON the page prints.

### Files (all scratch, all deleted at Gate 6)

- `scripts/spikes/s1-nesting.html` — outer page with a `securitypolicyviolation` log, a nested `allow-scripts` srcdoc child that posts `{hello:1}` up, a visible pass/fail, `db` write of `{childLoaded, messageArrived, violations[]}`. Published twice: as a hosted artifact (`capabilities: {db:{}}`) and as the S2/S10 chat page's first section.
- `scripts/spikes/s2-s10-chat.html` — three `<script src>` probes with `onload`/`onerror` flags (jsDelivr `/npm/` — a tiny known package; jsDelivr `/gh/` — a pinned file from this public repo; cdnjs — a known library), a `window.storage` probe (set → read → reload note; before/after publish), an `<a download>` and a clipboard button; prints a one-line JSON summary. The owner pastes it into claude.ai chat, opens it, publishes it, reopens it.
- `scripts/spikes/s3-sample.html` — declares `{sample:{}, db:{}}`; embeds `examples/chess/runtime-contract.json` rendered the way `renderRuntimeContract` does (copied text, not an import — scratch) and one canned Chess envelope from the KB's worked example; 4 arms × 10 calls (turn shape × tier), `cache:false`, `signal` per call, `onText` timestamps for first-token; parses each reply with a strict `JSON.parse` and records `{arm, i, ms, firstTokenMs, parsed, truncated, modelTierApplied, code?}`; a final arm re-sends arm (a) with `cache` default and a changed `state` to observe replay vs miss. Consent: the page notes at which call the prompt appeared.
- `scripts/spikes/s4-custody.html` — declares `{artifact:{}, downloads:true, db:{}}`; builds a 2 MB `Uint8Array` with a deterministic pattern + sha256; publishes it through the files form under each content type until one succeeds; reloads; fetches and hashes; offers `downloads.save`; writes `{contentTypeAccepted, publishMs, restoredSha, downloadOffered}`. Step (b) is performed by the session: republish the same html through the Artifact tool, then the owner reloads and the page re-checks `data/user.snug`.
- `scripts/spikes/s7-widget-size.mjs` — Node: read `examples/chess/app.html`, transform the `text/babel` block with `@babel/standalone` (`presets: ['react']`), replace the three CDN `<script src>` tags with the UMD bodies of `react@18`/`react-dom@18` fetched once into `scripts/spikes/vendor/` (gitignored), append a 6 KB stub "micro-runner" shell, write `scripts/spikes/out/chess-widget.html`, print byte counts per component and the total; run the same over every `examples/*/app.html` and print a table (which fit under 256 KB).
- `scripts/spikes/s9-cowork.md` — the owner's Cowork checklist (three questions, the exact commands to try, what to paste back).

### Order

S7 first (no owner needed; pure Node) → S1 + S4 published (owner opens both; results via `db`) → S3 published (owner opens, allows `sample`, runs the arms — ~10 minutes of wall time) → S2/S10 page handed to the owner for claude.ai chat → S9 checklist handed to the owner for Cowork. Numbers into the journal as they arrive; the parent's D2/D3/D11 lines updated in the same commit as each result.

### Test plan

Spikes carry no product tests. The verification standard is the pass condition per AC, recorded with: the artifact URL, the UTC time, the raw `db` document or pasted JSON, and the decision taken. Where a probe page has logic worth trusting (the sha256 round-trip in S4, the parser tally in S3), the page shows its own inputs so a wrong number cannot hide behind a green label (lesson 2026-08-20: assert the signal, not the API's return value; lesson 2026-07-31: use `securitypolicyviolation` events, never a call's success).

### Gate 6 obligations

Delete `scripts/spikes/`; unpublish the probe artifacts (or leave them private and list their URLs — owner's call); `lessons.md` gets one rule per surprise; the parent record's D-list and "Decisions & surprises" carry every number; `next-steps.md` untouched unless a spike created follow-up work.

## Decisions & surprises

Every number below was re-checked against the raw `results/*` documents and the owner's pastes on 2026-09-05 (an audit found one mislabelled figure and several inferred-not-measured claims; this section is the corrected record). "GO" means the pre-declared pass condition was met; anything inferred from a policy string rather than observed is marked **inferred**.

- **2026-09-05 — S1 (hosted): GO on nesting; AC1's second clause FAILED.** Results read 06:17Z from `results/s1`; hosted runtime (`claude.use` present), page origin `https://<artifact-id>.frame.claudeusercontent.com`, parent `https://claude.ai`, Chrome 151. Clause 1 — the nested `allow-scripts` srcdoc runner loads and talks to its parent — passed: inline script ran, `hello` at 65 ms, the child's report arrived, child origin `null`, `localStorage → SecurityError`, `eval` allowed inside the child, the injected `RUNNER_CSP` meta present and first in head, and `fetch('https://example.com')` rejected with **two enforced `connect-src` violations, one from the embedder's policy and one from `RUNNER_CSP`** — the runner's own CSP is enforced inside the nested frame. Clause 2 — "the violation log names only what `RUNNER_CSP` forbids" — **failed** (probe verdict `pass:false`, `cspClean:false`): four loads `RUNNER_CSP` permits were blocked by the embedder — a jsDelivr `/gh/` script, an unpkg script, a jsDelivr stylesheet (normalize.css), a jsDelivr font (woff2); by the policy text (**inferred** beyond those four) every CDN stylesheet and font is blocked (`style-src` allows only `'self' 'unsafe-inline' fonts.googleapis.com`; `font-src 'self' data: fonts.gstatic.com`). Loads that worked inside the nested runner: scripts from jsDelivr `/npm/` (uuid) and cdnjs (dayjs), `data:` and `blob:` images, inline script (inline style not probed). `frame-src 'self' blob: data:` did not stop the `about:srcdoc` child. **Decision taken (a third path, not one of D2's two — the owner approved it in the 2026-09-05 report):** keep the nested runner (D2's clause-1 outcome) and carry the narrowed CDN allowlist as a Binding-A skill rule + an embed-time lint (artifact-bound apps: scripts from jsDelivr `/npm/` or cdnjs only, inline CSS and fonts, no unpkg, no `/gh/`) — the in-page fallback would run under the same embedder policy and cure nothing. The hosted viewer's CSP, verbatim: `default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' blob: https://cdnjs.cloudflare.com https://cdn.jsdelivr.net/pyodide/ https://cdn.jsdelivr.net/gh/python-visualization/ https://cdn.jsdelivr.net/npm/ https://cdn.tailwindcss.com https://code.jquery.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: blob:; font-src 'self' data: https://fonts.gstatic.com; media-src 'self' data: blob:; connect-src 'self' https://fonts.googleapis.com https://fonts.gstatic.com; worker-src 'self' blob:; form-action 'self'; frame-src 'self' blob: data:; object-src 'none'; webrtc 'block'; base-uri 'self'; frame-ancestors 'self' https://claude.ai https://*.claude.ai`.
- **2026-09-05 — S1 (chat): GO on nesting only.** From the owner's pastes: the same nested child ran its inline script (`messageArrived` at 58–153 ms across six loads), origin `null`, and loaded a jsDelivr `/npm/` script inside the nested frame with no child violations. **Not measured:** `RUNNER_CSP` enforcement inside the chat nested child (that child ran no blocked-fetch probe) — T4 verifies it in the kit's boot check.
- **2026-09-05 — S2: GO — jsDelivr `/npm/` and cdnjs execute in a CHAT artifact; jsDelivr `/gh/` and unpkg are blocked** (six pastes, identical on every load). Chat runtime: the page runs as `about:srcdoc`, origin `null`, ancestors `https://www.claudeusercontent.com` → `https://claude.ai`; `window.claude` is flat `{complete}`, no `use`. Measured: `/npm/` (mitt) and cdnjs (dayjs) loaded and executed; `/gh/` (a file from this repo at `v0.1.3`) and unpkg raised `script-src-elem` violations. **Inferred from the policy, untested, and only for the OUTER bootstrap page (never inside the runner, whose `connect-src 'none'` still applies):** `connect-src` lists jsDelivr `/npm/` and cdnjs, `style-src` lists jsDelivr `/npm/`; both viewers also allowlist `cdn.tailwindcss.com`, `code.jquery.com`, jsDelivr `/pyodide/` and the `/gh/python-visualization/` prefix (allowlisted, not probed). **Consequence:** the A2 bootstrap loads the kit from jsDelivr `/npm/`, so **publishing `@snugprotocol/host` to npm (after registering the `@snugprotocol` scope) is a prerequisite owner act for the chat binding**; the plan's preferred `/gh/`-from-a-pinned-tag route was verified only from this machine, never from inside a viewer, and is dead in both. The chat viewer's CSP, verbatim: `default-src https://www.claudeusercontent.com; script-src 'unsafe-eval' 'unsafe-inline' 'self' https://www.claudeusercontent.com https://cdnjs.cloudflare.com https://cdn.jsdelivr.net/pyodide/ https://cdn.jsdelivr.net/gh/python-visualization/ https://cdn.jsdelivr.net/npm/ https://cdn.tailwindcss.com https://code.jquery.com; connect-src https://cdnjs.cloudflare.com https://cdn.jsdelivr.net/pyodide/ https://cdn.jsdelivr.net/gh/python-visualization/ https://cdn.jsdelivr.net/npm/ https://cdn.tailwindcss.com https://code.jquery.com https://www.claudeusercontent.com; worker-src 'self' https://www.claudeusercontent.com blob:; style-src 'unsafe-inline' 'self' https://www.claudeusercontent.com https://cdnjs.cloudflare.com https://cdn.jsdelivr.net/npm/ https://cdn.jsdelivr.net/gh/python-visualization/ https://code.jquery.com https://fonts.googleapis.com https://anthropic.com https://*.anthropic.com; img-src https://*.tile.openstreetmap.org/ blob: data: 'self' https://www.claudeusercontent.com; font-src data: 'self' https://www.claudeusercontent.com https://anthropic.com https://*.anthropic.com https://assets.claude.ai https://fonts.gstatic.com; frame-src 'self' blob:; object-src 'none'; base-uri https://www.claudeusercontent.com; form-action https://www.claudeusercontent.com; frame-ancestors 'self' https://www.claudeusercontent.com *.anthropic.com anthropic.com *.claude.com claude.com *.ant.dev https://claude.ai https://preview.claude.ai https://claude.site https://feedback.anthropic.com https://taiga.ant.dev app://localhost; webrtc 'block'; upgrade-insecure-requests; block-all-mixed-content; report-uri https://logs.browser-intake-us5-datadoghq.com/api/v2/logs?dd-api-key=pub71878585a5931c0a7125f59d8339f927&dd-evp-origin=content-security-policy&ddsource=csp-report`.
- **2026-09-05 — S3: GO — `sample` is a viable envelope brain in the HOSTED runtime.** 48 calls 06:18:42–06:20:13Z (92 s; the expected 5–60 s thinking pause on `default` never appeared — first token 1.1–1.4 s). Input per one-turn call = 3,439 (system) + 2 (joiner) + 1,392 (wire) = 4,833 bytes against `limits().maxPromptBytes` 65,536. Per arm (n = 10 unless noted): **(a) one user turn, `quick`, `sample.json`: median 1,201 ms (1,197 excluding the consent-stamped call), p90 1,344, platform-parsed 10/10, legal move 10/10** — first call 7,164 ms including the consent dialog, seen exactly once; (c) two user turns, `quick`: median 1,057, p90 1,377, 10/10, 10/10 (the contract says consecutive same-role turns are read as one; arm c confirms equivalence, so **T4 sends one concatenated user turn**; the planned two-turn × `default` arm was dropped for that reason and (b0)/(d) added); (d) one turn, `quick`, `sample()` TEXT + strict parse after fence-strip: median 1,183, p90 1,335, 10/10 parsed, `truncated:false` 10/10, `modelTierApplied:quick` 10/10; (b) one turn, `default`, `sample.json`: median 1,823, p90 1,992, 10/10, 10/10; (b0) `default` text: 2/2, tier honoured. **Raw strict `JSON.parse` of the streamed text: `quick` 0/36 (every reply code-fenced; the platform unfences for `sample.json`), `default` 12/12 (bare JSON) — the graduated parser's fence-strip rung is mandatory on `quick`.** Cache arm: with the default cache an identical input replays in 1–4 ms with identical text; a one-character change to `state.fen` misses (1,335 ms) then replays — irrelevant for real turns, which carry a fresh `requestId`. Sampling variety: none — `quick` answered g8f6 30/30 and `default` b8c6 12/12 for the same position (an opponent on `sample` is deterministic per input). **Shape split (the chess contract/schema mismatch, measured):** on `quick` 28/30 replies followed the CONTRACT text (`{from,to,message[,say]}` — `message` always present, `say` additionally in 5/30, never alone), so the chess reader's `d.move` is missing while `d.message` resolves; on `default` 10/10 followed the envelope's `responseSchema` (`{move:{from,to},message}`). **T4 consequences:** `sample()` TEXT + Snug's graduated parser as the one parse path (only the text verb reports `truncated`/`modelTierApplied`, which `stopReason` needs); `quick` as the default tier for runtime envelopes; `cache:false`.
- **2026-09-05 — S4: NO for the files form — `artifact-files` is unavailable in this view.** `artifact.publish({"data/user.snug": Blob})` rejected in 0–2 ms with `capability_disabled` ("publishing files is not available in this view") on both clicks; the page stopped its content-type chain on the permanent code, correctly. The 0.2.41 contract documents that code for publicly shared artifacts; it fired on the owner's own private one. **Not run:** the walk stopped there, so AC4(c) `downloads.save` of `snug-user.snug.json` in the HOSTED runtime and AC4(d) version-history readability have NO outcome (`c_exportAccepted:null`; `d_historyReadable` was a session-owned slot never filled) — **carried into T4** (hosted export path + a re-check that the files form stays disabled).
- **2026-09-05 — S4b: GO for all three fallbacks; the hosted custody ladder is measured.** `results/s4b`, builds b1 + b2, six page loads. **A `artifact-html`:** `publish(html)` of a 2,811,093-char document with the 2 MiB base64-embedded in `<script type="text/plain" id="snug-db">` succeeded in **1,816 ms** (b1, version `1788589605-fb8c`, decode 6 ms) and **1,826 ms** (b2, version `1788589735-cbae`, decode 10 ms); the shell reloaded the view each time (`loadedAfterPublish:true`) and the reloaded page hashed the embedded bytes **byte-identical** both times; the viewer injects its runtime into a page the page itself published. Implied ceiling: the 16 MB rendered-page cap bounds `artifact-html` at roughly 11 MiB of user bytes (base64 × 4/3 plus the page). **B `artifact-db`:** 14 × 200,000-char chunks + a manifest written in 6,193 ms (325–551 ms per chunk), read back and reassembled in 5,437 ms, sha equal, equal again after a reload. **C `localStorage`:** 2,796,204 chars written in 5 ms, equal, equal after a reload; `navigator.storage.estimate()` quota 10 GiB at the artifact origin. **Ladder for T4:** working copy in the artifact origin's browser bucket (localStorage proven; OPFS probed first at boot — if it works, the playground's existing backend serves unchanged), durable/shared record = `artifact-html` republish on explicit acts (a version + a reload per save), `artifact-db` chunks only for org-internal shared apps, the files form retried once at boot. **Unmeasured risk carried into T4:** with `artifact-html` the user's bytes ARE the page, so a session-side code republish through the Artifact tool replaces them unless the session re-reads the live page and re-embeds the `snug-db` block — `snug-embed` must merge that block, the custody chip must say a republish rewrites the file, and AC4(b) is re-run against the chosen mechanism in T4.
- **2026-09-05 — S7: GO (the widget binding can carry React starters).** `scripts/spikes/s7-widget-size.mjs` (Node 22; `@babel/standalone` 7.29.8 with preset `react` only, no env pass, no source maps; react 18.3.1 = 10,751 B and react-dom 18.3.1 = 131,835 B from jsDelivr, versions read from `x-jsd-version`; a 6,555 B handshake-only stub micro-runner that nests the app in an `allow-scripts` srcdoc with `RUNNER_CSP` injected, posts a protocol-correct `snug:host-ready`, escapes `</script` reversibly for the app document and refuses it in compiled/vendor output, and guards an unclosed `<!--` in script data). **Chess: 182,199 bytes / 182,100 chars — fits under 262,144 with ~80 KB of headroom**; Playwright chromium render check PASSED with the harness as the ONLY attempted request, 0 aborted, 0 console/page errors (the positive control is whatsapp's external chart.js tag, recorded as attempted AND aborted). Against the handshake-only stub **10 of 12** starters fit (trade-copilot 270,460 B and whatsapp 262,493 B + an un-inlined chart.js do not), but two are near-misses a real `WidgetBridge` + state-in-page shim will consume — gmail 8,227 chars / 533 B and ledger 14,662 chars / 8,359 B of headroom — so **8 of 12 with a realistic runner; gmail, ledger, trade-copilot and whatsapp need the compact variant** (12/12 by size; only chess's compact variant was render-checked). Fixed floor per widget = 149,243 B. Caveats for T5: the cap's unit (bytes vs chars) is assumed from the task text (unsourced; both readings tabulated) and is verified only by S6; the sizes are the optimistic bound (in-browser `@babel/standalone` with no `data-presets` would add `env` + inline source maps).
- **2026-09-05 — S10: GO — chat-artifact storage persists across reloads on BOTH the in-chat view and the published page, and the two views hold SEPARATE stores.** `window.storage` is present synchronously with `get/set/delete/list`; every response is a typed envelope (`StorageSetResponse` echoes key + value; `StorageListResponse` carries `keys`, `prefix`, `shared:false`). `get` of a never-written key throws `"Storage get failed: Unexpected response type"` — absence is an exception, and because the message is generic the kit maps it to "no file yet" **only after `list()` confirms the key is absent**, never on the message alone. Writes: in-chat view t:1 (06:51:59) → t:2 (06:52:57), each read back after a reload (`survivedFromPreviousLoad:true`); published page t:1 (06:51:33, unpasted) → t:2 (06:53:30) → t:3 (06:54:08), each read back after a reload — and the published page's `onLoad` never showed the in-chat view's values, so the stores are per view. **Not tested:** a never-published artifact (the owner published before the first write landed; the contract says storage is published-only). **Export in chat:** the `<a download>` link did nothing, `navigator.clipboard.writeText` is blocked by permissions policy, `document.execCommand('copy')` worked — copy-as-text is A2's only export path. **Chat brain UNMEASURED:** `window.claude.complete` was only detected; latency, JSON behaviour, consent and truncation are unknown — a follow-up chat paste with the S3 envelope, or T4's boot-time risk. A2 custody chip, as measured: "kept in this view's storage; the published link keeps its own; copy to move or keep it" (the 20 MB text cap is documented, unmeasured). Probe defect (mine): one uncaught `atob` InvalidCharacterError per load after a write; no reading depends on it.
- **2026-09-05 — incidental starter defect (queued in next-steps):** `examples/chess/runtime-contract.json`'s `responseGuidance` tells the model to reply `{"from","to","say"}`, while the app's `RESPONSE_SCHEMA` (rides in every envelope) and its reader (`app.html` ~line 580: `d.move.from`, `d.move.to`, `d.message`) expect `{move:{from,to}, message}`. Measured in S3: on `quick` 28/30 replies follow the contract, so the app plays a random legal move and shows the banter note "it answered off-script — a legal move was played for it" beside an empty message; on `default` 10/10 follow the schema. Fix = rewrite `responseGuidance`, shipped as a starter release per ADR-0045.
- **2026-09-05 — Chromium observation from the S1 harness (headless, before the real artifact):** an embedder policy of `frame-src 'none'; child-src 'none'` did NOT stop an `about:srcdoc` child from loading, while a missing `'unsafe-eval'` in the embedder's policy WAS inherited by the child. The real viewers confirmed both directions (S1 hosted: `frame-src 'self' blob: data:` and the child loaded; `'unsafe-eval'` present and eval allowed).
- **2026-09-05 — S3 fixture facts:** the exact runtime turn is `buildHostSystemPrompt({appRuntime:true})` + separator + `renderRuntimeContract` = 3,439 bytes of system text plus a 1,392-byte wire; the chess contract's `maxOutputTokens` is 512 but a schema-bearing wire is never bound by it (`apps/playground/src/agent/transport.ts` lines 155–169).
- **2026-09-05 — workflow mechanics worth keeping:** the probe pages went through two workflow rounds (author → contract + measurement verifiers → fix → re-check; then fix → final skeptic); the second round's final phase died on a session limit and was resumed by run id (`wf_5c37fd48-060`) — the four cached fixers replayed and only the four skeptics re-ran, with the fixed files still on disk. One of the two hand-applied final fixes (S4b) was found by a regex extractor cutting the page's `<script id="logic">` at a literal `</script>` inside a JS string — the same character at which the browser's HTML tokenizer would have closed the live element.

## Session journal (append-only, newest last)

### 2026-09-05 00:40 UTC — Jeetu (via Claude Code) — session (Gate 1–2, created with the parent)
- Done: task file drafted from the parent's spike table; probe designs written; result loop through the artifact `db` chosen so the owner never pastes hosted results.
- State: planned; waits for the program's plan approval.
- Next step: `/pickup TASK-20260905-host-bindings-spikes` → branch → S7 → publish S1 + S4 → publish S3 → hand S2/S10 and S9 to the owner.
- Open questions: none beyond the parent's D-batch.

### 2026-09-05 03:50 UTC — Jeetu (via Claude Code) — session (Gate 3–4: probes authored)
- Done: branch cut (stacked on #171); `.gitignore` entries for `scripts/spikes/{vendor,out}/`; `scripts/spikes/fixtures.mjs` generates the exact Chess runtime system text + wire into `out/chess-fixtures.json`; `scripts/spikes/s9-cowork.md` written (both URLs verified: the release asset answers 302 → asset, jsDelivr `/gh/` answers 200); the four probe pages + the S7 script are being authored and adversarially verified by a workflow (two lenses each: contract conformance vs the 0.2.41 `.d.ts` files, measurement validity vs this file's pass conditions; fix; re-verify).
- State: waiting on the workflow; nothing published yet.
- Next step: publish S1 + S4 (private), run S7's report into this journal, publish S3, hand `s2-s10-chat.html` + `s9-cowork.md` to the owner.
- Open questions: none.

### 2026-09-05 05:45 UTC — Jeetu (via Claude Code) — session (probes published, owner walks handed over)
- Done: the four probe pages went through two workflow rounds (author → contract + measurement verifiers → fix → re-check → fix → final skeptic; 33 agents in all) plus two hand-applied final fixes (S3's "done" gate now refuses when the final db write FAILED; S4 stashes to sessionStorage BEFORE any db round trip) — both re-verified with the skeptics' Playwright harnesses. Published PRIVATE under the owner's account:
  - **S1** https://claude.ai/code/artifact/9b0182d2-3f6c-41c3-ab96-5d0b2323e948 (`capabilities {db}`) — nothing to click; done when the db line reads "wrote results/s1 … (final)".
  - **S4** https://claude.ai/code/artifact/59dfe0a8-cbc0-46de-9633-27c95e72f557 (`{artifact, downloads, db}`) — six-button walk; step 4 needs the session to republish the page (build tag r1 → r2) between the owner's clicks.
  - **S3** https://claude.ai/code/artifact/d5df9417-0781-435e-8da0-d995d7438507 (`{sample, db}`) — "run all arms" (48 calls, ~10–12 min, consent on call #1), then "done".
  - `scripts/spikes/s2-s10-chat.html` (160 lines, complete document) is attached to a claude.ai chat by the owner ("render verbatim"); four pastes back (draft/1, draft/2, published/1, published/2). `scripts/spikes/s9-cowork.md` is the Cowork checklist.
- State: S7 GO (journaled above). S1/S3/S4 await the owner's opens; S2/S10 and S9 await the owner's pastes. Nothing else blocks.
- Next step: read `results/s1`, `results/s4`, `results/s3` with `read_db` as the owner reports; republish S4 as r2 when told "verified"; fill `sessionChecks` on results/s4; journal every number; update the parent's D2/D3/D11.
- Open questions: none.

### 2026-09-05 06:35 UTC — Jeetu (via Claude Code) — session (hosted spikes closed)
- Done: owner opened S1, S3, S4, S4b (b1 + b2); results read back through `read_db` and journaled above with numbers. S1 GO (nested runner + RUNNER_CSP enforced inside the artifact; four embedder-inherited CDN restrictions recorded verbatim). S3 GO (`quick` median 1.2 s, 48/48 legal, consent once; use `sample()` text + Snug's parser). S4 files form NO (`capability_disabled`). S4b A/B/C GO (html republish 1.8 s; db chunks ~6 s; localStorage 5 ms; 10 GiB quota).
- State: hosted spikes complete. Waiting on the owner's S2/S10 pastes and the S9 Cowork answers. S5/S6/S8 deferred to T3/T5/T9 per D14.
- Next step: fold S2/S10 + S9 when they arrive; update the parent's D2/D3/D11 (done in this commit for what is known); then Gate 5/6 for T1 — delete `scripts/spikes/` (numbers live here), lessons, PR.
- Open questions: none.

### 2026-09-05 06:58 UTC — Jeetu (via Claude Code) — session (chat spikes closed)
- Done: owner pasted six chat-artifact summaries (draft/1, draft/2, published/1 without a write, then draft with a write, draft after reload, published with a write); S2, S10 and the chat leg of S1 journaled above with the chat viewer's CSP verbatim and the storage API's observed shapes.
- State: all Claude-surface spikes answered. Only S9 (Cowork checklist, owner) remains inside T1's scope; S5/S6/S8 are deferred by D14.
- Next step: Gate 5/6 for T1 — lessons, delete `scripts/spikes/` (numbers live here and in the parent), unpublish or keep the four private probe artifacts (owner's call; URLs are journaled), update the parent's D-list and the ADR draft's facts, open the PR. S9 rides along whenever the owner runs it; if it lands after T1 closes it goes into the parent record at T2's Gate 2.
- Open questions: keep or unpublish the probe artifacts?

### 2026-09-05 07:30 UTC — Jeetu (via Claude Code) — Gate 5/6
- Done: the journal was audited against the raw `results/*` documents and the owner's pastes by a workflow (journal audit + lessons draft + docs delta, each with a skeptic; 27 audit findings — one wrong number, the rest overreach or omissions — all folded into the rewritten "Decisions & surprises" above). Lessons: two new bullets (verdicts and inherited restrictions; one store per view), one trimmed bullet (operator walks), one process rule (workflow run ids), three codas on existing entries (capability refusal codes; absent-key throws; the `</script>` terminator). Parent record D2/D3/D11/D14, its T4 row, T7 line, two errata bullets and a journal entry; ADR-0065 items 2, 8, 10, one alternative and the residuals (still `proposed`); `next-steps.md` gained one entry (chess contract/schema defect, the S9 checklist inline, S5/S6/S8 needs, four measurements owed at T4). `scripts/spikes/` deleted with its `.gitignore` lines (probes at `aaac1ad`, `5c8abc6`, `ec540ef` in this branch's history; numbers live above). The four probe artifacts stay private under the owner's account (URLs above) unless the owner unpublishes them.
- State: in review — PR open; gates run before the push are listed in the PR body.
- Next step: merge → `done/INDEX.md` line + delete this file (ADR-0027); then `/start-task` T2 `host-kit`.
- Open questions: owner's confirmation of the S1 third path (nested runner + Binding-A CDN rule/lint instead of D2's in-page fallback); keep or unpublish the probe artifacts.

