# TASK-20260821-ui-polish: rename/version-delete/deep-delete + multi-provider BYOK + settings redesign

- **Status**: in-review (implementation + verification complete; awaiting owner `/close-session`)
- **Owner**: jeetu (via Claude, autonomous session — owner pre-approved run-through 2026-08-21)
- **Risk tier**: **high** (touches `packages/protocol` `sidecar-contract.ts` (internal draft), the whatsapp-sidecar router + desktop Rust route table, credential lifecycle on delete, `packages/db` cascade)
- **Branch**: `feat/ui-polish` (owner explicitly named the branch — deviation from `feat/TASK-<id>` recorded here)
- **Packages touched**: `packages/db`, `packages/protocol` (sidecar-contract, internal draft only), `apps/whatsapp-sidecar`, `apps/desktop` (Rust route table + TS), `apps/playground`, `packages/adapters` (read-only consumers; catalog unchanged)
- **Spec impact**: none — `sidecar-contract.ts` is an internal draft OUT of `schemas/` SOURCES; no `USERDB_SCHEMA_VERSION` bump (new keys ride the existing `snug_settings`/namespaced-key precedent, ADR-0036 D2)
- **Related**: ADR-0036 (per-app model), ADR-0045 (starter versioning / pinned factory versions), ADR-0040 (sidecar identity lifecycle), ADR-0032/0033/0037 (sidecar), lessons 2026-08-18 (namespaced key owes a cascade; inheriting is an absence)

## Spec (what & why)

Six owner-requested improvements in one branch. (1) Installed apps can be renamed to any unique name. (2) Any stored version of an installed app can be deleted EXCEPT pinned factory versions and the currently-running version (owner decision 2026-08-21: ALL pins protected — keeps reset-to-factory and the ADR-0045 starter-update vouch chain intact). (3) Deleting an app removes everything it owns; for Telepath specifically the wipe extends BEYOND the user DB to a full device unlink: the helper's on-disk session keys, thread cache and minted token are erased and the helper stopped, so the phone shows the companion unlinked and a reinstall needs a fresh QR (owner decision 2026-08-21). (4) BYOK settings support BOTH Anthropic and OpenAI keys at once plus the demo brain (no key); each keyed provider gets a default model picked from the pinned catalog filtered to that provider; a default-provider control (auto: Anthropic when both keys exist, user-overridable) feeds every app that has not overridden it; the standalone "model" card is removed. (5) The build page gets the same model selector the app run header has, listing models from every configured provider grouped by provider (owner decision 2026-08-21); picking a model implies its provider per app. (6) The Settings page is completely redesigned — Apple-design-inspired, same theme tokens, responsive — with particular attention to the connections section.

**Acceptance criteria** (each becomes at least one test):

1. **AC1 rename**: an installed app can be renamed from the UI; `snug_apps.display_name` updates; the hub tile and run header show the new name after rename without reload.
2. **AC2 rename-unique**: a rename to a name already used by another installed app (case-insensitive, trimmed) is refused with a visible message and writes nothing; renaming an app to its own name (case change) is allowed.
3. **AC3 version-delete**: `deleteAppVersion(appId, version)` removes exactly that `snug_app_versions` row; the VersionsPanel offers delete on eligible rows and the row disappears.
4. **AC4 version-delete-guards** (negative): deleting a pinned version, the current version, or an unknown version each throws a named error and deletes nothing; the UI renders no delete control for pinned/current rows.
5. **AC5 telepath-unlink**: deleting the app that holds the LAST sidecar-ceiling connection triggers, after the DB cascade commits, helper start (idempotent) → `POST /session/forget` (best-effort Baileys logout, in-memory clear, persist tombstone, auth-dir wipe incl. `thread-cache.json`) → `sidecarCtl('forget')` (stop + Rust-side session-dir removal backstop); a helper test proves the auth dir stays empty **after a simulated exit flush**.
6. **AC6 telepath-unlink-scope** (negatives, in the SAME suites as AC5's positives): (a) deleting an app with no sidecar fact fires nothing; (b) deleting a sidecar app while a SECOND app still holds a sidecar fact fires nothing; (c) the helper's forget arm answers 401 to a valid APP bearer token (nonce-only); (d) a new TS equivalence assertion pins the Rust `WIZARD_ROUTES` table equal to the full `SIDECAR_ROUTES` set, and a cargo test pins `admit_wizard_request("POST","/session/forget")` positively.
7. **AC7 db-cascade unchanged**: existing delete-app cascade suites stay green, PLUS the new per-app `appProvider:<appId>` settings key (introduced by AC9) is swept by `deleteApp` (equality delete, mutation-checked).
8. **AC8 multi-key settings**: keys for anthropic AND openai can be saved side-by-side in the redesigned Settings (already true at the store layer; now true in the UI — both fields visible/editable, each with a per-provider default-model select filtered to that provider's catalog entries).
9. **AC9 default provider**: with no explicit user choice, the resolved default provider is anthropic when an anthropic key exists, else openai when an openai key exists, else the demo brain; an explicit user pick (new `providerChoice` settings row) overrides and persists; every LLM lane for an app with no per-app override routes to the resolved default provider + that provider's default model.
10. **AC10 per-app provider override**: picking a model of the OTHER provider on an app's header stores `appProvider:<appId>` + `appModel:<appId>` and that app's sends route there (adapter + key + model), while other apps keep following the default; clearing back to "default" deletes both rows (absence, not a copy).
11. **AC11 model card removed**: the standalone "model" card is gone; the legacy global `model` settings row is adopted forward into `providerModel:<provider>` on hydrate (idempotent, never deletes the legacy row); local mode keeps a model picker inside the local-endpoint section (Ollama-detection behavior preserved); subscription keeps a default-model control inside its own section.
12. **AC12 build-page selector**: the build page renders the model selector; with a build thread attached to an app it reads/writes that app's pick; with a fresh thread the pick routes the build turns and is applied to the app on install.
13. **AC13 grouped selector**: with both providers keyed, the selector (run header + build page) lists both providers' catalog models in provider-labelled groups; with one key, only that provider's group plus the inherit row; under demo brain it renders nothing (existing behavior).
14. **AC14 settings redesign**: the redesigned page keeps every pinned behavior green (mode segment, F15 import-confirm, protection offer/turn-on/change/off, account, data, connections approve/re-approve/revoke, theme) — existing suites are the regression net; a new render test pins the section structure; the connections rows render provider identity, status and actions in the new layout.

**Out of scope**: provider-side OAuth revocation on delete (unchanged); trash/undo for app or version delete; starter-shelf surfaces; new providers beyond anthropic/openai/mock; changing the pinned model catalog; web-BYOK CORS work; subscription-mode server changes; editing threat-model deltas (no delta bytes change — the forget route is an erase-only wizard-class sibling of `/pair/*`).

## Plan

Order chosen so DB/helper layers land test-first before UI, and the redesign lands last on top of stable behavior.

**Phase A — db: version delete + new per-app settings keys + cascade** (`packages/db`)
- `userdb.ts`: add `deleteAppVersion(appId, version)` — NOT_FOUND on unknown app/version (current == MAX, so a too-high version is unknown; tests pin the guard order), named refusals `VERSION_PINNED` / `VERSION_CURRENT`, plain DELETE + markDirty. No VACUUM — stated in the code comment: version HTML is not secret-bearing and the next deleteApp/export VACUUM reclaims it (review finding 13b).
- `app-settings-keys.ts`: add `appProvider:<appId>` AND `appRenamed:<appId>` key contracts beside `appModel:`; `deleteApp` gains both equality deletes; mutation-checked per lesson 2026-08-18. New `setAppProvider`/`listAppProviders` + `listRenamedApps` mirroring the appModel accessors.
- Rename guard: `updateAppMeta` already exists; uniqueness enforced at the playground state layer (no DB constraint — a schema bump for a UI refusal is not warranted).
- Tests FIRST: `delete-version.test.ts` (AC3/AC4), `app-provider-setting.test.ts` (accessors + cascade sweeps exactly its rows — AC7).

**Phase B — helper: session forget route** (`packages/protocol` sidecar-contract + `apps/whatsapp-sidecar` + `apps/desktop`) — REVISED per the 2026-08-21 fresh-context plan review (findings 1–5):
- `sidecar-contract.ts`: add `POST /session/forget` to `SIDECAR_ROUTES` (the `/session/` prefix keeps it out of `APP_REACHABLE_SIDECAR_ROUTES` automatically).
- **Forget is a `WaSocket` seam method** (review finding 1 — the seam has no logout today): best-effort `sock.logout()` (unlinks the companion on the phone), clear the in-memory store, cancel the save timer, and set a **tombstone that makes `persistNow` AND `saveCreds` no-ops** — THEN `resetAuthStore(authDir)`. Without the tombstone the SIGTERM exit flush re-writes `thread-cache.json` into the wiped directory (the stop is TERM-first precisely so that flush runs). Helper test must assert the dir stays empty **after a simulated exit flush**.
- **Router arm is nonce-only** (finding 3): the `/session/` branch accepts app tokens today (`router.ts:164`); the forget arm requires `wizardAuthorized` alone. Negative test drives it with a valid APP bearer token and expects 401.
- **Rust**: add the route to the hand-written `WIZARD_ROUTES` table, add a `sidecar_ctl("forget")` arm (stop/reap, then remove the session dir — the Rust-side guaranteed wipe when the helper cannot run, finding 4), and a positive `admit_wizard_request("POST","/session/forget")` cargo test. **New TS-side equivalence assertion: `WIZARD_ROUTES` == the full `SIDECAR_ROUTES` set** (finding 2 — the existing equivalence test only covers `APP_ROUTES`, so nothing forces the wizard table today).
- Playground `state/library.ts` `delete(id)`: BEFORE `db.deleteApp`, determine whether this app holds the **LAST** sidecar fact across ALL apps (finding 5 — `appHasSidecarFact` is in `state/sidecarLive.ts`; a second app still holding one must NOT be unlinked). AFTER the cascade commits: `sidecarCtl('start')` (idempotent — logout needs a live helper), wizard-fetch `POST /session/forget`, then `sidecarCtl('forget')` as stop+disk backstop. Best-effort with console surfacing; web no-ops (seams absent). Residual (recorded): if the helper cannot start, the phone-side unlink is skipped — the Rust `forget` still wipes the disk.
- Tests FIRST: helper router suite (forget wipes auth dir incl. after simulated exit flush; app-token 401; nonce 200), wizard-table equivalence, cargo admission tests, playground `library` test with spy seams (fires for the last sidecar app; silent for non-sidecar apps AND when a second sidecar app remains — positive+negative in one suite, finding 15).

**Phase C — provider/model state rework** (`apps/playground/src/state/mode.ts`, `appModel.ts`) — REVISED per review findings 6–11:
- New settings rows (all in `snug_settings`, no schema bump): `providerChoice` (explicit default-provider pick; absence = derived), `providerModel:anthropic` / `providerModel:openai` (per-provider default model; absence = adapter default), plus per-app `appProvider:<appId>` (Phase A key contract).
- `resolveDefaultProvider()`: explicit choice (if its key is still present, or it is mock) → anthropic-if-key → openai-if-key → mock. `providerStore` becomes the RESOLVED value, recomputed on key save/delete and choice change — downstream consumers keep reading `providerStore` unchanged. A **synchronous key-presence store** (finding 11) hydrates with settings and updates on `setByokKey` — `getByokKey` is async and a render cannot await it.
- **Legacy adoption on hydrate, BOTH rows** (finding 6): `provider` → `providerChoice` (a present row was a user act under the old UI) and `model` → `providerModel:<resolved provider>` — idempotent, write-once-if-absent, never delete the legacy rows (roaming files opened by an old build still read them). `mode.test.ts` provider assertions classified MIGRATED.
- `resolveModelForApp(appId)` precedence: app pin → **(byok: `providerModel:<resolveProviderForApp(appId)>`; local/subscription: `modelStore` — finding 9)** → undefined. `resolveProviderForApp(appId)`: `appProvider:<appId>` pin (ignored when that provider's key is gone — a keyless byok adapter silently hands back the mock) → resolved default.
- **Provider resolved PER SEND** (finding 7 — today it is CAPTURED at transport construction): `resolveAppTransport` and `createDirectBuilder.send` resolve via `resolveProviderForApp(appId)` when mode is byok, provider ≠ mock and an appId exists; the demo/webllm arms keep their explicit values. Routing test switches the pin mid-session WITHOUT a remount.
- **Adapter-construction census, not `runAgentTurn` census** (finding 8): the inference ladder (`agent/inferrerAdapter.ts` `decideWire`/`liveInferenceAdapter`) reads the global `providerStore` today and must thread `resolveProviderForApp(appId)`, or a cross-provider pin sends the other provider's model id to the global provider's adapter.
- Hydration for `appProvider:*` rows mirrors `hydrateAppModels` (`listAppProviders` in packages/db, parse-don't-prefix reader).
- Tests FIRST: `providerDefaults.test.ts` (AC9 derivation + explicit override + adoption + persistence), extend `appModel.test.ts` (AC10 absence-not-copy, both-row clear), `appModelRouting.test.ts` cross-provider rows incl. the mid-session pin switch and the inferrer lane (AC10).

**Phase D — selectors** (`run/ModelSelect.tsx`, `views/BuilderView.tsx`) — plus review finding 10:
- `ModelSelect`: grouped `<optgroup>` per configured provider (configured iff its key exists per the presence store; mock never listed); option value encodes `provider:model`; **ANY pick writes BOTH `appModel` and `appProvider` rows** (finding 10 — a same-provider pick with only a model row strands a foreign id when the default provider later changes; a pin is a pin, only inheriting must be an absence); the inherit row deletes both and labels the resolved default (`default (anthropic · Claude Sonnet 5)`); unknown stored ids still shown. Local mode keeps the Ollama single-list behavior (model row only, as today).
- Builder: mount the selector in the build page; `appId = chat.attachedAppId`; when absent, a session-scoped pending pick (`state/builderModel.ts`) routes fresh-thread build turns (agent memo keys on it) and is applied to the new app as its pin on install via the artifact-sink install hook, then cleared.
- Tests FIRST: extend/replace `appModelSelector.test.tsx` rows (AC13 groups, both-row writes, both-row clear), new `builderModelSelect.test.tsx` (AC12: renders, fresh-thread routing, transfer-on-install).

**Phase E — rename + version-delete UI** (`views/HubView.tsx`, `run/VersionsPanel.tsx`, `state/appMeta.ts`) — plus review finding 12:
- Rename lives on the hub tile (inline edit, Enter commits, Escape cancels). **The announce path overwrites `display_name` on every run** (`recordAppMeta`), so rename also writes an `appRenamed:<appId>` marker (Phase A key contract, deleteApp-swept) and `recordAppMeta`'s merge keeps the existing name for marked apps. Uniqueness via `userLibrary().list()` (there is no `useLibrary` hook — finding 12), trim + case-insensitive, self-rename allowed; inline refusal copy. The run header prefers the appMeta-store name (DB-backed, rename-aware) over the captured announce name so a rename shows without reload (AC1's run-header half).
- VersionsPanel: delete per eligible row (not pinned, not current), inline confirm matching the hub delete pattern; the SAME test proves eligible rows have the control and pinned/current rows do not (positive twin, finding 15a).
- Tests FIRST: `appRename.test.tsx` (AC1/AC2 + announce-does-not-clobber-rename), VersionsPanel delete rows (AC3/AC4 UI half).

**Phase F — settings redesign** (`views/SettingsView.tsx`, `views/ConnectionSlotsCard.tsx`, `theme/app.css`)
- Rebuild SettingsView as grouped sections (Apple Settings idiom: section label + inset rounded card of rows, clear hierarchy, `--space`/token-driven, no new palette): Brain (mode segment + per-mode content: byok → two provider rows each with key field + default-model select + default-provider control + demo brain row; local → endpoint + Ollama model picker; subscription → default model), Account, Your file (data + protection), Connections, Appearance (theme).
- ConnectionSlotsCard: redesigned rows — provider glyph (themable geometric, NOT emoji — lesson 2026-08-18), name + host line, status pill, actions aligned; responsive stacking below the 760px breakpoint; add a settings-specific responsive rule (grid → single column).
- Keep accessible names/testids that existing suites pin (`desktopSettingsView`, `connectionSettings`, protection suites, `mode.test`); where the removed model card's five `desktopSettingsView` tests die, classify each MIGRATED (into the local-section picker tests) or OBSOLETE (card gone) per the 2026-08-10 rule — never LOST. **e2e pins carried verbatim** (review finding 14 — a lane CI does not run): `getByRole('group', { name: 'where the agent runs' })`, the mode button labels (`hub subscription` etc.), and the `webllm-experimental-card` testid (`webllm.spec.ts:34,61,63,74`); the webllm experiment card stays in the redesign.
- Verification: real-browser pass (dev server + screenshot at ≥3 widths) — lesson 2026-08-20 "run the product before claiming a UI feature works".

**Cross-package impact** (graph): `protocol` change → run everything (root `pnpm test --force`); `db` → `sdk`, `playground`, `desktop`; helper + desktop cargo suites; playground vitest serially if red (classified flake). Gate-5: root forced run + `pnpm --filter desktop test` + cargo + examples.

**Spec-sync**: none (internal-draft surface only). **ADR**: one ADR for the provider-defaults model (supersedes the "global model" half of ADR-0036's precedence, keeps its per-app half) + records the delete-version protection rule and the Telepath deep-delete decision — drafted at Gate 6 entry, numbered against main at that time.

**High-tier requirement**: fresh-context AI plan review BEFORE implementation (dispatched from this session, findings folded below).

## Decisions & surprises

- Owner 2026-08-21 (interview): protect ALL pinned versions + running; full device unlink on Telepath delete; grouped both-provider selector; run straight through after plan (no owner plan-approval stop) — review at `/close-session`.
- Branch name `feat/ui-polish` is an explicit owner instruction overriding the `feat/TASK-<id>` convention.

## Session journal (append-only, newest last)

### 2026-08-21 — Claude (autonomous) — session
- Done: Gates 1–2 — explored settings/BYOK/model + app-lifecycle/delete code (two fresh-context maps), owner interview (4 decisions), task file + plan written, branch cut from clean `main` @ `c8bb448`.
- Done: High-tier fresh-context plan review (16 findings; verdict "implementable after folding"). ALL folded into the phases above. The four that would have shipped silent defects: (1) the helper's SIGTERM exit flush re-writes `thread-cache.json` after a naive wipe — forget needs a persist tombstone on the `WaSocket` seam; (2) the existing route equivalence test covers `APP_ROUTES` only, so nothing forces the Rust `WIZARD_ROUTES` table — a second equivalence assertion is required; (3) the helper router's `/session/*` branch accepts APP tokens — the forget arm must be nonce-only; (6/7) legacy `provider` row needs hydrate-adoption and provider is CAPTURED at transport construction today — per-send resolution required or a mid-session pin does nothing.
- State: plan final; Gate 3 (tests first) starting with Phase A.
- Next step: Phase A tests → implementation, then B→F in order.
- Open questions: none blocking. Recorded residual: if the helper cannot start at delete time, the phone-side unlink is skipped (Rust `forget` still wipes the disk).

### 2026-08-21 (later) — Claude (autonomous) — session (Gates 3–5 complete)
- Done, phases A→F in order, tests first per phase, one commit per phase:
  - **A** (`fe9cc30`): `deleteAppVersion` (guard order NOT_FOUND→VERSION_PINNED(all pins)→VERSION_CURRENT, no per-row VACUUM by comment) + `appProvider:`/`appRenamed:` key contracts, accessors (`setAppProvider`/`listAppProviders`/`setAppRenamed`/`listRenamedApps`), `deleteSetting`, two new deleteApp cascade lines (mutation-checked by `app-provider-setting.test.ts`).
  - **B** (`d38b11c`): `/session/forget` in the contract (wizard-only by prefix); `WaSocket.forget()` with the **persist tombstone** — mutation check exposed that the first test never drove `persistNow`, so the test now CALLS the real registered `'exit'` listener; router arm nonce-ONLY (app token → 401, pinned); Rust `WIZARD_ROUTES` + `sidecar_ctl("forget")` (stop + `remove_dir_all` of `~/Snug/whatsapp-session`, idempotent) + the NEW wizard-table equivalence assertion (the app-table one is blind to wizard routes — verified red-first after a protocol rebuild; the first run passed VACUOUSLY against a stale dist, the 2026-08-15 lesson live); `library.delete` fires only for the LAST sidecar-fact holder (`appHoldsLastSidecarFact`), after the committed cascade, best-effort, with the two-sidecar-apps and no-fact negatives beside the positive.
  - **C** (`c8e85b6`): `providerChoice`/`providerModel:<p>` rows + synchronous key presence; `resolveDefaultProvider` (explicit choice UNCONDITIONAL — silently re-routing a stale choice is worse than a visible keyless failure; derived anthropic→openai→mock); legacy `provider`+`model` hydrate-adoption (idempotent, never deletes, local-mode `model` excluded); `resolveModelForApp` mode-branched; `resolveProviderForApp`; per-send pin override in transport (mutation-checked: 2 tests red under `provider: provider`), builder, and the inferrer `decideWire(appId)` (review finding 8). `appModel.test`/`appModelRouting`/`appModelSelector` rows MIGRATED with the vacuity trap noted (a 'claude-sonnet-5' fixture equals the adapter default — migrated fixtures use `claude-opus-4-8`).
  - **D** (`166e4e0`): grouped `provider:model` selector (both keyed catalogs as optgroups; any pick writes BOTH rows; legacy model-only pins display under the resolved default; "(key missing)" group label for a keyless pin); `BuilderModelSelect` + `state/builderModel.ts` session pick, routed via the agent memo and TRANSFERRED on install (wiring mutation-checked through the real BuilderView build path).
  - **E** (`2721195`): `library.rename` (unique trimmed case-insensitive, self allowed) + `appRenamed` marker with the announce-clobber guard at BOTH altitudes (store merge + async DB write re-check — the race test pins the db-side guard); hub tile rename editor (Enter/Escape/inline refusal); run header prefers the DB-backed name; VersionsPanel per-row delete with inline confirm, positive+negative in one suite.
  - **F** (`5bc435b`): Settings rebuilt as five labelled sections of inset grouped rows (brain/account/your file/connections/appearance); multi-provider BYOK rows (key chips, per-provider default-model selects, default-provider seg with demo brain, keyless choices disabled); standalone model card retired (local half kept `#model-select`/`#model-id` + all five Ollama states — desktopSettingsView 11/11 green untouched; subscription half kept `#model-id`); ConnectionSlotsCard rows relaid (glyph ⚯, identity column, status pill classes, action cluster; ALL testids + derived copy verbatim); responsive rules at 760px; e2e pins carried ('where the agent runs', mode labels, webllm-experimental-card).
- Verification (Gate 5): **root `pnpm exec turbo run test --force` — 23/23 tasks, Cached: 0, exit 0** (playground 1442/1442 in PARALLEL, no flake this run); `check-threat-model` 139/139, `check-sandbox-guard`, `check-gate-local` all green; cargo 100 passed; whatsapp-sidecar 158; protocol 346; db 416+ (all inside the root run). **Real-browser pass**: dev server + screenshots — settings dark/light at 1280px, 390px stack, hub, build; then the LIVE flow: typed an Anthropic key through the redesigned UI → "key saved" chip + per-provider model select appeared + default provider resolved to Anthropic + the build page grew the grouped selector with the honest inherit label ("default (Anthropic · Claude Sonnet 5)").
- Docs in-branch: ADR-0046 (+ index row, ADR-0036 status amended), architecture.md multi-provider section, code-map.md rows (App delete + per-app model selection) + `update-code-map` count regen, lessons.md ×2 (write tombstone / subset-equivalence blindness).
- High-tier self-sign-off: the plan review's 8 majors are each addressed with a test that fails without the fix (tombstone, wizard-table pin, nonce-only arm, orphanhood negatives, legacy adoption, mid-session pin switch, inferrer lane, both-row pins). Consciously accepted from review: choice/pin honored when keyless (finding-10 discussion resolved toward honesty over silent re-routing; recorded in ADR-0046 §1/§3).
- State: all six owner asks implemented and verified; branch `feat/ui-polish` at Phase F + docs; NOT merged, NO PR (owner instruction: wait for `/close-session`).
- Next step: owner `/close-session` — gate legs choice, review, PR, merge, done-move.
- Open questions: none. Residuals recorded: helper-cannot-start skips phone-side unlink (disk still wiped); keyless pin routes keyless (visible failure, selector marked).
