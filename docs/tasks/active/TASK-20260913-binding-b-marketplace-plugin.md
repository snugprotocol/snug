# TASK-20260913-binding-b-marketplace-plugin: Binding B complete — the skill in the plugin, a brain that pre-warms and names itself, a launcher that starts under a desktop host, the tree the marketplace takes

- **Status**: in-progress — Gates 1–5 in one autonomous session on the owner's ask ("implement and complete the rest for Binding B with plugin, ready for the Anthropic marketplace"); plan reviewed by four fresh-context finder angles before code and folded (below); **built under the recommended defaults — any Q answered otherwise is a one-word reversal**; the owner walks (AC9) are printed, not performed
- **Owner**: Jeetu
- **Risk tier**: **high** — plugin packaging is release config (a marketplace clone runs the bundle as the user); the brain shim spawns and pre-warms the user's own CLI (the child-env allowlist is a C1-adjacent boundary); the threat-model delta gains rows. Plan review with finder angles before Gate 4 (done); explicit self-sign-off at Gate 5.
- **Branch**: `feat/TASK-20260913-binding-b-marketplace-plugin` off `main` `730198c` (PR #177 merged)
- **Packages touched**: `apps/host-mcp` (`brain-resolve.ts` + `install-roots.json` new; `brain-child.ts` new; `brain-claude.ts` — five states, the pool, `stream()`; `loopback-server.ts` — the chat route streams; `runner.ts` — the brain is held and stopped), `apps/host` (`src/local/compose-local.ts` chip copy; `e2e/local.spec.ts` the `outdated` pin), `packages/knowledge` (`prompts/skills/snug/SKILL.md` source; `scripts/gen-content.mjs` exclusion; `prompts/README.md`; `content.ts` regenerated for the README), `scripts/` (`lib/plugin-launcher.mjs`, `lib/skill-build.mjs` new; `lib/plugin-manifests.mjs`, `build-plugin.mjs`, `check-host-mcp.mjs` grown), `docs/`
- **Spec impact**: none — `packages/protocol` untouched; no schema, no spec-changelog entry
- **Related**: [ADR-0069](../../decisions/0069-bindings-and-brains-two-axes.md) (this task's decision) · [ADR-0068](../../decisions/0068-local-host-process.md) · [ADR-0065](../../decisions/0065-skill-delivered-host-kit-and-bindings.md) · program record [TASK-20260904-skill-only-snug](TASK-20260904-skill-only-snug.md) (T6 the skill + T8 marketplace packaging + T3's Binding-B residuals) · `docs/security/threat-model-delta-local-host-process.md`

## Spec (what & why)

**What.** Binding B (ADR-0068) is merged and walked, but the plugin it produces is not yet something a person can install from a marketplace and use: it carried the process and the page and **no skill** — an agent that installed it got four tools and a 37-line launch note, not the app-authoring knowledge base, so it could not build a Snug app. Its `.mcp.json` said `command: node`, which fails under a desktop host whose spawned processes have no user PATH (measured: `launchctl getenv PATH` is empty on the owner's Mac; `node` and `claude` live under nvm). Its brain spawned a cold `claude -p` per think (~3.5 s of process overhead each on 2.1.270), could not tell an outdated CLI from an unknown failure (the owner's 2.1.211 had silently stopped working for every call), and buffered a whole build before the page saw a byte. This task closes those gaps and assembles the tree the community marketplace takes, then stops at the owner acts (create the distribution repo, push, submit).

**Why now.** The owner's direction of 2026-09-13 (ADR-0069): Binding B with the child-CLI brain is the primary path for Claude Code and Claude Desktop/Cowork; the audience is non-technical; setup must be one plugin install.

**Acceptance criteria** (each has tests; owner walks are marked 🔑; ✅ = built and green in this branch):

1. ✅ **The skill rides in the plugin.** `dist/plugin/snug/skills/snug/SKILL.md` — frontmatter `name: snug`, a 186-char `description` naming app/game/tracker/tool and Snug, `license: MIT`; the body: what a Snug app is, the runner detection order (the Snug tools → the local runner; an `Artifact` tool → the artifact runner; neither → the install line), the launch protocol **inserted verbatim from `apps/host-mcp/src/instructions.md`** with headings demoted, the authoring order over `references/`, the bundle shape (`connections: []` always — the truth per Q5), the custody sentences, the never-list. `references/*.md` are the ten KB files as `@snugprotocol/knowledge` renders them (headers stripped, protocol placeholders resolved, headings byte-identical); the skill folder also carries `assets/snug-host.html` and `scripts/snug-embed.mjs` + `lib/page-blocks.mjs`, so the folder alone uploads to claude.ai and still has the artifact route. `claude plugin validate --strict` (marketplace and plugin — `--strict` turns warnings into a non-zero exit, so a pass is a zero-warning pass) and `agentskills validate` exit 0 on the real tree. Tests: `scripts/lib/skill-build.test.mjs` (18) — frontmatter limits and trigger words, the insertion and demotion, the references' headings, every rule's mutant, the real KB.
2. ✅ **The SKILL.md source lives in the prompt store and not in `content.ts`.** `packages/knowledge/prompts/skills/snug/SKILL.md` carries the store's mandatory header ABOVE its frontmatter (the header-check lint requires it; the build strips it); `gen-content.mjs` exports `EXCLUDED_FROM_CONTENT = ['skills/snug/']`; `content-drift.test.ts` pins the exclusion and that the source exists; the store's lints (header, placeholder, ancestor-token, centralization) stay green; `prompts/README.md` maps it.
3. ✅ **The launcher starts the process with no user PATH.** `.mcp.json` is `{"command":"/bin/sh","args":["${CLAUDE_PLUGIN_ROOT}/scripts/snug"]}` — `/bin/sh` by absolute path (present on every Mac and Linux host, not subject to the empty PATH the launcher exists to survive); the `args` array is passed by the hosts as literal argv, so a plugin root with a space is one argument. `scripts/snug` is GENERATED by `scripts/lib/plugin-launcher.mjs` from the one list `apps/host-mcp/src/install-roots.json`: builtins only (no `dirname` — there may be none on PATH), PATH entry by entry (an empty entry never becomes the working directory), then the fixed dirs, then one version under each versioned root; the first Node that runs and is ≥ 20 wins; with none it prints one line naming https://nodejs.org and exits 1. Tests: `scripts/lib/plugin-launcher.test.mjs` (9) — a real `/bin/sh` with a fake `node`, the roots isolated under the temp dir (the first run found the owner's real Homebrew node). **Smoke on this Mac:** `env -i HOME=$HOME /bin/sh dist/plugin/snug/scripts/snug status` found nvm's Node with an empty PATH and reached the bundle.
4. ✅ **The brain finds its binary and names five states.** `brain-resolve.ts`: PATH, then `~/.local/bin`, `~/.claude/local`, `~/.volta/bin`, `/opt/homebrew/bin`, `/usr/local/bin`, then nvm/fnm versions highest first — HOME and PATH read **by name** (the release gate's whole-env count stays 3). `probeBrain` returns `ready | logged-out | outdated | absent | unknown`; `outdated` from the CLI's `or newer is required` / `claude update` sentence; `absent` decided WITHOUT a spawn, its remedy a page to visit (https://code.claude.com/docs/en/quickstart) then `claude` + `/login` — never a curl-into-bash line. The chip says each in words; the e2e pins `outdated`. Tests: `brain-resolve.test.ts` (12), `brain-claude.test.ts` (the states), `composeLocal.test.ts` (the labels).
5. ✅ **The brain pre-warms its children; every child serves exactly one request** (redesigned at the plan review — see D-B5 below). `brain-child.ts`: `ClaudeChild` over `--input-format stream-json` (one user message per child, `text_delta`s forwarded, `thinking_delta`s private, the `assistant` echo not repeated, `result` settles, `is_error` rejects with the CLI's words, an exit before the result rejects with the stderr tail, a split line is reassembled); `ChildPool` keyed by `sha256(system)` — `acquire` takes the pre-warmed virgin or spawns, pre-warms a replacement at once (a failed pre-warm is never the request's failure), `release` kills (single-use), a busy child is never shared, `maxWarm` 2 keys LRU-evicted, a 5-minute idle TTL on a virgin, `stop()` reaps everything and refuses further acquires, a virgin that died is not handed out. `createClaudeBrain` holds the pool; `stream()` frames each delta by one `JSON.stringify` of the whole payload (a delta containing `\n\ndata:` rides inside its frame — pinned), a cold-start bound to the first delta (180 s, "did not answer within") then an idle bound between deltas (60 s, "stopped answering for"), the page closing its request reaps the child, `BrainStreamError.partial` says whether anything reached the sink; `complete()` is the buffered form; `stop()` drains. The runner holds the brain and stops it first. **Measured on the real CLI (the opt-in `brain-live.test.ts`, `SNUG_LIVE_BRAIN=1`):** see the journal. Tests: `brain-child.test.ts` (19), `brain-claude.test.ts` (the end-to-end block over the scripted fake child).
6. ✅ **The page sees tokens as they arrive.** The chat route writes the first chunk with a 200 + `text/event-stream` and each later chunk as it comes; a failure BEFORE any delta is a 502 the page reads; a failure AFTER a delta ends the stream with no finish (the adapter's dropped-stream reading, pinned); a malformed body is a 400 and spawns nothing; the page aborting its fetch aborts the brain's signal. Tests: `loopback-server.test.ts` (the route block, 5).
7. ✅ **The assembled tree is complete and gated.** `build-plugin.mjs` writes the marketplace (`source: ./snug`) + the plugin: manifests (with `repository`), `.mcp.json`, the launcher, bundle, page, the skill folder, `README.md` (the reviewer's content bar: both prerequisites, the install, the update caveat, the no-hooks statement), `LICENSE`, and `PROVENANCE.json` (the monorepo commit + the sha256 of every file). `check-host-mcp.mjs` **builds the tree on every run** (a missing input is CANNOT RUN by name), byte-compares SKILL.md and the references against a fresh render, checks the launcher, assets, README bar, LICENSE, the absence of `hooks/` and of a `hooks` key, the provenance, then runs both validators when present and prints `NOT VERIFIED — <name>` when absent; the release-bundle sweeps stand. Tests: `build-plugin.test.mjs` (5), `check-host-mcp.test.mjs` (28, incl. the SKILL.md-edited, hooks-dir, provenance and validator-absent mutants).
8. **Docs.** ADR-0069 recorded and folded; ADR-0068/0065 status lines; `architecture.md` (the two axes; the pool), `code-map.md` row + counts, `glossary.md` (**Brain** beside **Binding**), the threat-model delta (the pre-warmed child; the launcher's trust) + ledger re-pin, the program record (D12 table rewritten), `next-steps.md`, `lessons.md`. **Q1's rename is its own follow-up PR** (plan review: a 30-file mechanical rename does not belong in the same diff as new logic).
9. 🔑 **Owner walks, printed not performed** (Gate 6 lists the exact commands): install from `dist/plugin` in Claude Code, a fresh session, `/mcp`, "build me a chess app" end to end; the same under **Cowork**, INCLUDING the no-CLI case (does the chip's `absent` sentence show, rather than a silent demo brain?); the **Ledger** walk to live SimpleFIN data; then the distribution repo created from `dist/plugin` (a verbatim copy, never hand-edited) and the community-marketplace form.

**Out of scope** (each named with where it lives): the `codex`/`hermes`/`openclaw`/`ollama` shims (T3's remainder — the pool takes a second CLI as a second `spawnBinary` + parser); the self-contained runtime download when no Node exists (ADR-0060's pattern; needs a Release — owner act); Binding A-connected and S12 (ADR-0069 §4); the hand-in of requirement-carrying bundles (Q5); transcript continuation for the builder (needs the context block to ride in the user turn on this binding — next-steps); `--system-prompt-file` for Linux's per-argument limit (next-steps); `snug_list_apps` reporting the real library; a plugin update while the process runs (the README's restart caveat; next-steps); an `evals/` suite for `claude plugin eval`; Windows; the `packages/db` `onPersistError` seam.

## Decisions taken at Gate 2 and at the plan review (defaults in bold; each reversible by one word)

- **Q1 — Rename the process and its artefacts to `local-host` before anything is published?** **Yes** (ADR-0069 §3) — as its **own follow-up PR** right after this one merges, before the first publish (scope review). The kit's npm name `@snugprotocol/host` is unchanged (coherence review).
- **Q2 — Commit a generated staging tree (D6's letter) or keep the plugin a gitignored build output?** **Build output only.** The gate builds and validates the tree on every run; the SOURCE (SKILL.md in the prompt store) still diffs in every PR; the distribution repo is the committed form, tied to a commit by `PROVENANCE.json`. What is lost is only the rendered copy's diff, which nobody reviewed line by line (scope review's narrower framing).
- **Q3 — Ship both kits inside the plugin (4.7 MiB) or fetch from a Release (D11)?** **Ship inside, for now.** The artifact kit rides as the SKILL's `assets/`, so the skill folder is self-contained.
- **Q4 — The pool's limits.** **2 pre-warmed keys, a 5-minute idle TTL, one pre-warm after each request** — revised from 4/10 min after measuring ~257 MB of idle memory per child. Every child is single-use (D-B5).
- **Q5 — Admit requirement-carrying bundles at Binding B's hand-in?** **Not here** (the D6 admission pin: own task + ADR). Queued in next-steps; the SKILL.md tells the agent the truth (`connections: []`, the user connects in the runner).
- **Q6 — Marketplace target.** **The community marketplace** (form → pinned SHA); the curated one has no submission path.
- **Q7 — The launch protocol's one source.** **Generated FROM `instructions.md`**: the skill build inserts the process's `initialize` text at a marker with headings demoted; the gate byte-compares the result. The agent reads one protocol twice.
- **D-B1 — `/bin/sh` by absolute path**, not `sh` (security review 3.3 + a test harness that found the resolution fails with an empty PATH).
- **D-B2 — One install-roots list** (`install-roots.json`) read by the TypeScript resolver and templated into the launcher (security review 4.2).
- **D-B3 — Named env reads only** in the resolver; the release gate's whole-env count stays 3 (security review 4.1 — the plan's "rises to 4" escape hatch deleted).
- **D-B4 — `PROVENANCE.json` + a verbatim-copy rule** for the distribution repo (security review 5.1/5.3).
- **D-B5 — Single-use, pre-warmed children; no transcript continuation** (feasibility review F1–F4): on this binding the builder's system prompt carries the app's whole html and changes on every build, the wire text differs from the persisted text, and the history window drops whole rows — "continue the child's conversation" would never fire while promising a prompt-cache win that does not exist here. `--max-turns 1` stays and is exact (F2). Only `text_delta`s are forwarded (F4). Abort on the response's `close`; a cold-start bound then an idle bound (F5).
- **D-B6 — The chip links to the install page**, never a curl pipe (security review 6.1).
- **D-B8 — The children run in a neutral cwd with `--setting-sources local --strict-mcp-config`** (security review 1 + measurement): a plugin process inherits the agent host's cwd; a marker `CLAUDE.md` there reached the model, and so did the owner's own `~/.claude/CLAUDE.md` (1,319 input tokens for a one-word answer against 446 without). `--bare` skips keychain reads ("Not logged in") — D5's ban stands. **Q8:** `local` (built) or `user`.
- **D-B9 — Fail on `close`, decode statefully, mark a spawn error dead, cap the in-flight children at 4, spawn before evicting** (correctness review 1–5, security review 2–4) — each with its mutant test.
- **D-B7 — The runtime contract rides as `system` on this binding** (feasibility review F6 corrects ADR-0065's "leading user turn on both host brains" for B) — which is what makes an app's pool key stable across its thinks.

## Plan (as executed)

1. Docs first: ADR-0069; the done-move for TASK-20260907; this file. ✅ (`1f144cb`)
2. Plan review — four fresh-context angles (security, feasibility against the real request shapes, scope, coherence) → folded into ADR-0069 and this file. ✅
3. AC4 — the resolver, the states, the chip copy. ✅ (`13e6780`)
4. AC3 — the launcher, the manifest, the assembly, the gate rule. ✅ (`980bbb4`)
5. AC1/AC2/AC7 — the skill source + exclusion, the skill build, the tree, the gate with validators. ✅ (`1b97153`)
6. AC5/AC6 — the session pool, the shim over it, the streaming route, the runner's stop. ✅ (this commit)
7. Gate 5 — suites of every package touched + dependents, root `pnpm test` (`turbo --force`), `gate:local --legs=workspace,e2e`, the live opt-in test, diff review with finder angles → fold.
8. Docs (AC8), lessons, the program record's D12 table, next-steps.
9. Gate 6 — journal, the owner-walk commands, PR.

### Files (new unless marked)

`apps/host-mcp/src/{install-roots.json, brain-resolve.ts, brain-child.ts}`, `src/__tests__/{brain-resolve, brain-session, brain-live}.test.ts` + `__tests__/fixtures/fake-claude-child.ts`; edits: `src/{brain-claude, loopback-server, runner}.ts` and their tests. `apps/host/src/local/compose-local.ts`, `src/__tests__/composeLocal.test.ts`, `e2e/local.spec.ts` (edits). `packages/knowledge/prompts/skills/snug/SKILL.md`; edits: `scripts/gen-content.mjs`, `prompts/README.md`, `src/__tests__/content-drift.test.ts`, `src/generated/content.ts`. `scripts/lib/{plugin-launcher, skill-build}.mjs` + tests; edits: `scripts/lib/plugin-manifests.mjs`, `scripts/{build-plugin, check-host-mcp}.mjs` + tests. Docs per AC8.

### Cross-package impact

`apps/host-mcp` consumes `@snugprotocol/auth` (deep) + `@snugprotocol/protocol` — unchanged. The skill build consumes `@snugprotocol/knowledge`'s **built** `dist/` from `scripts/` — a new consumer, so the knowledge package must be built before the gate (it is, by `turbo build`). `apps/host` reads `/status`'s `brain.state` — an additive string value. Dependents run: `host-mcp`, `host`, `knowledge`, `playground` (the lint), root.

### Threat model (rows for the delta)

- **The pre-warmed child**: up to two idle `claude` processes per user (~257 MB each), each holding one system prompt in argv and nothing else until its one request; reaped after five idle minutes, on stop, on parent death (their stdin is a pipe from the parent — EOF ends the CLI), on abort, on error. No credential is anywhere near it; a child answers only the request that took it.
- **The launcher's PATH walk**: executes the first `node` it finds under the user's own home — the user's shell's trust (residual 1); it downloads nothing.
- **The binary resolution**: same posture for `claude`.

### Verification standard

`turbo run test --force` for any "nothing broke" claim; the gate verdict line carried verbatim into the journal and the PR, deselected legs named; every guard mutation-checked (the tests above each carry their mutant).

## Decisions & surprises

- **2026-09-13 — Binding B IS the owner's "path 2".** The memory summary and the package names said "MCP"; the code says: the host spawns a process, the process serves the page, the page's executor calls the process, the process runs `claude -p`. → ADR-0069 §1–3; Q1.
- **2026-09-13 — the owner's CLI was broken for every call and nothing in Snug knew.** `claude -p` on 2.1.211 answered `400 … version 2.1.251 or newer is required` with `duration_api_ms: 0`; `probeBrain` would have said `unknown`. Updated to 2.1.270 in-session (the CLI's own instruction). → AC4's `outdated`.
- **2026-09-13 — measured, CLI 2.1.270, allowlisted env:** cold `-p` 5,047 ms wall / 1,546 ms model (second call; the first 7,263 / 3,567); one warm child: `system/init` at 2,815 ms, turn 1 4,211 ms, turn 2 **1,424 ms**, turn 3 **1,321 ms**, conversation kept, `session_id` constant; `--include-partial-messages` yields `stream_event/content_block_delta` with `text_delta`; exit 0 after stdin end. **A child left idle 5 s with NO input then sent one message answered in 1,663 ms wall (1,619 ms model)** — the start-up happens before input; it emits `system/init` only on the first message; idle RSS 262,880 KB. `--input-format stream-json` is in `--help` on 2.1.211 and 2.1.270 though absent from the public docs; `--max-turns` still is not.
- **2026-09-13 — `launchctl getenv PATH` is EMPTY on the owner's Mac;** `node` and `claude` are nvm installs. Claude Desktop's `Application Support/Claude/claude-code-vm/2.1.266/claude` is a **Linux ARM64 ELF** (Cowork's VM); the Mac has no Desktop-provided CLI. → AC3, AC4.
- **2026-09-13 — the launcher test harness failed on its own PATH**: `spawnSync('sh', …, {env: {PATH: '<no /bin>'}})` resolves `sh` against the CHILD's PATH → ENOENT for every case. And the first script called `dirname`, which is not on an empty PATH either. → `/bin/sh` absolute in the manifest and the tests; builtins only in the script. Then every "no node anywhere" case exited 0 because the real list names `/opt/homebrew/bin`, where the owner has a Node → the tests isolate the roots under the temp dir.
- **2026-09-13 — the store's header lint requires every prompt file to START with the header comment; a skill must start with frontmatter.** → the source carries the header above the frontmatter; the build strips it. The placeholder-integrity lint forbids raw `snug:<word>` literals → the launch marker is `<!-- launch-protocol -->`. The prompts README rides in `content.ts` → regenerated.
- **2026-09-13 — the Agent SDK bundles its own binary and authenticates with an API key or a cloud provider**, never the user's subscription. → not a brain option.
- **2026-09-13 — the curated marketplace has no submission path; the community one has a form and pins a SHA.** → Q6.
- **2026-09-13 — every `{{appBuilderToolName}}` in KB files 10–95 sits in the stripped header comment**; the auth reference keeps `{{api_key}}`-style RUNTIME template placeholders on purpose (the first reference test flagged them; the check now names the protocol placeholders only).
- **2026-09-13 — the hand-in under Binding B refuses a bundle whose `connections` are requirements** (`handin.ts`, `runner.ts`); the agent's declaration is lost. → Q5, next-steps.
- **2026-09-13 — `snug status` through the launcher found a LIVE runner on 43127** (the owner's own session) — the e2e's isolated `SNUG_HOME` and ephemeral fallback keep the suites off it.

## Session journal (append-only, newest last)

### 2026-09-13 — Jeetu (via Claude Code, autonomous) — session (Gates 1–2)
- Done: read the program record, ADR-0065/0068, the merged Binding-B code and its threat-model delta; measured the cold/warm/streaming CLI shapes and the desktop PATH; researched the marketplace, Cowork and the SDK; wrote ADR-0069 and this file; the done-move for TASK-20260907.
- State: `1f144cb` on the branch.
- Next step: plan review (finder angles) → fold → Gate 3.

### 2026-09-13 — Jeetu (via Claude Code, autonomous) — plan review folded, Gates 3–4
- Done: four reviews (security 12 findings, feasibility 8, scope 5 + a minimal-AC list, coherence 10) folded into ADR-0069 and the decisions above; AC5 redesigned to single-use pre-warmed children after the feasibility review showed transcript continuation could never fire on this binding, and after measuring that a stream-json child starts before its first input (1.7 s to answer after a 5 s idle). Built AC4 (`13e6780`), AC3 (`980bbb4`), AC1/AC2/AC7 (`1b97153`), AC5/AC6 (this commit). The real gate: bundle 246,863 B, tree built (4.7 MiB, 24 files), `claude plugin validate --strict` ok ×2, `agentskills validate` ok.
- State: host-mcp 18 files / 261 tests + the opt-in live test; host 228; knowledge lints green; scripts: launcher 9, skill-build 18, build-plugin 5, gate 28.
- Next step: Gate 5 (e2e, root `pnpm test`, gate:local, diff review) → docs → Gate 6.

### 2026-09-13 — Jeetu (via Claude Code, autonomous) — Gate 5: the gate, the e2e, the live measurement
- **Gate verdict (`node scripts/gate-local.mjs --legs=workspace,e2e`), carried verbatim:** `PASS workspace · FAIL e2e — pnpm --filter playground test:e2e exited 1 · DESELECTED (not run, not verified): smoke, rust, desktop, release`. The e2e leg failed on SIX playground specs (five in `starters.spec.ts`, one in `starters-connect.spec.ts`) and, because the leg stops at the first failing package, never reached the host e2e.
- **What the six reds are — measured, both confounds removed:** (1) the same specs re-run SERIALLY on this branch: 14/14 passed, with `starters-connect` github/spotify/weather + the read-only route ✘ on their first attempt (7–21 s) and passing on retry; (2) the same specs on a clean **`main` worktree** (`730198c`), serially: 14/14 passed with the IDENTICAL four first-attempt failures. This branch touches nothing on the playground's starter path. It is the playground's known load-sensitive class (next-steps 2026-09-07; the gate ran under `turbo --force` load with parallel workers). Recorded in next-steps, not fixed here.
- **The host e2e, run on its own:** 29/29 (kit + artifact + local; the `outdated` pin included) after rebuilding the kit pages — the first run failed the new pin against a page built five days earlier (lesson written).
- **The live brain (`SNUG_LIVE_BRAIN=1`, the owner's real CLI 2.1.270):** the first think 4,626 ms on a cold spawn; the second, on the pre-warmed child, **1,675 ms**; both finished with `finish_reason: stop` and `[DONE]`.
- **Also green:** host-mcp 18 files / 261 tests; host 228; knowledge lints; scripts (launcher 9, skill-build 18, build-plugin 5, gate 28); `check-threat-model` 235/235; `check-host-mcp` with the tree built (4.7 MiB) and `claude plugin validate --strict` ×2 + `agentskills validate` ok. Bundle 246,863 B, whole-env reads 3.
- **Owner's CLI updated in-session:** `claude update` 2.1.211 → 2.1.270 — the CLI's own instruction for the 400 every `-p` call returned; without it nothing could be measured. Stated here because it changed the owner's machine.
- Diff review: three fresh-context angles (security, correctness, simplicity) — the first launch died on a network error and was relaunched; folded below when in.
- State: 8 commits on the branch; `dist/plugin` built; memory files updated.
- Next step: fold the diff review → Gate 6 (this journal, the owner-walk commands, push, PR). The merge is the owner's: Gate 2's approval never happened in this session, so the PR is where Q1–Q7 get their yes.

### 2026-09-13 — Jeetu (via Claude Code, autonomous) — Gate 5: the simplicity review folded
- Reviewer angles: security, correctness, simplicity — the first two launches died on a DNS error (twice; relaunched on the owner's "try now"); the simplicity review (28 items) landed and is folded here:
  - **The probe now runs the brain's own wire** (`buildStreamArgs` + `ClaudeChild`), so a stream-json break can no longer hide behind a `--output-format json` probe that said ready. Deleted with it: `buildClaudeArgs`, `parseClaudeOutput`, `spawnClaude`, `completionToSseBody`, `CliCompletion`, `SHIM_TIMEOUT_MS`, `BrainDeps.run/pool`, `ClaudeArgsOptions`; one `POSTURE` constant; `ProbeDeps` split from `BrainDeps` (`firstDeltaMs` vs the probe's `timeoutMs`). The live test (real CLI) proved the probe on the new wire: ready, then 1,611 ms pre-warmed.
  - **`.codex-plugin/` is out of the tree**: its interim `.mcp.json` carried an ABSOLUTE path to this machine's launcher, and the tree is copied verbatim into the distribution repo. A test now sweeps the tree for the build machine's path. T9 adds the published-package form.
  - **A skill that cannot ship is a named problem from `buildPlugin`**, never a stack. The launch marker is replaced through a function (a `$&` in `instructions.md` would otherwise be interpreted).
  - **Named for what it holds**: `brain-session.ts` → `brain-child.ts`, `ClaudeSession` → `ClaudeChild`, `SessionPool` → `ChildPool` ("session" already means the Claude Code session the env allowlist guards against; every child is single-use); the process handle is `.process`; "virgin" → "unused". `startedAt`/`now` deleted (never read). The brain option types derive from `Brain` (`Pick`) in the server and the runner. One path per artifact in the gate (`SOURCES.bundle`, `INSTRUCTIONS_SOURCE`, `INSTALL_ROOTS_FILE`).
  - **One contract, two artifacts → a fence**: `validateSkill` byte-compares the install line and the repository in SKILL.md against `plugin-manifests.mjs`. The MCP-outside-the-never-list rule strips whole `never` lines. SKILL.md trimmed of what the inserted `instructions.md` already says (the intro's "not a service…", the hand-in behaviour, the duplicated connection rules) — 141 → 129 source lines.
  - The resolver's `exists` requires a FILE (a directory named `claude` is executable and is not a binary); the runner's browser-fallback remedy names the launcher (`sh <plugin>/scripts/snug open`), not `node`; `install-roots.json` says which resolver takes the highest version.
  - **Kept, with reasons:** the gate's tree checks (they catch a builder that stops writing a file or a manifest not from the module — the fixture caught the launcher rule the day it was written); the README's "where things are"; `stats()`; `Brain.complete` (the live test and the fakes use it). **Queued (next-steps):** `check-host-mcp --against <distribution checkout>` — the byte-compare that would verify the "verbatim copy" claim once the repo exists.
- Verdicts after the fold: host-mcp 18 files / 252 tests (+ the live one), scripts 64, tsc clean, `check-host-mcp` ok (bundle 243,411 B, tree 4.7 MiB, validators ok ×3).

### 2026-09-13 — Jeetu (via Claude Code, autonomous) — Gate 5: the security and correctness reviews folded
- **Security (10 findings) — folded:** (1, major) the children inherited the agent host's cwd → **measured** that a project `CLAUDE.md` AND the user's own memory file rode into every think; every child now runs in `~/Snug/host/brain/` with `--setting-sources local --strict-mcp-config` (login intact, 446 vs 1,319 input tokens; `--bare` measured as "Not logged in" — D5 stands) → D-B8/Q8, delta rows, R-43; (2) a spawn `error` now marks the child dead; (3) a dead warm entry's timer is cleared; (4) `POOL_MAX_LIVE` = 4, refused by name; (5) `-dirty` provenance; (6) `skills-ref==0.1.1` pinned; (7) the launcher searches the absolute dirs with no HOME; (8) the env sweep is no longer uppercase-only; (9) the child's PATH starts with the host's Node; (10) a 16 MiB line cap.
- **Correctness (10 findings) — folded:** (1, major) a **stateful UTF-8 decoder** on stdout/stderr — a split em dash was reaching the page as U+FFFD ×3 inside valid JSON; (2) = security 2; (3, major) the request fails on **`close`**, never `exit` — a CLI that prints its error result and exits at once would have lost the line naming the remedy; (4) = security 3; (5) spawn before evicting; (6) = security 7; (7) relative PATH entries refused in both resolvers; (8) the MCP rule exempts the `## Never` SECTION and has its mutant; (9) a malformed message entry is a 400, nothing spawned; (10) bundle problems printed before CANNOT RUN, `PROVENANCE.json` moved to the MARKETPLACE root so the root manifest is hashed, guarded JSON reads. Residuals adopted: `INSTALL_REMEDY` says "machine", `OUTDATED_RE` no longer matches a passing "claude update". Tests added for every finding, plus the SIGKILL fallback and `runner.stop() → brain.stop()`.
- Verdicts after the fold: see the next entry (the suites, the gate, the live probe).

### 2026-09-13 — Jeetu (via Claude Code, autonomous) — Gate 5 verdicts after both folds
- host-mcp 18 files / 271 tests + the live one; host 228; scripts 73 (launcher 11, skill-build 21, build-plugin 6, gate 29 — the new mutants included); `check-threat-model` 235/235; `check-host-mcp` ok (bundle 245,811 B; tree 4.7 MiB; `claude plugin validate --strict` ×2 and `agentskills validate` ok).
- **Live, on the owner's CLI after the isolation:** the probe on the stream wire → ready; first think **2,117 ms** cold (down from 4,626 — the user's memory file and hooks no longer load), second **950 ms** pre-warmed.
- The gate's own verdict stands as journaled above (workspace PASS; e2e's six playground reds proven identical on clean `main`; host e2e 29/29 on its own).
- State: PR #181 (draft → ready), 11 commits. Next: the owner's walks (AC9) and Q1–Q8; the merge is theirs.
