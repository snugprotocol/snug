# TASK-20260803-spec-v02: Spec v0.2 draft — Portable User Database Format + doc updates (child 6 of portable-hub)

- **Status**: planned
- **Owner**: Jeetu
- **Risk tier**: **high** (spec surface)
- **Branch**: `feat/TASK-20260803-portable-hub` (umbrella branch)
- **Packages touched**: docs, spec-repo prep (local only — push needs explicit ask)
- **Spec impact**: spec v0.2 draft (new normative section; wire frames/envelope unchanged at v1)
- **Related**: umbrella [TASK-20260803-portable-hub](TASK-20260803-portable-hub.md), ADR-0007/0008/0009, SPEC_SYNC.md

## Spec (what & why)

Publishable articulation of the three-actor model: spec v0.2 prose "Portable User Database Format" (tables, versioning semantics, namespace rule, size caps, mandatory Export, SyncProvider contract, hub `/userdb` HTTP contract, secrets posture), sourced from `packages/protocol/src/userdb-schema.ts`. Update architecture.md (new diagram: three actors, serverless path), code-map.md, product-vision.md, glossary. Spec-changelog entry. **No push to `snugprotocol/spec` without explicit ask.**

**Acceptance criteria** (umbrella AC10):
1. Spec v0.2 draft prose prepared locally (spec-repo clone or `docs/` staging) traceable to the DDL constants; schemas dir untouched unless wire constants moved (not expected).
2. `docs/spec-changelog.md` entry (date, v0.2-draft, task id, summary).
3. architecture.md / code-map.md / product-vision.md / glossary.md reflect the shipped reality (no claim exceeding implementation).

**Out of scope**: pushing to the spec repo; npm publish; deploy.

## Plan

After children 1–5 merge-ready: write prose from `userdb-schema.ts` + ADRs → docs updates → changelog. Verified against implementation, not intentions.

## Decisions & surprises

—

## Session journal (append-only, newest last)
