# Snug — Code Map ("where does X live")

> Status: scaffold — stubs marked ⭕; update at Gate 6 as code lands.
>
> **Reading the Tests column:** every package's `test` script now runs `tsc -p
> tsconfig.test.json && vitest run`, so "green" means the package COMPILES (tests
> included) as well as passes. That was true only of `packages/adapters` until
> 2026-08-11 — `db`, `protocol`, `knowledge` and `playground` ran `vitest run` alone,
> which let a package report green while not compiling (it cost this repo three
> false-greens in one task, and switching the guard on surfaced 40 hidden type errors).
> Counts here are regenerated by `pnpm run update-code-map` from a green root run.

| Area / concept | Location | Tests |
|---|---|---|
| Envelope message types, zod schemas, JSON Schema export | `packages/protocol/src` ✅ (frames/envelope/reply/security/json-schemas) | `packages/protocol` vitest (346) |
| **Net frames** (AL-03, internal draft — OUT of `schemas/` SOURCES: strict `net-request`/`net-response`, own size class `MAX_NET_FRAME_BYTES` (1 MiB+64 KiB margin — no silent drop, B1), `NET_ERROR_CODES`, response-header whitelist + `x-ratelimit-*` glob, `NET_METHODS`; GET/HEAD body strict-reject (R2), NO appId (R5), credential-header reject (C1); `normalizeAuthHost` retrofitted to punycode both sides (B3)) | `packages/protocol/src/{frames,constants,auth-schema}.ts` ✅ | `packages/protocol` `net-frames.test.ts` + `punycode-hosts.test.ts` |
| Iframe runner, CSP/sandbox, bridge host, transport/db-driver seams, browser-CSP suite template; **TransportResult carries optional `stopReason`** (TASK-20260812 AC3) — a parse failure with `max_tokens` charges NO strike and answers HOST_ERROR "cut off by the output token limit" instead of PARSE_FAILED | `packages/runner/src` ✅ | `packages/runner` vitest (119) + child-6 Playwright |
| **NetHandler seam** (AL-03: host-assigned `netAppId` binding — never app-claimed, mirrors `dbNamespace` F5/R5; routes net-request → handler, posts net-response; value-blind lint proves the runner imports no fetch-calling module, R4; oversized net-response → terminal `NET_SIZE_EXCEEDED`, B1; `net` capability flag) | `packages/runner/src/{host,transport}.ts` ✅ | `packages/runner` `host-net.test.ts` + `net-value-blind.test.ts` |
| In-app hooks (useSnugApp/usePersistedState/useAppDB/**useConnectedFetch**; embedded + module forms, KB≡SDK sync) | `packages/sdk/src` + `packages/sdk/embedded` ✅ | `packages/sdk` vitest (41) |
| Per-app DB (sql.js + OPFS/IDB/memory, .snug export/import, kv, DbDriver) | `packages/db/src` ✅ | `packages/db` vitest |
| **File protection** (ADR-0043: the `SNUGENC1` container — AES-256-GCM whole-file, PBKDF2-SHA256 600k, file key wrapped independently per slot so a passphrase change rewraps 48 bytes and the Recovery Key survives it; header bound as GCM AAD **plus** an unkeyed checksum that separates *damaged* from *wrong secret* — conflating them either tells a user their good file is broken or invites them to destroy it; ≥128-bit Recovery Key (27 symbols × log2(**30**) = 132.5 bits — the alphabet is 30, not 32, and a base-32 assumption silently ships 127.6); fresh CSPRNG nonce per operation, never a counter, because the OPFS A/B slots can carry one logical save twice) | `packages/db/src/crypto/{container,encrypted-backend}.ts` ✅ | `packages/db` vitest (container 21 + open/protect 18 + second-device 8 + encrypted-sync 6) |
| **File naming + adopt-forward** (ADR-0042: `user.snug` canonical, `user.sqlite` READ when absent and adopted forward on the next write — never renamed, copied or deleted; the same for the sync sidecar and the Dropbox path, whose orphaning would have manufactured a phantom divergence and, via "use the origin copy", discarded local work) | `packages/protocol/src/userdb-schema.ts` · `packages/db/src/{userdb/userdb,sync/{sidecar,dropbox,loop}}.ts` ✅ | `packages/db` vitest (legacy-adoption 7 + sidecar-adoption 5 + dropbox-migration 5) |
| **Protection UI** (the offer latch with its OWN key — never entangled with the desktop welcome, whose dismissal fires on any exit; three-step setup stating the cost on the same screen as the offer; Recovery Key behind a mandatory acknowledgement checkbox (a typed phrase until TASK-20260821-site-playground-polish — owner call); unlock screen with no destructive escape; Settings turn-on/change/turn-off) | `apps/playground/src/vault/*` + `App.tsx` · `views/{HubView,SettingsView}.tsx` ✅ | `playground` vitest (setup 11 + unlock 7 + offer 7 + render 4 + wiring 4) |
| **User DB** (ADR-0007/0010: one file/user, factory-pinned versions+revert/reset, chats w/ pinned bootstrap + meta, per-app wiki docs, schema registry + DDL audit, install-source dedup, corrupt-quarantine, size caps) | `packages/db/src/userdb` ✅ | `packages/db` vitest (419 total) |
| **App delete** (cascade across all six `app_id` tables + `thread_id` join, ignores `pinned`, `BEGIN IMMEDIATE`/ROLLBACK, VACUUM, driver `evict` + `deletedApps` tombstone — delete is terminal. FOUR one-per-app `snug_settings` keys swept by equality: `appModel:` `starterVersion:` `appProvider:` `appRenamed:` (TASK-20260821). **Deleting the LAST sidecar-fact app is a full device unlink** (ADR-0046 §7): after the cascade commits, `library.delete` fires helper `POST /session/forget` (nonce-only; Baileys logout + auth-store wipe behind a persist TOMBSTONE that silences the exit flush and `creds.update`) then `sidecar_ctl("forget")` — reap + Rust removal of `~/Snug/whatsapp-session`, the disk backstop when the helper cannot run; a sidecar fact on any OTHER app vetoes (`appHoldsLastSidecarFact`). Sibling: **`deleteAppVersion`** — one row, guards unknown→`VERSION_PINNED`(ALL pins)→`VERSION_CURRENT`; VersionsPanel offers it with inline confirm on eligible rows only) | `packages/db/src/userdb/userdb.ts` (`deleteApp`, `deleteAppVersion`) + `apps/playground/src/state/{library,sidecarForget,sidecarLive}.ts` + `apps/whatsapp-sidecar/src/{router,baileys-socket}.ts` + `apps/desktop/src-tauri/src/sidecar.rs` ✅ | `packages/db` `delete-app`/`delete-version`/`app-provider-setting` tests · playground `telepathDeepDelete` (5) + `versionsPanelDelete` (4) · whatsapp-sidecar `router` (+4) + `baileys-socket` forget suite (2, incl. the exit-flush tombstone driven through the REAL registered 'exit' listener) · desktop `sidecarContract` wizard-table equivalence + cargo (+3) |
| **On-demand helper download** (ADR-0060: content pin in `src-tauri/helpers.json`, minisign with the updater key, manual host-allowlisted redirects, admission-checked unpack with inflated cap, two-rename swap, `dev`/`downloaded` stamps; the consent card at the install/pairing/autostart moments; per-arch archives with a pinned Node runtime; helper releases are GitHub pre-releases) | `apps/desktop/src-tauri/src/helper_install.rs` + `helpers.json`, `apps/desktop/src/sidecar.ts` (seat), `apps/playground/src/ui/ConfirmOverlay.tsx` (the portaled overlay), `apps/playground/src/state/helperInstall.ts`, `apps/playground/src/connections/HelperInstallCard.tsx`, `apps/playground/src/desktop/HelperSurface.tsx`, `apps/whatsapp-sidecar/{helper-tree,pack-helper}.mjs` + `node-runtime.json`, `scripts/release-helper.mjs`, `scripts/check-helper-pin.mjs` | `helper_install::tests` (fixtures under `src-tauri/fixtures/helper/`, throwaway key), `helperInstall.test.tsx`, `pack-helper.node-test.mjs`, `release-helper.test.mjs`, gate rows `ipc-helper-install-refused`/`ipc-helper-status-dispatchable` |
| **Materializer** (ADR-0010: native `app_<token>__*` rest tables ⇄ per-app runtime DB, verbatim-DDL registry, fail-closed name gate, sequence continuity, sync-hash stability) | `packages/db/src/userdb/userdb.ts` (`materializerBackend`/`writeBack`) ✅ | `packages/db` `materializer.test.ts` (17) |
| **Data-lane execution** (ADR-0019 D7/D8): `scratchRun` runs LLM-authored SQL on a THROWAWAY sql.js instance over the app's exported materialized bytes — read-only by construction, no flag. Statement guards are shared with the real executor from one definition: `forbiddenStatementReason` (ATTACH / `PRAGMA writable_schema` / `load_extension`) and, since the PR #35 review, **`nonDataStatementReason` — the write lane is DML-only**, because the approval card's only impact signal is the affected-row count and `sqlite3_changes()` is 0 for ALL DDL (a `DROP TABLE` previewed as "0 row(s)" and the drift guard compared 0 to 0). `changes` is attached by statement KIND, since `sqlite3_changes()` is sticky across non-modifying statements. Approved writes run in `BEGIN IMMEDIATE`/`COMMIT` with rollback | `packages/db/src/{driver,userdb/userdb}.ts` + `apps/playground/src/agent/dataTools.ts` ✅ | `packages/db` `scratch-run.test.ts` + playground `dataTools.test.ts` |
| App-attached chat (context assembler w/ caps, durable thread pin, bootstrap pinning, threads/docs UI). **Intent-routed since TASK-20260811** (ADR-0019): every message beside an INSTALLED app is classified first, and the intent picks BOTH the context (`buildIntentTurnContext` — data lanes get DDL + doc titles, never the HTML or the rewrite instruction) AND the tool set (`BuilderTurn.tools` override). Fails closed to a clarifying question — never to a lane | `apps/playground/src/agent/{appContext,intentContext,chatRouter,useBuilderChat,artifactSink}.ts` + `run/{DocsPanel,VersionsPanel}.tsx` ✅ | playground `chatRouter.test.ts`, `chatRouterLifecycle.test.tsx`, `intentTurnContext.test.ts` |
| **Build thread sessions** (TASK-20260903, ADR-0062): a turn belongs to its THREAD, not the view — `agent/threadSessions.ts` is a module-level registry (`Map<threadId, session>` on the hand-rolled `createStore`) holding messages/busy/steps/the AbortController/the round-trip inspector; `useBuilderChat` keeps its API but reads and writes the session, so leaving `/build` (or `/run/:id`) mid-turn leaves the turn streaming; there is NO unmount abort — `stop` (→ `stopThread`) and the swap seams (`resetThreadSessions` at import/pull, restore, recover-fresh, app delete, thread delete) are the only aborts. Idle sessions evicted LRU beyond `MAX_IDLE_SESSIONS`; busy never. `state/buildThread.ts` is the per-tab active thread (same `snug:thread` key); the hub create bar MINTS a fresh thread. `views/ThreadSidebar.tsx` lists every DB thread with live badges, rename (`upsertThread` title) and delete (`db.deleteThread`, never the app). | `agent/threadSessions.ts`, `state/buildThread.ts`, `views/{BuilderView,ThreadSidebar}.tsx`, `packages/db` `deleteThread`, `agent/adapter.ts` `?demoslow=` e2e pacing seam | `threadSessions.test.tsx`, `builderNavigation.test.tsx`, `threadSidebar.test.tsx`, `e2e/build-continuity.spec.ts` |
| Schema/doc tools (`schema_apply`, `app_doc_write` — store-sourced, sink-pinned targets) plus (TASK-20260811) `runtime_contract_write` (ADR-0018: version-attached, refuses before the first artifact) and `artifact_edit` (ADR-0019 D10: unique-match-or-fail, atomic, same sink as `artifact_write`) | `apps/playground/src/agent/tools.ts` + `packages/knowledge/prompts/tools/` ✅ | playground `agentTools.test.ts`, `runtimeContractTool.test.ts`, `artifactEdit.test.ts` + knowledge suites |
| User-DB spec constants (DDL, limits, OPFS names — spec v0.2 surface, snapshot-locked) | `packages/protocol/src/userdb-schema.ts` ✅ | `packages/protocol` vitest |
| Crash-safe OPFS persistence (A/B slots + pointer, teardown-proof) | `packages/db/src/persistence.ts` ✅ | `packages/db` vitest |
| Sync (ADR-0009: SyncProvider, sidecar, serialized loop, LWW-on-explicit-action, hub-origin + Dropbox providers, restore) | `packages/db/src/sync` ✅ | `packages/db` vitest (70) |
| **Dynamic Auth pure core** (AL-02, ADR-0014: DI-pure OAuth service — PKCE S256 default, per-flow `expectedFlowId` binding, userLayer unwrap, refresh skew/rotation, best-effort revoke, frozen-host outbound ceiling; CredentialStore seam over `auth:` secrets; async WebCrypto template engine; params→spec transformer; pinned provider registry w/ human-reviewed `apiHosts` + per-provider consent params + per-entry pinned `scopes` (ADR-0028, TASK-20260815: ENTRY-level only, emitted/substituted/rendered like every pinned seat — Spotify pins the 7-scope read+playback set; a scope CHANGE on an approved row always re-consents and deletes the old tokens) — REGISTRY-AUTHORITATIVE for kind+fields since TASK-20260812: every entry declares its own `kind` (required seat; GitHub `bearer_token` w/ ceiling-load-bearing OAuth endpoints kept, D5), `requirementFromRegistryEntry` is the ONE entry→requirement emitter (AC3 structural suite), and `INFERRER_ALIASES` is an inferrer-rung-1-only alias map that never touches `lookupWellKnownProvider` (D3) — note the NAMED split-brain: admission's borrow ban stays kind-AGNOSTIC (substitutes fields, never kind; AC10 pins it, follow-up queued). MULTI-OPTION entries since TASK-20260812-auth-kind-choice (ADR-0020): entries may carry `authOptions` (complete alternate flows; Coinbase retail OAuth, GitHub OAuth app), the emitter takes an option argument, the inferrer result carries an `alternatives` seat (registry options + bounded validated model proposals), and Guard 2b's exemption AND substitution are driven by ONE matched-option handle (`matchAuthOption`) so the blessed list and the substituted shape can never disagree; RedirectUriProvider/CallbackSink transport seams; `postForm` `redirect:'manual'` — B2. NO strictness knob anywhere — C1) | `packages/auth/src` ✅ | `packages/auth` vitest (915 total, tsc-gated) |
| **Connected-fetch executor** (AL-03, plan D2/D3: the ONLY seat that reads a credential value AND calls fetch; 10-gate order — strict shape, host-assigned binding, `status===approved`, https-only + punycode ceiling, SSRF literal guard, mutating-confirm, credential-header strip, per-kind injection read PER USE, `redirect:'manual'`, 1 MiB cap while reading + scrubber; OAuth 401 → one refresh-retry. The WhatsApp sidecar send is gate **6a** — AFTER the mutating-confirm gate, not before it: TASK-20260820's audit found it returning 28 lines early under a comment asserting the confirm "has answered", so every send reached the helper with credentials injected and no confirmation, voiding ADR-0033's standing-approval governance for the one connection class that ADR governs. `sidecarClass` now stands down ONLY the gate-5 SSRF literal check (`.localhost` is an identity the frozen ceiling holds, never an address dialled). NO strictness flag — audit bug 3 dies by construction, AC-linted) | `packages/auth/src/{connected-fetch,scrub,net-guards,session-confirm}.ts` ✅ | `packages/auth` `connected-fetch.test.ts` + `scrub`/`net-guards`/`session-confirm`/`postform-redirect`/`ceiling-punycode` tests |
| **Connection requirement schema + storage** (internal draft, NOT in `schemas/` export: 6-kind flat requirement, `declaredApiHosts` rules, derived-host union incl. refreshUrl, three-status enum; `snug_connections` v4 table, slot-keyed, requirement/grant split + staged pending re-approval + delta-aware import reconciliation + `auth:` key namespace helpers. v3's `snug_auth_specs` was DROPPED at userdb v5, P3) | `packages/protocol/src/{auth-schema,connection-requirement}.ts` + `packages/db/src/userdb/{userdb,auth-secrets}.ts` ✅ | protocol `{auth-schema,connection-requirement,userdb-schema}.test` + db `{connections,auth-custody,auth-secrets}.test` |
| **Net capability wiring** (AL-03 playground half: `state/net.ts` builds the executor over the page user DB + a session-remember confirm gate → NetHandler; **Connections** settings panel — lists kind/provider/FULL frozen host list/status, Approve/Re-approve/Revoke via AL-02 accessors, invalidates grants on every transition R3; the mutating-call confirm dialog; RunView binds net to the installed app's id) | `apps/playground/src/{state/net.ts,views/SettingsView.tsx (renders the v4 `ConnectionSlotsCard` — see the wizard row),run/{NetConfirmDialog,RunView}.tsx}` ✅ | playground `netState`/`connectionSettings`/`confirmDialog` tests + `e2e/net.spec.ts` (8, self-signed https stub) |
| **Connection wizard + requirement inferrer + directive** (Dynamic Auth v2, TASK-20260810 P2–P4 — supersedes the AL-04 row: `llmProposalSchema`, `auth-spec-inferrer.ts`, `spec-scope.ts`, `state/wizard.ts`, `AuthWizardSheet.tsx` and `e2e/auth-wizard.spec.ts` are all DELETED. Requirements are inferred at BUILD time and persisted before first run (R1); the run surface carries NO inference seam (R2/Q5, a removal not a gating). `finalizeConnectionDeclaration` runs POST-TURN from `useBuilderChat` — NOT from `artifactSink`, because `artifact_write` is a mid-turn tool call and the directive arrives after it. Canonical-hash skip rules; an auth-touching edit on an approved row STAGES via `stagePendingRequirement` and renders an old→pending diff; `user` provenance is never overwritten by inference. Wizard step machine `review → register? → credentials → connect? → done`, one decision per screen, verb-named buttons, ADR-0014 clause-5 custody copy verbatim, Q3 provenance-branched console URLs, plain-text registration steps, confirm before discarding an in-flight sign-in (owner decision), post-revoke disclosure keyed by provider/host so a slot rename cannot evade it. TASK-20260812: rung 1 emits the ENTRY's kind/fields via `requirementFromRegistryEntry` — never a hardcoded OAuth shape; wizard-open refusals are never silent — the zero-row guard writes an explanation and `ConnectionWizardNote` (app-level mount) is the note store's ONE renderer; the ConnectScreen retry CATCHES into the flow-status store so all three pre-status throws render their literal copy with a retry. TASK-20260815 (ADR-0028/0029): the ReviewScreen renders a `review-scopes` box and the ReapprovalDiffScreen a `reapproval-scope-diff` delta (a scopes-only staged edit is a VISIBLE change); scope-drift healing rides `migrateConnectionRegistryDrift` (detection + promotion guard + `reapproveFromDiff` routing all driven by ONE `requirementScopesDigest`); console-URL clickability keys on registry-pinned BYTES via `resolveRegistryEntryByName` (any provenance; near-miss stays copy-only; desktop opens via the system browser). VERIFIED-CONNECTION REFRESH (TASK-20260819-inbox-copilot-fixes): the DoneScreen offers "load my real data" once a connection is VERIFIED (a passing probe, or an OAuth kind whose minted token already proved the round trip — the same `awaitingProbe` gate the heading uses), and only when `hasLiveAppHost` says a frame is actually mounted. Confirming rings the app through `state/appHosts.ts` — a module registry that exists because `RunnerHost` lives only in RunView's ref while the wizard is a SIBLING (App.tsx), so there is no prop path; registration is token-scoped against the StrictMode mount(A)/mount(B)/unmount(A) ordering. The signal reuses the EXISTING `connection-event` host-event (open `event` namespace, ADR-0034) and carries an INVALIDATION only, never data: host-event frames have no `instanceId` to verify a sender and an oversize frame is dropped silently. AUTH-KIND CHOICE (TASK-20260812-auth-kind-choice, ADR-0020): multi-option providers surface a choice card — `AuthChoiceCard` is LIVE-ROW-GATED (renders nothing unless `declared`+non-`user`), registry options resolve from the pinned registry at render, inference alternatives ride message meta with re-admission on read, and `chooseAuthOption` (`state/authKindChoice.ts`) is the ONE production writer of the `user` channel — gesture-bound literal, enforced by an executable source scan) | `packages/protocol/src/{connection-requirement,render-directive}.ts` + `packages/auth/src/{connection-requirement-inferrer,requirement-admission,template-lint}.ts` + `packages/knowledge/prompts/tools/connection-requirement-inferrer.md` + `apps/playground/src/{state/{connectionWizard,authKindChoice,appHosts}.ts,connections/{ConnectionWizardSheet,ConnectionWizardNote}.tsx,views/{ConnectionSlotsCard,AuthChoiceCard}.tsx,agent/{connectionPipeline,connectionInferrerAdapter,authChoiceCard,demoRequirement}.ts}` ✅ | protocol `connection-requirement.test` · auth `channel-admission`/`template-lint`/`template-parity`/`static-kind-registry`/`registry-self-containment`/`matched-option-admission`/`connection-requirement-inferrer`/`browser-safe` · knowledge `auth-kb.test` · playground `connectionWizard`/`appHosts`/`connectionSettings`/`connectionOauthFlow`/`connectionWizardGuards`/`connectionWizardRestoredGuards`/`connectionPipeline`/`coinbaseBuildFixture`/`coinbaseJourney`/`connectErrorSurfacing`/`netErrorCtaVisibility`/`authKindChoice`/`inferenceAdapterLadder`/`starterInstallAct` + `e2e/connection-wizard.spec.ts` |
| **KB teaching for useConnectedFetch + auth directives** (AL-05: `90-auth-and-connected-apis.md` — the two-part contract (app code via `useConnectedFetch` with a designed pre-connection state; declaration via ONE fenced-json `auth_wizard` directive, emit-iff-newly-needed), app-called-hosts doctrine, famous-provider names-not-endpoints, no-directive negative space, keyless-not-supported-at-1.0 teaching, frames-timeline structure-only note; retrieval DELIVERY tested — always-on summary trigger clause + app-builder tool description/query auth wording, build-time queries must hit the emission section top-3; `AUTH_WIZARD_DIRECTIVE_KIND` single-homed across schema/prompts/scanner/seam; D8 primary-host bias + rule-7 left-out-host evidence valve; honest paste-box wire copy from the shared `inferenceWireCopy` decision ladder) | `packages/knowledge/prompts/knowledge-base/app-authoring/90-auth-and-connected-apis.md` + `packages/knowledge/prompts/tools/app-builder.md` + `apps/playground/src/agent/inferrerAdapter.ts` ✅ | knowledge `auth-kb` (17) + `auth-inferrer-prompt` AC6 rows · playground `authKbEmission` (2) + `inferrerAdapter` AC7 rows |
| Central prompt store (ADR-0004: system/KB/tools/skills/templates/ui layers) + typed loaders, assembly, search, centralization lint (TASK-20260824: its `You are` marker is ANCHORED to a line/literal start — legal prose says "the version you are running"; the lint primitives live in `__tests__/helpers.ts` so `centralization-lint-marker.test.ts` pins the real regex, not a copy) | `packages/knowledge/prompts` + `packages/knowledge/src` ✅ | `packages/knowledge` vitest (195) |
| Provider adapters (anthropic/openai/mock), runAgentTurn choke point, http SSE transport, 48-iteration ceiling, partial-text-on-drop, 128K max output (8K local), `round_trip_start`+`round_trip`+timed tool events, wire model name, cache-token usage, **per-TURN** `cache` and `maxOutputTokens` on `AdapterRequest` (never per-adapter — one instance serves turns of different shapes; `maxOutputTokens` CLAMPS against the construction ceiling so local mode's 8K rule survives a contract asking for more, and is forwarded by `runAgentTurn`); **`stopReason` is read from the WIRE** (TASK-20260812 AC3: anthropic `message_delta.stop_reason`, openai `finish_reason:"length"` → `'max_tokens'`; `AgentTurnResult` carries the final iteration's; SSE `done` events forward it; round-trip snapshots include `maxOutputTokens` as sent; `stream-fidelity.test.ts` replays the owner's delivery-loss fixture at every chunk boundary — if it reds, our SSE layer acquired the loss bug, P0) | `packages/adapters/src` ✅ | `packages/adapters` vitest (130) |
| **Per-app model selection — MULTI-PROVIDER since TASK-20260821 (ADR-0046)**: keys for BOTH providers side-by-side; `providerStore` carries the RESOLVED default (explicit `providerChoice` row unconditionally → anthropic-if-keyed → openai-if-keyed → demo brain; legacy `provider`/`model` rows adopt forward once at hydrate, never deleted); per-provider default models (`providerModel:<p>` rows; local/subscription keep global `model`); a per-app pick stores provider AND model (`appProvider:<appId>` + `appModel:`, both deleteApp-swept, clear deletes both); provider resolves PER SEND at every adapter-construction site (transport gate, builder send, inferrer `decideWire(appId)`) — a captured provider freezes a mid-session pin; grouped `provider:model` selector on the run header AND the build page (`BuilderModelSelect` — attached thread = the app's own control; fresh thread = session pick in `state/builderModel.ts`, transferred to the new app on install); a keyless pin routes honestly and renders "(key missing)", never silently re-routed. Settings redesigned (sectioned inset groups; `settingsRedesign.test.tsx` pins structure + the e2e accessible names). ORIGINAL (TASK-20260817, ADR-0036: an app may PIN a model and every app-scoped LLM call for it routes there — app-frame turns, the builder lane, app-attached chat, and the two inference call sites that have an app id. `resolveModelForApp(appId)` is the ONE precedence rule (pick → Settings default → `undefined`, where undefined deliberately means "let the adapter apply its own default"), read PER SEND because RunView/useBuilderChat memoize their transport+agent. INHERITING IS AN ABSENCE, not a copy, so an un-pinned app FOLLOWS a later change to the Settings default. Storage is `snug_settings['appModel:<appId>']` — no schema bump, no migration, no spec-changelog (D2); the key shape is single-homed in `app-settings-keys.ts` (the `auth-secrets.ts` precedent) and `deleteApp` step 3c cascades to it by EQUALITY. Catalog is a pinned ≤5-per-provider list containing each provider's own `*_DEFAULT_MODEL` so the two literals cannot drift. Hidden under the webllm/demo brain and for `mock` — the brain overrides the configured mode entirely (ADR-0015), so a picker there would imply routing that cannot happen; a stored id absent from the catalog is still SHOWN, never silently dropped) | `packages/adapters/src/model-catalog.ts` + `packages/db/src/userdb/app-settings-keys.ts` + `apps/playground/src/{state/appModel.ts,run/ModelSelect.tsx,run/RunHeaderActions.tsx}` ✅ | adapters `model-catalog` (6) · db `app-model-setting` (5) · playground `appModel` (14) + `appModelRouting` (10, one test per lane — reverting one lane reds only its own) + `appModelSelector` (12, rendered) + `runHeaderIcons` (11, rendered — the per-app cluster is ICON buttons: `aria-label` is the accessible NAME (two e2e specs locate export by "export .sqlite"), `title` is the tooltip; order is model select → connections → export) |
| Reference backend (/invoke SSE + model override, C1 boundary, artifact cache, Google OIDC + sessions/CSRF **+ `picture` claim**, /userdb CAS endpoints incl. revision-on-412, static hosting, fail-closed config, 30-min lifetimes + `step` progress events, prompt caching on the BUILDER path only — derived from `withTools`, never the app-frame envelopes; `done` SSE carries `stopReason`; **a `responseSchema` envelope is never bound by the contract's `maxOutputTokens`** — TASK-20260812 AC1 owner rule, mirrored in the playground direct transport) | `apps/server/src` ✅ | `apps/server` vitest (126) |
| Playground (hub · builder · run+think-panel+versions · byok/local/subscription (+ experimental webllm behind `?webllm=1`) · user-DB state · sync/export/import · login · **Connections**) | `apps/playground/src` ✅ | vitest (1639) + Playwright (74 + 1 skip-by-default real-model spec, incl. the C2 real-browser CSP gate + the AC5 inspector-LAYOUT gate (measures rendered boxes — jsdom cannot) + AC2 serverless gates + owner-report regressions + the 8 pillar-starter tests + the 3 install-act + 5 connection-declaration specs + the 6 starters-connect specs + webllm gates + the 8 AL-03 net specs + the 12 connection-wizard specs — `auth-wizard.spec.ts` is deleted with the v3 surface) |
| **Brain disclosure** (TASK-20260826, ADR-0059: `adapterKindFor` — THE routing derivation, `createTurnAdapter` dispatches through it (keyless keyed provider ⇒ demo, loud throw on drift); `resolveActiveBrain`/`useActiveBrain` — the live "what would a turn run on" read (webllm override → subscription → byok/local leaf; GLOBAL route only — the fresh-pick/per-app-pin gap is triaged in the task file); header `BrainChip` + popover (pinned honest copy, short-label swap <760px); per-turn `brainKind` row provenance (stamped by `onBrain` from the send config, persisted in message meta, `ChatLog` tags demo rows); `demoCallout` latch + `DemoBrainCallout` first-contact note) | `apps/playground/src/{agent/{adapter,builder,useBuilderChat}.ts,state/{activeBrain,demoCallout}.ts,views/{BrainChip,DemoBrainCallout,ChatLog}.tsx}` ✅ | playground `adapterKind` (6) + `activeBrain` (5) + `brainChip` (8) + `demoCallout` (5) + `demoTurnProvenance` (6) · e2e `demo-brain-clarity.spec.ts` (3) + mobile chip row |
| Header identity menu (avatar w/ initial fallback, sign-out via `signOut()` NOT bare `logout()` — F14 stale-CSRF trap) + ember-niche logo/favicon | `apps/playground/src/App.tsx` + `ui/Logo.tsx` + `public/` ✅ | playground `identityMenu.test.tsx` + `brandAssets.test.ts` |
| **Feedback deep links** (ADR-0052: NO hosted receiver — prefilled GitHub issue-form/Discussions URLs; scrub-before-assembly via the single-homed **`security/credentialShapes.ts`** (patterns only, never credential-store reads; TWO modes — `display` for the LLM inspector's aggressive masking, `prose` digit-guarded so "Basic authentication failed" stays diagnostic; llmInspector consumes it, inferrerAdapter migration queued); EVERY free-text input bounded + any shortening carries a visible `…[truncated]`; preview-confirm before ANY navigation (the query string transmits on open), Escape stops propagation (the wizard Sheet listens on the same window keydown), click-away closes, a failed/blocked open keeps the preview up with a plain-link fallback; inline `ReportErrorLink` at build/wizard/run/boot error surfaces; header `FeedbackMenu` (hidden ≤760px — the 375px header has no slack) + Settings `FeedbackCard`, both over one `routes.ts` list; desktop confirm rides the NEW side-effect-free **`platform.openExternalUrl`** seat (`openInSystemBrowser` — never `oauth.openExternal`, whose pending-flow bind is an OAuth side effect); `repro` field deliberately unprefilled) + **hubAuth gate** (ADR-0052 §5: optional `capabilities.hubAuth`, absence=off, gates the `/auth/me` probe AND `login()`; `logout()` deliberately ungated — the cure for a stale session; hub sync origin requires BOTH seats via `hubOriginAvailable()`; web opt-in `VITE_SNUG_HUB_AUTH=1`) | `apps/playground/src/feedback/{githubReport,environment,routes}.ts` + `feedback/{ReportPreviewPopover,ReportErrorLink,FeedbackMenu,FeedbackCard}.tsx` + `security/credentialShapes.ts` + `config/site.ts` + `state/{auth,sync}.ts` + `platform/platform.ts` + `apps/desktop/src/platform-desktop.ts` | playground `githubReport` (19) + `feedbackSurfaces` (9) + `feedbackMenu` (6) + `feedbackDesktopOpen` (2) + `feedbackMounts` (6) + `hubAuthGate` (8) + `llmInspector`/`desktopSettingsView`/`platformBackendWiring` migrated rows |
| **Platform seam** (TASK-20260812-desktop-hub-scaffold: `SnugPlatform` set-once-before-boot — fetchImpl for connected-fetch+adapters+OAuth exchange (`connectedFetchDepsFor`'s default param is THE seam, both net-handler and wizard-probe paths), userdbBackend (userdb + sync sidecar share it), oauth transport {redirectUriFor/openExternal/channelFor/cancel}, saveFile, probeOllama, onOpenUserFile, capabilities {subscriptionMode/hubSyncOrigin/lanHttpPrivate/`hubAuth?` (ADR-0052 §5 — absence=OFF, gates the /auth/me probe + login(); hub sync origin needs BOTH seats via `hubOriginAvailable()`)}; web default = prior behavior byte-for-byte (hubAuth from `VITE_SNUG_HUB_AUTH`). TASK-20260823 added `openExternalUrl?` — the GENERIC side-effect-free system-browser opener (desktop → `openInSystemBrowser`): `oauth.openExternal` BINDS a pending flow's loopback listener as a side effect and must never be reused for plain links (three legacy call sites queued to migrate). TASK-20260812-desktop-auth-awareness added TWO LAN seats whose ASYMMETRY is the guard: `lanFetch?(url, init, pin)` — threaded into `connectedFetchDepsFor` and used by the executor at gate 9a — and `lanPair?(url, init)`, which is NOT threaded into the executor's deps, so a request-time path to accept-and-capture does not exist. Both are pinned by identity tests from the integrating side (`platform.lanFetch toBe lanFetch`) after deletion mutants left every desktop test green TWICE — the cross-package seam is what neither package's suite watches. TEST TRAP: getPlatform locks on first read — desktop-behavior tests need `vi.resetModules` + `setPlatform` before importing consumers) | `apps/playground/src/platform/platform.ts` + consumers `state/{net,userdb,sync,mode,ollama}.ts`, `agent/adapter.ts`, `state/connectionWizard.ts`, `run/exportDb.ts`, `platform/openFile.ts`, `desktop/{firstRun.ts,DesktopWelcome,ModeCoercionNote}.tsx` ✅ | playground `platform`/`platformBackendWiring`/`platformLanFetchWiring`/`platformPromptWiring`/`ollamaState`/`openFile`/`exportDbPlatform`/`desktopFirstRun`/`modeCoercion`/`desktopWizardSheet`/`hubDesktopStarter`/`userdbLoadFailure` tests |
| **Registry request seats + host-side signing** (TASK-20260812-desktop-auth-awareness, ADR-0022: `WellKnownOauthProvider`/`WellKnownAuthOption` gain optional `request` (`headerTemplate` + NEW `queryTemplate`) and `testRequest`, emitted by `requirementFromRegistryEntry` and substituted channel-agnostically by `applyRegistryValues` — refusal and substitution from ONE `matchAuthOption` handle; `occupiedPromptSeats` counts a request carrying EITHER template (a queryTemplate-only request previously sailed past Guard 2b), and values byte-matching the MATCHED option's pinned values are exempt because admission runs TWICE on the production path. Template grammar's FIFTH helper and second signing family: `{{cdp_jwt(api_key, ed25519_private_key)}}` mints an EdDSA (Ed25519) JWT per request (`iss:'cdp'`, `sub`, `uri:'<METHOD> <host><path>'`, `exp:+120s`, header `kid`+`nonce`) — per-argument lint STRICTER than generic (both args must be declared field keys; the engine gets resolved values and cannot re-check, so the render gate is the enforcement seat). `ed25519-key.ts` canonicalizes every CDP secret shape (PKCS#8 PEM, base64 seed‖pubkey, bare seed) to the 32-byte seed and re-wraps it in the fixed PKCS#8 prefix WebCrypto requires (ADR-0030 — ES256/`es256-key.ts` was v1, dropped when the portal moved to Ed25519-by-default), with honest typed errors that NEVER echo pasted content — a PEM's "context" is the secret. Coinbase rewritten to CDP (`['api_key','ed25519_private_key']`, ADR-0030); the old HMAC+passphrase entry was never connectable AND its scheme expired provider-side 2025-02-05) | `packages/auth/src/{well-known-providers,ed25519-key,template-engine,template-lint,requirement-admission}.ts` + `packages/protocol/src/connection-requirement.ts` ✅ | auth `cdp-jwt`/`registry-request-seats`/`registry-query-credentials`/`registry-template-parity`/`registry-self-containment`/`static-kind-registry`/`matched-option-admission`/`channel-admission` · protocol `connection-requirement` · playground `registrySeatPersistence`/`coinbaseJourney`/`starterQueryCredentialJourney` |
| **Query-credential placement + auth-shaped failure observer** (same task, ADR-0022 §3/§4: `queryTemplate` renders into the OUTBOUND url only, AFTER every gate — the confirm gate captures the PRE-injection URL and the app gets back the URL it asked for; a request seat carrying either template SUPPRESSES the kind default so a query credential is never also `X-Api-Key`. Scrub sites are ENUMERATED (P0 amendment 14): `NET_FETCH_FAILED`'s message (previously unscrubbed while fetch errors embed full URLs), body/header scrub, inspector, RunView — and P6 fixed a live C1 leak where candidates carried the RAW value while the URL carried the PERCENT-ENCODED one; both forms now derive from `URLSearchParams` itself so they cannot drift. `onAuthShapedFailure(slot, status, detail?)` at the deps seam / `(appId, slot, status, detail?)` at the playground altitude fires ONLY on the final delivered result — a refresh-cured 401 fires nothing, and `executeConnectionTestRequest` strips the seat. `detail` (TASK-20260815 AC4): a ≤160-char plain-text extract of the provider's own error reason, read from the gate-10-SCRUBBED delivered body only (recognized JSON shapes / text head; HTML and unrecognized JSON yield none), rendered as a "{provider} says:" line — text only, no-linkify pinned by test. TASK-20260819 moved that line OFF the run surface into the wizard's derived attention gate; the run surface keeps `AuthRepairChip` in the app header, and the store's TWO writers — the iframe lane (`state/net.ts`) and the provider chat lane (`agent/providerTools.ts`) — both surface through it) | `packages/auth/src/{connected-fetch,scrub}.ts` + `apps/playground/src/{state/net.ts,run/AuthRepairChip.tsx,connections/ConnectionWizardSheet.tsx}` ✅ | auth `connected-fetch-query-observer`/`connected-fetch` · playground `authShapedFailureSurface`/`wholeSurfaceP6` |
| **LAN-class providers** (same task, ADR-0023: `connectionRequirementSchema` gains optional `lanHost {class,label}` with `declaredApiHosts` **required-XOR-lanHost** — a router-assigned device address is pinnable by nobody, so the wizard COLLECTS it (RFC-1918 IPv4 literal, `isRfc1918Ipv4Literal` deliberately RESTATED in protocol because auth→protocol makes reuse a dependency cycle; the two are pinned equivalent cross-package). Binding order collect → approve → freeze → pair: a pre-collection row derives an EMPTY ceiling that refuses everything. Admission forks three ways (`registryHostIndex` skips lanHost entries — without it ONE hostless entry makes EVERY admission of ANY requirement throw; `applyRegistryValues` PRESERVES the declaration's host instead of substituting; Guard 2c re-validates the class on every channel INCLUDING `registry`), a scoped carve-out of ADR-0020's "hosts are ALWAYS the entry's". `hue` entry: kind `api_key`, field `application_key`, header `hue-application-key`, pairing `POST /api` → `secretPath [0,'success','username']` (array OUTERMOST — the P5 correction) + REQUIRED `pairing.verify` seat (ADR-0025, TASK-20260814: GET `/clip/v2/resource/bridge` fired by the wizard over the pinned transport with the just-minted key BEFORE any connected claim; verify-first, ONE state write per outcome; `AuthConnectionState.lanVerifiedAt` is written by the verify step and nothing else, and the wizard's pairing-owed gate reads `connected && lanVerifiedAt` so legacy unverified claims re-pair honestly). Still NO `testRequest` (no PRE-pair probe can succeed and no user-facing test button is offered — verification moved INSIDE the pairing act) and NO `desktopRedirectPosture` (a LAN device runs no OAuth), all journaled rather than silent) | `packages/protocol/src/connection-requirement.ts` + `packages/auth/src/{well-known-providers,requirement-admission,connected-fetch}.ts` + `apps/playground/src/{state/connectionWizard.ts,connections/ConnectionWizardSheet.tsx}` ✅ | auth `lan-class-registry`/`lan-pinned-transport` · protocol `connection-lan-host` · playground `lanWizardFlow`/`lanConsentCopy`/`hueStarterManifest` |
| **Connection-relative addressing** (TASK-20260814-hue-starter-real-connection, ADR-0026: `snug-connection://<slot><pathAndQuery>` lets an app name its CONNECTION instead of a host it cannot know — grammar protocol-owned (`CONNECTION_URL_SCHEME` + `parseConnectionUrl`, slot charset by `CONNECTION_SLOT_RULE` import, strict three-way result: not-connection-url / malformed / ok). Executor resolution sits after gate 1, before the URL gates: the calling app's OWN slot, approved, ceiling of EXACTLY one host, resolved to `https://<host><path>` and re-checked canonically (`isHostAllowed`) — then the ENTIRE pipeline runs on the resolved URL with `resolveGrant` re-run against the SAME rows read (one read, no TOCTOU; two approved slots claiming the host refuse fail-closed). Disclosure boundary: resolved host reaches the USER (confirm dialog) and never the APP — host-clean symbolic-path refusals, error-only scrub of resolved host/href (`NET_FETCH_FAILED` only; response bodies deliberately NOT scrubbed — provider data surface). Refusals: unknown slot `NET_INVALID_REQUEST` · unapproved `NET_NOT_APPROVED` · not-one-host or two-slot claim `NET_AMBIGUOUS_CONNECTION`. Threat delta: `docs/security/threat-model-delta-connection-addressing.md`. Spec: INTERNAL-DRAFT changelog entry, no schema bytes changed. The hue starter is the first consumer — real CLIP v2 rooms + grouped_light scene writes, probe-on-load doubling as the data fetch, code-keyed fallbacks) | `packages/protocol/src/connection-url.ts` + `packages/auth/src/connected-fetch.ts` (resolution) + `packages/sdk/embedded/snug-hooks.js` (doc line, lockstep) + `packages/knowledge/.../90-auth-and-connected-apis.md` + `examples/hue/` ✅ (folder renamed at the TASK-20260815 re-curation) | protocol `connection-url` · auth `connection-url-resolution` · sdk `kb-sync` · examples `validate` (hooks identity + real-connection pins) |
| **Pinned-TLS LAN transport** (same task: ONE Rust command `lan_fetch` in two EXPLICIT modes — `pair` accepts-and-CAPTURES the leaf's SHA-256 fingerprint+CN INSIDE a rustls verifier (reqwest never exposes the peer cert to callers), `pinned` requires a 64-hex pin and refuses any other leaf; an unknown mode is an ERROR, never defaulted to the "safer" one. Enforced in RUST before a socket opens: RFC-1918 IPv4 literals only (loopback/link-local/CGNAT/public/DNS/IPv6/octal all refused), `Policy::none()` unconditionally, 1 MiB cap on the STREAM before bytes cross IPC, and a FRESH client per call — the verifier runs only at handshake, so a pooled connection established under one pin would serve a later call carrying another with no check at all. Executor routes at gate 9a on `lanPrivateHost && protocol === 'https:'` — the SCHEME axis matters: ADR-0021's http-to-private-literal rung is a DIFFERENT transport for devices that serve no TLS, and routing it here would refuse it forever. Neither absence is a fallback: no dep → named refusal (never `?? fetchImpl`), no pin → named refusal (never accept-invalid-certs). Pin custody: `AuthConnectionState.lanPin` in `auth:<appId>:<slot>:_connection` (ADR-0014), NOT a db column. IPC gate carries a PER-COMMAND `ipc-lan-fetch-refused` check with its own callback slot so a refusal proven for `write_user_file` cannot be credited to `lan_fetch`) | `apps/desktop/src-tauri/src/lanfetch.rs` + `apps/desktop/src/{lan-fetch.ts,platform-desktop.ts,gate/ipc.ts}` ✅ | desktop `lanFetch`/`gateIpc` (85, tsc-gated) + cargo (48, incl. flipped-pin refusal, different-cert-same-address refusal, host-class refusals, plain-http refusal, size cap) |
| **Platform truth in prompts + inference** (same task, AC1/AC2: `HostSystemPromptOptions.platform?: 'web'\|'desktop'`; KB layer `95-platform-desktop` appended LAST on desktop only — web assemblies are byte-identical without it, so NO web variant exists to drift. Corrects KB copy that taught desktop-native fetch is "a FUTURE rung that does not exist yet" and that the runtime "blocks private ranges" — the latter moved in LOCKSTEP with `sdk/embedded/snug-hooks.js`, all 14 example `app.html` copies and `sdk/types.ts` (a three-way byte-compare demanded one commit). The inferrer's USER slot carries platform facts (system slot stays static, D2); the extract-never-invent rule is restated for static kinds as BINDING security copy — a bridge IP is user-typed, never model-proposed) | `packages/knowledge/prompts/system/95-platform-desktop.md` + `packages/knowledge/src/{assemble,layers}.ts` + `apps/playground/src/agent/{builder,transport,connectionInferrerAdapter}.ts` ✅ | knowledge `platform-truth` (+ snapshots) · playground `platformPromptWiring` |
| **Desktop registry seats** (`desktopRedirectPosture` per entry AND per option — loopback/loopback-fixed-port/custom-scheme/https-bridge/device-flow, dashboard-verified w/ citation comments; `browserCallable` disclosure; STRUCTURAL RULE: `pkce:false` + loopback posture is refused at test time — auth-code injection has no defense without provider PKCE binding; `resolveDesktopPosture` option-over-entry; **web-surface seats since ADR-0049** — ENTRY-level `webRedirectPosture: 'origin-callback'` + `webRegistration`, gmail first, resolved at wizard render time by `webSurfaceRegistrationFor` on the web runtime and NEVER emitted into a requirement — deliberately NOT an authOption: options are kind-discriminated and a web flow shares the entry's kind) + **desktop loopback OAuth transport core** (`SNUG_DESKTOP_OAUTH_PORT` 41420, `buildLoopbackRedirectUri` — literal 127.0.0.1 only, recorded-string identity across both oauth-service call sites incl. after listener teardown, single-flight, TTL, bind-collision honesty, delivery-only — no state validation here) + **LAN transport policy** (`transportPolicy.allowHttpForPrivateHosts`: RFC-1918 IPv4 LITERALS only, octet-parsed 172.16/12 boundary-tested; loopback/link-local/localhost/.local/DNS/IPv6 always refused; browser profile byte-identical when absent) | `packages/auth/src/{well-known-providers,desktop-transport,connected-fetch,net-guards}.ts` ✅ | auth `desktop-posture` (46) + `desktop-transport` (22) + extended `connected-fetch`/`net-guards` (729 total) |
| **Desktop app icons** (TASK-20260813 AC3 generated-never-hand-drawn; TASK-20260819 per-platform masters. `scripts/generate-icons.mjs` renders the SAME 32-unit Ember Niche geometry the app and favicon use as TWO 1024 masters — mac (`icon.icns`, `icon.png`): Apple-grid squircle, 824px artwork centred with a TRANSPARENT margin (macOS does not mask/inset icons; `tauri icon` does NOT add the safe area — shape and margin are baked into the master), Apple ~22.4% radius per owner decision, niche painted opaque `#171310`; win master (32/128px): full-bleed brand-radius tile on an opaque `#171310` plate (`icon.ico` was DROPPED 2026-08-20 with the macOS-only bundle restriction — the master that makes it is kept, so post-1.0 Windows is a one-line restore). The niche dark is a solid single-subpath layer under the evenodd ember — the 20260813 revision filled the whole mark path nonzero as backing and the inner subpath's opposite winding knocked the niche out of THAT too, shipping a white niche + baked white corners. Rasterizes via Playwright's Chromium with `omitBackground: true`, runs `tauri icon` per master, copies exactly the five shipped files) | `apps/desktop/scripts/generate-icons.mjs` + `src-tauri/icons/` ✅ | desktop `appIcon.test.ts` (decodes PNGs inline via `node:zlib` and the icns container's embedded PNGs; per-platform groups: mac — corners AND margin band alpha-0, ember at squircle edges, artwork on the 824/1024 grid, niche samples exactly `#171310`; win — corners opaque PLATE (never white), edge-midpoint ember, ≥95% span, niche `#171310`. Colour-at-point assertions exist because coverage-only ones passed on a white niche) |
| **Connection surfaces** (TASK-20260813 AC9/AC10: a `🔌 connections` control in the app's OWN header beside export/theme — gated on the app having rows whatever their STATUS, since an already-connected app was precisely the case with no way back; counts `listConnections(id)` and re-reads on `connectionWizardRevisionStore` so a first connection reveals it without a reload; starters excluded (bundled manifest, no persisted rows → the wizard would open empty). `.connection-note` is the calm surface for "needs a connection" — app surface colour, ember rule, one primary action + quiet dismiss — with an `is-error` variant keeping `--danger` for a genuine credential rejection: same structure, different temperature. Before this every connection surface wore `.error-note`, so routine setup arrived in the same alarm red as a failure. **TASK-20260819 moved the credential-rejection surface off the app page entirely** — it displaced the app's own UI and, because the host cannot tell an expected refusal from a broken key, fired on every launch of a healthy connection. The danger temperature now lives on the wizard's attention screen; the run header carries a quiet ember `.auth-repair-chip` that opens it) | `apps/playground/src/run/{RunView,RunHeaderActions,AuthRepairChip}.tsx` + `connections/ConnectionWizardSheet.tsx` + `theme/app.css` ✅ | playground `connectionSurfaces.test.tsx` + `authShapedFailureSurface`/`netErrorCtaVisibility` |
| **Linked-device connections + the WhatsApp sidecar** (TASK-20260816-whatsapp-twin, ADR-0032/0033: some providers authenticate a DEVICE, not a request — a QR scan and a long-lived session the iframe and the request/response executor structurally cannot host. `AUTH_KINDS` gains `linked_device` (APPENDED, never inserted, so no stored kind is re-read as another) with a coherence arm requiring its token field and refusing OAuth endpoints AND any `lanHost` seat. **The helper is a capability, not a host**: no ceiling entry, no `lanHost` class, `isForbiddenNetHost`/`transportPolicy` untouched — the original loopback-class draft was withdrawn at Gate 2 as unsafe (a host-granular ceiling has no port dimension, so `127.0.0.1` grants Docker/Ollama/Jupyter/another app's sidecar) and, as the adversarial review then proved, UNSTORABLE (`CONNECTION_HOST_RULE` is LDH-only and `normalizeAuthHost` requires an empty port). Reachability is `sidecar_fetch` over a **unix-domain socket** at `~/Snug/whatsapp-sidecar.sock` (0600) — owner decision, making port squatting unrepresentable rather than mitigated — admitting METHOD+PATH against the derived contract, traversal on the DECODED form, 1 MiB cap enforced WHILE READING. `/pair/*`+`/session/*` are wizard-only (that subset IS the refusal for cross-app token capture: `GET /pair/status` releases the token) and EVERY route needs a credential, pairing included, via the spawn nonce. Session keys never leave the helper's disk store — asserted against a POPULATED auth store, since an empty one passes for the wrong reason. History sync is PUSHED and its completion sometimes only INFERRED, so `explicit:false` rides with every page and the app must render partial. ADR-0033's `StandingApprovalGate` WRAPS the session confirm gate rather than widening it (a `(appId,host,method)` key cannot tell one chat thread from another, and every armed send is a POST to one host) — arming surface DEFERRED to a follow-up, so Twin ships manual-Reply-only and says so) | `packages/protocol/src/{auth-schema,connection-requirement,sidecar-contract}.ts` + `packages/auth/src/{well-known-providers,standing-approval,session-confirm,connected-fetch}.ts` + `apps/whatsapp-sidecar/src/{router,store,wa-socket}.ts` + `apps/desktop/src-tauri/src/sidecar.rs` + `apps/desktop/src/sidecar.ts` + `apps/playground/src/{state/connectionWizard.ts,state/net.ts,connections/ConnectionWizardSheet.tsx}` + `examples/whatsapp/` ✅ | protocol `connection-linked-device`/`sidecar-contract` · auth `linked-device-registry`/`standing-approval`/`confirm-seat-scope`/`sidecar-transport` · whatsapp-sidecar `router`/`server`/`message-mapping`/`session-reset`/`store.persistence` (50) · desktop `gateIpc` + cargo (79, incl. the Rust/TS route-table equivalence test, the node-version preflight and the managed-state registration check) · playground `linkedDeviceWizard`/`linkedDeviceSheet` · examples `whatsapp-analysis` (19, parser + scrub) |
| **Telepath: sidecar surface v2 + the host live pump + doc ingestion** (TASK-20260817-telepath, ADR-0034/0035 — the WhatsApp Twin POC rebuilt into a full client). THREE app-reachable routes join the contract: `GET /events` (long-poll over a bounded ring of LEAN HINTS `{seq,jid,kind,ts}` — no bodies, no thumbnails), `GET /chats/:jid/media/:id`, `GET /chats/:jid/picture`; `:jid` generalized to any `:param` with the traversal guard — not the pattern — still doing the refusing. **Push is a HOST PUMP forwarding invalidations, never content**: `hostEvent` frames ride the ordinary 256 KB class and the runner's `post()` drops an oversized frame SILENTLY, and they carry no `instanceId` for an app listener to check — so hints keep the frame un-outgrowable AND make a stale event cost one redundant GOVERNED refetch instead of injecting state. The pump reads through the SAME `connectedFetchDepsFor` assembly as every app request (C1 identical), runs only for an app whose connection is approved with the symbolic host in its frozen ceiling, and is epoch-tokened so StrictMode's double-mount cannot run two loops on one cursor. The sidecar OWNS the unread counter (Baileys reports it only as a sync snapshot) and the CONTACT DIRECTORY — address book from `messaging-history.set`+`contacts.upsert/update`, group rosters via lazy `groupMetadata`, and **LID↔phone resolution** (a `123@lid` is an internal id, NOT a number: rendering its digits produced confident wrong phone numbers). Media refuses-never-truncates above ~700 KB and wires `downloadMediaMessage`'s `reuploadRequest` context so expired CDN links re-request. A half-linked session is NAMED and reported as `needsRelink` on the sync state — carried on `GET /chats` too, because an empty list is exactly where "wedged" and "still syncing" are indistinguishable. **The predicate reads the MATERIAL a completed pairing leaves** (`account` + a non-empty `signalIdentities` beside a saved `me`), NOT `creds.registered` — TASK-20260818-registered-flag: that flag is written by exactly one site in baileys@7.0.0-rc14 (`messages-recv.js:940`, the phone-code flow), so every QR-paired session keeps it false forever; the original check fired on every healthy session, the app said "re-link", and re-linking wiped the working session. A false positive on a predicate wired to a destructive remedy destroys the thing it calls broken. `installStarterDocs` seeds `snug_app_docs` from the starter's `authoring/` bundle at install (absent slugs only — the wiki is living memory), in its OWN module because the AC9 pin asserts every glob in `starterApps.ts` is app-html-shaped | `packages/protocol/src/sidecar-contract.ts` + `apps/whatsapp-sidecar/src/{event-buffer,thread-store,baileys-socket,router,wa-socket}.ts` + `apps/desktop/src-tauri/src/sidecar.rs` + `apps/desktop/src/gate/ipc.ts` + `apps/playground/src/{state/sidecarLive.ts,starter/starterDocs.ts,run/RunView.tsx,views/HubView.tsx}` + `examples/whatsapp/` ✅ | protocol `sidecar-contract` (24) · whatsapp-sidecar `router`/`thread-store`/`event-buffer`/`message-mapping`/`session-reset` (102) · desktop cargo (84, incl. the `RunEvent::Exit` shutdown reap, `:id` traversal + the widened route-table equivalence) + `gateIpc` (the POSITIVE twin `ipc-sidecar-fetch-dispatchable` — a negative check that passed while the command was UNREGISTERED is a check vouching for nothing; **since TASK-20260821-launch-security-review the wizard-only `sidecar_wizard_fetch` has its own pair too** — `ipc-sidecar-wizard-fetch-refused`/`-dispatchable`, closing threat-model R-12: it fronts `GET /pair/status`, which RELEASES the access token, and had no per-command row while its lower-privilege sibling did) · playground `sidecarLive` (10) / `starterDocs` (8, incl. the REAL glob) / `starterTileName` (4) · examples `whatsapp-analysis` (29: pseudonym round-trip both directions, byte-budgeted request builder, chart aggregators, the doorbell driven with protocol constants) |
| **Legal surfaces + consent bands (ADR-0055)** (TASK-20260823-legal-terms-privacy-eula: `/terms` + `/privacy` are DISCLOSURE, never a gate — plain-data content modules in `legal/` rendered by the playground (offline in the desktop shell, `views/LegalPage.tsx`) AND by the website through the `@playground` alias (`apps/website/src/components/LegalDoc.astro`); one shared vocabulary `legal/legalShared.ts` (the two-party "we/us/our" definition — TechVoyage LLC operates the site, Jeetu Maker distributes the app — the byte-pinned R-30 sentence `UPDATE_CHECK_DISCLOSURE`, the CDN hosts RESTATED and pinned equal to `CDN_ALLOWLIST` because the website build has no `@snugprotocol` dependency); `legal/claimDiscipline.ts` mirrors check-whitepaper AC6 (zero-knowledge / end-to-end / never-leave-your-file only in negation; ADR-0043 + ADR-0040 bounds travel with their features) over terms, privacy AND the EULA; the DMG's license screen is the product's ONE clickwrap — `legal/eula.ts` `EULA_TEXT` byte-copied to `apps/desktop/src-tauri/EULA.txt`, embedded via `bundle.licenseFile` (tauri-bundler → `bundle_dmg.sh --eula` → `hdiutil udifrez`), shape-checked by `checkEulaText` (defined ONCE in `scripts/release-desktop.mjs`, ASCII/74-col/60-line) and PROVEN on the built image by `verifyDmgCarriesEula` (`hdiutil udifderez -xml`, base64-decoded SLA text; udifrez/udifderez are Apple-deprecated, named in the refusal); shell footer (`App.tsx` `ShellFooter`, hidden on `/run/*` + `/oauth/*`) and Settings → about (renamed from "app"; EULA rendered offline on desktop). CONTEXTUAL CONSENT stays the primary instrument, four `role="note"` bands that warn and never refuse: BYOK saved-key (`byok-consent-band`), local-model remote endpoint (`local-endpoint-remote-band` — `security/privateHost.ts` lifts the LAN band's `isPrivateNetworkHost` bytes-unchanged and adds `isLocalEndpointHost` for localhost/`[::1]`/IPv6-local; the default `localhost:11434` stays quiet, lookalike names raise), sync origin (`sync-origin-secrets-band` — ADR-0014 §2's words, re-copied for as long as selected; `sync-origin-hub-band` only where `hubAuth` offers the option), WhatsApp link (`linked-device-third-party-band` — R-9 other-party non-consent + R-10 on the screen with "start linking")) | `apps/playground/src/legal/{legalShared,privacy,terms,eula,claimDiscipline}.ts` + `views/LegalPage.tsx` + `ui/ExternalLink.tsx` + `security/privateHost.ts` + bands in `views/SettingsView.tsx` / `connections/ConnectionWizardSheet.tsx`; `apps/desktop/src-tauri/EULA.txt` + `tauri.conf.json` `bundle.licenseFile`; `scripts/release-desktop.mjs` (`checkEulaText`, `verifyDmgCarriesEula`) + `scripts/fixtures/udifderez-{with,no}-sla.xml`; `apps/website/src/{pages/terms,pages/privacy}.astro` + `components/Legal{Doc,Runs}.astro` + the footer Legal column ✅ | playground `legalContent` (29) / `legalPages` (12) / `settingsAbout` (3) / `privateHost` (38) / `consentBands` (19) / `linkedDeviceThirdPartyBand` (3) · desktop `dmgEula` (11) · `scripts/release-desktop.test.mjs` (+5) · website `legalPages` (7) |
| **Host-enforced pseudonymisation backstop (R-9)** (TASK-20260820-host-pseudonymisation: the third-party scrub is no longer only the example app's convention. INGRESS — `sidecarIdentity.ts` harvests contact names + jids from `/chats` bodies at the `sidecarAppFetch` seat (extraction IS the scrub: known fields of the one name-bearing route; non-group names ≥3 chars, jid-placeholders skipped; memory set updated synchronously BEFORE the body returns so the first wire cannot outrun it; persistence is ready-gated fire-and-forget — awaiting `getUserDb()` at the seat would BOOT a db and hang db-less reads). Directory persists under ONE `snug_settings` key (`sidecar-identity-keys.ts` — a namespaced key, NOT a new table: that would be a spec-normative format change) and is WIPED with the last approved sidecar-ceiling connection (revoke + deleteApp cascade, in-transaction). EGRESS — `pseudonymizeEgress.ts` guards INSIDE BOTH leaf transports (`createDirectAppTransport` + `createServerAppTransport` — a caller of a lower export is bound too) PER SEND (stale-capture class) for the sidecar connection FACT in any status (a `declared` imported row binds: its replayable app data rode the same import; Gate-5 review C1): one compiled (memoized per directory revision) longest-first case-insensitive word-boundary alternation + jid pattern + ≥7-digit runs over the parsed envelope, values AND keys, `responseSchema`/`action` included (a valid schema was an in-band smuggling channel — plan-review blocker 1) — EXCEPT envelope ids (verbatim: the phone pattern rewrote ~35% of real UUIDs, breaking the model's requestId echo) and the `responseSchema` subtree, where keys/`required` stay untouched and the directory matches case-SENSITIVELY (a contact saved as "Home" must not break a `home` property); malformed wires are scrubbed raw with an unescape-normalised shadow pass that never corrupts benign `\\uXXXX` data (commutation test); unreadable directory REFUSES the send; the app identity falls back to the envelope's host-stamped appId when the transport has none. The ADR-0031 provider lane gets the same scrub on sidecar-class results in `renderProviderResult` (blocker 2), classified via the CANONICAL `parseConnectionUrl` grammar + the executor's own trim/strip normalization (the hand-rolled regex missed `SNUG-CONNECTION://` and padded spellings the executor happily served — Gate-5 review) and `isSidecarSlotFact`. Session lifecycle: the in-memory harvest resets on import/pull/restore/revoke/deleteApp (`resetSidecarIdentitySession`) so one user file's contacts can never re-persist into another, and a wiped directory stays wiped; the persisted row deliberately SURVIVES import (rows demote to declared; the scrub must keep binding them). ANTI-NAIVE, not anti-adversarial: numeric smuggling, obfuscation, and data-lane replay are DISCLOSED residuals in threat-model R-9; P-labels/`YOU` never rewritten, the shipped app's own stable-label scrub stays as defense in depth) | `apps/playground/src/agent/pseudonymizeEgress.ts` + `apps/playground/src/state/sidecarIdentity.ts` + seams in `agent/transport.ts`, `agent/providerTools.ts`, `state/net.ts` + `packages/db/src/userdb/sidecar-identity-keys.ts` ✅ | playground `pseudonymizeEgress` (27) / `sidecarIdentityHarvest` (7) / `providerToolsSidecarScrub` (4) · db `sidecar-identity-wipe` (7) |
| **Telepath linking & background sync** (TASK-20260818-telepath-linking-sync, ADR-0037 — five owner-reported defects). QR: `beginDeviceLink` POLLS `/pair/qr` until the handshake delivers a code (deadline → NAMED failure; `alreadyLinked` when the route answers `state:'linked'`, since autostart makes that the common wizard state), and the sheet refreshes the rotating code. NAMES: `toWaMessage` carries `senderPushName` (never from fromMe rows — that pushName is the USER's), `ingest` feeds the directory, and the store tracks the TIER a name came from (contact > verified > push; lower never displaces higher) with `nameKind:'push'` riding chats/participants so the app renders `~`; the app's `chatDisplayName` order is saved → ~push → formatted number, NEVER a raw jid, and the directory map keeps BARE names (the `~` is render-only — a decorated string can never match a body, which would defeat the LLM scrub). AVATARS: a no-URL answer is a TTL'd miss (ambiguous with an expired privacy token), thrown failures uncached, lookups through a 3-slot limiter. SYNC: `createThreadCache` persists a versioned/0600/atomic(temp+rename)/quarantining snapshot of the thread store + history state beside the session keys (plus `rosterDiagnostics` failure classes, so a stalled sweep is diagnosable from disk); the store's `onChange` schedules saves BY CONSTRUCTION; the helper resumes a completed pairing at boot (`isResumableStore`, same material read as the wedge predicate) and infers completion (`explicit:false`) when a RESUME sees no history chunk in a grace window — WhatsApp re-pushes history only at pairing; the shell autostarts the helper at launch iff a session store exists and reaps TERM-first (KILL backstop) so the exit flush runs. NAMES PHASE: a background ROSTER SWEEP fetches rosters proactively (3-slot limiter; per-group exponential backoff; `rate-overlimit` — Meta's throttle, not ours — pauses the WHOLE sweep on an escalating cooldown and charges no group; `item-not-found`/`forbidden` are ANSWERS and write off after one attempt), FULL roster rows feed the directory (each participant is a Contact whose `phoneNumber` pairs its LID; a pairing copies known names across BOTH spellings — one-way resolution left known people as +numbers), and the header shows a two-phase capsule ("Syncing · N%" → "Names · n/m", write-offs leave the target) fed by a `/chats` poll (`/session/status` is wizard-only) that extracts ONLY `{progress,complete,needsRelink,rosters,names}` and retires on convergence/needsRelink/minutes-long stall; the app polls as a safety net beside the ADR-0034 doorbell (thread delta 10 s, list 25 s — push delivery on hardware still an open verification) and names the given-up groups' fate in-app. **HELPER SINGLETON** (TASK-20260818-sidecar-orphan-reap, ADR-0037 addendum): the exit reap covered only CLEAN quits, so a crash / `kill -9` / `tauri dev` rebuild orphaned the helper and the next launch's autostart spawned a rival against one auth store — WhatsApp allows ONE live connection per linked device, so the two replaced each other's stream forever (`conflict type=replaced`, owner-reported 2026-08-19 with pids 60490 ppid-1 and 50057). Both ends now answer: the helper's `watchParent` exits through the SAME clean shutdown SIGTERM uses when its ppid CHANGES (not `=== 1` — a Linux subreaper adopts to another pid), and the shell records the pid beside the socket AFTER the survival check and reaps a stale one BEFORE spawning (order is the fix; reaping after leaves both live long enough to ping-pong). `stale_helper_verdict` is pure and is where the danger concentrates: pids are recycled, so a Kill needs the live process's command line to name THIS shell's helper entry — a bare number would let the fix kill the user's editor | `apps/whatsapp-sidecar/src/{thread-cache,thread-store,baileys-socket,parent-watch,cli}.ts` + `apps/desktop/src-tauri/src/sidecar.rs` (+ `lib.rs` setup hook) + `apps/playground/src/{state/connectionWizard.ts,state/sidecarLive.ts,run/RunHeaderActions.tsx,connections/ConnectionWizardSheet.tsx}` + `examples/whatsapp/app.html` ✅ | whatsapp-sidecar `thread-cache` (8) / `baileys-socket` (12, the event WIRING the defect lived in — mocked Baileys) / `thread-store` (+9) / `message-mapping` (+3) / `parent-watch` (6: reparent fires once, stable stays quiet, a throwing probe is UNKNOWN not orphaned, unref'd) · cargo (+3: autostart gate both ways, setup-hook source pin, TERM-trap reap marker; +8 orphan-reap: the verdict four ways incl. the recycled-pid and self-pid refusals, a live bystander SURVIVING the real reap, a look-alike being reaped, pidfile removed with the socket, and a source-order pin that the reap precedes the spawn) · playground `linkedDeviceWizard` (+4) / `linkedDeviceSheet` (+3, one-click QR + rotation + already-linked completes) / `sidecarLive` (+7) / `runHeaderIcons` (+4) · examples `whatsapp-analysis` (+5, `chatDisplayName`) |
| **Self-healing WhatsApp auth store** (TASK-20260822-wa-authstate-corruption: Baileys' `useMultiFileAuthState` tears files on a kill mid-write and its `readData` SWALLOWS the parse error — corruption reported as *absence* ("failed to find key"), parking app-state sync forever on the owner's machine (a key file ending in a stray `"}` since Aug 20; the app-state lane — archive/pin/mute/read — never applied). `createFileAuthState` replaces it: same folder/file-names/`BufferJSON` format so an existing session resumes unchanged — pinned BOTH directions against the real library as oracle — with the thread-cache write rules plus what credentials additionally need: unique-tmp + **fsync** + rename at 0600 (a fixed tmp name is a cross-process race in the documented rival-helper case; rename alone can commit before the data blocks on power loss), trailing-garbage SALVAGE of **any JSON type** healed in place, unsalvageable files quarantined under a CONTENT-HASH aside name (repeat corruption never clobbers earlier evidence) and NEVER deleted, and "absent" meaning ENOENT alone — any other read error THROWS rather than minting fresh creds the next save would overwrite a live session with. `creds.json` quarantines by COPY: the desktop autostart predicate and the wedge predicate both read that path BY NAME, so renaming it away would disable autostart forever while signal material lingered aside with no UI forget path. Persist failures log-and-continue (an ENOSPC creds save must not kill the helper through `void saveCreds()`). `isResumableStore` reads through the exported `salvageParse`, so predicate and store cannot contradict each other when a heal-write fails. **The review catch:** `SignalDataTypeMap['lid-mapping']` is `string` — 1,644 bare-scalar files live — and an object-only guard would have quarantined the whole LID↔phone directory on first read, suite green. The socket also gets a `warn`-floor logger whose `child()` ACCUMULATES bindings — info-level protocol chatter no longer reaches the desktop terminal; faults still do, class/msgId context intact) | `apps/whatsapp-sidecar/src/{auth-state,baileys-socket}.ts` ✅ | whatsapp-sidecar `auth-state` (16: two-way format compat incl. a `/`+`:` id and SCALAR round-trip, atomicity, the live corruption shape, double-quarantine uniqueness, EACCES-throws, creds preserve-in-place) + `baileys-socket` (+2 logger floor/bindings) + `session-reset` (+1 lenient resume) |
| **Desktop shell** (Tauri 2, ADR-0021: Rust commands — atomic user-file (temp+fsync+rename, bare-filename charset, `~/Snug`), single-use open-event allowlist (`.snug`/`.sqlite` existing files only; argv flag/URL shapes inert), consent-scoped export (dialog+write in ONE command — webview never names a path); capabilities main-window-ONLY; `'file'` PersistenceBackend over the command seam (basename reduction at fs.ts — Rust owns the dir); loopback listener via tauri-plugin-oauth; system browser via plugin-opener (https-only guard); native fetch via plugin-http; subtle-fallback (pure-JS SHA-256/HMAC, RFC-4231-pinned, installed only when the webview lacks WebCrypto — measured NOT needed on macOS, tauri:// is secure); HashRouter desktop entry aliasing playground src) + **shell-gate harness** (AC7/AC8 `pnpm --filter desktop gate`: env probes + 14 browser CSP checks + IPC-unreachability-from-srcdoc-iframe (absence-of-effect on keyless invoke) + wizard journey 1 vs net-stub http mode + debug-only host remap (cfg(debug_assertions)+env-once; release-inertness asserted by `strings`) — macOS GREEN 2026-08-12; Windows RAN 2026-08-13 and is deliberately RED (3 IPC checks fail — ADR-0021 D8 trigger met, owner platform decision pending)) | `apps/desktop/{src,src-tauri/src,gate}` + `.github/workflows/ci.yml` ✅ | desktop vitest (85, tsc-gated; + `bundleTargets.test.ts` (12) pinning ADR-0021 D8's macOS-only bundle: `targets` = `["app","dmg"]`, no `.ico` in config OR tree, and the icon generator's SHIP list agreeing — it rewrites that directory, so a stale generator silently restores what the config drops) + cargo (48, incl. the 22 `lanfetch.rs` pinned-TLS tests) + gate JSON verdicts (25 checks incl. the per-command `ipc-lan-fetch-refused` + 13 journey steps) |
| **Token-claim connections + the SimpleFIN provider** (TASK-20260818, ADR-0038: third `WellKnownPairing` variant `token-claim` — registry data, zero protocol bytes; `performTokenClaim` pure mint (decode → ceiling gates incl. default-port/userinfo/`redirect:'error'` → claim POST → access-URL parse with pinned `accessPath` invariant → ADR-0025 verify → write-together `commit`; the THIRD named network seat, oauth-service's class); `simplefin` entry pinned to `beta-bridge.simplefin.org` (apex is a 302 alias — owner-found; probe recorded beside the seat); `claimVerifiedAt` = third verify-marker sibling; wizard family routing `tokenClaimPairingFor`/`isTokenClaimRequirement` + paste screen + third `saveConnectionCredentials` refusal w/ positive twin + no-re-claim gate; drift migration gate gained HOST drift (was fields/seats/scopes-blind — an approved row could never heal a moved registry host); executor wall clock 60 s, self-naming on abort, deps-level `requestTimeoutMs` override) | `packages/auth/src/{token-claim,well-known-providers,connected-fetch,credential-store}.ts` + `apps/playground/src/{state/connectionWizard.ts,connections/ConnectionWizardSheet.tsx}` ✅ | auth `token-claim` (26) + `token-claim-registry` (21) + structural suites · playground `simplefinWizardFlow` (16, incl. 2 host-drift healing) · threat delta `docs/security/threat-model-delta-simplefin-token-claim.md` |
| **Open-url capability** (ADR-0038 D5: internal-draft `snug:open-url-request`/`-result` frames — strict, https-only, userinfo-free, URL-only, 256 KiB class, OUT of `schemas/`; published half = host-ready's optional `openUrl` flag (gen:schemas + spec-changelog, the `net` precedent); runner `OpenUrlHandler` seam — value-blind, NAMED refusal when absent (never the silent unknown-frame drop), single-pending per instance, stale-drop; playground `OpenUrlConfirmDialog` + `state/openUrl.ts` — provenance copy, NORMALIZED-href + punycode host (homograph renders as xn--), synchronous `window.open('noopener,noreferrer')` inside the gesture, desktop via `oauth.openExternal`; RunView binds installed apps only — starters stay capability-false) | `packages/protocol/src/{frames,constants}.ts` + `packages/runner/src/{host,transport}.ts` + `apps/playground/src/{run/OpenUrlConfirmDialog.tsx,state/openUrl.ts}` ✅ | protocol `open-url-frames` (10) · runner `host-open-url` (9) · playground `openUrlConfirm` (6) · e2e `openurl.spec.ts` (2, own scoped project — REAL-tab popup-escape proof on production runner bytes) |
| Starter identity rule (`starter:<id>` → `install_source`; ONE definition, imported by hub + run view) | `apps/playground/src/starter/starterApps.ts` (`starterInstallSource`) ✅ | playground `starterInstall.test.tsx` |
| **Four** `runAgentTurn` call sites in the playground — builder chat, the app-frame transport, and (TASK-20260811) the intent CLASSIFIER and the contract-SYNTHESIS fallback. The data-lane turns are NOT a fifth: they reuse the builder's call site with a `BuilderTurn.tools` override, which is why lane scoping did not need a new turn runner. EVERY call site wires `onLlmEvent` (the whole event union, not just completions) or its turns are invisible in the surface built to show token spend; every one also decides caching explicitly — the builder opts in, the app-frame path must NOT (D0/Q2), and both new one-shots do not (each sits below any cacheable-prefix minimum) | `apps/playground/src/agent/{builder,transport,chatRouter,runtimeContractSynthesis}.ts` ✅ | playground `appTransportRoundTrips.test.ts`, `chatRouter.test.ts`, `runtimeContractSynthesis.test.ts` |
| **WebLLM experimental brain** (AL-07/ADR-0015: `?webllm=1` flag → `resolveBrain` OVERRIDE of the configured mode — never a `PlaygroundMode`; WebGPU probe requires a real adapter, not just `navigator.gpu`; engine singleton behind a dynamic import (code-split); SAME `AgentAdapter` contract through `runAgentTurn`; tools REFUSED (0.2.84 function calling = 8B-Hermes-only + no custom system prompt) → tool-free fenced-HTML build path with reply-text extraction; demo-brain fallback + pinned banner when WebGPU is absent; every "is this a server turn" branch must read the BRAIN (`resolveTurnMode`), not the raw mode; the app-frame transport resolves the brain PER SEND, never at creation) | `apps/playground/src/state/webllm.ts` + `agent/webllm/{model,engine,webllmAdapter,appHtml}.ts` + `views/WebllmBanner.tsx` ✅ | playground `webllm*.test.*` (52) + e2e `webllm.spec.ts` (flag-off zero surface + zero engine bytes, forced-no-WebGPU fallback, sticky-flag SPA nav, rail-copy wiring, skip-by-default real load) |
| **LLM round-trip inspector** (in-memory only — held on the THREAD SESSION since ADR-0062, so it survives navigation and thread switches and dies with the page, not the view; live in-flight entries + nested per-tool timings; payloads kept WHOLE and bounded by a **total-bytes budget** with oldest-first eviction and payload elision for an entry too large to fit — NOT the old per-field ingest cap; redact-at-ingest still applies. The SIBLING of the structural frame inspector, deliberately not an extension: `inspector.ts` never captures values, this one renders bodies on purpose. **TASK-20260813:** entries carry `startedAt` so the live ticker measures the CALL, not its own mount (the rail's tabs unmount it); `RoundTrip` is keyed by `entry.index` ALONE (a positional key changed identity whenever `evict()` shifted the array); and `round_trip` settles the oldest **UNSETTLED** entry at its index, because `index` restarts at 0 per turn and two turn sources share this reducer — a plain `.find()` settled an already-done entry and left the live one ticking forever) | `apps/playground/src/run/llmInspector.ts` + `LlmInspectorPanel.tsx` ✅ | playground `llmInspector*.test.*` (incl. `llmInspectorLive`, `llmInspectorPanel`) |
| **Think panel** (ONE rail tab rendering the LLM round trips; icon tabs keep their text as the accessible name. **TASK-20260813 AC11:** the app↔host frame timeline that sat below them is GONE — structure-only rows with no values read as neither debugging nor narrative. `run/inspector.ts` and its `onFrame` wiring deliberately REMAIN and are still byte-locked: `inspector.inFlight` drives the app-frame "thinking" pulse, `sawDbOp` gates the export button, `readySeen` the header fallback. Deleting the feed with its view kills three unrelated features) | `apps/playground/src/run/ThinkPanel.tsx` ✅ | playground `railTabs.test.tsx` |
| **Run-view either/or (mobile band)** (TASK-20260821 item 5; band widened to ≤1000px TASK-20260823 — phone landscape ~850px + iPad portrait 768–834px sat BETWEEN the 760px shell breakpoint and the desktop split and got a 340px rail on glass. Below 1001px: never a split — full app view OR full think view; think hidden by default on EVERY mount (local state, deliberately never persisted); the header toggle renders in BOTH states; iframe hidden-not-unmounted so the running app keeps state. The matchMedia literal and app.css's media block cannot import each other — `mobileThinkBreakpoint.test.ts` byte-pins the lockstep) | `apps/playground/src/run/RunView.tsx` (`mobileView`/`isMobile`) + `theme/app.css` (the ≤1000px block) ✅ | playground `mobileViewToggle` (3) + `mobileThinkBreakpoint` (3) + e2e `mobile.spec.ts` (5 — 375px AND 820px bands) |
| **Think-rail layout** (TASK-20260813 AC4/AC6: the rail's hard 340px became `var(--rail-width)`, driven by a persisted global store — a workspace preference like the theme, not a per-app one. `RailDivider` drags on pointer events with `setPointerCapture`, and marks `.run-layout.is-resizing` so the app iframe stops eating the pointer — capture does NOT cross into a cross-origin document, which is the classic splitter-over-iframe failure. Keyboard-operable as a `role="separator"`. The toggle defaults ON and only the literal string `'false'` hides it, so a corrupted key fails safe) | `apps/playground/src/state/railLayout.ts` + `ui/RailDivider.tsx` ✅ | playground `railLayout.test.tsx` + e2e `inspector-layout.spec.ts` |
| **Inspector layout guard** (TASK-20260813 AC5: measures the RENDERED box at 280/340/520px rails, because the defect is pure geometry and jsdom returns 0×0 for every rect. The collapse lived in the round-trip SUMMARY line — `.llm-summary` (`flex: 1`) squeezed to a one-glyph min-content by `.llm-meta` (`flex-shrink: 0`) — not in the `<pre>` payload block; the head now wraps rather than starving the summary) | `apps/playground/e2e/inspector-layout.spec.ts` ✅ | Playwright, chromium project |
| Build step MODEL (ordered per-turn steps w/ completion; fed by direct-mode `tool_call`/`tool_result` and subscription `step` SSE). No longer RENDERED as a timeline — a rotating `StatusLine` replaced both it and the reasoning pill, keyed on `busy` (NOT `steps`, which outlives the turn); the factual per-tool record lives in the inspector, nested under its round trip | `apps/playground/src/agent/useBuilderChat.ts` + `views/{ChatLog,StatusLine}.tsx` ✅ | playground `buildSteps.test.tsx` + `statusLineBrand.test.tsx` |
| Artifact target pinning (F9: per-app chat pins, builder thread installs-then-versions) | `apps/playground/src/agent/artifactSink.ts` ✅ | playground vitest |
| **Specification 1.0** (normative; consolidated: wire · storage · connected apps · runtime contracts · linked-device; §17 provisional — ADR-0050, TASK-20260822-spec-10-final; staged for the spec repo where SPEC.md becomes the document) | `docs/spec-drafts/SPEC-1.0.md` ✅ | — (push needs explicit ask) |
| Example apps / starter shelf (single-file, embedded hooks byte-synced to sdk; 12 folders — 4 keepers (trivia-night removed 2026-08-21, TASK-20260821-hardening-polish) + the 5 gold-standard connected starters (TASK-20260815-starter-apps-rebuild, ADR-0031) + `whatsapp` (TASK-20260816-whatsapp-twin, ADR-0032/0033) + `gmail` (TASK-20260819-gmail-starter, ADR-0039 — the AI inbox manager: never-replied/unsubscribe flags, SVG trend charts, preview-then-one-confirm mass cleanup on `messages.batchModify`, extracted-core-tested in `gmail-analysis.test.mjs`. TASK-20260819-inbox-copilot-fixes/-persistence added: a `connection-event` listener that re-syncs when the wizard reports a VERIFIED connection, a selectable sync window (`newer_than:` 7d/90d/6m/1y/none) that the chart axis follows, a `From:` header resolved from `users/me/profile` — its absence bounced every unsubscribe — and DB-BACKED storage on Ledger's pattern: `messages`+`sync_runs` with a `sample` provenance column, so a relaunch loads the user's own mail instead of reverting to the demo) + `ledger` (TASK-20260818-ledger-starter, ADR-0038 — the personal-finance flagship: sample household with planted leaks, deterministic radar/time-machine analytics extracted-core-tested in `ledger-analysis.test.mjs`, five agent lanes, connection-relative SimpleFIN sync), 8 carrying `connection.json`, each connected app also shipping `runtime-contract.json` + its `authoring/` provenance bundle; validate enforces LLM posture per ADR-0011 AND the no-form rule — the sandbox blocks form submission before the submit event, so `onSubmit` never runs in a real browser — AND that `APPS` matches the folders on disk, so a new app can never ship unvalidated by simply not being listed) | `examples/{chess,flying-pig,adventure-quest,quiz-me,trade-copilot,spotify,hue,weather,github,whatsapp,ledger,gmail}` ✅ | `examples` tests (343 across `validate.test.mjs` + `infer-connection.test.mjs` + `connection-manifests.test.mjs` + `whatsapp-analysis` + `ledger-analysis` + `gmail-analysis` + `sample-mode.test.mjs` (TASK-20260819: five flagship connected starters — spotify/Rewind, trade-copilot, hue/Moodboard, whatsapp/Telepath, gmail/Inbox Copilot — open onto a marked, deterministic, render-only sample block with a labelled banner + the full five-doc authoring set; weather/github have no sample mode, and ledger's DB-seeded sample predates the marker convention, locked by `ledger-analysis` instead); incl. the literal-SQL lint + the network/allowlist rule-behavior tables + the hue real-connection pins) + playground `starterShelf.test.tsx` + `e2e/starters.spec.ts` (8 Playwright, incl. the export-bytes zero-trace guard) |
| **Install-act connection declaration** (`connection.json` → the wizard's `declaration` field; the third trust rung after the user and the builder directive. TWO independent facts required: `install_source` resolves to a bundled manifest AND the app's HTML matches the bundled starter — BOTH the NEWEST PINNED factory version (v1 at install, each starter update pins the new release — ADR-0045; an empty pinned set refuses) AND the running `current_version` (`starterDeclaration.ts`; the running-version check is the stated security property) — `install_source` alone is attacker-writable via whole-DB import. Never consults the registry; a mismatch is REPORTED, never silently withdrawn) | `apps/playground/src/starter/starterDeclaration.ts` ✅ + `examples/weather/connection.json` (fixture folder since the TASK-20260815 re-curation) | playground `starterDeclaration.test.ts` (31) + `wizardDeclaration.test.ts` (10) + `authWizardDeclaration.test.tsx` (11) + `starterUpdate.test.ts` vouch block (4, incl. the zero-pin twin) |
| **Starter versioning + the update channel** (ADR-0045: per-starter `starter.json` — integer `version`, cumulative Tesla-style `changelog`, `appHash` binding the metadata to the html bytes so the examples validator REFUSES an app.html edit that ships without a release; hub tile reports "update · vN" (own badge class — `dedup.spec.ts` strict selector) while the ONE update write act lives in the run header: chip + release-notes sheet + update button, one-click for unedited copies, one confirm for re-authored ones; the act lands the bundle as a NEW PINNED version via `saveAppVersion` opts `{pinned, contract}` (atomic contract override), re-runs the declared-only connection refresh + absent-only docs seed, records `starterVersion:<appId>` (settings row, deleteApp-swept, self-healing on retry); `resetToFactory` restores the NEWEST pin; data/credentials/connections/chat survive by construction — all keyed on `app_id`) | `examples/*/starter.json` + `apps/playground/src/starter/{starterMeta,starterUpdate}.ts` + `run/{StarterUpdateControls,ReleaseNotesSheet,VersionsPanel}.tsx` + `views/HubView.tsx` + `packages/db/src/userdb/{userdb,app-settings-keys}.ts` ✅ | examples validator (ADR-0045 block ×13 + 2 glob pins) · db `starter-update-versioning.test.ts` (10) · playground `starterUpdate` (17) + `starterMeta` (4) + `starterUpdateControls` (6) + `hubUpdateBadge` (3) + `versionsPanelFactory` (2) |
| Spec publication process | `docs/engineering/SPEC_SYNC.md` + `docs/spec-changelog.md` | future `scripts/check-spec-sync` |
| Protocol whitepaper, edition 3 — the 1.0 edition (PDF; HTML+CSS Paged Media → headless Chrome, no TeX/npm/network. Fixture = `docs/spec-drafts/SPEC-1.0.md` + `packages/protocol/schemas/` pre-publication; `--spec <clone>` after reads the clone's SPEC.md) | `docs/whitepaper/` (`src/paper.html`, `src/paper.css`, `src/figures/*.svg` ×10, `build.mjs`, `dist/*.pdf` ✅) | `scripts/check-whitepaper.mjs` (104 checks, `pnpm run check-whitepaper`) |
| Code-map test-count regeneration (`pnpm run update-code-map` — rewrites only resolvable numbers in THIS file's Tests column from a green root run; Playwright counts untouched) | `scripts/update-code-map-counts.mjs` ✅ | `scripts/update-code-map-counts.test.mjs` (node:test, runs with the script) |
| **Consolidated threat model** (TASK-20260820, the public security document `SECURITY.md` forward-references: scope · assets · adversaries · trust boundaries · enforced invariants WITH an enforcement point and a test per row · residuals stated as accepted with equal prominence · reporting. Written after a five-surface adversarial audit, not transcribed from the deltas — the audit found three credential-leak paths the eight per-change deltas and their green suites had all missed. The checker hash-pins every `docs/security/threat-model-delta-*.md` so a NEW or EDITED delta fails until the model is re-read against it, and proves every enforcement/test path the invariants table cites still exists. **Stated limit** (§8, after a Gate-5 review found three dropped residuals while it was green): a hash proves a delta has not MOVED, never that its residuals were CARRIED) | `docs/threat-model.md` ✅ + `SECURITY.md` | `scripts/check-threat-model.mjs` (169 checks) + `scripts/check-threat-model.test.mjs` (17, node:test) — both run by `pnpm test` and CI. **v2 (TASK-20260821)** folds in the four post-v1 surfaces (`.snug` encryption — a RETROACTIVE delta for a change that amended the model in place, the starter update channel, multi-provider BYOK + deep delete, and the desktop update channel), and adds **TM7: residual ids are unique** — v1 shipped two R-14s and the hash-pin mechanism is structurally blind to that class (a delta can be unmoved while the document numbers two residuals alike); the encryption residual is now R-27 |
| **The merge gate — CI *and* local** (ADR-0058 supersedes ADR-0041's operative half, 2026-08-26: **CI is LIVE and ENFORCING again**. Both of ADR-0041's premises expired at flip-public — billing dissolved (public repos get unlimited standard-runner minutes) and required status checks became available — so `push`/`pull_request` triggers are restored and `workspace` + `desktop-shell (macos-latest)` are REQUIRED checks pinned to `integration_id: 15368`, proven to block by a probe PR. The **Windows leg is PARKED** with Windows desktop: since TASK-20260820 removed `icons/icon.ico` it died at COMPILE and never reached the `keyReachable` assertion ADR-0021 D8 wanted it red for, so a real R-5 regression and a missing icon were the same colour. `spec` deliberately has NO required check — it has no workflows, so one would deadlock it. `gate:local` KEEPS its job as the pre-push gate and a deliberate SUPERSET of `ci.yml`: SIX independently selectable legs — `workspace`+`smoke` default ON (~2 min), `e2e`/`rust`/`desktop`/`release` opt-in, `--all` ~15–20 min — chosen per merge at `/close-session` time so the owner holds the time/assurance dial. A deliberate SUPERSET of `ci.yml`: it adds Playwright (CI ran none) and the server smoke leg carrying the byte-exact `RUNNER_CSP` assertion (threat-model R-11). A NARROW purpose-built extractor reads `jobs.*.steps[].run` out of the workflow — no `yaml` dependency for one file we own — and AC3 fails if any CI step loses its local counterpart, so the gate cannot silently rot behind the workflow it replaces. THREE states, not two: PASS / FAIL / **DESELECTED**; a selected leg that CANNOT run (cargo absent, `appIsPresent()` false — which would silently convert 10 of 15 e2e specs into skips) is a FAILURE, never a skip; a partial run prints `PARTIAL PASS`, enumerates what it did NOT verify and denies ci.yml equivalence, which only `--all` may claim — and even that discloses the Windows leg with no local counterpart (ADR-0021 D8). Mutation-tested: four mutants die — unmapped CI step, dropped local command, empty extractor, always-ok status) | `scripts/gate-local.mjs` ✅ + `.github/workflows/ci.yml` (`push`+`pull_request`+dispatch since 2026-08-26; still the graded contract) + `.claude/commands/close-session.md` (pins `model: claude-opus-5`; asks which legs, refuses on red, then docs → PR → merge → main) | `scripts/gate-local.test.mjs` (14, node:test; `pnpm run check-gate-local`, run by `pnpm test` AND by the gate's own workspace leg) |
| **Workspace sandbox guard (C2)** (the runner's own `source-guard.test.ts` roots at `packages/runner/src` and cannot see the app directories where a NEW iframe is most likely to appear — while the whitepaper tells implementers a frame carrying `allow-same-origin` "fails the build". This makes that claim true workspace-wide. Reads the sandbox VALUE rather than banning `setAttribute`, because the desktop C2 gate must build probe frames dynamically) | `scripts/check-sandbox-guard.test.mjs` ✅ | itself (4 checks, node:test; `pnpm run check-sandbox-guard`, run by `pnpm test` and CI) |
| **Desktop distribution + the shell update channel** (ADR-0047, TASK-20260821: GitHub Releases hosting, `tauri-plugin-updater` + `-process`, minisign artifact signing with owner-custody keys, Apple Developer ID signing + notarization LIVE since v0.1.0 (2026-08-25, ADR-0047 §7 amendment): `appleSigningPlan` demands a real notarization trio (tauri's bundler reads `APPLE_ID`+`APPLE_PASSWORD`+`APPLE_TEAM_ID` or the API-key trio, never a notarytool keychain profile) and REFUSES the half-configured middle; the DMG gets its OWN `notarytool submit` because a ticket is keyed to one file's hash; `checkUniversalArchs` refuses a build whose `lipo -archs` is not both `arm64` and `x86_64`; `checkStapleOutput`/`checkSpctlOutput` require a stapled ticket and `source=Notarized Developer ID`; the updater tarball is REBUILT from the stapled `.app` (tauri writes it pre-staple) and re-signed; the EULA check runs AFTER stapling because stapling rewrites the DMG. `src-tauri/entitlements.plist` carries only the WKWebView JIT pair — entitlements govern the shell process, never the app iframe, so C1/C2 are untouched. ONE endpoint constant in the PLAYGROUND (`releaseChannel.ts`) that the desktop config is BYTE-COMPARED against — tauri.conf.json cannot import TS, and the dependency direction stays desktop→playground. Update UX is OFFERED, never auto-applied (ADR-0045 doctrine): a launch check (toggleable, quiet-fail — the pre-flip private repo 404s by design), a non-blocking header chip, and a Tesla-style sheet whose FETCHED notes are plain-text-only because the minisign signature covers the ARTIFACT, not the manifest. `relaunch()` reaps the sidecar FIRST — `AppHandle::restart()` skips `RunEvent::Exit` on the main thread, so the exit-time reap cannot be assumed. C2: per-command keyless-refusal gate rows for `plugin:updater\|check`/`download_and_install`/`plugin:process\|restart` + a positive twin. The release gate gained a MUST-APPEAR scan of the production endpoint, which catches both a dropped config and a dev-overlay-built release. **ONE COMMAND since 2026-08-26 (v0.1.2):** credentials resolve themselves — updater key from `~/.tauri` custody, `APPLE_SIGNING_IDENTITY` + `APPLE_TEAM_ID` parsed out of `security find-identity` (ambiguity REFUSED, never guessed), `APPLE_ID` from git config — and the app-specific password is PROMPTED with echo suppressed and never stored; `--publish` performs the `gh release create` (the flag IS the explicit ask), `--no-prompt` keeps a non-interactive path, and `refuseUnsignedRebuildOverSignedBuild` refuses an uncredentialed rebuild over an already-notarized artifact. Three same-day defects are pinned by tests: `checkStapleOutput` accepts BOTH `stapler` wordings, and the updater re-sign passes EXACTLY ONE key spelling (`-k`/`TAURI_SIGNING_PRIVATE_KEY` and `-f`/`..._PATH` conflict as flags *or* as env vars)) | `apps/playground/src/desktop/{releaseChannel.ts,desktopReleases.ts,desktop-releases.json,AppUpdateControls.tsx}` + `apps/playground/src/{state/appUpdate.ts,views/DownloadView.tsx}` + `apps/desktop/src/app-updates.ts` + `apps/desktop/src-tauri/{tauri.conf.json,capabilities/main.json}` + `scripts/release-desktop.mjs` ✅ | desktop `appUpdates` (7, incl. the reap-before-relaunch call-order spy) + `updaterConfig` (5) + `versionSync` (2) + `gateIpc` (34) · playground `appUpdate` (9, incl. the composition-root launch-check wire + its mutation twin) + `downloadSurfaces` (5) · `scripts/release-desktop.test.mjs` (21, node:test via root `check-release-desktop`) |
| **Public website** (ADR-0048, TASK-20260821-website-launch: Astro + Starlight static site — marketing landing (dark-committed ember brand, responsive teaser video via unit-tested `pickTeaserSource`, animated wire demo spelling real `snug:app-message`/`snug:app-response` frames), `/download` (URLs identity-equal to `releaseChannel.ts` via the `@playground` alias — desktop→playground's direction reused), **`/architecture/`** — the public "How it works" page (nav label; route is `/architecture/`), six hand-authored inline-SVG figures + a "What this doesn't do" limits section, token-only styling (TASK-20260826) — and the `/docs` hub: curated Get Started/concepts/build guides + spec pages GENERATED verbatim from `docs/spec-drafts/SPEC-1.0.md` + `packages/protocol/schemas/` by `sync-spec.mjs`, whitepaper PDF served. **Positioning (TASK-20260827, ADR-0061):** the site leads with the OWNERSHIP boundary — hero "Your software shouldn't need a landlord.", a thesis section + own/move/keep-private framework, `components/PersonalSoftware.astro` ("Not every app needs to become a service.", whose caveat is a visible margin note, not fine print), differentiators remapped to ownership, and six `.fig-takeaway` lines on `/architecture/` with Figs 4/5/6 promoted as the ownership/privacy peaks; MCP is no longer the foil anywhere. Claims are pinned in BOTH directions by `positioning.test.ts` — the negative assertions (no "never leaves your machine" outside the local-model figures, no "100% private", the anti-SaaS section scoped to an *application-specific* backend) are half its value, and it also greps for a `minmax(` whose first argument is a bare length after the 24rem-vs-375px overflow. Sync doctrine: `docs-sync.json` hash manifest → root gate `check-website-sync` reds on source drift naming the stale pages; `/sync-website` command is the remedy; `PUBLIC_SITE_MODE=local` repoints playground→`localhost:5173` and download→a DMG staged by `stage-local-desktop.mjs` for full local E2E. **Social cards (TASK-20260825-social-preview-og):** `components/SocialMeta.astro` spells the whole card set ONCE for the marketing shell (absolute URLs built from `Astro.site` — a root-relative `og:image` is DROPPED by every scraper, which is the bug it fixes, live in production until then), `components/Head.astro` is the Starlight override adding **only** `og:image`+dimensions+`twitter:image` because Starlight already generates every other OG tag from `utils/head.ts` (emitting the full set there would duplicate meta on 22 pages), and `config/socialImage.ts` single-homes the image path with its REAL probed dimensions) | `apps/website/` (`astro.config.mjs`, `src/{pages,components,layouts,content/docs,config/site.ts,config/socialImage.ts,lib/teaser.ts,components/HeaderSocialIcons.astro,components/SocialMeta.astro,components/Head.astro,pages/architecture.astro,components/ArchitectureFigures.astro}`, `scripts/{sync-spec,stage-local-desktop}.mjs`, `docs-sync.json`) + `scripts/check-website-sync.mjs` + `scripts/build-social-previews.mjs` + `.claude/commands/sync-website.md` ✅ | website vitest (573: teaser 5 · siteLinks 10 incl. the new-tab pins · buildOutput 13 · **marketingNav 14** — the header's destinations, the `<details>` mobile disclosure, and the single-homed 860px breakpoint pair asserted complementary at 375/768/850/1280 (its AC4 test is the regression guard: a nav link hidden at mobile width with no disclosure home goes red) · **architecturePage 19** — token-only styling, the five pinned threat-model residuals, the SimpleFIN "accumulates" concession, and a **rendered-prose scan over built html for lost spaces at inline-tag boundaries** (Astro trims the newline before an inline tag; three shipped before the guard existed) · navIntegrity 6 incl. the slug-in-dist + dist-freshness tripwire that closes the stale-dist mask · legalPages 7 · externalLinkTargets 2 — every external anchor in .astro sources carries `target="_blank"` PLUS pins on the two framework wirings a source scan cannot see: the `HeaderSocialIcons.astro` SocialIcons override and `rehype-external-links` in astro.config (dist-swept to zero same-tab external anchors, 2026-08-23) · socialMeta 215 + socialAssets 226 — both assert over the BUILT `dist/` across all 27 pages, since neither shell's head is visible to a source grep; socialAssets additionally pins `site:` and the `Head` registration, the wiring whose one-line deletion silently blanks the docs half) + `scripts/check-website-sync.test.mjs` (7, node:test via root `check-website-sync`) |
| **Dependency-advisory gate** (ADR-0056, TASK-20260824-dependabot-triage: `pnpm audit --json` classified against a checked-in allowlist of TIME-BOXED acceptances. Reds on any un-allowlisted high/critical AND on any acceptance whose `reviewBy` has lapsed — an accepted risk cannot silently become permanent; a malformed entry (no `reason`/`task`/`reviewBy`) reds too, and does NOT shadow the advisory it failed to excuse. Moderate/low are reported, never gating. `tool: "cargo"` entries document Rust-side dismissals the npm gate ignores. Deliberately NOT in root `pnpm test` — the root gate stays offline-runnable) | `scripts/audit-deps.mjs` + `scripts/audit-allowlist.json` ✅ | `scripts/audit-deps.test.mjs` (17, node:test via `pnpm audit:deps`; the last case drives the REAL checked-in allowlist through the real classifier, so an expired acceptance fails at author time) |
| **Website + playground deploy tooling** (ADR-0054; tooling TASK-20260823-web-deploy, FIRST PRODUCTION DEPLOY 2026-08-24 TASK-20260824-web-deploy-execute — `snugprotocol.org` + `playground.snugprotocol.org` are LIVE; note the website project's real pages.dev subdomain is `snug-website-c7z.pages.dev`, NOT `snug-website.pages.dev`: Cloudflare Pages DIRECT UPLOAD from an owner-run script — `init` prints the one-time project creation; `<website|playground|all>` pre-flights (account id from gitignored root `.env`, wrangler ≥4 + `whoami` on THAT account + project already exists), git-gates (production = clean `main` == `origin/main`; `--preview` = any OTHER branch — Pages decides production purely by branch name, so preview on main is refused; no dirty override), enforces the hosted posture (ADR-0013: build env pins `VITE_SNUG_HUB_AUTH=''` + `PUBLIC_SITE_MODE=production`, refuses app-level `.env*`, a staged `local-artifacts/`, and local-mode HTML fingerprints), builds from a DELETED dist with `turbo --force` (the cache replays stale dist over gitignored inputs), verifies against Pages limits + per-app invariants (website: `404.html` present; playground: `404.html` ABSENT — the SPA fallback keys on it — + sql.js wasm), then PRINTS the wrangler argv and stops; `--deploy` uploads, `all` only after both verified. www→apex, zone script-injection features OFF, preview Access policy, pages.dev Bulk Redirect live in the ZONE, recorded in the runbook) | `scripts/deploy-web.mjs` ✅ + `docs/runbooks/deploy-web.md` + `.env.example` ✅ | `scripts/deploy-web.test.mjs` (26, node:test via root `check-deploy-web`; every refusal has a passing twin, `main()` through an injected io seam — dry default never calls wrangler deploy, real dists verified with a NAMED failure when absent) |
| **Site↔playground polish surfaces** (TASK-20260821-site-playground-polish: root `pnpm dev` — `scripts/dev.mjs` builds BOTH dependency closures (`--filter server... --filter playground^...`, cold-clone-safe since TASK-20260823 — the bare `server` filter was green only on trees with stale dist/) then runs server+playground with prefixed output and SIGTERM teardown of BOTH (half-killed vite servers corrupt `node_modules/.vite` into the 504 "Outdated Optimize Dep" state; recovery = delete that dir); `WebsiteLink` shell-nav link to the website — label `about ↗` since TASK-20260822 (domain moved to tooltip + aria-label; the 16-char domain crowded the header), `WEBSITE_URL` single-homed in `config/site.ts` (website→playground dependency direction, never reversed), desktop clicks routed through `oauth.openExternal` (migration to `openExternalUrl` queued); desktop-only badge = short `desktop` tag whose title carries the why, `z-index: 1` load-bearing — without it the tile's card button wins hit-testing and the /download link is pointer-dead (jsdom-invisible); Recovery-Key gate = mandatory checkbox; `className="row"` banned by source scan (`.field-row` is the layout)) | `scripts/dev.mjs` + `apps/playground/src/{config/site.ts,ui/WebsiteLink.tsx}` + `theme/app.css` ✅ | scripts `dev.test.mjs` (5, node:test via `check-dev`, run by root `pnpm test`) · playground `websiteLink` (4) + `unstyledRowClass` (1) + e2e `desktop-badge.spec.ts` (1, hit-target click, mutation-verified) |
| Process, tiers, release rules | `docs/engineering/PROCESS.md` | — |
| **Community/flip surface** (honest SECURITY.md, process-aware CONTRIBUTING, Covenant 2.1 CoC, `.github/` CODEOWNERS + yml issue forms + config + security stop-template + PR template w/ risk-tier + C1/C2-impact sections; curated good-first-issues mirrored as tracker issues #9–#18) | root `*.md` + `.github/` + `docs/good-first-issues.md` ✅ | — (docs; issue forms schema-validated by GitHub at render) |
| Pre-launch strategy (private, C4) incl. the staged flip-public runbook | maintained outside this repo — never tracked | — |
