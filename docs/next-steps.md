# Snug — Next Steps

Dated, ordered backlog. Append with a date; ✅ when shipped. Task files are the workspace; this is the queue. Full 6-week plan: `internal/01-extraction-launch-plan.md`.

---

## Now / next

- ✅ **2026-08-13 — MERGED (PR #42 → `main` `b851015`; task file in `done/`): `TASK-20260812-desktop-auth-awareness`.** Desktop-aware dynamic auth: platform truth in prompts/inference (the KB had been teaching the model that desktop-native fetch "does not exist yet"), the Spotify opener one-line capability fix, the **Coinbase CDP repair** (the registry could not carry a signing template, so the saved `api_secret` was read by NO code path — and retail HMAC keys had expired provider-side 2025-02-05), `queryTemplate` credential placement + the two broken starters, the auth-shaped 401 banner with registry-seat drift migration so EXISTING rows heal, and the **full Hue LAN connector** (ADR-0022/0023). Merged green on macOS + workspace at 21/21 uncached. **Two things the merge did NOT close:** the owner's three manual verifications (Spotify sign-in · Coinbase re-credential · Hue pairing on real hardware — procedures at the top of the done/ task file), and the **ADR-0021 D8 decision** below. `main`'s Windows desktop gate is **deliberately RED** — see the blocker entry.

- **2026-08-13 — CI outcome for PR #42 (recorded so the next session does not re-derive it):** `workspace` **PASS** and `desktop-shell (macos-latest)` **PASS**; `desktop-shell (windows-latest)` **FAIL — the three known IPC checks only** (see the blocker entry below; the failure is deliberate and the gate must stay red). Two CI-only defects were found and fixed on the branch: (1) the gate driver's hand-typed twin of `IPC_CHECK_IDS` went stale when P5 added a check — the driver now DERIVES the list from `src/gate/ipc.ts` and `gateDriverExpectations.test.ts` pins it locally, because **nothing on a dev machine runs `run-gate.mjs`**; (2) three async-start races in `desktopWizardSheet` (the retry `onClick` returns an unawaited promise; the status is written only after a db open + PKCE mint, which a microtask drain cannot advance) — all three now await the condition, no production code changed. Queued, small: **audit the remaining gate-driver expectation lists** (`EXPECTED_HARNESS_IDS_STATIC`, `EXPECTED_PERSIST_IDS`, `EXPECTED_JOURNEY_STEPS`) — they are the same hand-typed-twin shape that just bit us, and only the IPC one was converted.

- **2026-08-13 — 🔴 BLOCKER (owner decision needed): Windows desktop is structurally broken; ADR-0021 D8's Electron-fallback trigger is MET.** The desktop in-shell gate ran on Windows for the FIRST TIME (PR #42) and failed 3/40: `window.__TAURI_INTERNALS__` — which carries the invoke key as a **plaintext literal** (`tauri-2.11.5/scripts/ipc-protocol.js:12`) — is present **inside `sandbox="allow-scripts"` app iframes**, because wry's WebView2 backend **discards `for_main_frame_only`** (`wry-0.55.1/src/webview2/mod.rs:492-494`, documented at `src/lib.rs:2494-2496`) while the macOS path honors it (`wkwebview/mod.rs:643-644`). No off-switch exists at the wry, tauri, or WebView2 SDK layer — exactly D8's stated condition. Behind that bridge sit the user-file commands and `lan_fetch`. **macOS is unaffected** (40/40 green; WKWebView honors the flag) and **no Windows build has ever been distributed**. Full root cause with citations: `docs/solutions/2026-08-13-webview2-subframe-ipc-injection.md`; threat-model R-5 upgraded from "unverified" to "known defect". **The gate must stay red on Windows — do not soften `keyReachable`; it is the only check that reasoned about key reachability rather than transport presence.** Owner's call, three options: (a) Electron fallback through the same seams (ADR-0021 pre-committed to this and the seams are shell-agnostic by construction), (b) ship macOS-only and mark Windows unsupported, (c) upstream a `for_main_frame_only` fix to wry/WebView2 and wait. Optional confirmatory probe (booleans only, never the key): assert `metadata` in `Object.getOwnPropertyNames(window.__TAURI_INTERNALS__)`, or attempt a real KEYED `write_user_file` against the existing sentinel — that upgrades "structurally reachable" to "demonstrated exploitable", though D8 triggers on reachability alone.

- **2026-08-13 — 🔨 BUILT on branch `fix/TASK-20260812-desktop-auth-awareness` (unpushed, awaiting owner manual verification): `TASK-20260812-desktop-auth-awareness`** (High tier; ADR-0022 + ADR-0023, both now **accepted**). Five owner-reported desktop defects closed test-first across P0–P6. **Shipped:** platform truth in prompts + inference (new KB layer `95-platform-desktop`, a `platform` seat on `HostSystemPromptOptions`, inferrer user-slot facts — the KB no longer teaches that desktop-native fetch "does not exist yet" on the platform that ships it); the Spotify opener fix (the bare `opener:allow-open-url` grant was an EMPTY url scope, so every `openUrl` was refused — now scoped `https://*`, with differentiated wizard copy for opener denial vs port-41420 collision vs genuine browser failure); registry `request`/`testRequest` seats + the `cdp_jwt` ES256 signing helper + the Coinbase CDP entry rewrite (the old entry was **never connectable** — no template seat existed, so the saved `api_secret` was read by no code path, and the retail HMAC scheme it encoded expired provider-side in Feb 2025); `queryTemplate` credential placement + an enumerated scrub site list, fixing openweather and crypto-portfolio at injection; the auth-shaped failure observer + RunView repair banner (a credentialed 401 was silent at every layer); wizard-open registry **seat-drift migration** so existing rows pick up new registry seats without re-crediting; and the full Hue LAN connector — a `lanHost` protocol seat with `declaredApiHosts` required-XOR-`lanHost`, a wizard address→approve→pair flow, and a Rust `lan_fetch` pinned-TLS command (TOFU pin captured inside a rustls verifier, RFC-1918 literals only, fresh client per call). Threat surface: **`docs/security/threat-model-delta-desktop-auth.md`**. Root `turbo run test --force` **21/21, 0 cached**; cargo 48. **Owner manual verification is the closing AC and is spelled out at the TOP of the task file** — Spotify desktop sign-in, the Coinbase CDP re-credential + portfolio, and the 8-step Hue pairing procedure against the real bridge.
- **2026-08-13 — Queued from TASK-20260812-desktop-auth-awareness close (P6), each with its reason:**
  1. **Approved-host disclosure frame (protocol decision — BLOCKS the Hue starter driving the bridge).** The executor takes a LITERAL url and checks it against the frozen ceiling, and **no protocol frame tells an app which hosts it may reach** (grepped `frames.ts`, `runtime-contract.ts`, the SDK hooks — nothing). A placeholder host does not parse; a hardcoded one is right for exactly one user. So hue-lights-party **declares** the connection, holds the governed seam, and keeps its apply control honestly greyed with copy naming the real reason. Handing sandboxed app code the user's home-network address is a NEW disclosure needing its own protocol decision — the starter may not take it unilaterally.
  2. **Windows gate leg for `lan_fetch` and `cdp_jwt`** (amendment 9; the desktop-scaffold Windows leg it rides on is still item 1 of the 2026-08-12 desktop follow-ups). `cdp_jwt` requires NATIVE WebCrypto ECDSA — the desktop subtle-fallback is HMAC-only — so absence surfaces an honest loud error; the Rust guards are platform-independent by construction, but that is an argument, not a gate run.
  3. **Signify-CA pinning for Hue bridges** (threat-delta R-3): would close the pairing-window MITM residual. Deferred, not lazy — the CA material is behind a login-gated portal, old bridges are self-signed and would fail CA validation outright, and CN==bridgeId still needs the pairing handshake to learn the bridgeId, so TOFU collects both facts in one step.
  4. **Untrusted-import pin/secret overwrite — its own task** (threat-delta R-4). An import can carry a `lanPin` + paired secret under an approved row. **PRE-EXISTING and generalizes beyond LAN** — the identical shape carries OAuth refresh tokens and API secrets. Today's bounds hold (local secrets win the merge; imported rows demote to `declared`+`imported=1`, so re-approval is required), but the general question — what an import channel gets to say about credential state — deserves one decision rather than a per-credential-type answer.
  5. **The web Hue tile is fully locked (owner UX call).** Over-strict on purpose: the tile is `desktopOnly` and greyed on web rather than opening read-only. Correct today, but it is a product judgment the owner should confirm rather than a security rule.
  6. **Sweep for OTHER env-gated e2e specs that rotted like `starters-connect` did.** That spec opened hue by clicking a button P3 had made `disabled`, and both hue specs sit behind `SNUG_E2E_HAS_APP`, so the suite could not tell it had been broken for three phases. Any spec behind an env gate is a spec nobody is running — enumerate them and either run them in CI or delete them.
  7. **Apple Music's registry endpoints are INVENTED URLs** (found in recon, deliberately out of scope). `deriveConnectionAllowedHosts` unions endpoint hosts into the frozen ceiling, so invented endpoints silently WIDEN the ceiling. Needs its own registry decision — verify against Apple's docs or drop the endpoints.
  8. **google / gmail / googledrive / slack registry entries are wizard-incomplete** (no fields, no registration walkthrough), so their wizard journeys dead-end. Unreported by the owner, hence out of this task's scope; queued so they are not silently broken.
  9. **Latent hazard: both templates on ONE requirement.** A `pathAndQuery`-signing header template beside a `queryTemplate` would sign the **pre-injection** path and send the **post-injection** one — a signature that never matches, failing closed but mystifying. **No shipped entry does this** (verified), and the seat suppression means a query credential is never also sent as a header. Worth a lint or an explicit refusal before a third entry combines them.
- **2026-08-13 — Queued from TASK-20260812-desktop-auth-awareness P4 (starter realignment):**
  1. **The `html_mismatch` surface has no renderer.** `resolveDeclaredIntent` reports
     `mismatch: 'html_mismatch'` when an installed starter's code no longer matches the
     bundled one, and its declared connection is withdrawn — but **nothing renders it**. A
     `console.warn` at the detection site (`starterDeclaration.ts`) is the only signal, so a
     user who edits an installed starter loses its guided connection setup SILENTLY. The
     code comment claimed a Settings surface rendered this; it never existed, and P4
     corrected the comment rather than build the surface (out of scope). The seat and its
     tests are in place, so the surface is a UI-only change: render the withdrawal, with the
     "no manifest at all" case kept distinct (a scary banner on every never-declared app is
     the failure mode to avoid — pinned by test).
  2. **CoinGecko has no `testRequest`, and cannot get a useful one today.** Verified live
     2026-08-13: `api.coingecko.com` is a documented "Keyless Public API" — every endpoint
     answers 200 with no key, so any probe would report CONNECTED for a typo'd demo key;
     `/api/v3/key` is Pro-plan-only on `pro-api.coingecko.com` and would fail for every
     CORRECT demo key. Omission is journaled in the registry entry. Revisit if CoinGecko
     ever key-gates the demo host, or if a Pro-tier entry is ever authored.
  3. **`browserCallable` for CoinGecko rests on reflective CORS.** A live OPTIONS probe
     showed CoinGecko reflects any requested header back in `access-control-allow-headers`
     rather than serving a fixed allow-list. Our pin uses the QUERY form, which needs no
     preflight at all, so we do not depend on that behaviour — but any future entry that
     pins a custom-header credential for a CORS-reflecting provider is one policy change
     away from breaking in the browser. Prefer the query form where a provider offers both.
- ✅ **2026-08-12 — Desktop Hub scaffold SHIPPED (TASK-20260812-desktop-hub-scaffold, A6, ADR-0021).** Tauri 2 shell at `apps/desktop` wrapping the playground source: native fetch (CORS wall dead — the 2026-08-12 advisory's rung 2), `~/Snug/user.sqlite` file backend, loopback OAuth (fixed 41420, system browser, RFC 8252), Ollama autodetect + first-run, `.snug` association, registry `desktopRedirectPosture`+`browserCallable` seats, LAN `transportPolicy` (RFC-1918 literals). In-shell hard gate GREEN on macOS (14 CSP + 4 IPC checks + wizard journey 1); first CI ever authored (`.github/workflows/ci.yml`). **Desktop is NOT yet "first-class": the Windows/WebView2 gate leg pends the first CI run.**
- **2026-08-12 — Desktop follow-ups queued (from TASK-20260812-desktop-hub-scaffold):**
  1. **Windows gate leg** — verify `pnpm --filter desktop gate` on the CI windows runner; only then call desktop first-class (roadmap L6 language).
  2. **DNS pin-resolution for native fetch** (threat-delta residual 2) — resolve-then-connect-by-IP so a public hostname cannot rebind to a private address mid-flight; unlocks removing the "browser edition" caveat on `isForbiddenNetHost`.
  3. **Device-flow transport (RFC 8628)** — GitHub's oauth_app option is postured `device-flow` and refuses today (PAT is the steered default); nicest UX of all when built.
  4. **https-bridge page** (CF Pages static; Slack-family + Coinbase/Apple-Music postures point at it) — code+state relay to a deep link; PKCE keeps the static page custody-safe. Re-check Coinbase's dashboard for 127.0.0.1 literal acceptance first (registry comment cites the ambiguity; loopback would be cheaper than the bridge).
  5. **Export `STATE_TTL_MS` from oauth-service** and drop `DESKTOP_FLOW_TTL_MS`'s pinned mirror (two files must change together today; a test pins them equal).
  6. **Desktop ConnectScreen retry** re-reads stored client creds through the slot store so retry-after-cancel re-opens the browser instead of erroring (W2a concern; web parity bug, low).
  7. **Hue/LAN starter** — the desktop-only tile is labeled and gated; the actual Hue connector (pairing flow, `https`-v2 bridge) is its own task.
  8. **Installer/signing/notarization + download links** (A13 landing page dependency) — dev builds proven; distribution artifacts not in scope of the scaffold.

- **2026-08-11 — ✅ IMPLEMENTED (P0–P4, awaiting review/merge): `TASK-20260811-lean-runtime-data-chat`.** Owner approved the plan (all of §5 (a)–(e) as planned) and the whole task shipped test-first on branch `feat/TASK-20260811-lean-runtime-data-chat`: userdb v6 + the contract lifecycle (copy-forward, revert-from-target, imported-contracts-dropped incl. **sync pulls** — verified, the deferred plan question), the lean `appRuntime` assembly at both call sites, `runtime_contract_write` + KB + post-turn synthesis + starter contracts, the intent router with lane-scoped context AND tools, the scratch-isolated data tools with a TOCTOU-revalidated approval card, and `artifact_edit`. **Measured saving is ~1.26 KB/turn, not the ~3 KB the plan estimated** (ADR-0018 corrected). Queued follow-ups below.
- **2026-08-11 — Queued from the lean-runtime/data-chat close (none blocking):** (a) **subscription-mode server twins** for `runtime_contract_write` and the data tools — the gap is stated in-UI, and it now has three members (`schema_apply`, `app_doc_write`, and these); (b) ~~the `/invoke` contract seat is unreachable from the shipped client~~ **FIXED in the P4 review round** — `createHttpTransport` gained a per-send `getContract` seat and `createServerAppTransport` now forwards the app's contract, so subscription turns get the same framing as byok/local; (c) **C1 scanner**: `KNOWN_KEY_PREFIX` is `^`-anchored so a key embedded in prose is missed on EVERY envelope path, not just the new one — a scanner-level change with its own false-positive budget; (d) **executed-row counts** are the re-validated counts because `DbDriverResult` carries none — widening that shape would let the audit record a true post-execution count; (e) **drift detection compares counts, not rows** — a concurrent change preserving the count passes; (f) a **pre-write confirm on classifier-routed `app_change` turns** if the honest asymmetry in the threat model proves uncomfortable in use; (g) **persist a staged write proposal** — `dataWrite` is React state, so a reload leaves the assistant's "waiting for your approval" text with no card; persisting it needs a `PersistedMeta` seat AND a rehydration staleness rule (a proposal previewed against data that has since moved), which is why it was queued rather than rushed.

- **2026-08-11 — ✅ DONE (same branch): `test` now type-checks everywhere.** It was FOUR packages, not the three originally named — `knowledge` excludes `src/__tests__` from its build tsconfig too. `db`, `protocol`, `knowledge` and `playground` each gained a `tsconfig.test.json` (the `adapters` pattern) and run `tsc -p tsconfig.test.json && vitest run`. Switching it on surfaced **40 pre-existing type errors across 11 files**, all fixed, two of which were real test defects rather than cosmetics (a dead block-counting helper sitting beside the live separator-forging security assertions, and a drift assertion that narrowed only on `ok` so its `'failed'` branch would have skipped silently and still passed). Guard verified to bite by injecting a deliberate error. 19/19 green uncached with typecheck in the path; no test deleted or weakened.

- **2026-08-12 — 🔨 AC1+AC3 BUILT on branch `fix/TASK-20260812-app-reply-parse-failure` (unpushed, awaiting owner): `TASK-20260812-app-reply-parse-failure`.** Pickup session shipped the owner-approved-regardless half test-first, whole workspace green (protocol 253 · adapters 117 · runner 110 · server 126 · playground 769). **AC1 (owner cap rule)** at BOTH cap seams — playground direct transport and server `/invoke` app path each parse the wire and drop the contract's `maxOutputTokens` when the envelope carries a `responseSchema`; schema-less requests still honor it (D4). **AC3 (stopReason end-to-end)** — the drop was one layer deeper than diagnosed: adapters never read the provider's stop reason at all; now anthropic (`message_delta.stop_reason`), openai (`finish_reason:"length"`), and mock carry `max_tokens` through `AgentTurnResult` → both transports (server SSE `done` gains `{stopReason}`; optional on `TransportResult` so older servers degrade) → the bridge charges NO strike for a cap-cut reply and answers `HOST_ERROR` "cut off by the output token limit" instead of PARSE_FAILED. **Diagnosis re-ranked: hypothesis 1 REFUTED by parser mechanics** (characterization tests added, protocol source untouched): a bare array of row OBJECTS silently succeeds with only the FIRST row (latent hazard → AC4 input); the shape matching the owner's symptom is an unclosed outer object — i.e. truncation, which AC1+AC3 now fix and make self-naming in the inspector. **AC2 CLOSED same day — owner delivered the raw stream and the diagnosis flipped again: DELIVERY LOSS.** `stop_reason` was `end_turn` (truncation refuted); the wire billed 215 output tokens but delivered ~245 chars missing the `"sql":"WITH ordered AS (…)` head that the delivered tail still references — the model generated the full reply, deltas were lost upstream of the client. Our SSE layer is exonerated **with proof**: `stream-fidelity.test.ts` replays the fixture byte-faithfully through the real anthropic adapter at every possible chunk boundary (~2.7k splits) with identical assembly; `parseAgentReply` refusing the corrupt bytes is pinned as correct. Failure is INTERMITTENT (owner's later retry parsed and ran — returning zero rows, a separate model-SQL/data question). **Remaining owner decisions:** the retry policy (leading candidate: non-streaming `stream:false` fallback retry after a parse failure — no delta framing to lose), the AC4 first-row-wins array hazard, and optionally a HAR/curl capture to name the losing hop. Original diagnosis for the record: Owner repro on the app "kept" (BYOK): an in-app data question returned the app's "the agent didn't answer (agent reply was not a parseable JSON object)" copy while the reply visibly contained SQL **and JSON data**. Traced read-only: the refusal is the bridge (`runner/host.ts:323` → `parseAgentReply`), NOT the rail data lane; leading hypothesis after the owner's JSON observation is a **non-object top-level shape** (the parser accepts plain objects only — a bare rows ARRAY fails with valid JSON on screen); confirmed regardless: the adapter's `stopReason` is DROPPED at `agent-turn.ts:139`, so truncation is indistinguishable from non-compliance, charges a parse strike, and retries can never succeed. **Owner decision recorded: a request carrying `responseSchema` is NEVER bound by the contract's `maxOutputTokens`** (structured replies must finish). Repro-first at pickup: the raw failing reply from the owner's LLM inspector is the fixture. Independent of both auth branches; cut `fix/…` off `main`.
- **2026-08-12 — 📋 ADVISORY RECORDED (no code): BYOK browser CORS on Coinbase connected-fetch.** The CORS block is Coinbase policy (per-provider: GitHub/CoinGecko/OpenWeather allow browser calls); **subscription mode does NOT currently avoid it** — `createConnectedFetch` runs with page `fetch` in every mode (`state/net.ts:119-126`), `apps/server` has no connected-fetch relay. Recommended ladder (owner has not yet commissioned): (1) **local sidecar relay** on `apps/server` behind the existing `fetchImpl` seam — same-machine custody, ceiling revalidated in the relay; (2) desktop shell native fetch (same seam, zero security-model change — the roadmap's future rung); (3) subscription server twin LAST (credential custody moves server-side — needs its own task + ADR). Plus a **connect-time disclosure**: a registry `browserCallable`-style flag so the wizard says "this provider can't be called from a browser" BEFORE credentials are pasted, instead of an opaque runtime "Failed to fetch". Refused options: public CORS proxies, disabling browser security.
- **2026-08-12 — ✅ MERGED (PR #38 → main `eaf9cb1`; task file in done/): `TASK-20260812-auth-kind-choice`** (owner-commissioned from a live repro; ADR-0020). Multi-option auth resolution: registry entries may carry `authOptions` (Coinbase retail OAuth + GitHub OAuth app authored first, each a COMPLETE flow with its own fields/endpoints/walkthrough); the inferrer result gains one uniform `alternatives` seat (registry options + ≤3 model proposals validated exactly like the primary, fail-soft); build persists the DEFAULT so nothing blocks, and a live-row-gated choice card lets the user rebind — `chooseAuthOption` is the FIRST and ONLY production writer of the `user` channel (gesture-bound literal, enforced by an executable source scan), and R3 makes the choice durable against later inference. **The plan survived its own adversarial review the hard way**: 2 BLOCKERs — Guard 2b's substitution half would have silently rewritten the chosen variant back to the default (fixed with ONE matched-option handle driving both the exemption and substitution; owner explicitly approved this widening of a security guard's substitution contract). Whole-surface journeys: Coinbase default→done, Coinbase switched-to-OAuth→connect (ceiling gains login.coinbase.com from the option's own endpoints), GitHub both. auth 468 · knowledge 164 · playground 764+. **Queued follow-ups:** (a) borrower-declared OAuth `endpoints` still survive substitution when the matched option has none — pre-existing, endpoint hosts union into the derived ceiling; decide flag-vs-replace at the next threat-model pass; (b) inference-alternative labels are kind-derived copy — consider letting the model propose a short label through a bounded, linted seat; (c) variants for more providers as owners ask.
- **2026-08-12 — ✅ MERGED (PR #37 → main `414d31a`; task file in done/): `TASK-20260812-registry-authoritative-auth`.** Owner approved at pickup; shipped test-first on the branch. **P0's mandated reproduction REFUTED the plan's causal chain**: `persistConnectionRequirement` does NOT refuse the inferrer's broken shape — admission on the registry channel substitutes the pinned fields and a row persists **with the wrong kind** (`oauth2_auth_code` for Coinbase), routing an api_key provider into an OAuth sign-in that cannot succeed; the owner's row-LESS state is historical (pre-P3-fold builds), and every planned fix survived the narrative correction. Landed: registry entries each declare their own `kind` (GitHub `bearer_token` per D5; Apple Music pins the status-quo `oauth2_auth_code` — the one entry AC1's table didn't name, journaled); ONE emitter `requirementFromRegistryEntry` (AC3 structural suite proves all 10 entries compose+parse and every seat rides through); inferrer rung 1 consults exact key THEN `INFERRER_ALIASES` (D3 — `lookupWellKnownProvider` untouched; `Coinbase Pro`/`OpenWeatherMap` authored; lookalikes fall through, AC6); AC4 restated as call-recording order-of-effects with a bypass mutation; AC10 pins the D6 kind split-brain (admission stays kind-agnostic — queued follow-up); **F4 fixed at the decision altitude** — the zero-row wizard-open refusal now writes an explanation and `connectionWizardNoteStore` finally has a renderer (`ConnectionWizardNote`, app-level mount; the store previously had NO subscriber, so every refusal was silent); ConnectScreen's `void` swallow replaced by a catch into the flow-status store, so all three pre-status throws render their literal copy with a retry (AC7/AC8 — incl. the newly-found "retry always threw `Missing required client credential: client_id` and swallowed it"); whole-surface Coinbase journey test (build→recovery→api_key row→wizard routes credentials→done); **test-script audit closed the M4 claim: `runner` and `sdk` also lacked the tsc gate** — added (one pre-existing type error in runner's test harness fixed). auth 357→409, playground 740→750+. Queued: admission kind-substitution decision (D6/AC10, own task+ADR); Apple Music's truthful kind; owner-assist: read the real rows in the owner's browser to confirm the historical no-row diagnosis.
- **2026-08-12 — (superseded by the entry above — kept for the record) 🟡 PLANNED, awaiting owner approval: `TASK-20260812-registry-authoritative-auth`** (owner-commissioned, High tier — `packages/auth`). Branch `feat/TASK-20260812-registry-authoritative-auth`, 6 commits, **unpushed, no implementation code by design** (Gate 2 stop). Four findings: **F4 is the owner's actual bug** — opening an app with no connection row shows the `NET_NOT_APPROVED` banner whose `connect this app` CTA **silently no-ops** (`openConnectionWizardForApp` returns `false` at `connectionWizard.ts:166` on zero rows, and RunView correctly declines to dismiss). **F2 is its probable root cause** — `connection-requirement-inferrer.ts:130` hardcodes `kind: 'oauth2_auth_code'` for EVERY well-known-registry hit and discards the entry's `fields`, so all 10 providers (incl. api_key ones like Coinbase and OpenWeather) come out as fieldless OAuth requirements that persistence then refuses; **verified by executing the real inferrer**. F1 (an unhandled-rejection swallow at `ConnectionWizardSheet.tsx:873`) and F3 (registered providers should short-circuit inference — the ladder is already right, aliases are the gap) are real but independent. Plan was **replanned twice**: a fresh-context review found 3 BLOCKERs (the alias design would have inverted a documented trust boundary by aliasing the RESOLUTION path; "Guard 2b plumbing exists" was half-true and would have created an inferrer/admission kind split-brain; F1's causal chain was refuted), then the owner's real symptom revealed F4, which neither diagnosis had. 12 ACs, several restated so they can actually fail. **Pick up with `/pickup TASK-20260812-registry-authoritative-auth` — the task file's HANDOFF entry lists what not to re-derive and the probes already run.**

- **2026-08-11 — ✅ ALL SEVEN review findings FIXED (same branch), tests-first.** The P0 is closed by CLASS: `nonDataStatementReason` in `packages/db` restricts the data lane to INSERT/UPDATE/DELETE, applied at the propose handler AND re-applied at the execute gate, so the two ends of an approval cannot disagree. Re-running the original exploit now returns `Error: DROP is not allowed in a data change…`, stages nothing, and leaves every row intact. R-B2: `createHttpTransport` now actually puts the resolved contract in the POST body (omitted entirely when absent), with three seam tests — the gap that let 16 server tests pass for a field the client never sent. R-M1: `changes` is attached by statement KIND, not unconditionally, so a sticky `sqlite3_changes()` can no longer report "3 rows" for a SELECT (a modifying statement matching nothing still reports a real 0). R-M2: the import guard now keys the exemption on the CALLER (`trustedOrigin`, set by the recovery restore and the sync pull) rather than on an empty local set — the first attempt keyed it on "the hub has no apps", which broke three AC-F1-7 tests and would have traded a security guarantee for a usability fix, since a hostile donor arrives at exactly the same empty hub a restore does. R-M3: a turn cancelled during classification now persists nothing (the adapters collapse abort into the same `ok:false` a failure produces, so the clarify lane was inventing a durable assistant reply that then steered later routing). R-M4: approved multi-statement writes run in `BEGIN IMMEDIATE`/`COMMIT` with rollback, so "nothing was changed" is now true when it is displayed. R-M5: staged proposals persist on message meta with validate-on-read, and resolutions persist via a new narrow `updateChatMessageMeta` accessor that MERGES rather than replaces (a message can carry both an artifact card and a data-write card). **19/19 green uncached at 2183 tests** (from 2156), root build type-clean 9/9.

- **2026-08-11 — 🔴 (SUPERSEDED by the entry above — kept for the record) BLOCKERS against PR #35, found by a fresh-context review at pickup.** (1) **P0 — destructive DDL passes the data-write approval gate showing "0 row(s)" and lands on the real DB.** Reproduced by execution through the real `buildDataTools` → `executeApprovedWrite` path: `DROP TABLE expenses` previews as "would affect 0 row(s)", the TOCTOU drift guard compares 0 to 0 and passes, and the table is permanently gone — the data lane has no versioning or revert, so there is no undo. `forbiddenStatementReason` blocks only ATTACH/`PRAGMA writable_schema`/`load_extension`; DDL was never restricted, and `sqlite3_changes()` is 0 for all of it. Same FAMILY as the P4 blocker, different instance: P4 fixed the count being dropped, this is the count being legitimately 0 while the statement is destructive. Recommended fix is a DML-only allowlist in `packages/db` beside `forbiddenStatementReason` (so the real executor and the scratch preview cannot disagree; `schema_apply` already owns the reviewed DDL path). (2) **High — subscription mode never sends the runtime contract**: `createHttpTransport.send()` computes `const contract = await options.getContract?.()` and then builds the POST body without referencing it, so F1 is silently inert for every subscription user while 16 server-side tests pass for a field the client never sends. This is the P4 round's own claimed fix — the seat was added but never wired. No test crosses the `createHttpTransport` seam. Five further MEDIUM findings (sticky row counts on non-modifying statements; fresh-hub import nulling every contract on backup-restore/new-device sync; abort-during-classify persisting a fabricated exchange; no transaction around multi-statement approved writes; the already-queued unpersisted proposal) are recorded in full in the task file's journal.

- **2026-08-11 — the P4 whole-surface review earned its keep (8 agents: 4 traces + 4 adversarial verifiers).** It found TWO BLOCKERs that all 2100+ tests had missed, both now fixed and regression-tested: (1) `scratchRun` treated sql.js `getRowsModified()` as cumulative and took a delta, but it is `sqlite3_changes()` — per-statement — so any second write reported a WRONG (often negative) affected-row count, the approval card rendered it, and the TOCTOU drift check could not catch it because it re-ran the same broken arithmetic on both sides; a `RETURNING` clause separately suppressed the count entirely. (2) `renderRuntimeContract` stripped only the EXACT layer separator, so a contract containing `\n\n\n---\n\n\n` forged a system-block boundary and its text became a peer of `10-host-identity` (verified: 5 blocks where 4 were expected, from every free-text seat and from a settings VALUE). Also fixed from the same round: query results now ride a defanged `<query_result>` block instead of raw concatenation, one approval card per turn, and a single-flight guard on approve. **The lesson worth keeping: every existing multi-statement test paired a write with a SELECT, and a SELECT leaves `sqlite3_changes()` untouched — the bug needed two consecutive WRITES to appear. A test matrix that never combines two of the same kind of operation is a matrix with a hole in it.**

- **2026-08-11 — superseded (kept for history): PLANNED, awaiting owner approval: `TASK-20260811-lean-runtime-data-chat`** (owner-commissioned): **F1 lean runtime turns** (per-app runtime contracts authored at build time, version-pinned on `snug_app_versions` at userdb v6, host-assigned injection, `appRuntime` system assembly replacing the ~3 KB of builder layers that ride every app-frame turn today, per-turn `maxOutputTokens`) + **F2 intent-routed app data chat** (classification-first scoped context; LLM-written SQL over the app's materialized data via a read-only-by-construction scratch executor; writes propose→preview→approve→revalidate→execute; bounded `artifact_edit`). Gates 1–2 + ADR-0018/0019 drafts on branch `feat/TASK-20260811-lean-runtime-data-chat`; two fresh-context plan reviews (arch+security) folded — see the task file §6. Wire protocol v1 untouched; spec staging internal only (AL-12 still held). Implementation starts in a fresh session AFTER owner approval of the plan (open decisions §5 a–e in the task file).

- **2026-08-10 — Dynamic Auth v2 REWRITE COMPLETE across P0–P5, six branches, NONE MERGED — awaiting owner review + PRs.** `docs/TASK-20260810-plan` (docs-only, off main) · `feat/TASK-20260810-p0-contracts` → `-p1-runtime` → `-p2-pipeline` → `-p3-wizard` → `-p4-starters` → `-p5-security-close`, each cut from its predecessor so the chain is linear. Final: root **19/19 uncached** · protocol 221 · auth 357 · db 236 · knowledge 120 · playground 596 · examples 170. **The founding defect is closed end-to-end**: Coinbase key+secret+passphrase flows registry → admission → wizard → signed request, signature verified against an independent recompute. Both named deletions done: `llmProposalSchema` (P4) and the v3 `snug_auth_specs` table (P3, schema v5 with a real `DROP TABLE`). ADR-0017 + a staged v0.3 draft + a threat-model delta (`docs/security/threat-model-delta-dynamic-auth-v2.md`) all landed. **AL-12 stays HELD** — only the LOCAL staged draft was created; nothing was pushed to `snugprotocol/spec`.
- **2026-08-10 — Queued from the Dynamic Auth v2 close (owner decisions / follow-ups, none blocking):** (a) **merge order matters** — the six branches are a linear chain and must be PR'd in P0→P5 order or the cutover rule breaks (v4 lands alongside v3 in P0; the v3 deletions are named exit items of P3 and P4). (b) A **flaky Playwright journey**: 1 failure in 5 full-suite runs, a DOM-detach race in `openWizardFromCard` (`connection-wizard.spec.ts:82`); passes 5/5 in isolation — diagnose before it trains anyone to re-run reds. (c) The four `?demoreq=starter-*` variants have vitest coverage but **no Playwright journey**. (d) The **ASCII-lookalike** registry-borrow limit (`Sp0tify`, `Spotlfy`) is accepted-with-disclosure per ADR-0017 — carried by the review's provenance copy, not by the guard; revisit if an untrusted declaration channel ever opens. (e) ADR-0017's residual-risk #2 wording is slightly imprecise in both directions versus shipped scrub behavior; the threat-model delta states it correctly as a per-case table — align the ADR when convenient.
- **2026-08-10 — Process lesson worth institutionalizing (from the same run):** the coverage-migration audit (classify every deleted test as MIGRATED / OBSOLETE / LOST) caught 14 unguarded shipped behaviors in P3 and the same shape again in P4. **Make it a standing step of any task that deletes a surface**, not a one-off — see `docs/lessons.md` 2026-08-10.

- **2026-08-10 — Queued from Dynamic Auth v2 P3 (TASK-20260810-p3-wizard):** three gaps left OPEN on purpose, each with its dependency named.
  1. **The v4 OAuth popup flow is not rebuilt.** The v3 store owned the popup lifecycle (`startOAuthFlow`, the BroadcastChannel-by-flowId binding, the popup-closed backstop, `requireApprovedSpecScope`) and went with the v3 deletion; `OAuthService` itself is untouched and still covered by 35 package tests. P3's wizard routes an `oauth2_auth_code` requirement to a `connect` STEP, but that screen is a placeholder — it does not mint an authorize URL. **Consequence: OAuth providers cannot be connected end-to-end today**; every static kind (api_key/bearer_token/basic_auth) works. The v4 rebuild needs a slot-keyed `requireApprovedConnectionScope` to replace the deleted app-keyed one. `connection-wizard.spec.ts` journey 4 covers it and fails at the popup.
  2. **The install-act (starter) channel still reads `llmProposalSchema`.** Its rewire is P4's exit item, so a chat-less starter cannot currently reach a connection through the CTA. The pre-install disclosure is unaffected. `connection-declaration.spec.ts` T8/T8b are parked with that reason inline.
  3. **Journey 1's live-fetch tail needs e2e stub work:** the Coinbase requirement authors the REAL `api.coinbase.com` (the deliberate stub-host pattern), but the `connection-wizard` Playwright project maps only `stub.snug.test` and no fixture route verifies the HMAC signature (`sawSignature`). The whole WIZARD walkthrough passes; only the final connected-fetch assertion fails.
- **2026-07-31 — Bootstrap** (this scaffold; ADR-0001..0003). ✅ scaffold committed; pending: GitHub org + repos + push, npm org registration, Cloudflare email verification click.
- ✅ 2026-07-31 — **Week 1–4 delivered in one umbrella (TASK-20260731-build-hub):** protocol v0.1 + runner (C2-gated) + knowledge store (ADR-0004) + sdk/db + server/adapters + playground/examples. Remaining from the original week-1 line: spec-repo push (needs explicit ask). Original items follow for traceability:
- **2026-07-31 — Week 1 (plan §5):** extract envelope protocol → `packages/protocol` · runner skeleton with C2 sandbox locked by tests · knowledge base port → `packages/knowledge` · draft SPEC.md v0.1 skeleton sync (first spec push — needs explicit ask).
- **Week 2:** `apps/server` (/invoke + artifact store) · `packages/adapters` (anthropic, openai, mock).
- **Week 3:** `packages/db` — sql.js + OPFS, `.sqlite` export/import · `useAppDB`.
- **Week 4:** Playground + examples (chess, flying-pig, habit-tracker).
- **Week 5:** polish, docs, demo videos, private beta (10–15 people).
- **Week 6:** launch (see `internal/LAUNCH_OPS.md` — landing page T-3, flip public T-2, Show HN).
- **v1.1 (+4 weeks):** `packages/auth` credential broker per `internal/03-audit-auth.md` build order.
- **2026-07-31 — Post-hub next:** eval harness for prompt changes (phase 2 — prompts are eval-addressable by path per ADR-0004) · demo videos + quickstart timing run (week 5) · spec v0.1 push to `snugprotocol/spec` (needs explicit ask) · auth broker v1.1 per `internal/03` build order.
- **2026-08-03 — Portable user-owned hub umbrella (TASK-20260803-portable-hub, in progress):** single per-user SQLite (ADR-0007) · serverless run + BYOK/local LLM (ADR-0008) · SyncProvider origins + Dropbox (ADR-0009) · Google OIDC on sample hub · spec v0.2 draft.
- **2026-08-03 — Queued from portable-hub review (F3):** true network-offline app runtime — vendored/inline-runtime app template variant (srcdoc iframes can't be SW-cached; CDN allowlist is the current runtime source) + offline Playwright gate (`context.setOffline(true)`).
- **2026-08-03 — Queued (later phases from vision):** desktop local hub example · OneDrive/Drive/S3 SyncProviders · CRDT/changeset multi-device merge · marketplace/spotlight curation.
- ✅ 2026-08-03 — **Living apps (TASK-20260803-living-apps, branch `feat/TASK-20260803-living-apps`, awaiting PR/merge):** per-app data as LLM-designed NATIVE tables + verbatim-DDL schema registry (ADR-0010, schema v2) · app-attached chat (context assembler, durable thread pin, pinned bootstrap turn) · factory-pinned versions + reset · per-app knowledge wiki + `schema_apply`/`app_doc_write` tools · marketplace install dedup (`install_source`) · SSO retrofit (identity chip, post-login return, logout→sync rebuild).
- **2026-08-03 — Queued from living-apps:** subscription-mode server twins for `schema_apply`/`app_doc_write` (today the new tools run in byok/local direct mode; subscription keeps artifact parity only) · per-table dirty tracking in the materializer (write-amplification lever) · doc-history/versioned wiki if pages start churning.
- ✅ 2026-08-03 — **Hub ops (TASK-20260803-hub-ops):** the real long-build fix (iteration ceiling 6 → 48 — there was never a timeout) + partial-text preservation on dropped streams · 128K max output on both adapters, `max_completion_tokens` added to OpenAI, 8K cap on local · server 30-min lifetimes + `step` SSE progress events · build step timeline (replacing the last-write-wins activity pill) · **LLM round-trip inspector** (in-memory, ring-buffered, redacted — a sibling of the structural frame inspector, never an extension of it) · `deleteApp` cascade with tombstone (delete is terminal; pinned rows included) · LLM-optional app doctrine (ADR-0011) + pig game ported to the contract.
- ✅ 2026-08-05 — **Queued from hub-ops review (closed by TASK-20260805-doctrines-devex):** `importUserDb` clears `lastSavedHash` but **not** `namespaceByFile` — same cache-coherence family as the F1 resurrection bug. Closed: import now resets the whole family (`lastSavedHash` + `namespaceByFile` + `deletedApps` tombstones for apps the imported file contains). The *observable* member was the tombstone: restoring a pre-delete backup left the app refusing every frame. Regression + tombstone-asymmetry guard in `packages/db` `import-cache-coherence.test.ts`.
- ✅ 2026-08-04 — **Hub polish (TASK-20260804-hub-polish):** sign-out moved into a header identity menu + Google avatar (`picture` claim through the 5-place chain) · ember-niche logo, favicon and 2× wordmark · the `llm` and `inspector` rail tabs merged into one **think** surface with icon tabs (text kept as the accessible name) · build view finally renders its round trips · **the app-frame transport's round trips too** (there are TWO `runAgentTurn` call sites; only the builder's was wired, so a Chess move was invisible) · starters open read-only with an explicit Install in the run view · a built app's run view opens on the build conversation · a revision-less 412 reaches the divergence resolver instead of throwing.
- **2026-08-04 — Queued from hub-polish:** the `applyRemote()` empty-origin dead end is fixed, but the sync **resolver copy** still reads as a generic two-button choice — worth a pass to make each button state its consequence.
- ✅ 2026-08-05 — **Code-map count regeneration (A15, closed by TASK-20260805-doctrines-devex):** `pnpm run update-code-map` regenerates the resolvable test-count numbers in `docs/code-map.md` from a green root run (Playwright numbers untouched); first run fixed real drift (db 163→170 total, sync 44→51, adapters 90→92).
- ✅ 2026-08-05 — **Observability + caching (TASK-20260804-observability-caching):** round trips and tools appear LIVE as they start, each timed, tools nested under the round trip that requested them · the wire model name (never a UI guess) · prompt caching on the stable tools+system prefix of **builder turns only** — a per-TURN request flag, because one adapter instance serves turns of different shapes · cache hits reported as a cached % (absent, not "0%", when the provider said nothing) · payloads kept whole behind a total-bytes budget with oldest-first eviction · starter tiles became one card control · brand wordmark −20% · a rotating status line replaced the duplicate step timeline + reasoning pill.
- ✅ 2026-08-05 — **Queued from observability-caching review (closed by TASK-20260805-doctrines-devex):** `supportsCaching()` matched the Anthropic host with `endsWith('api.anthropic.com')`, over-matching sibling subdomains (`notapi.anthropic.com`). Closed: exact `hostname === 'api.anthropic.com'` (port-insensitive on purpose), locked by two adapter tests — red-first sibling-domain refusal + non-default-port still caches.
- ✅ 2026-08-06 — **WebLLM spike (AL-07, TASK-20260806-webllm-spike):** experimental in-browser brain behind `?webllm=1` — brain override (never a mode), WebGPU probe with demo-brain fallback + pinned banner, webllm AgentAdapter through `runAgentTurn` (wire model name, usage, abort, typed errors), tool-free fenced-HTML build path (web-llm function calling is 8B-Hermes-only + forbids custom system prompts), engine singleton behind a code-split dynamic import, default model per ADR-0015.
- **2026-08-06 — Queued from the webllm spike (1.2 GA scope unless noted):** model picker + cold-start/download UX + cache management (roadmap 1.2-1) · strip the extracted HTML fence from the chat bubble and render an artifact-style card instead (today the reply shows the whole document — ugly, honest) · measure app-attached context blocks against the 4 K context (a big app's code+docs suffix may not fit — needs a truncation rule before webllm chats attach to big apps) · `context_window_size` override vs KV-memory tradeoff · promote the adapter into `packages/adapters` at GA · consider surfacing engine load progress in the builder status line (today: shell banner only) · real-model e2e stays skip-by-default (`SNUG_E2E_WEBLLM_REAL=1`, needs Chrome channel + persistent profile).
- **2026-08-05 — Queued from observability-caching review:** the OpenAI adapter always reports `prompt_tokens_details.cached_tokens`, including `0`, so an OpenAI turn renders "0% cached" where an Anthropic turn with no caching renders nothing. Both are correct under the absent-vs-zero rule (one provider reported a zero, the other reported nothing) but they read inconsistently side by side — worth a UI pass deciding whether a reported zero is worth showing at all.
- **2026-08-05 — Queued from observability-caching:** AC6 now has a documented exception — a single round trip too large for the whole byte budget has its payloads **elided** rather than shown whole. That trade is invisible in the UI beyond the elision marker; if it fires in practice, the panel should say *why* the payload is missing rather than just that it is.
- **2026-08-04 — Queued from hub-polish (TASK-20260804-hub-polish) Q1 decision:** subscription-mode parity for observability — server-side `app_doc_write`/`schema_apply` (so a subscription build can write its wiki at all) and a round-trip wire event that carries **no** tool payload and **no** credential. Deferred deliberately: `invoke.ts` today forwards tool NAME + phase only, and that boundary is not worth breaking without a designed contract. Until then the UI states plainly why those panels are empty in subscription mode.
- **2026-08-05 — Milestone roadmap authored:** Alpha → 2.0 product roadmap (repos-only scope) drafted in `internal/07-roadmap.md` — milestone ladder, starter portfolio, todo-item dispositions, spec-repo twins. Awaiting owner review; approved items graduate into task files here.
- **2026-08-05 — Roadmap v2 (owner-directed):** Dynamic Auth (OProject LLM-inferred credential framework per `internal/03-audit-auth.md`) pulled INTO the 1.0 launch in local/BYOK form — connected-fetch envelope capability, auth-spectrum starter set (none/api-key/PAT/OAuth-BYO-registration/LAN-desktop), Desktop Hub (Tauri) on launch day, WebLLM experimental at 1.0. Broker/subscription custody layer: RFC at 1.6 → GA at 2.0. Alpha is now the heaviest milestone by design. Details + open decisions in `internal/07-roadmap.md` §7; awaiting owner go-ahead on next steps.
- **2026-08-05 — Spec publication gating (owner decision):** the Alpha spec push is v0.1 + v0.2-draft ONLY; the v0.3 auth/net surface is staged internally in `docs/spec-drafts/` (C3/SPEC_SYNC unchanged) and publishes as an explicitly-marked draft at 1.0 **or** 1.1, gated on a Beta-exit churn checkpoint. Local-auth RFC opens at 1.1; broker RFC stays at 1.6. Spec-from-working-code doctrine (`internal/01` §9) upheld.
- **2026-08-06 — Queued from the AL-01 adversarial review (id-collision revival):** deleting app id K and then importing a CRAFTED file in which a *different* app occupies id K drops K's tombstone and serves the imposter's data (probe-confirmed; unreachable by accident — ids are `crypto.randomUUID()`). Documented in the `importUserDb` comment block; belongs to the A10 threat model's untrusted-import section (fuzzing of userdb import is already queued at 1.6-4).
- ✅ 2026-08-06 — **Queued from the AL-01 adversarial review for AL-14 (closed by TASK-20260806-flip-prep):** `docs/tasks/` ships public at flip — task files needed a sweep for machine paths and key-location breadcrumbs. Closed: sweep executed (3 scrubs — 2 machine paths, 1 live-key-location note; findings in the task file) and added to the flip runbook as a permanent stage-1 step. Codenames stay per the approved indirection.
- **2026-08-06 — Queued from the AL-01 live sweep (4 non-blockers, none launch-blocking):** (1) `/settings` re-mounts the saved BYOK key into the DOM as a serialized `value` attribute on the password input — storage/exports/iframe stay clean so C1 holds, but the harder pattern is a write-only field ("key saved — replace?") that never echoes the secret; fold into AL-10's secrets-path review. (2) ~~Playing an UNINSTALLED starter writes an orphaned `app_<hex>__snug_kv` table into the user DB~~ — **FIXED by the AL-08 review round (TASK-20260806-starters-pillars)**: a read-only starter browse now runs on an ephemeral in-memory DbDriver, never the user DB; export-bytes guard in `e2e/starters.spec.ts` (pre-existing orphans from earlier browses are not migrated — they age out with the file or a future vacuum pass). (3) Builder chat renders raw markdown (`**bold**`, unrendered artifact links; the `/artifacts/…` path is also a dead route in serverless byok) — cosmetic. (4) "this hub" sync while signed out reports "hub rejected pull (401)" instead of "sign in first" — fold into the queued sync-resolver copy pass. Sweep also verified live: restore-after-delete runs (state intact, frames accepted), no ghost rows, per-turn cache hits 43%→10% on real Anthropic builder turns, and no key material in export bytes, DOM, storage, or network.
- **2026-08-06 — Queued from the AL-02 live sweep (pre-existing, dev-only, not this branch's regression):** the hub "talk. build. run." prompt → build handoff dies under StrictMode double-mount on `pnpm dev` — the unmount cleanup aborts the `?idea=` auto-started turn (`useBuilderChat.ts` cleanup vs `BuilderView.tsx` `sentInitial` ref guard blocking the remount resend), so the turn shows CANCELLED and never retries. Prod builds are unaffected (StrictMode double-invoke stripped), but it is the marquee flow on dev demos — small fix, decision altitude is the `?idea=` effect. Sweep otherwise verified live: v2→v3 migration on a real pre-migration export (apps run with prior state; re-exported bytes carry `user_version=3` + `snug_auth_specs`), F15 re-confirm on key-stripped imports, real byok build with per-turn cache hits, and multi-encoding key probes finding nothing in export bytes, storage, console, or DOM.
- ✅ 2026-08-06 — **Pillar starters (TASK-20260806-starters-pillars, umbrella child AL-08):** five new starters shipped to the shelf — Adventure Quest (agent-as-DM + SQL pack/journal, both pillars), Quiz Me (agent-written quizzes, hard shape validation, bank fallback, scores in SQL), Trivia Night (pass-and-play, LLM-free), Family Trip Planner (single-user v1, three SQL tables), Pocket Ledger (integer-cents ledger, SQL-summed totals). Validate suite 18 → 73 (per-app ADR-0011 posture check, the new **no-form rule**, a literal-SQL lint, and extracted-source parseCents money cases); playground vitest +2 (`starterShelf`); Playwright +7 (`starters.spec.ts`: one interaction test per starter on the keyless demo brain + the review round's zero-trace export-bytes guard and owned-copy persistence test). Adversarial review (MERGE-AFTER-FIXES, 5 items) also landed: **uninstalled starters run on an ephemeral in-memory driver** (browsing leaves zero trace in the user file; closes AL-01's kv-orphan leak), comma-aware money parsing, a pre-existing dedup.spec URL race fixed, Enter on the quiz topic input, and claim corrections. **Field find:** the C2 sandbox (`allow-scripts`, no `allow-forms`) blocks form submission BEFORE the submit event fires, so React `onSubmit` never runs in a real browser — jsdom-green, browser-dead; the shipped habit-tracker's add/ask flows were dead and are fixed, and the rule is now suite-enforced. Also: `SNUG_SERVER_PORT` override for vite proxy + e2e so parallel worktrees can run Playwright beside a dev server on 8787.
- **2026-08-06 — Queued from AL-08 (starter App Autopsy gap):** installing a starter seeds an app row but NO chat thread, so a starter's run view opens an empty conversation — the "view the build conversation" autopsy surface exists only for BUILT apps (pinned bootstrap turn → main-thread rule). Owner call wanted: should starters ship a pre-seeded bootstrap thread (a curated "how this app was made" conversation) so the autopsy story holds on the factory shelf? No infrastructure invented at AL-08 per the umbrella's instruction.
- **2026-08-06 — Queued from AL-08 (kv write-vs-hydration race):** a `usePersistedState` write landing in the first moments after boot is silently overwritten when the initial `kvGet` resolves (hydration always wins, and pre-hydration writes are not persisted). Trivia Night hit it live (roster add vanished) and moved its roster to SQL; the hook itself can't signal "hydrated" to apps today. Consider exposing hydration state from the SDK hook (byte-sync + KB update cycle) and a KB note steering early-write state toward `useAppDB`.
- **2026-08-06 — Queued from AL-08 (KB no-form rule):** the KB app-authoring template never *teaches* form/onSubmit, but three independent authors (habit-tracker + both new form-using starters) reached for it naturally and it is dead-by-construction under the sandbox. Add an explicit "no form elements — the sandbox blocks submission before the submit event" line to the KB template so generated apps can't repeat it (prompt change → its own task per ADR-0004 discipline).
- ✅ 2026-08-06 — **Spec push (TASK-20260806-spec-push, umbrella child AL-13, A12):** first real publication to `snugprotocol/spec` (owner-authorized Phase-0 decision 2) — spec **v0.1** wire protocol (SPEC.md + 10 JSON Schemas byte-identical to `packages/protocol/schemas/` at `6704d95`) and spec **v0.2 as an explicitly-marked DRAFT** (`SPEC-v0.2-draft.md`, v3-internal-draft note carried from auth-core `750ca29`, merged to main unchanged in PR #6). Spec commits `f148c22` + `ed6e596`, pushed `43f65e0..ed6e596` 2026-08-06 08:16 UTC; fresh-clone diff + zero-auth-literal grep verified (single allowed `AUTH_REQUIRED` R5 hit). Auth content excluded per A12; v0.3 auth/net stays internal (AL-12, Beta-exit gate).
- ✅ 2026-08-06 — **Flip-prep (TASK-20260806-flip-prep, umbrella child AL-14, A11 at Phase-0 decision-5 prep-only depth):** community file set landed — honest SECURITY.md (replacing the broker-overclaiming one; C1/C2 stated falsifiably, private-reporting route, safe harbor), process-aware CONTRIBUTING, full Covenant 2.1 CoC, `.github/CODEOWNERS` (root copy deleted), `.yml` issue forms + config + security stop-template, PR template with risk-tier + C1/C2-impact sections · `docs/good-first-issues.md` re-verified to a TRUE ten (three drafted entries had already been fixed by AL-01 — dropped, replaced from the queue above) and **opened as real labeled issues #9–#18** on the private repo · the queued `docs/tasks` breadcrumb sweep executed and made a permanent runbook step · README flip-critical corrections only (incl. the factually-wrong "BYOK key in sessionStorage" claim → `snug_secrets`) · flip runbook staged in untracked `internal/` with syntax-verified `gh` commands (branch protection via `checks:[{context}]`, purge probe, PVR enable); stages 0–1 and 4–8 remain owner-gated. **Maintenance rule until flip: fixing a listed good-first-issue closes the tracker issue AND updates the doc in the same branch.**
- ✅ 2026-08-05 — **Doctrines + dev-ex (TASK-20260805-doctrines-devex, umbrella child AL-01):** ADR-0013 (hosted hub is static files only — zero-backend doctrine) + ADR-0014 (credentials are local-first — custody doctrine) recorded as accepted owner decisions · `pnpm run update-code-map` count regenerator (A15) · the two queued bug fixes above (importUserDb cache family, supportsCaching exact host), both red-first + mutation-checked. Root suites green at 910 (was 906). Also: C4/C5 sweep found and scrubbed real source-system names from two tracked task files (codenames only now); the `no-ancestor-tokens` guard test still carries its probe tokens by design.
- ✅ 2026-08-06 — **Envelope net capability (TASK-20260806-connected-fetch, umbrella child AL-03, branch `feat/TASK-20260806-connected-fetch`, awaiting PR/merge):** the connected-fetch runtime on top of AL-02 — an app's only governed path to the network it cannot touch itself (C2 keeps `connect-src 'none'`). `packages/protocol`: internal-draft `net-request`/`net-response` frames (out of `schemas/` SOURCES), own size class `MAX_NET_FRAME_BYTES` so no oversized response is ever silently dropped (B1), `NET_ERROR_CODES`/whitelist/methods, GET-HEAD-body + credential-header strict-reject, `normalizeAuthHost` punycode retrofit (B3, closes AL-02's IDN asymmetry). `packages/auth`: the `createConnectedFetch` executor — the ONLY seat that reads a credential value AND calls fetch — enforcing the pinned 10-gate order (shape · host-assigned binding · `status===approved` · https-only + punycode ceiling · SSRF literal guard · mutating-confirm · credential-header strip · per-kind injection read PER USE · `redirect:'manual'` · 1 MiB cap-while-reading + scrubber), OAuth 401→one-refresh-retry; the ported scrubber (bodies + whitelisted headers, R1; base64 boundary documented for AL-11); the SSRF literal guard (honest browser edition); the session-remember confirm gate (R3); and `postForm` `redirect:'manual'` (B2, the sibling credential path). `packages/runner`: the value-blind `NetHandler` seam (host-assigned `netAppId`, R4/R5 lint) mirroring the db driver. `packages/sdk`: `useConnectedFetch` (embedded+module, KB≡SDK byte-locked). `apps/playground`: executor wiring, the permanent **Connections** settings panel (list/approve/reapprove/revoke via AL-02 accessors, grant-invalidation R3), the mutating-call confirm dialog, and an 8-test self-signed-https e2e stub on production bytes proving injection+scrub+every negative. **Named AC met: audit bug 3 dies by construction — no strictness flag/parameter exists anywhere (AC-linted).** Root suites green at **1068** (was 1052 + protocol 45, auth 61, runner 17, sdk 6, playground 14 new, minus AL-02's 91→152 auth already counted) — see report; 25 guard mutations checked RED→restored; Playwright 45 (37 + 8 net).
- ✅ 2026-08-06 — **Dynamic Auth pure core (TASK-20260805-auth-core, umbrella child AL-02, branch `feat/TASK-20260805-auth-core`, awaiting PR/merge):** ported the Dynamic Auth core from the two source systems (OProject/IProject), local-first per ADR-0014 — 5-kind strict auth-spec schema in `packages/protocol` (INTERNAL surface, out of the `schemas/` export; storage schema v3 adds `snug_auth_specs`) · host-freeze at the db write boundary (`HostFreezeViolation`; widen only via `reapproveAuthSpec` with the full host list re-displayed) · delta-aware import reconciliation inside `importUserDb` (byte-identical re-import keeps approval; doctored/changed rows demote to `imported_unapproved`; unknown-keys-only demote-and-preserve; unusable rows dropped + surfaced) · four-path custody byte-probes + finding-14 chat canary · `packages/auth`: CredentialStore over `auth:` secrets, async WebCrypto template engine, params→spec transformer, registry extended with human-reviewed `apiHosts` + per-provider consent params, DI-pure OAuth service (PKCE S256, `expectedFlowId` flow binding fixing audit bug 2, userLayer unwrap fixing bug 1, frozen-host outbound ceiling, zero token cache) · AC5 browser-safety + no-strictness-knob lint. Audit bug 3 (strict host injection) is AL-03's named AC — this child ships no injection runtime at all. Root suites green at 1051 (was 910); 16 guard mutations checked RED→restored.
- **2026-08-06 — Queued from the AL-03 final-gate live sweep (inspector lead resolved, non-blocker):** the think-panel frame timeline DOES route net frames (runner `onFrame` fires for inbound `net-request` and outbound `net-response`; live-verified on a fresh dev server), but `inspector.ts`'s `summarize` has no net case, so net round trips render as anonymous "frame" entries with empty detail — and the `ready` entry's capability summary says "streaming + db", omitting the new `net` bit. The structural-only guarantee holds either way (generic entries show NO content; C1-probed live: no injected value in the rail). Polish when convenient: structural net summaries (`net · GET` + status/size class, never values, per the AC-4 rule) + the `net` capability in the ready line. **AL-05 (auth KB):** teach that net traffic appears in the frames timeline (structure only) — ✅ 2026-08-06 AL-05: taught in `90-auth-and-connected-apis.md` §"What the user sees". **AL-11 (threat model):** state the inspector's value-blindness for net traffic as a deliberate boundary, not an omission. For the record: the interrupted sweep's "NO entries at all are showing" was a stale-harness artifact — a 2.5-day-old vite dev server predating all AL-03 code plus a stale agent-browser daemon; a fresh server shows entries immediately.
- **2026-08-06 — Queued from the AL-03 final-gate live sweep for AL-04 (revoke leaves credential values, non-blocker):** the Connections panel's revoke calls `deleteAuthSpec` only — the `auth:<appId>:*` secret slice survives revoke (probe-confirmed live; app DELETION does wipe the slice in `importUserDb`'s delete path). Fail-closed holds (no spec → `NET_NOT_APPROVED`, probe-confirmed live), so the lingering value is unreachable by any app, but a re-declared + re-approved spec for the same appId would resume injecting the OLD stored credential without re-entry. AL-04's wizard owns credential lifecycle: decide revoke-vs-disconnect semantics — `CredentialStore` already exposes the slice-wipe as "disconnect's final act".
- ✅ 2026-08-06 — **Auth wizard + inferrer + render directive (TASK-20260806-auth-wizard, umbrella child AL-04, branch `feat/TASK-20260806-auth-wizard`, awaiting review/PR/merge):** the Dynamic Auth loop closed for a stranger — `packages/protocol`: internal-draft render-directive contract (`authSpecHintsSchema` single-source with `ParamsToAuthSpecInput` re-derived type-only; `llmProposalSchema` structurally excludes registration copy + headerTemplate; evidence-free persisted `authWizardDirectiveSchema` with display-only confidence/provenance; strict `authRequiredPayloadSchema`; out of `schemas/` SOURCES, guard extended) · `packages/auth`: DI-pure `auth-spec-inferrer` (registry rung short-circuits the completion seam; provenance from the ladder alone; malformed confidence strict-rejected → EMPTY confirm; poisoned-docs quintet named-tested), `requireApprovedSpecScope` (B1: review → put → approve → ONLY THEN credentials — typed `spec_not_approved` wall), registry `registration` walkthrough + Spotify stub, import-specifier lint + clean-isolation load gate (the AL-03 phantom-dep class, executable) · `packages/knowledge`: D8 inferrer prompt (static system slot; delimited untrusted docs in the user slot; 3 few-shot incl. the honest-refusal anchor; R1 cross-package contract test over the example fixtures) · `apps/playground`: singleton `wizardStore` with the HOST-side ladder re-run at the directive mount (B2 — registry hit discards directive hints), store-owned OAuth flow + BroadcastChannel-named-by-flowId + `/oauth/callback` route, app-level `AuthWizardSheet` (provenance-branched review, full punycoded host list on every path, re-approval union diff with NEW/removed flags, write-only credential fields, docs tripwire BEFORE the BYOK wire), chat directive card (parse-don't-trust; malformed dropped with a note; persisted meta = the validated directive only), AgentAdapter-layer inferrer wiring (no tools, no cache, no inspector hook — D10), code-keyed net-error→CTA (blocked codes NEVER open the wizard), ConnectionsCard innards swap + revoke now wipes the credential slice (closes the AL-03 sweep's zombie-credential item). All 30 mutation rows executed with recorded RED evidence. Playwright +3 in a dedicated project (api_key→scrubbed fetch through the real shell; PKCE popup vs a local fake IdP; poisoned-directive review showing registry values).
- **2026-08-06 — Queued from AL-04 (for AL-10):** structured `NET_AUTH_FAILED` sub-code on the net-response frame if UX needs finer CTA copy (N1 — today the mapping is code-keyed and all causes are auth-repairable) · grant bookkeeping + the `ConnectedFetchDeps.clock` reserved seat (N6 — declined by AL-04, nothing in the wizard reads time) · poisoned-docs fixtures (five) + directive schema into the fuzz corpus · the no-fetch-on-inference-path and auth import-specifier lints are named surfaces.
- **2026-08-06 — Queued from AL-04 (small):** `HostCapabilities` in `packages/sdk/src/types.ts` still lacks the `net` flag AL-03 added to `hostReadySchema` (pre-existing drift — noted, not widened at AL-04) · `packages/knowledge` `renderedStore()` in test helpers covers `app-builder`/`artifact-write`/`auth-spec-inferrer` but still omits `schema-apply`/`app-doc-write` (pre-existing gap; the new prompt was added, the old two were left for a dedicated pass) · the `?demoauth` demo-brain seam (`src/agent/demoAuth.ts`) is a TEST seam for the wizard e2e — AL-05 owns teaching the real builder to emit directives, and may retire or formalize the seam — ✅ 2026-08-06 AL-05: seam FORMALIZED (kept — the wizard e2e needs a deterministic brain; header repointed at the shipped KB teaching; the taught emission format is scanner-sync-tested in `authKbEmission.test.ts`). The `HostCapabilities` net-flag drift and the `renderedStore()` helper gap in this row remain queued.
- **2026-08-06 — Queued from AL-04 (turbo cache correctness):** the `examples` turbo test task caches on its own files only, so the KB≡SDK byte-sync suite silently replayed a stale green from before AL-03 changed `packages/sdk/embedded` — a forced run at AL-04 exposed 8 drifted examples + a rule conflict (repaired in `a7bfee2`, verified pre-existing on main in a clean worktree). Fix the class: add `packages/sdk/embedded/**` to the examples task's turbo `inputs` so embedded-SDK edits invalidate the cache.
- ✅ 2026-08-06 — **AL-04 live sweep (TASK-20260806-auth-wizard, branch `feat/TASK-20260806-auth-wizard`) — FIXED-NOW-CLEAN.** Fresh servers only (killed a stale playground vite + esbuild pair before starting). One launch-blocker found and fixed on-branch: **the OAuth popup was opened AFTER three awaits inside `startOAuthFlow` (`getUserDb` → `requireApprovedSpecScope` → `generateAuthUrl`), so `window.open` was not associated with the click gesture — a default desktop popup blocker (Chrome/Safari/Firefox) blocks it; it only passed because headless Chrome and the e2e never enforce the blocker.** Fixed at `58be23c`: the "connect account" handler now opens a BLANK `about:blank` popup SYNCHRONOUSLY on click and `startOAuthFlow` navigates it after the URL mints (`WizardPopupLike.navigate` + `openBlankOAuthPopup`); aborts close the blank window so it is never orphaned. Red-checked (2 new tests both RED against pre-fix) + one mutation RED→restored; full playground vitest 377 green; auth-wizard e2e 3/3 green; verified live (single synchronous `window.open('about:blank')` in the click frame, popup navigates, token exchange completes). The existing `oauthPopupFlow`/e2e tests could not catch this (they inject a fake popup / use `waitForEvent('page')`, both timing-agnostic). Non-blockers (rows below). Everything else PASS: real BYOK inference returns a valid strict-schema proposal (see task journal), fail-closed path triggers on malformed reply, B1 approval-before-credentials wall holds (`spec_not_approved` thrown live), B2 poisoned registry-claim discarded (Spotify registry values win, no `evil.example`), re-approval diff flags NEW/removed hosts incl. punycode, revoke wipes the credential slice (re-approve does NOT resume the old secret), code-keyed net-error CTA correct (`NET_NOT_APPROVED`/`NET_AUTH_FAILED`→connect; `NET_HOST_BLOCKED` off-ceiling→NO CTA), C1 probes clean (credential/docs-canary/byok-key absent from default export, DOM, storage, and the LLM inspector; only the opt-in include-secrets export carries the credential + byok key by design), C2 sandbox `allow-scripts` only, console clean (sql.js wasm-fallback lines only).
- **2026-08-06 — Queued from the AL-04 live sweep (non-blocker, real-inference host-list breadth):** a real BYOK inference over plausible messy provider docs (3 documented base hosts: production/telemetry/sandbox) returned ALL THREE in `declaredApiHosts` at confidence 0.85 — correct extraction, but the wizard then displays a 3-host approval ceiling where a cautious user likely wants only production. Not a security issue (every host is reviewed + user-editable before the freeze, and the model quoted its evidence per host), but the D8 prompt could bias toward the single primary host and let the user opt into secondary hosts. Fold into AL-05's KB/prompt work or AL-10 hardening — prompt-side only, never a schema change. ✅ 2026-08-06 AL-05: folded — rule 3 now biases `declaredApiHosts` to the host the docs present as production/live, rule 7 keeps quotes for hosts deliberately left out (the reviewer opts in via wizard edit), and a multi-host few-shot example anchors both; schemas untouched.
- **2026-08-06 — Queued from the AL-04 live sweep (non-blocker, UI polish):** the wizard "approve connection" / "connect account" / "run it" buttons sit below the fold in a tall sheet/thread and are not scrolled into view on render — a real user must scroll to reach them, and automated ref-clicks missed until scrolled. Consider a sticky action footer on `Sheet.tsx` (contract-preserving) so the primary action is always visible. Cosmetic; no functional impact. *(AL-05 sweep 2026-08-06: same class confirmed on the chat CONNECT CARD's button — clicks silently no-op until the card is scrolled into view; add the card to whichever fix lands.)*
- **2026-08-06 — Queued from the AL-05 live sweep (small, D8 observation + chat cosmetic):** (a) the inferrer echoes a GIVEN kindHint even when pasted docs describe a different kind (probe: hint api_key, docs say Bearer → proposal kept api_key; the Bearer quote sits in evidence and spec_confirm forces review, so no security exposure — but AL-10/AL-11 should decide whether the D8 prompt should let strong doc evidence override the hint). (b) The builder reply renders the directive's ```json fence as literal text below the connect card — cosmetic; consider having ChatLog collapse or pretty-print a scanned-directive block.
- **2026-08-06 — Queued from the AL-04 fix-first pass (AL-10 candidate, bounds remainder):** nonBlocking 8 bounded exactly the verdict's list (providerName, declaredApiHosts entries+items, scopes/userLayerScopes entries+items). Still unbounded in the chat-persisted hints shape: `kindHint` (open string by design — the transformer owns the unknown-kind failure, but a length cap would be additive) and the `z.url()` slots (`docsUrl`/`homepageUrl`/endpoint URLs — zod validates shape, not length; a multi-megabyte URL passes). Bound them the same way if/when the AL-10 fuzz corpus flags them.
- **2026-08-06 — Queued from the AL-04 fix-first pass (AL-05/AL-10, keyed-subscription inference disclosure):** the keyless half of the verdict's nonBlocking 2 is fixed (needs-BYOK guard, `0f5c5ad` — a real inference never runs on the mock demo brain). The KEYED half stands as designed-but-undisclosed: a subscription-mode user WITH a stored BYOK key gets a browser-direct-to-that-provider wire for the inference turn while the paste-box copy says "pasted text goes to your configured model". Either add a server twin for the inference turn (the journal's original flag) or make the copy name the actual wire. Do not widen at AL-04. ✅ 2026-08-06 AL-05 (keyed half, honest-copy option): the paste-box label now names the actual wire from the SAME decision ladder the adapter uses (`inferenceWireCopy` in `inferrerAdapter.ts` — keyed subscription reads "even in subscription mode, pasted text goes directly from this browser to <provider> using your saved key"). The server-twin alternative stays open as an AL-10 candidate if custody should move server-side.
- **2026-08-06 — Queued from AL-05 Gate 2 (keyless public APIs — spec-level gap + AL-09 coordination flag):** keyless connected hosts are NOT expressible at 1.0 — every persisted spec is a five-kind credentialed union (static kinds require at least one credential field) and connected-fetch fails closed (`NET_NOT_APPROVED`) for unapproved hosts, while the builder cannot propose field definitions (llm omit-set). AL-05's KB teaches the truth (prefer a credentialed provider; genuinely keyless → tell the user + ship a manual/sample-data mode). Queue the mechanism (a `none` kind or optional-credential approval — a SPEC change through SPEC_SYNC) as an AL-12-staging/post-alpha candidate. **AL-09 FLAG: the "Crypto Portfolio (none/CoinGecko)" starter collides with this gap head-on — resolve demo-key vs manual-data at its Gate 2 before authoring.**
- **2026-08-06 — Queued from the AL-04 delta re-review (AL-10, tripwire URL-borne secrets):** the nonBlocking-7 fix strips URLs from the entropy heuristic (killing the false-positive class), which also dropped entropy-only TRUE positives that ride URLs — Slack incoming-webhook paths, Azure SAS `sig=`, S3 presigned `X-Amz-Signature`. The six explicit TRIPWIRE_PATTERNS still catch key-shaped secrets anywhere incl. inside URLs. Add explicit URL-borne-secret patterns (webhook path shapes, `sig=`/`X-Amz-Signature=` query params) to TRIPWIRE_PATTERNS at AL-10 — pattern additions, not heuristic changes, so the false-positive kill stays intact.
- ✅ 2026-08-07 — **AL-05 post-merge review fold (TASK-20260807-al05-review-fold, M62–M66):** AL-05's fresh-context adversarial review (`wf_bd1ffa6e-08e`, 3 lenses + refute-first verify) completed AFTER PR #23 merged, so its findings landed on `main`. 9 of 11 deduped findings survived verification; 4 were the already-remediated `5ad60d3` cluster and 2 were refuted (the 90-file heading-token "crowding" — no relevant displacement, collision pre-dates the diff; and D8 rule-3 singular-host "under-declaration" — the Gate-2 decision verbatim, with the rule-7 evidence valve as the widening path). The 5 live ones are fixed test-first, each mutation-evidenced and independently re-mutated by a fresh verifier: **M62** the C1 paragraph no longer claims a hardcoded key "could not work" (false — header-NAME-only strip, zero query-string inspection, and OpenWeather's real `?appid=` transport is the file's own example); **M63** the emission doctrine gained the multi-provider branch (one connection per app — `snug_auth_specs.app_id` is a PRIMARY KEY — declare one, tell the user, ship the second feature manual/sample); **M64** corpus reconciliation (the "three hooks are the ONLY way" sentence was false once `useConnectedFetch` shipped as a fourth; Section Map row + fetch-prohibition pointers added, CSP prohibition still primary); **M65** the local-mode disclosure no longer reads "your local mock server" (`WireDecision`'s local variant carries `localUrl`, not a provider it never reads); **M66** natural auth vocabulary now retrieves the teaching (`authentication` previously zero-scored the corpus and dumped the ~54 KB fallback — the searcher does not stem and keeps `bearer_token`/`oauth2_auth_code` whole). knowledge 96→112 tests, playground 407→409.
- **2026-08-07 — Queued from the AL-05 review fold (AL-10 candidate, URL-borne credential channel):** M62 made the KB *honest* about hardcoded credentials, but the underlying mechanical gap is unpatched: `stripCredentialShapedHeaders` matches header NAMES only (5-name `STRIP_SET` + `/api[-_]?key/i` + `/^x-.*(auth|token)/i`), so names like `appid`, `key`, `access-key` pass, and **nothing in the 10-gate pipeline inspects URL query strings** — an app-authored `?appid=SECRET` to an approved host goes out intact. Note the tension that makes this a design decision rather than a patch: OpenWeather and other providers legitimately authenticate via query param, so the host's own injection path may need that channel; any filter must distinguish host-injected from app-authored params. Decide at AL-10 (hardening) or AL-11 (threat model) whether to flag/deny app-authored credential-shaped query params, and state the boundary explicitly in the threat model either way.
- **2026-08-07 — Queued from the AL-05 review fold (small, retrieval):** `http request` still does not retrieve the connected-API teaching (it serves 30-bridge-protocol / 50-app-catalog / 10-overview). Left unfixed and unpinned deliberately — the query's natural home is arguably the bridge doc, and forcing it would mean keyword-stuffing the 90-file. Revisit if a live sweep shows a builder actually stranded by it.
- ✅ **2026-08-08 — SHIPPED (PR #30, `main` @ `ab2b8f3`): `TASK-20260807-connection-reachability`.** A chat-less app reaches a connection through the **install act** — a starter carries `examples/<folder>/connection.json`, validated against the same bounded `llmProposalSchema` as LLM proposals, **never persisted**, resolved on demand only when TWO independent facts hold (`install_source` resolves to a bundled manifest AND the installed HTML matches the bundled starter's, both pinned v1 and `current_version`, compared normalized with any mismatch reported in Settings rather than silently withdrawn). The declaration rides in its own immutable wizard session field, so it **always** forces the strong field-by-field review and no mid-session "infer from docs" click can launder it into the light path. `packages/protocol` untouched — no new provenance literal, no persisted-enum widening, no spec-sync. **Posture ratified: an app may never propose a connection at runtime** (ADR-0016). The original row follows for traceability:
- **2026-08-07 — Raised by AL-09, promoted to its own task (`TASK-20260807-connection-reachability`, High, PARKED pending green light):** a **chat-less app can never become a connected app**. The only non-test `putAuthSpec` lives inside the wizard (`apps/playground/src/state/wizard.ts:328`), and every wizard entry needs a directive (needs a build conversation), an existing row (Settings renders `db.listAuthSpecs()` — empty), or a net-error CTA that opens over no row and no proposal (an empty manual review). So starters, imported HTML, and hand-authored apps are structurally excluded from connections. Not broken code — AL-08's chat-less starters, AL-04's directive-only proposals and the wizard's row-or-proposal model each correct alone. **A proposed fix (app-declared `authRequired` in the announce frame, host seeds an unapproved row) FAILED its 3-lens design review with 3 BLOCKERs + 9 MAJORs** — chiefly that "registry hit discards declared hosts" does not exist for static kinds (`params-to-auth-spec.ts:181` vs `:97`; the B2 rung hard-codes `oauth2_auth_code` at `wizard.ts:241–248`), the announce payload is unbounded where `llmProposalSchema` keeps AL-04's bounds, and a seeded row would get a LIGHTER review than the same content via directive. Full constraint list + four design directions in the task file. **Also corrected there: AL-09's D7 was wrong — a fresh installed app gets `NET_NOT_APPROVED` (Gate 3), never `NET_HOST_BLOCKED` (Gate 4, post-approval only).**
- **2026-08-08 — Queued from connection-reachability Gate 2 (BLOCKER prerequisite, not a nice-to-have):** a **charset/confusable guard on `providerName`** is a hard prerequisite for ANY future *untrusted* declaration channel (an app-import flow above all). Today's install-act declarations are first-party, in-repo, PR-reviewed content, which is the ONLY reason the gap is tolerable — `normalizeProviderKey` strips non-`[a-z0-9]` so Cyrillic `ѕpotify` → `potify` misses the registry while the raw string becomes the display name. The same review also established that registry effects on a declared proposal are **three**, not one (display name; the registration walkthrough rendered with wizard-grade legitimacy; and for `oauth2_auth_code` the manifest's endpoints/hosts WIN over the registry's — the inverse of the directive channel's discard). Any untrusted channel needs both the charset guard AND a registry-borrow ban before it can exist. Cited: `TASK-20260807-connection-reachability.md` §Security review of plan v1.
- **2026-08-08 — Correction + reinforcement of the revoke-tombstone row above (from the same review):** connection-reachability's post-revoke rule ("the prefilled declaration applies only when the app has never had a row in this session's lifetime") is **UX friction, not a security boundary** — nothing persists that state, so it dies on page reload and an app that induces a refresh gets the prefilled sheet back. This makes the **revoke tombstone (AL-10) the real fix**, and it should not be read as already-mitigated by that rule. Also corrected: the CTA *is* app-timed (the app's own net attempt fires it), so an earlier claim that "nothing app-timed exists" is false — the terminality invariant itself still holds, because the only non-test `putAuthSpec` remains behind an explicit user approval.
- **2026-08-08 — Queued from connection-reachability Gate 4 (AL-10, UX cost of the MAJOR-1 security fix — OWNER-ACCEPTED, shipping as-is):** the install-act declaration resolver requires the app's **pinned factory v1 AND its `current_version`** to both match the bundled starter HTML, because the iframe executes `current_version` and credential brokering is keyed on `appId` — vouching for bytes that never run let a whole-DB import pair a pristine v1 with attacker code and still collect the approval. **Consequence accepted by the owner: a user who legitimately edits their installed declaring app loses the guided setup** and falls back to the plain wizard plus the Settings "no longer matches the starter" notice. This **reverses an earlier in-task call** ("a user edit shouldn't retract the install act") whose test had blessed the attack shape — recorded so it is not re-reverted. The honest fix (re-vouch on every version write, so an edit keeps the declaration without re-opening the import hole) **depends on the revoke tombstone already queued to AL-10** and must not be attempted without it. Cited: `TASK-20260807-connection-reachability.md` §Gate 4.
- **2026-08-07 — Queued from AL-09 (AL-10/AL-11 candidates, found by the seam design review, independent of whether that seam is ever built):** (a) **`putAuthSpec` fails OPEN on an existing *unapproved* row** — a silent full `UPDATE` of spec + allowed_hosts with no guard (`packages/db/src/userdb/userdb.ts:1439–1443`), while `approveAuthSpec` re-derives the union from the LIVE row; the approved-row freeze invariant has no unapproved-row twin. (b) **Revoke is not terminal**: `clearApp` + `deleteAuthSpec` leave no tombstone (`:1490–1493`) and the app stays installed, so any future re-proposal channel silently reverses a revoke. (c) **Provider-name confusables**: `normalizeProviderKey` strips non-`[a-z0-9]`, so Cyrillic `ѕpotify` → `potify` misses the registry while the raw string becomes the display name; both `providerName` fields are bare `z.string()` with no charset guard. (d) **Two SDK announce faces can drift silently**: `packages/sdk/src/hooks.ts:34–41` (typed, `SnugAppMeta` in `types.ts:4–13`) vs `packages/sdk/embedded/snug-hooks.js`, with the contract suite asserting via `toMatchObject`.
- **2026-08-07 — Correction to the 2026-08-06 AL-04 turbo-cache row above (verified at AL-09 Gate 2):** the suggested one-line fix does not exist as described — `turbo.json` declares **no `inputs` key on any task** (`test` is `{"dependsOn": ["build"]}`). The underlying cache-correctness concern stands; the remedy needs authoring, not a one-liner. Force-run the examples suite when `packages/sdk/embedded/**` changes until it lands.
- **2026-08-08 — Queued from AL-09 Gate 3 (real-API verification, honest gap):** the five auth-spectrum starters ship verified against **stubs and fixtures through the real wizard + injection + scrub path** — the path is real, the providers are not (D3; owner had no OpenWeather/CoinGecko/GitHub/Spotify keys on hand, Phase-0 decision 4). What is therefore **NOT yet proven end-to-end with live traffic**: each provider's actual auth transport (OpenWeather and CoinGecko authenticate via **query string**, not a header, which is the same URL-borne credential channel already queued above), Spotify's real PKCE round trip against `accounts.spotify.com`, and GitHub's PAT scope behaviour. None of this blocks the starters — they are correct by construction and statically enforced — but the README claims and the shelf's promise should be confirmed against real providers before the shelf is used as launch evidence. Cheapest path: one free key per provider, run each starter's connect flow by hand, record the result in the task file.
- **2026-08-08 — Queued from the AL-09 adversarial review (correctness lens), reframed after verification: there is NO CI, so no suite gates a merge automatically.** The reviewer flagged that every e2e spec is gated on `test.skip(!hasApp, AWAITS_INTEGRATION)` and could not confirm from source whether CI sets it. Verified: `.github/` contains only `CODEOWNERS`, `ISSUE_TEMPLATE/` and `PULL_REQUEST_TEMPLATE.md` — **there are no workflow files at all**, so the concern is real but differently shaped. The e2e suite is not silently skipping in CI; CI simply does not run it, because CI does not exist. `SNUG_E2E_HAS_APP` derives from `appIsPresent()` at config load (`playwright.config.ts:20–22`) and resolves TRUE locally, which is why the AL-09 specs genuinely ran (6/6, then 7/7 — confirmed by watching them execute, not by a green summary line). **Consequence to fix before flip-public:** every gate in this repo is currently enforced by a human choosing to run it. A CI workflow running root `pnpm test` + Playwright on PRs belongs in AL-14's flip-prep runbook (or earlier), and until it exists, "green" claims in task files rest entirely on the author having actually run the suites.
- **2026-08-09 — Queued from an owner-hit bug (AL-10 candidate, self-healing schema guard): a user DB stamped at the CURRENT version but missing a table has no recovery path except a full wipe.** Owner hit `no such table: snug_auth_specs` on a local hub and had to clear OPFS to get moving. Root cause is structural, not a coding error: `migrate()` is forward-only — `for (let v = found; v < USERDB_SCHEMA_VERSION; v++)` then `PRAGMA user_version = <current>` (`packages/db/src/userdb/userdb.ts:493–500`) — so it trusts `user_version` as a complete description of the file's shape. A DB created **mid-development of a version** gets stamped v3 before that version's DDL was final, and the v2→v3 step that would create `snug_auth_specs` (`userdb.ts:386–392`) is then skipped forever. Fresh DBs (v0 → full DDL) and genuinely-old v2 DBs are both fine; only ones born inside that window are stranded. **Why it must be fixed before flip-public:** post-launch the same shape arises from any interrupted/partial migration or a restored backup written by a mid-version build, and a stranded user's only option is destroying their data — unacceptable for a local-first product whose whole promise is that the file is theirs. **The fix is cheap and already half-built:** every migration step is an idempotent `CREATE TABLE IF NOT EXISTS` replay, so on open, verify the expected table set (`USERDB_TABLES`) against `sqlite_master` and, on any miss, replay `USERDB_DDL` + `USERDB_INDEX_DDL` before trusting the stamp — self-healing with no data movement. Guard it with a red-first test that stamps a DB v3, drops `snug_auth_specs`, reopens, and asserts the table returns with existing rows in other tables untouched. Related: the same "the persisted version lied" failure mode is already documented one layer up at `userdb.ts:487–492`.
- **2026-08-09 — Queued from an owner question about the shared callback URL (AL-10, OPEN QUESTION on the auth surface — resolve before Beta): is the audit's bug-2 session binding actually present in our `oauth-service.ts`, or was it dropped in re-implementation?** The umbrella's AC5 names three OProject audit bugs that must be "provably fixed", of which (b) is *"`handleCallback` receives `expectedSessionId` (binding check live)"*. **`expectedSessionId` does not exist anywhere in `packages/auth`** (grep: zero hits). What ships is a binding on `expectedFlowId` + `payload.appId` (`oauth-service.ts:380–393`), which fails closed and burns the flow row on mismatch — genuinely good, and it makes multi-app callback routing safe on ONE shared URL because identity rides the HMAC-signed `state`, never the path. **The open question is whether flow binding SUBSUMES session binding or is a weaker sibling.** They answer different questions: flowId asks "is this the callback for the flow I started?", sessionId asks "is this the callback for the browser session that started it?" — the second is what stops a callback delivered into a *different* session (a shared machine, a second tab, a copied URL) from completing against the first session's expectation. `expectedFlowId` is held by the caller, so if the caller's own state is per-session then the two collapse into one; if it is per-app or persisted, they do not. **Resolve by reading the caller (`apps/playground/src/state/wizard.ts` flow store) and deciding whether AC5(b) is satisfied under a different name — then either mark AC5(b) provably met with the citation, or implement the binding.** Note the original audit finding was that OProject's ROUTE never passed the parameter, making its own check dead code; ours is a re-implementation, so "we named it differently" and "we dropped it" are both live possibilities and only the caller can distinguish them. Flagged from a UX question, not a security review — no reviewer has been pointed at this specific claim.
- **2026-08-10 — OWNER REDIRECT: the dynamic-auth surface is being re-planned ground-up (`docs/tasks/active/TASK-20260810-dynamic-auth-rewrite.md`).** The owner named AL-09's "several issues" and they are architectural: inference must run at BUILD time and persist a complete requirement (every field a provider needs — the current `llmProposalSchema` structurally cannot express Coinbase's key+secret+passphrase), run time must only guide (no re-inference), edits re-infer only when they touch the auth surface, starters bake dev-time-inferred requirements copied at install, storage re-keys for multiple connections per app, and the wizard is rebuilt on OProject's step grammar. **Routing consequence for the rows above:** the confusable guard, `putAuthSpec` fail-open, revoke tombstone, `authRequiredPayloadSchema` deletion, self-healing schema guard, and the AC5(b) session-binding question are all ABSORBED into that task's phases (P0/P1) rather than waiting for AL-10 — the surface they patch is being rebuilt. AL-10/11/12/15 remain HELD; AL-09's branch is parked unmerged as a harvest source pending the rewrite's Q8.
- **2026-08-10 — Owner answered the rewrite's Q1–Q9 (all nine on the tabled recommendation); the routing above is now settled, not pending.** Confirmed consequences for the rows in this file: the **self-healing schema guard** (2026-08-09 row) is absorbed into the rewrite's **P0** with a stale-v3 fixture test and is no longer an AL-10 candidate (Q9 — no migration for v4; existing local DBs self-heal rather than needing a wipe). The **AC5(b) session-binding open question** (2026-08-09 row) resolves in **P1** while `oauth-service.ts` is on the bench. **AL-09 is harvested, never merged** (Q8) — its branch stays parked at `86a564c` as the source for the five starters' HTML, the shelf looks, the Hue posture, the Spotify registry polish, and `bearer_token` coverage; its manifests and auth-flow tests rebuild on the v4 schema, which also dissolves the deferred review of its last five commits *for merge purposes* (harvested HTML still gets reviewed as it lands in P4). Also decided: LLM-authored header/signing templates are ADMITTED with lint + verbatim review + registry-borrow ban (Q2 — the Coinbase unblock), the schema is slot-keyed with the `none` kind included (Q4/Q6), and run-time inference is REMOVED rather than gated (Q5). The 2026-08-08 real-API gap row is untouched by all this and still stands.
- ✅ **2026-08-14 — SHIPPED: `TASK-20260813-ui-polish-inspector`** (six owner-reported UI defects, 11 ACs, PR pending at time of writing). Header wordmark descender (`.brand` pinned `line-height: 1`, so the `g` tail fell outside the line box and `.brand-word`'s ellipsis `overflow: hidden` clipped it — one fix covers web AND desktop, which mount the same `App`); desktop icon regenerated from a committed generator so the mark's own tile fills the canvas (**ADR-0024**); the think rail is user-resizable and dismissible (**ADR-0024**); the app↔host frame view is deleted while its reducer stays (three unrelated features read `inFlight`/`sawDbOp`/`readySeen`); a `🔌 connections` control in the app header plus a calm `.connection-note` surface that stops spending alarm red on routine setup; and the stuck inspector timer, which had **three independent causes** (adapter rejection skipping the terminal emit → fixed in `packages/adapters`; a positional React key invalidated by `evict()`; and — the one the owner kept hitting — an app transport that never reset, so entries accumulated and collided on `index` where `settleEntry` settled the wrong one).
- **2026-08-14 — Queued from TASK-20260813 (owner-blocking verification, cheap):** the desktop icon is verified at the PIXEL level (`appIcon.test.ts` decodes each PNG and asserts edge coverage ≥95%) but has **never been seen on a real dock or taskbar** — no bundle was built in that session. Run `pnpm --filter desktop bundle` on real hardware and eyeball it before the icon is treated as done. Same class as the 2026-08-08 real-API gap: correct by construction, unconfirmed in the wild.
- **2026-08-14 — Queued from TASK-20260813 (small, test-infrastructure gap that hid a real bug):** this repo has **no browser-layout coverage of the run view's chrome**, which is why a one-character-per-line collapse shipped and survived a full task before the owner re-reported it — jsdom returns 0×0 for every rect, so CSS assertions were the only guard and they proved nothing about geometry. `e2e/inspector-layout.spec.ts` now measures the round-trip summary and payload blocks at 280/340/520px rails; extend the same pattern to the other width-sensitive surfaces (the run header's action cluster, the connection banners at narrow widths, the builder's `<details>` inspector) rather than waiting for each to be reported.
- **2026-08-14 — Observed at the close of TASK-20260813 (flaky suite, unattributed — needs a repro before anyone trusts a green playground run):** a forced uncached root run on `main` at `5472811` failed `playground#test`, and **four consecutive forced re-runs immediately after were all green at 1014/1014** — so roughly a 1-in-5 intermittent failure. The failing test was NOT captured (turbo's summary reported only `Failed: playground#test`; the re-runs erased the detail before the per-test line could be read), so the subject is unknown. Not attributed to that task's changes — its branch went green repeatedly, including a forced uncached 21/21 immediately before merge — but that is an absence of evidence, not a clearance. **Why this matters more than a normal flake:** this repo's CI runs `turbo run test --force`, so the same 1-in-5 will fail PRs at random, and the standing risk is that a genuine red gets waved through as "just the flake" (the 2026-08-04 lesson about a false flakiness report costing real time is the same shape from the other direction). **Next action:** run the playground suite in a loop (`for i in $(seq 20); do pnpm --filter playground test 2>&1 | tee run-$i.log; done`) to capture the failing test name, then decide whether it is test-order dependence, a timing assumption, or a shared-state leak between suites.
