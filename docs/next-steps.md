# Snug — Next Steps

Dated backlog of OPEN work only (ADR-0027: shipped/superseded history lives in git, `docs/spec-changelog.md`, and `docs/tasks/done/INDEX.md`). Append with a date; prune on every touch; work happens in task files.

---

## Now / next

- **2026-09-04 — 🎉 APP SHARING MERGED (TASK-20260904-app-sharing, PR #161, ADR-0063 + ADR-0064; CI green on both required checks).** A shared app is a starter that travels: a strict-JSON `snug-app-bundle/1` in a `.snug` file (code + connection SHAPES + contract + schema DDL + chosen docs; never data, secrets, grants, history or chat), a "shared with you" shelf, a consent-gated read-only preview, install/update on the new `shared` provenance, and a link path whose relay stores ciphertext it cannot read. **What remains, all owner acts:**
  1. 🔑 **The relay is BUILT, not deployed** (ADR-0064 §4 — an explicit-ask act). Order matters and the script enforces it: **merge is done, so start from a clean `main`**, then `pnpm exec wrangler r2 bucket create snug-share-bundles` → `pnpm exec wrangler r2 bucket lifecycle add snug-share-bundles expire-shares --expire-days 31` → the **one dashboard-only step**, the WAF rate-limit rule (POST `/v1/bundles`, 20/min/IP, block 10 min) → `node scripts/deploy-relay.mjs` (dry run) → `--deploy` → verify two bodiless 404s → journal it. Runbook: `docs/runbooks/deploy-share-relay.md`.
  2. 🔑 **Then turn the link on in the hosted playground**: pin `VITE_SNUG_SHARE_RELAY=https://share.snugprotocol.org` into `PINNED_BUILD_ENV` in `scripts/deploy-web.mjs` (its own small PR — it changes a deploy invariant), then redeploy the playground. Until that lands the hosted build ships the ATTACHMENT path alone and renders no copy-link action, by design.
  3. 🔑 **The `snug://` hardware walk waits for the next desktop release** — a URL scheme registers with the bundle, so "open in Snug for Mac" cannot be exercised from a dev build.
  4. **Follow-ups, queued not planned:** demote an installed copy's approved connections to `declared` when a shared update changes its html (threat-model R-39 — a sixth `snug_connections` writer, so a spec change with its own ADR; today an always-on confirm names the inherited grants instead); an opt-in that ships builder-seeded reference rows (the migration log, not the registry — R-g); a QR on the link panel (needs a dependency); drag-and-drop a `.snug` onto the hub; Turnstile on the relay if abuse appears; promoting `snug-app-bundle/1` into `schemas/` (its own spec-sync); **the website has no "share" page and `product-vision.md`'s roadmap still lists share as post-launch community work — now false, fix in the announcement pass.**
  5. The three pre-existing e2e reds below are unchanged by this task (`share.spec.ts` passes alone and in the full run).

- **2026-08-27 — Positioning pivoted to user ownership; MCP is no longer the foil (TASK-20260827-ownership-positioning, ADR-0061).** Every public surface now leads with *the application and its accumulated state belong to the user, while a conforming host supplies runtime intelligence* — website hero ("Your software shouldn't need a landlord."), a new thesis section + own it/move it/keep it private, a new "Not every app needs to become a service." section, reheaded differentiators, six figure takeaways on `/architecture/` (Figs 4/5/6 promoted as the ownership/privacy peaks), a de-MCP'd architecture closer, Playground hub + "apps that outlive the AI that created them" beside the brain control, metadata across all three surfaces, both READMEs, the org profile banner and the docs hub. `docs/product-vision.md` moved FIRST as the hashed upstream source and gained a **claims-discipline block** (application-specific backend only; hosted models see inference data; the fully-local sentence is local-config only; credentials ≠ app data). Claims are pinned in BOTH directions by `positioning.test.ts` (53) + `ownershipCopy.test.ts` (15) — the negative assertions are half the point. **✅ ALL THREE FOLLOW-UPS CLOSED 2026-08-27.** (1) **`snugprotocol/spec` PUSHED** — PR #1, squash **`06139cf`**, merged 20:35:08Z; a direct push to `main` was *rejected by the `main-pr-only` ruleset* (protection working), so it went through a PR like everything else. Verified live on `raw.githubusercontent.com`: the new §1 opener is served and the old one returns 0, closing the public divergence between the site's generated spec pages and the spec repo. Pre-push checks: §1 byte-identical to the master draft, `schemas/` byte-identical to `packages/protocol/schemas/` (`diff -rq` clean), **zero** MUST/SHOULD/MAY sentences in the diff. (2) **`snugprotocol/.github` PUSHED** — PR #1, squash **`e062c32`**; banner serves 200 at 481,540 bytes, README carries the new positioning, old slogan returns 0. (3) **The social card was REBUILT before any re-scrape, and that turned out to matter.** Preparing the re-scrape surfaced two defects: the card image was the teaser's poster frame — a screenshot **of the Playground hub**, still showing `talk. build. run.` — and `og:image:alt` still read "tiny, user-built apps that live in one portable file you own", a single-homed string in `socialImage.ts` the first positioning pass missed. Re-scraping would have cached a card whose *picture contradicts its own title* on every platform, keyed per URL and sticky. Fixed in PR #156 (`0097069`): the card is now **drawn, not captured** (`ogCardSvg` in `build-social-previews.mjs`, 1200×630, the org banner's lockup and palette), ships with the site rather than being hand-uploaded, and carries the positioning so it cannot go stale behind a UI change. **Website redeployed 2026-08-27T20:47Z** (deployment `ee4a1c1d` @ `0097069`); the served card is byte-identical to the local file (sha `a65e16d1…`), both shells emit it, and all four crawler user-agents (Twitterbot, facebookexternalhit, LinkedInBot, Slackbot) fetch the new tag. **🔑 Only the browser half of the re-scrape remains** — LinkedIn's Post Inspector and Facebook's Sharing Debugger need an authenticated session, so those two buttons are the owner's; X usually refreshes off the validator's own fetch. Do it before any announcement. **Deliberately NOT done:** the whitepaper needed no change (its §11 already frames MCP as complementary related work, and it never used the slogan — verified, 104/104 gate green); the desktop app was not rebuilt (its shell compiles the playground's source, so the new copy ships with the NEXT release — fold it into the v0.1.1→v0.1.2 update walk already queued below rather than cutting a release for messaging).

- **2026-08-26 — Backlog hygiene: two 2026-08-21 review findings struck as already-fixed, threat-model R-11 errata (TASK-20260826-backlog-hygiene-stale-findings, docs only, no ADR).** Items 5 (`healMissingTables`) and 6 (`SNUGENC1` §11.1 layout) of the launch security review were fixed in `a40302e` (#103) the day after they were found but never struck, and read to an outside auditor as an open normative-spec interoperability bug. Threat-model R-11 and two sibling passages carried "CI billing-blocked since ~2026-08-18" as a present-tense premise; each now has a dated errata pointing at ADR-0058 while keeping the caveat that still holds (Chromium CSP probes never run in CI; WebView2 leg parked). **Still open from that review, each its own task:** LAN 15 s vs executor 60 s timeout (`lanfetch.rs`), pseudonymisation digit floor 7-vs-8, NBSP/zero-width in contact names, `RUNTIME_CONTRACT_MAX_BYTES` counting UTF-16 units, the `.snug` import confirmation gate (item 2), the three IPv6-embedding SSRF forms (item 3).
- **2026-09-03 — Two Playwright specs are RED ON `main` (found while gating TASK-20260903-build-thread-continuity, PR #159; reproduced on a clean `main` worktree, so not that branch's):** `e2e/starters-connect.spec.ts` "degraded pre-connect state" for github / spotify / weather + "a READ-ONLY starter route reaches nothing" (20 s timeouts; ledger passes), and `e2e/connection-wizard.spec.ts` journey 4 (oauth2 popup: "Target page, context or browser has been closed"). The merge gate's required checks are `workspace` + `desktop-shell`, so CI does not see these; per lesson 2026-08-22, capture the failing run's TEXT first and diagnose from it — likely environmental (a stub server / popup handling on this machine) rather than product, but unproven.
- **2026-08-26 — 🎉 DEMO-BRAIN DISCLOSURE SHIPPED + **v0.1.1 RELEASED** (TASK-20260826-demo-brain-clarity; PR #147 squash `3da8ef6`, PR #148 `1703674`; ADR-0059).** The Gate-5 blocker (fresh-thread pick vs the chip's claim) was fixed and mutation-verified before the PR. **Playground deployed to production** and verified live (the disclosure copy is in the shipped JS, not merely in the build). **Desktop v0.1.1 published** — signed, notarized + stapled on **both** the DMG and the inner `.app`, universal `x86_64 arm64`, EULA carried, byte-identical after re-download; **auto-update proven end-to-end** (`latest.json` 200 at the single-homed URL, both darwin keys → the one universal artifact, tarball URL resolves; `/releases/latest/download/Snug.dmg` redirects to v0.1.1, so the version-agnostic website needed no change). **Two standing threads CLOSED by this release:** the v0.1.0→v0.1.1 update walk (owner-verified) and `release-desktop.mjs`'s first true end-to-end run. **Landmine fixed in `1703674`:** the script's `TAURI_SIGNING_PRIVATE_KEY || ..._PATH` guard accepted a spelling `tauri build` cannot read, so `_PATH` alone passed the guard and died after a full universal compile; the guard now materialises the contents. **Still QUEUED (post-merge design, owner-prioritized, not blockers):** per-app-pin awareness for the chip (a pinned provider whose key was deleted runs demo turns under a chip naming a real brain — the per-turn tag and ModelSelect "(key missing)" are the interim disclosure); chip disclosure of the F15 needs-confirm state; demo-callout + protect-offer latches not re-read on import/sync-pull/recover-fresh (shared pre-existing gap).
- **2026-08-26 — connection-wizard e2e journey 4 flake MEASURED: 2-in-6 on `main` (and identically on the task branch).** `popup.waitForURL(/oauth/callback)` races the callback popup's self-close — the log shows the URL was REACHED, then "Target page has been closed". Pre-existing; per the 2026-08-19 lessons amendment it was not patched blind from an unrelated branch. Likely fix shape: wait on the WIZARD's own "connected" state (the act's completion signal) instead of the popup's navigation, or catch the closed-page race as success when the callback URL was observed. Related to the 2026-08-10 "flaky Playwright DOM-detach race" entry — different symptom, same suite.

- **2026-08-26 — 🎉 THE FLIP HAPPENED. Both repos are PUBLIC** (TASK-20260825-flip-public-execute; `spec` first, then `snug`, at `main` @ `89b57e7`). Stage 8 ran the same hour and is **verified, not assumed**: private vulnerability reporting on both · **secret scanning + push protection enabled via `PATCH /repos/{r}` with `security_and_analysis`** (the runbook said Settings-UI-only; the API works once public) · `main-pr-only` ruleset on both · **all 13 must-be-zero scrub terms return 0** in the public code search (note: the search API rate-limits after ~10 queries — a 403 is NOT a pass, sleep and retry) · anonymous reachability confirmed for repo pages, `/security/policy`, `/issues`, `/security/advisories/new` · 0.4 purge probe re-run post-flip still fails · org-landing pre-launch note removed (`snugprotocol/.github@4a641c3`). **🔑 STILL OWED BEFORE ANY ANNOUNCEMENT:** (1) the two repo **social-preview uploads** — the control EXISTS now that the repos are public; until then every shared link unfurls with GitHub's generic fallback; (2) the **X/LinkedIn/Facebook re-scrape** of the site card (browser act, `runbooks/social-preview.md` §2); (3) **dismiss secret-scanning alert #1** — a FALSE POSITIVE: the synthetic `sk-Ab3d…` fixture at `packages/protocol/src/__tests__/security.test.ts:44` is the *input* proving the C1 guard detects OpenAI-shaped keys (gitleaks passes 401 commits clean; `.gitleaksignore` covers it). Left open deliberately so the dismissal carries the owner's name; (4) one **prefilled ADR-0052 feedback deep-link** end-to-end, which 404'd by design while private.

- **2026-08-26 — CI IS LIVE AND ENFORCING again (TASK-20260826-ci-restore, PRs #141 `c666e2b` + #142 `bcf0738`, ADR-0058 supersedes ADR-0041's operative half).** Billing was never fixed — **going public dissolved it** (public repos get unlimited standard-runner minutes), confirmed by dispatching BEFORE editing anything. `push`/`pull_request` triggers restored; `workspace` + `desktop-shell (macos-latest)` are **REQUIRED checks pinned to `integration_id: 15368`**, proven blocking by a probe PR (*"the base branch policy prohibits the merge"*). `gate:local` keeps its job as the pre-push superset; AC3 parity 14/14. **Open follow-ups:** (1) **Node 20 deprecation** — `actions/checkout@v4`, `actions/setup-node@v4` and `pnpm/action-setup@v4` all warn they are forced onto Node 24; non-blocking today, will break eventually, wants a v5 bump task. (2) **`CONTRIBUTING.md` may want a line** on what the two checks cover and that a maintainer still runs `gate:local` for the legs CI does not (Playwright, server smoke). (3) **Should `spec` get a minimal CI** so it can carry a required check too? It deliberately has none today — a required check on a repo with no workflows deadlocks it permanently. (4) `strict_required_status_checks_policy` is **false**, so a PR can merge without rebasing onto a newer `main`; correct for a solo maintainer, revisit if a second committer appears. (5) The **Windows leg is PARKED**, not merely failing — restore preconditions are in the `ci.yml` matrix comment and match ADR-0021 D8's post-1.0 list; **do not soften `keyReachable`** to make it green.

- **2026-08-26 — Public "How it works" page SHIPPED + the mobile nav bug FIXED (TASK-20260826-website-architecture-page, no ADR).** New marketing page at **`/architecture/`** (nav label **"How it works"**): six hand-authored inline-SVG figures — runtime agent bridge · the frozen envelope + swappable adapters · `connect-src 'none'` and the connected-fetch boundary · **Ledger vs. the aggregator model** · private/local-model mode · the portable `.snug` file — plus a "Six choices that make it work" section and a prominent **"What this doesn't do"** section quoting five threat-model residuals (model DOES see app data; R-5 Windows/WebView2; unrecoverable if both secrets are lost; auth broker unbuilt; packages pre-1.0). Written for a cold, skeptical public reader (Show HN and similar); every claim traces to a public in-repo source, and Fig 4 explicitly concedes that **SimpleFIN is itself an aggregator** — the claim is "no intermediary that *accumulates*", never "no intermediary". Styling is token-only (no hex literals, **no font host** — the built `dist/` still reaches only snugprotocol.org, github.com, json-schema.org and the playground subdomain). **The mobile nav bug is fixed:** the header hid Docs/Spec/Playground below 560px with *nothing in their place* — they were unreachable on a phone for the site's whole public life. Now a zero-JS `<details>` disclosure carrying every destination, breakpoint **560px → 860px** (an iPad portrait and a phone in landscape did not fit six items), single-homed as a custom property with complementary `min-width:860px` / `max-width:859.98px` rules. **Header `Download` button REMOVED** (owner call) — the landing page keeps three other download paths and the footer keeps one everywhere; `/docs/*`, `/privacy/`, `/terms/` now have no above-footer download affordance, which is accepted. Verified in a real browser at 375/560/768/850/**859/860**/1024/1280px: exactly one nav at every width, **zero horizontal scroll everywhere**, disclosure operable by click and keyboard with a visible focus ring. Website suite **520 green**; root `pnpm test` exit 0. **Open follow-ups:** (1) the page inherits the site-wide OG card (the 16:9 teaser poster) rather than a purpose-made one — same deliberate call as the 2026-08-25 entry's item (3), and it is the URL most likely to be posted publicly, so revisit if the unfurl looks weak. (2) A JS overlay menu (focus trap, scroll lock) was scoped OUT in favour of `<details>`; revisit only if a real-device walk finds the disclosure thin. (3) **`AC11 was withdrawn`** — see the entry immediately below.
- **2026-08-26 — `cookie` in `apps/website` STAYS (re-confirmed the hard way).** This task planned to drop it as an unused dependency and **removed it mid-session before catching the 2026-08-24 decision above and reverting**. Recording the mechanism so the third attempt does not happen: **Astro 7 itself declares `cookie: ^2.0.1` and imports `parseCookie` from it**; nothing in `apps/website/src` imports it directly, which is exactly what makes it *look* dead to a grep. The declaration is a **shim that wins Node resolution against a stray hoisted `cookie@0.5.0`** (the `~/node_modules` failure in `lessons.md`). A green build proves nothing here — the machine that had the stray tree had it deleted on 2026-08-24, so the local build is green either way, while another machine or a CI runner is not. **Do not remove it; a `depcheck`-style tool will keep flagging it.**
- **2026-08-25 — Social cards MERGED (TASK-20260825-social-preview-og, PR #136 squash `dc3802d`, no ADR) + the org profile banner (`snugprotocol/.github@0450950`). Two 🔑 items remain, and one of them REORDERS THE FLIP CHECKLIST.** The site shipped `og:image` as a **root-relative** path and had done so in production: Open Graph requires an absolute URL and every real scraper *drops* a relative one rather than resolving it, so every share of `snugprotocol.org` previewed imageless. Fixed sitewide across all 26 built pages (4 marketing via `SocialMeta.astro`, 22 Starlight via a `Head.astro` override that adds **only** `og:image` — Starlight already generates every other OG tag, so emitting the full set there would have duplicated meta on all 22). **Open follow-ups:** (1) 🔑 **The two repo social-preview uploads must happen AFTER the flip, and before any announcement.** GitHub renders the Settings → General → Social preview control **only on public repos** (its doc: *"…or to a private repository to which you have previously uploaded an image"*) — both repos are private and have never had one, so the control is absent; there is no API either ([community #172072](https://github.com/orgs/community/discussions/172072) is an open, unanswered request). Files are ready at `docs/assets/social/{snug,spec}-repo-preview.png`; steps in [`runbooks/social-preview.md`](runbooks/social-preview.md). Until uploaded, a shared repo link unfurls with GitHub's auto-generated fallback card (`opengraph.githubassets.com` returns 200), so the failure mode is *generic branding*, not a blank card. (2) ✅ **DEPLOYED 2026-08-25T22:28Z — the production card is FIXED** (owner's explicit ask; `node scripts/deploy-web.mjs website --deploy` from clean `main` @ `df2781b`; deployment `571e909d`, 26 files uploaded, 57 unchanged). **Verified live on the apex, not the pages.dev URL:** `og:image` is `https://snugprotocol.org/videos/poster-landscape.jpg` on BOTH shells; that URL returns **200 / 91,786 bytes / image/jpeg** (a well-formed URL pointing at a 404 is the same blank card, so this half matters); `twitter:card=summary_large_image` present; 9 pages sampled across both shells all absolute, 0 relative. ADR-0013 posture re-confirmed post-deploy: **0 `cdn-cgi` hits**. **Still owed: the re-scrape**, which is a browser act — X/LinkedIn/Facebook cache per URL and will keep showing the imageless card until their inspectors re-fetch (links in `runbooks/social-preview.md` §2). Do it before any announcement. The playground was NOT deployed — it needs no change and a deploy is its own ask. (3) The site's own card still uses the 16:9 teaser poster (1920×1080) rather than a purpose-made 1200×630 — deliberate (it is real, on-brand, and already ships) and revisitable. (4) ~~`apps/playground` has **no OG tags at all**~~ **DONE 2026-08-26 (PR #139 `dca9e4b`)** — full og/twitter set in `index.html`, absolute `og:image` reusing the marketing origin's poster frame (not copied — that would fork one asset), pinned by a 7-test `socialMeta.test.ts` over `dist/` and mutation-proven. (5) `hub-talk-build-run.png` is now unreferenced in `snugprotocol/.github` — deleting it is an owner call, harmless at 66 KB.
- **2026-08-25 — org 2FA: enable it (owner did, this session); the SECOND-OWNER decision is still open.** Measured before the change: `two_factor_requirement_enabled: false`, exactly one member who is also the sole admin, zero outside collaborators, and the `2fa_disabled` member filter empty — so enforcement removed nobody and broke no token (2FA requirements do not affect PATs, deploy keys, or GitHub Apps). **The residual risk is custody, not configuration:** with enforced 2FA and a single owner, a lost 2FA device is an unrecoverable organization. A second owner is the actual mitigation; recovery codes stored somewhere reachable are the minimum. Same family as the updater-key custody item — both are single-point-of-failure secrets held by one person.
- **2026-08-24 — Dependabot triage DONE (TASK-20260824-dependabot-triage, ADR-0056): all 36 alerts dispositioned — 32 fixed by upgrade, 4 dismissed with recorded evidence.** Fixed: vite 5.4.21→6.4.3 (desktop, playground), vitest 2.1.9→3.2.7 (root, website), nanoid pinned ≥3.3.18 via `pnpm.overrides` (it comes from **postcss**, not vitest), astro 5.18.2→7.2.5 + @astrojs/starlight 0.35→0.41.7 (absorbs the closed PR #121; also drops sharp 0.34.5). Dismissed: 3× react-router (`not_used` — declarative router only, no data router, no attacker-controlled `to`) and glib (`tolerable_risk` — Tauri gtk-0.18 pins it, Linux is not a shipped target). **Open follow-ups:** (1) **react-router 6→7 migration, reviewBy 2026-11-30** — the acceptance in `scripts/audit-allowlist.json` EXPIRES then and `pnpm audit:deps` reds the day after; GHSA-jjmj has no 6.x fix. (2) Re-check glib when Tauri moves to the gtk-0.20 stack (same date). (3) `pnpm audit:deps` is a NEW local gate (not in root `pnpm test` — the root gate stays offline-runnable); add it to the pre-flip runbook checks. (4) AC1's final confirmation (Security tab shows 0 open) can only be read AFTER this branch merges to `main`.
- **2026-08-24 — `apps/playground` now DECLARES `zod` (`^4`), and code-map test counts were stale (TASK-20260824-centralization-lint-false-positive).** The zod declaration was missing entirely: `src/agent/cards.ts` imports it for `chatCardSchema` (ADR-0031 §3 strict re-validation) and was resolving up into the stray `~/node_modules`; deleting that tree turned it into a `desktop#build` TS2307, because desktop compiles playground source via the vite alias. Declared `^4` to match `packages/protocol` + `apps/server`; the ambient version had been 3.25.76, so the 21 card tests were re-run against 4.4.3 (green — both `z.strictObject` and `z.ZodIssueCode.custom` exist in v4). **Worth a sweep some session:** this was found by accident, so other packages may carry undeclared imports satisfied only by hoisting — a check that every runtime import has a dep in its OWN package.json would be a cheap new gate. Separately, `docs/code-map.md` counts had drifted on THREE rows — `knowledge` 183→195 (already stale by 2 before this task), playground 1471→1639, db 416→419 — all corrected by `pnpm run update-code-map`, which measures a real run and rewrites the rows. The script exists and works; it simply is not part of any gate, so the counts rot between the sessions that remember to run it. **Cheap fix worth considering:** add a `--check` mode to it and hang it off root `pnpm test` beside the other `check-*` seats, so drift reds instead of accumulating.
- **2026-08-24 — the stray `~/node_modules/` is GONE (TASK-20260824-centralization-lint-false-positive).** 478 MB / 315 packages / 502 `.pnpm` entries, plus the orphaned `~/package.json` and its three lockfiles (yarn Feb 2025, npm Jan 2026, pnpm Feb 2026 — three package managers, no project in the home tree declaring those deps; all ten sibling JS projects carry their own `node_modules`). Deleted on the owner's call after a root `pnpm test` baseline; root `pnpm test` and `pnpm --filter website build` both exit 0 afterward. **The `cookie: ^2.0.1` dependency in `apps/website` STAYS** — an explicit dep on a directly-imported package is correct regardless of the shadowing, and it protects any other machine or CI runner with a stray hoisted tree; do not remove it as dead. Inventory in the task journal (git history).
- **2026-08-26 — 🎉 DESKTOP v0.1.2 RELEASED (TASK-20260826-helper-bundle-update-sheet; `release/v0.1.2`).** Published 2026-08-27T00:17Z: signed (Developer ID Application: Jitendra Maker, 2KC5X47563), notarized + **stapled**, universal `x86_64 arm64`, EULA carried; verified by **re-downloading the published DMG** (`stapler validate` ✔, `spctl` → `source=Notarized Developer ID`). `releases/latest/download/latest.json` serves **0.1.2** with both darwin keys → the one universal artifact and a 400-byte updater signature; the helper release stays a **pre-release** beneath it, exactly as ADR-0060 requires. **This is the first release that ships on-demand helper distribution to users.** `release-desktop.mjs` gained, in this session, three fixes and a one-command flow: credentials resolve themselves (updater key path, signing identity + team id from the keychain, Apple ID from git config) with the app-specific password PROMPTED and never stored; `--publish` creates the release; `--no-prompt` keeps the non-interactive path. **Still owed:** the **v0.1.1→v0.1.2 auto-update walk on real hardware** (the update path itself is unproven for this release — the shipped `latest.json` is verified, the in-app install+relaunch is not), plus the AC14 helper walk and an Intel walk below.
- **2026-08-26 — On-demand helper distribution SHIPPED (TASK-20260826-helper-bundle-update-sheet, ADR-0060; `helper-whatsapp-sidecar-v0.1.0` published as a pre-release). Owner walks owed:** (1) **AC14 — the public path on real hardware**: move `~/Snug/helpers/whatsapp-sidecar` aside, open Telepath from a *release* build, accept the download card, watch progress → "done", then pair a device end-to-end; (2) **an Intel walk** — the x86_64 archive is verified only under Rosetta (`bin/node --version` + a 2 s helper start); (3) **the v0.1.1→v0.1.2 shell update** (still needs the second-release walk from 2026-08-25) now also exercises `release-desktop.mjs`'s new "pinned helper tag is published" refusal. Residuals: no uninstall surface for `~/Snug/helpers` (~140 MB); the owner's own dev-stamped tree is exempt from skew checks by design (threat-model delta R-e). Rebuild the dev tree with `install:helper` (it now writes the `dev` stamp).
- **2026-08-25 — FIRST SIGNED RELEASE SHIPPED (TASK-20260824-first-signed-release, PR #134 squash `36fe45c`; ADR-0047 §7 amendment).** `v0.1.0` is published on `snugprotocol/snug` — signed with **Developer ID Application: Jitendra Maker (2KC5X47563)**, notarized + stapled (both the DMG and the `.app`, each its own submission), **universal `x86_64 arm64`**, five assets, verified byte-identical after re-download from the release. Gatekeeper paragraphs removed from `/download` + `download.astro`; threat-model **R-29 partially resolved** (the Gatekeeper half; the TOFU half explicitly REMAINS — notarization proves the binary came from this Developer ID, not that this Developer ID is Snug's). **Open follow-ups:** (1) ✅ **DONE 2026-08-26 — the update walk was owner-verified against the published v0.1.1.** Previously: needed a SECOND release (its own explicit ask) — check → chip → sheet → update now → restart, watching the single-instance lock race and whether the WhatsApp helper is actually reaped across the restart. (2) ✅ **DONE 2026-08-26 — v0.1.1 was its first true end-to-end run** (and surfaced a real guard bug, fixed in `1703674`). Previously: **`release-desktop.mjs` has never run start-to-finish**: this release's tail (DMG notarization, staple, tarball rebuild, staging) was completed against already-notarized artifacts rather than burning a third Apple submission. The script is correct by construction and unit-tested, but v0.1.1 is its first true end-to-end exercise. (3) **Updater key custody is now load-bearing**: `~/.tauri/snug-updater.key` (`F3922E7DDE0069B6`) has NO passphrase — the file IS the secret — and every client installing v0.1.0 trusts it. The owner's backup must leave `~/Desktop/` for a password manager / encrypted volume. Losing it orphans every installed client's update path with no escrow. (4) A published, independently-reachable key fingerprint would close R-29's remaining TOFU half; unscoped. (5) CI is `workflow_dispatch:`-only, so no automated check ran on PR #134 — the local root `pnpm test` (exit 0) was the gate.
- **2026-08-24 — Legal surfaces MERGED (TASK-20260823-legal-terms-privacy-eula, PR #125 squash `9d2f56a`, ADR-0055): `/terms` + `/privacy` (disclosure, no gate), the DMG EULA clickwrap, four consent bands. Open follow-ups:** (1) **Counsel review of `/terms` before flip-public** — the drafts are the agent's, written from the codebase's disclosed facts and the owner's spec (§1668 carve-out, four-item indemnity, no 18+ gate); the clauses are self-contained paragraphs in `apps/playground/src/legal/terms.ts` so any one can be swapped. (2) **DONE 2026-08-25** — first signed release shipped (see the 2026-08-25 entry); review F9's interaction verified, Gatekeeper paragraphs removed, R-29 partially resolved. (3) `hdiutil udifrez/udifderez` are Apple-deprecated (warning printed on macOS 26; still functional) — the release script's refusal names it; if a future macOS removes them, tauri-bundler's `--eula` path breaks with it and the clickwrap needs a new mechanism (e.g. a first-launch in-app screen — rejected today in ADR-0055 as duplicative, acceptable then). (4) **DONE 2026-08-25** — owner walked the DMG on real glass: Agree screen legible, plain double-click opens the installed app. (5) TRACKER D-02 (USPTO applicant = individual) is flagged for re-confirmation now that TechVoyage LLC is a known fact — an owner decision, not this task's.
- **2026-08-24 — 🎉 STAGE 0 CLEARED + STAGE 1 SCRUB DONE (TASK-20260824-flip-public-scrub, no ADR).** **The #1 flip blocker since 2026-08-19 is gone:** GitHub Support acted on ticket #4692616 and the stage-0.4 verification probe now **FAILS as required** — both `698028a…` and `809fb47…` return `not our ref` on fetch and `422 No commit found` from the commits API; `forks_count` 0. Re-verified twice that session. **Stage 1 executed:** codenames 34 → 0 across source/tests/ADRs (the *Source systems* paragraph deleted from all three root AI files, preserved privately; C4 rewritten to survive the folder's removal); 43 markdown references to the private strategy path rewritten, with the **10 C4-guard lines deliberately keeping the literal string**; breadcrumb sweep clean (machine paths 3 → 0, emails 0). **Stage 1.4 term list SPLIT** into must-be-zero (13 terms, returns **0**) and review-only (4 terms, never counted) — the single list returned 119 hits and was unreadable by design. `gitleaks` over all refs: **no leaks found** (376 commits). **Stage 1.1 done:** the private strategy tree moved out of the repo (verified byte-identical, 30/30 files; destination confirmed outside any git work tree and not a synced folder; exact path in the private launch notes). **Stage 0.5 done:** pre-scrub history bundled outside the repo and **verified restorable** (clone reaches `698028a` with both purged commits, 76 files) before the local branch was deleted. **New gate:** `scripts/check-public-scrub.mjs`, the eighth root `check-*` seat — codenames (plaintext, word-bounded, case-sensitive), ancestor identifiers (hashed, reusing the knowledge guard's set, now repo-wide for the first time), and references to the private strategy path. **Open follow-ups: (1) vocabulary drift — ~14 tracked hits still say "source system" where the new term is "ancestor system"** (`packages/auth`, `packages/protocol/src/auth-schema.ts:325`, `docs/lessons.md:147`, `docs/decisions/0028:9`); generic, not a leak, but the tree speaks two dialects — own pass. **(2) ✅ DECIDED 2026-08-24 (ADR-0057): the codenames stay in git history — ACCEPTED, flip with full history.** Measured that day: all 372 commits on `main` contain them (present since the bootstrap commit), so the tree-level scrub could never have removed them and ADR-0053 retains history at the flip. Accepted because the codenames are the *sanitised* form and name no real system; the REAL identifiers were never committed (`git log --all -S` empty for every term, the 12 long-form must-be-zero terms return 0, `git log --all -- <private path>` empty, gitleaks clean over 376 commits); and the knowledge-store guard still ships with its full seven-hash set (195/195). Rejected: flattening history (discards 120 merged PRs — a launch asset), and `filter-repo` (invalidates every SHA cited across those PRs and re-runs the stage-0 purge problem). **Live consequence: the public repo has NO automated codename tripwire** — the scrub tooling is gitignored (owner call: a tool built to hide a thing must not publish it), so run `node scripts/check-public-scrub.mjs` BY HAND before any flip or release. The path back, if the public tripwire is ever wanted, is hashing the codenames into `scrub-tokens.json` beside the existing seven. **(3)** The must-be-zero grep has **no automated seat** — it is a manual pre-flip step against a file that now lives outside the repo; candidate for a seat that takes the list path as an argument. **(4) At flip hour re-confirm `.claude/settings.local.json` is still gitignored** — it names a real ancestor-system directory in its paths (untracked today, verified by `git check-ignore`; a `git add -f` or `.gitignore` edit would expose it). **(5)** `apps/website/src/__tests__/navIntegrity.test.ts` throws rather than failing cleanly if `apps/website/src` or `scripts` goes missing, and is currently vacuously green — it is public-facing C4 enforcement after the flip.
- **2026-08-24 — `gate:local`'s workspace leg skips four root `pnpm test` seats** (found wiring `check-deploy-web`, TASK-20260823-web-deploy): the leg runs `turbo run test` + `check-threat-model` + `check-sandbox-guard` + `check-gate-local` — the ci.yml mirror — but root `pnpm test` also runs `check-release-desktop`, `check-website-sync`, `check-dev`, `check-deploy-web`. The gate's `--all` therefore claims ci.yml equivalence truthfully while NOT being root-`pnpm test` equivalence; a release-desktop or deploy-web regression merges past a green gate. Cheapest fix: add the four seats to the workspace leg (and to ci.yml so AC3's mapping stays exact); until then, cite root `pnpm test` exit 0 for scripts/ changes.
- **2026-08-24 — `@snugprotocol/db` crypto suites time out under full-workspace load (from TASK-20260823-web-deploy's gate runs; NOT touched by that branch).** Two consecutive `gate:local` workspace legs went red on `Test timed out in 5000ms` in key-derivation-heavy tests (run 1: 1 test, `encrypted-open`; run 2: 4 tests across `encrypted-sync`/`legacy-adoption`/`encrypted-open`/`second-device`) — a DIFFERENT set each time, while `pnpm --filter @snugprotocol/db test` in isolation ran 3/3 green (419/419). The 2026-08-22 lesson applies: state the rate over ≥6 loaded runs BEFORE changing anything; likely fix is a per-suite `testTimeout` (the siblings already take 1–2 s each at idle) rather than a global one. Until measured, a db-only timeout red in a loaded gate run needs the isolated rerun to be believed.
- **2026-08-23 — LAUNCH-READINESS PASS DONE (TASK-20260823-launch-readiness, ADR-0053): repo hardened for the flip; the triaged flip checklist lives in that task file + the private flip-public runbook (refreshed same day).** Decided: **full git history + all 120 PRs/issues are RETAINED at flip** (ADR-0053), gated on the stage-0 purge; **CI billing is NOT a flip blocker** (owner call — flip on local evidence, restore billing post-launch; supersedes ADR-0041's "hard flip-public blocker" line below). Done that day: branch cleanup (origin = `main` only; 41 squash-merged/stale branches deleted, SHA ledger in the task file; instagram-starter withdrawn from origin per owner — LOCAL-only until post-1.0, bundle backup beside the repo); full-history gitleaks over all refs clean (54 findings all verified-FP, pinned in committed `.gitleaksignore`); PR/issue-corpus scan clean (110 bodies + comments); repo/org/spec metadata + topics + Dependabot + `delete_branch_on_merge` set; runbook stage 4 rewritten for the ADR-0041 world (flip-hour ruleset, 0-approval require-PR; no required check until billing returns). **Still owed before flip, in binding order: (1) stage-0 GitHub-Support purge ticket (probe re-verified still FETCHABLE 2026-08-23 — file it first, days of turnaround); (2) the private strategy tree off-tree move + 22-file reference sweep; ~~(3) website/playground deploy~~ **DONE 2026-08-24 (TASK-20260824-web-deploy-execute): both sites are LIVE at `snugprotocol.org` / `playground.snugprotocol.org`; zone injectors OFF (Email Obfuscation was found `on` and fixed). Remaining slivers, both blocked only on two API-token permissions: the `www`→apex 301 rule and the `pages.dev` Bulk Redirect — see the 2026-08-24 entry below. Preview Access policy deferred, so `--preview` is off-limits until it exists;** (4) security@/hello@ round-trip (Cloudflare click pending). Before HN: first GitHub Release (own ask), the owner walk ladder, the never-run cold-clone AC3 timed test, landing-copy pass.**

- **2026-08-22 — Feedback channel SHIPPED as GitHub deep-links (TASK-20260822-feedback-loop, ADR-0052); two standing notes.** (1) The prefilled issue/Discussions links 404 for outsiders while the repo is private — the `/download` designed-quiet state; they go live at flip-public with no code change (verify one prefill end-to-end then). (2) **The hosted anonymous receiver was REJECTED for launch and parked to 1.1 ("Alive & listening"), evidence-gated**: revisit only if real non-GitHub reporters are demonstrably being lost; the full Worker+D1 design lives in ADR-0052's first draft (git history, commit `d9240e0`). Sign-in is now flag-gated (`VITE_SNUG_HUB_AUTH=1` to re-enable; runbook updated with the /invoke no-authz caveat). **Queued from the Gate-5 review (applied findings are in the branch; these are the deliberately-deferred remainder):** (a) migrate the three legacy external-link call sites (`ui/WebsiteLink.tsx`, `desktop/DesktopWelcome.tsx`, `run/OpenUrlConfirmDialog.tsx`) off `oauth.openExternal` onto the new side-effect-free `platform.openExternalUrl` seat — the OAuth opener BINDS a pending flow's loopback listener as a side effect, which is why the feedback path got the new seat; (b) extract a shared `useDismissableMenu` hook when a THIRD Escape/outside-click popover appears (IdentityChip + FeedbackMenu are the two copies); (c) migrate `agent/connectionInferrerAdapter.ts`'s tripwire list onto `security/credentialShapes.ts` (the single-homed shape list llmInspector + feedback now share); (d) render-twin fixtures for the feedback mount source-scans (App header / RunView need store scaffolding once). **2026-08-23 riders, same branch:** header icon pass (⚙️/💬/`about ↗`, mobile feedback restored); run-view either/or band widened to ≤1000px (owner found the split on real hardware — the walk item paid off; breakpoint lockstep-pinned); website external links all new-tab (SocialIcons override + rehype-external-links; dist-swept to zero). Owner re-check on a real phone in landscape still worthwhile; push + PR awaits the owner's ask.

- **2026-08-22 — GitHub front doors SHIPPED (TASK-20260822-github-readmes-org-landing): one flip-day edit owed.** Both repo READMEs rewritten (visual-first, final one-minute teaser cuts everywhere incl. the website renditions), and the org landing page exists: `snugprotocol/.github` is **public** (owner-approved) with `profile/README.md`; local clone at `../.github-org`. **At flip-public: remove that profile's "pre-launch note" paragraph** (repo links currently say the repos open at launch) — also recorded in the private launch-ops notes' registration state.
- **2026-08-22 — WhatsApp auth-store corruption FIXED (TASK-20260822-wa-authstate-corruption, PR #114); ONE owner walk owed.** The ~10-minute `parking after 2 attempts` loop was a *corrupt* app-state key file, not a missing one — Baileys' `useMultiFileAuthState` writes in place and swallows parse errors, so a torn write reads as absence forever (and the archive/pin/mute/read lane silently never applied). Replaced by the sidecar's own `createFileAuthState`. **Owner walk (AC6):** restart the desktop app so the live helper respawns on the new code, then watch the terminal across ≥2 resync cycles (~20 min) for `synced regular to vN` and the ABSENCE of new parking lines; the benign `identity key changed` chatter should also be gone (info is now floored at warn). The live corrupt file was already hand-healed (backup kept beside it at `app-state-sync-key-AAAAAK7c.json.pre-heal-backup` — delete it once the walk passes). **Deferred follow-ups, none blocking:** (1) extract a shared `fs-safe.ts` (atomic write + quarantine) for `thread-cache.ts` + `auth-state.ts` — today they are two hand-rolled copies whose behaviors deliberately differ (never-throw cache vs fsync'd credentials), so reconcile before extracting; (2) consider `makeCacheableSignalKeyStore` in front of the store if per-message sync I/O ever measures as a stall (the stock store was uncached too — no regression, just an available win); (3) the desktop `should_autostart` predicate could learn to surface a `creds.json.corrupt-*` sibling as a "needs attention" state rather than a silent nothing-linked. **Accepted risk, recorded:** Baileys fault payloads can carry contact jids/message keys into the LOCAL terminal — a strict reduction versus the stock logger, and local stderr is not an R-9 egress surface; revisit only if terminal logs ever leave the machine.
- **2026-08-22 — ~~PRE-EXISTING e2e reds on `main`~~ FIXED same day (TASK-20260822-mobile-e2e-reds).** Six specs failing on `main` since #100 (found by the first `gate:local --all` since): the 375px header overflow fixed by nav compaction inside the existing ≤760px block (no wrap — `.run-layout` derives from the 61px single-row header), and the two AC8/AC9 badge assertions migrated to #100's deliberate short `desktop` tag PLUS a new `title` pin on the why-copy. Full e2e leg 75 passed/1 skipped; playground unit 1482/1482; 375/1440 screenshots reviewed.
- **2026-08-24 — WEBSITE + PLAYGROUND ARE DEPLOYED AND LIVE (TASK-20260824-web-deploy-execute; ADR-0054/0048).** `https://snugprotocol.org` and `https://playground.snugprotocol.org` serve the build from `main@231f982` (Pages projects `snug-website` / `snug-playground`; deployments `7ac34266…` / `6e7594b3…`, both Production/branch `main`). Done: 🔑 project creation, 🔑 custom domains + their CNAMEs, 🔑 first production `all --deploy`, zone script-injectors OFF. **A REAL ADR-0013 violation was found and fixed in passing: zone Email Address Obfuscation was `on`** (it injects `/cdn-cgi/` and rewrites `security@`); all six injectors now read off from the API (`email_obfuscation`, `rocket_loader`, `fonts`, `mirage`, `polish` = off; `bot_fight_mode` = false). Verified live: both hosts 200, `cdn-cgi` 0/0, playground SPA fallback 200, `/docs/spec/` + whitepaper PDF 200, local-mode tripwire 0. **STILL OPEN, both blocked ONLY on two API-token permissions** (token `921ab154…` needs Zone → **Config Rules: Edit** and Account → **Account Rulesets: Edit**): **(a) the `www` → apex 301 Redirect Rule** — the proxied `AAAA www 100::` placeholder EXISTS, the rule does not, so `www.snugprotocol.org` currently does not redirect; **(b) the `*.pages.dev` Bulk Redirect** — sources are **`snug-website-c7z.pages.dev`** (NOT `snug-website.pages.dev` — Cloudflare suffixed it) and `snug-playground.pages.dev`, so both `pages.dev` hosts presently serve a duplicate origin (a playground on a second origin = a second per-origin `.snug` store). **Then: revoke that API token** — routine deploys need only the wrangler OAuth session. **The preview Access policy is DEFERRED, not done (owner call): `--preview` MUST NOT be run until it exists** — pre-flip, an open preview URL publishes the launch site. `/download` links 404 by design until the first GitHub Release (ADR-0047). Landing-copy owner pass before HN still owed. Local E2E unchanged (`dev:local` + `stage-local-desktop`; the staged `public/local-artifacts/` must be deleted before a production deploy — the script refuses it).

- **2026-08-21 — LAUNCH-READINESS VERDICT: PASS WITH CONDITIONS** (TASK-20260821-launch-security-review; full verdict + what was NOT checked in that task file, and in `docs/threat-model.md` §9). C1 and C2 were re-attacked across seven fresh-context lanes and **neither is false**; storage custody held under direct probing including cross-user read/write attempts. Four defects were fixed rather than described — one a real C1 credential leak. **Verification performed:** `gate:local --all` **FULL PASS 6/6** (its own verdict: "equivalent to ci.yml on macOS"), plus `pnpm --filter desktop gate` **GREEN in a real WKWebView** — both new IPC rows executed and passed there, so R-12's closure is proven behaviourally rather than only unit-pinned. **The conditions, in the order they bind:** (1) **CI billing** — already a hard flip blocker, and it means every green above is the owner's machine, not an independent runner; (2) **no Windows verification**, so R-5's only detector is dark (the macOS-only decision stands and is now stated in the build, README, SECURITY.md and threat model); (3) the reference server's missing authorization needs at minimum the SSO-runbook caveat before anyone self-hosts; (4) owner hardware walks remain owed. **Also still open for the flip:** runbook stage 0 (purging pre-scrub objects from the GitHub remote — owner-destructive, its probe must FAIL before flipping) and the the private strategy tree reference scrub (36 files measured; only the compiled prompt-corpus subset was fixed).

- **2026-08-21 — From TASK-20260821-launch-security-review (threat model v3). Findings triaged as NOT launch-blocking, each with evidence.** Four blocking items were fixed in that branch (the OAuth encoded-spelling credential leak, the confirm dialog's missing URL, R-12's per-command IPC row, and a plaintext-codename leak in a knowledge guard test). These remain open, in rough value order:
  1. **`apps/server` has no authorization on `/invoke` and `/artifacts`, even with `SNUG_AUTH=google` on.** Enabling login authenticates `/auth/*` and `/userdb` only; artifacts and threads are one global namespace, so an anonymous caller can list and read every artifact, read another user's thread history into their own turn, and spend the operator's API key. Probed and confirmed. **Bounded by ADR-0013 — the shipped hub is static with zero backend, so nothing we deploy is exposed** — which is why this is not a launch blocker. But `docs/runbooks/enable-google-sso.md` carries no caveat, and a self-hoster following it would reasonably conclude login gates the server. Cheapest correct fix is the runbook caveat; the real fix is a session gate on `/invoke` plus per-user scoping of artifacts and threads.
  2. **An untrusted `.snug` import replaces the whole database; only connections and runtime contracts are reconciled.** `snug_secrets`, `snug_apps`, `snug_app_versions.html` and `snug_sync` are adopted verbatim, with no confirmation gate at `importUserFile`. Worst case: the victim's credentials are destroyed, attacker HTML is installed as apps, and the sync origin is repointed. Requires the user to open a hostile file, and connection-grant demotion does hold. Related to the already-queued R-20.
  3. **Three IPv6-embedding forms bypass the SSRF literal guard** — IPv4-compatible (`::7f00:1`), NAT64 (`64:ff9b::/96`) and 6to4 (`2002::/16`) are not in `net-guards.ts`'s range list. Measured. **Not reachable by an app alone**: gate 5 sits below the approval gates, so the literal must already be in a user-approved frozen ceiling, and only a *bracketed* exotic literal matches. Either add the three ranges or narrow the threat-model row, which currently says "IPv6 forms" unqualified.
  4. **The LAN transport's timeout is 15 s while the executor's is 60 s**, under a Rust comment claiming the two are kept in step, and a test named `the_timeout_matches_the_executors_own_budget` pins the stale value. Fails safe, but the Rust-side abort fires first so the executor's self-naming timeout message never runs — the user gets the raw transport spelling instead. Same "a bound must name itself when it fires" lesson, on the leg that did not inherit it.
  5. ~~**`healMissingTables` false-misses on every healthy file.** `USERDB_TABLES` still lists `snug_auth_specs`, which v5 dropped and `USERDB_DDL` does not create, so `missing` is never empty and `healed` is always true — `markDirty()` fires on every open, which for a protected file means a full container re-seal (up to 64 MiB) per open. The guard can no longer distinguish a damaged file from a healthy one. Not data loss.~~ **FIXED `a40302e` (#103, 2026-08-22)** — the expected set is now derived from `USERDB_DDL`, never `USERDB_TABLES`, with a fail-loud check that every DDL statement parses as a CREATE (`userdb.ts` `healMissingTables`). Struck 2026-08-26 (TASK-20260826-backlog-hygiene-stale-findings) — it had stayed un-struck for four days and read as an open defect.
  6. ~~**The `SNUGENC1` container layout in the spec is not the layout the code writes.** Spec §11.1 documents 13 bytes per slot; `container.ts` uses `SLOT_LEN = 61` with the wrapped keys stored separately. Since the whole header is the GCM AAD, an independent implementation written from the spec computes a different AAD span and **cannot open reference files** — which undercuts the portability rule the section is normative for. Internally self-consistent, so round-trips work today. Fix the spec draft (preferred) or the code.~~ **FIXED `a40302e` (#103, 2026-08-22) — in the spec, before 1.0 was published.** §11.1 now documents the 61-byte stride (kind + IV + 48 reserved MUST-be-zero bytes), the 160-byte two-slot header, and *why* the stride is load-bearing for the AAD; the published `snugprotocol/spec` `SPEC.md` carries it and its revision note records the correction. The 13-byte layout never appeared in a normative document. Struck 2026-08-26 (same task as item 5).
  7. **Smaller, each measured:** the pseudonymisation digit floor is documented as 7 but is structurally 8 (`PHONE_PATTERN` needs 8 chars), so a bare 7-digit local number passes; NBSP/zero-width whitespace inside a contact name defeats the identity matcher (an *accidental* occurrence class, so it dents the anti-naive claim rather than only the anti-adversarial one); identity harvest is coupled to HTTP 200 and to the `/chats` route shape, neither documented as a dependency; `RUNTIME_CONTRACT_MAX_BYTES` counts UTF-16 code units, not bytes, so a CJK contract can exceed the stated 2560; and two presentational MUST NOTs in the spec (§12.6, §14.1) bind UI copy and are unenforceable by any test, though Part VI implies every conformance item is test-backed.

- **2026-08-21 — From TASK-20260821-hardening-polish (MERGED, PR #94 `31350c7`; ADR-0047; threat model v2.0). Owner verifications owed:** (1) **The desktop update walk, end to end, on real hardware** — the one thing no suite can perform: build a v0.1.0 bundle, install it, publish a v0.1.1 (its own explicit ask), and watch check → chip → sheet → update now → **restart**. Two specific things to look at: whether the relaunched instance wins the single-instance lock cleanly (a race here surfaces as "I clicked update and the app just quit" — threat-model R-g/R-30 family), and whether the WhatsApp helper is actually reaped across the restart (the reap ORDERING is spy-pinned, the real restart is not). Pre-flip, testing against the private repo needs the dev overlay: `pnpm --filter desktop exec tauri dev --config updater-dev.json` against a local static stub serving `latest.json` + the artifact + `.sig`. (2) **The `/download` page and the Settings "app" section on the desktop shell** — browser-verified only. (3) **The mobile either/or view on a real phone** — Playwright measures geometry at 375px, which is not the same as a thumb on glass; the default-to-app-view choice is the thing to feel. (4) **One installed starter taking a docs-only update** — install a starter, bump nothing, take the offered v2, and confirm the wiki pages appear and your own edits to a page survive. (5) **DONE 2026-08-25** — the Apple Developer ID landed and the first `gh release create` shipped v0.1.0.


- **2026-08-21 — From TASK-20260821-ui-polish (ADR-0046; merged on a PARTIAL gate — workspace+smoke green, e2e/rust/desktop/release deselected at the owner's pick, though cargo (100), desktop vitest (141) and whatsapp-sidecar (158) ran green in-session):** (1) **Owner hardware walk of the Telepath deep delete** — the phone-side unlink is untestable by construction: with a REAL linked session, delete Telepath from the hub and confirm the phone's linked-devices list drops the companion, `~/Snug/whatsapp-session/` is gone, and a reinstall demands a fresh QR. (2) **Owner eyeball of the redesigned Settings on the desktop shell** — the browser walk covered web at three widths (screenshots in the task journal); the WKWebView rendering has only suite coverage. (3) **Keyless-pin residual (recorded in ADR-0046 §3):** an app pinned to a provider whose key was later deleted routes keyless (the adapter silently hands back the mock) — the selector marks "(key missing)", but consider a run-surface hint if the demo-answer confusion shows up in use. (4) The wizard requirement inferrer still has no app id (2026-08-18 entry below stands — `decideWire(appId)` threads it only where callers have one).

- **2026-08-20 — `.snug` + opt-in passphrase encryption SHIPPED (PR #89 merged, TASK-20260820-snug-file-and-encryption, ADR-0042 + ADR-0043; spec pushed as `snugprotocol/spec@8ea69b8`). One thing remains owed.** **An owner manual walk on the DESKTOP shell.** This task's browser walk covered the web path end to end (offer → passphrase → Recovery Key → `SNUGENC1` on disk → relaunch unlock → wrong-passphrase message → Settings turn-off → plaintext again), and all six `gate:local` legs are green — but the desktop shell's own path has only unit + gate coverage: the `'file'` backend under `~/Snug`, the Tauri save dialog naming `snug-user.snug`, and a `.snug` double-click arriving at a **locked** hub (which now refuses with a message rather than hanging). Worth doing by hand because three defects in this task were isolated tests passing over a broken composition. **Standing caveat for anyone testing:** protection is opt-in and reversible from Settings, but a file whose passphrase AND Recovery Key are both lost is unrecoverable by design (threat-model R-27 — renumbered from R-14 in the v2 pass, which fixed a duplicate id).

- **2026-08-20 — Threat-model v1 follow-ups (TASK-20260820-threat-model-v1's adversarial audit; each is its own task, deliberately out of that documentation task's scope).** All are stated as residuals in [`docs/threat-model.md`](threat-model.md) rather than hidden, so none is a silent gap — but each is a real reduction available. In rough value order — item (1), enforcing macOS-only in the BUILD, is **DONE 2026-08-20** (TASK-20260820-desktop-bundle-targets-macos: `bundle.targets` is `["app","dmg"]`, `icon.ico` deleted, pinned by `bundleTargets.test.ts`; threat-model R-5b rewritten to the narrower remaining claim). Item "move WhatsApp pseudonymisation into the host" is **DONE 2026-08-20** (TASK-20260820-host-pseudonymisation: host harvests identities at the `sidecarAppFetch` seat into a persisted directory — revoke-wiped — and redacts them + jid/phone primitives from the whole app-message envelope on BOTH transports and from sidecar-class provider-lane results; fails closed; R-9 rewritten to the anti-naive claim with disclosed residuals — numeric smuggling, obfuscation, data-lane replay stay open by class). The rest stand: (2) **A per-command gate row for `sidecar_wizard_fetch`**, which fronts the token-releasing `GET /pair/status` while its lower-privilege sibling has one (R-12). (3) **Fence the classifier's replayed history lines** — they render as bare list items outside the fence the rest of the pipeline uses, and the defang covers one tag spelling (R-8). (4) **Shrink R-11's cadence gap** once CI billing is fixed: the 14 real-browser CSP checks never run in CI on the WEB path (no `e2e` turbo task — they run in WKWebView/WebView2 but not Chromium, which is what the hosted Playground ships to), and `apps/server`'s CSP-header assertion lives in `smoke.ts`, which CI never invokes. (5) Tests for `export_user_bytes` (currently zero); widen the anti-knob/`process.env` lint beyond `auth`+`runner`; unify the two hand-rolled percent-decoders in the shell, which today have OPPOSITE failure modes on one IPC surface (`sidecar.rs` fails closed, `exportfile.rs` copies through).

- **2026-08-22 — Spec 1.0 PUBLISHED (`snugprotocol/spec@3ac7700`, pushed 16:08 UTC on the owner's ask; TASK-20260822-spec-10-final, ADR-0050).** The v0.3 RC promoted to `docs/spec-drafts/SPEC-1.0.md` (normative; §17 provisional); spec-repo SPEC.md IS the 1.0 document (old draft filenames → pointer stubs); whitepaper edition 3 published; website spec/docs pages regenerated. Verified by fresh clone (schemas byte-identical, PDF SHA match). Branch merged as `a40302e` (#103), task retired (#104). **Still open from this task, owner acts:** (1) website DEPLOYMENT (entry below); (2) the pre-existing e2e reds entry above before HN. (The staged spec-clone correction `2132692` was PUSHED 2026-08-22 17:27 UTC on the owner's ask — changelog entry records it.) This resolves the 2026-08-20 follow-up "(3) the 1.0 promotion decision" — PR #90 merged as `c16c061`.

- **2026-08-20 — DECIDED: the merge gate is `pnpm run gate:local`, not GitHub Actions (ADR-0041, TASK-20260820-local-ci-gate).** Actions stays billing-blocked by owner choice; `.github/workflows/ci.yml` is now `workflow_dispatch`-only so it stops emitting misleading red X's. Two facts settled the shape: CI **never actually blocked a merge** (private repo on a free org plan — branch protection and rulesets both answer `403 Upgrade to GitHub Pro`), and a billing-blocked run is *worse* than no run because it looks like an ordinary test failure. The local gate has six selectable legs (`workspace`+`smoke` default on, ~2 min; `e2e`/`rust`/`desktop`/`release` opt-in; `--all` ~15–20 min), is a deliberate SUPERSET of ci.yml (adds Playwright, which CI never ran, and the server smoke leg carrying the byte-exact `RUNNER_CSP` assertion — threat-model R-11), and a test fails if any CI step loses its local counterpart. ~~**Still open, and it is a hard flip-public blocker:** an outside contributor's PR cannot be gated by a command that only runs on the owner's Mac — restore billing before flipping public, then re-run recent PRs to confirm the suites pass in CI.~~ **SUPERSEDED 2026-08-23 (owner call, TASK-20260823-launch-readiness): NOT a flip blocker — flip on local evidence; restore billing + re-run workflows post-launch; until then outside-contributor PRs are gated by maintainer-run `gate:local` on the fetched branch.** **Also still open:** no post-2026-08-18 merge has been machine-verified on Windows at all, and the Windows leg staying red was the only detector for an R-5 regression (ADR-0021 D8 accepts this through 1.0; the gate's `--all` verdict names it out loud).

- **2026-08-18 — starters-connect DEGRADED rows: three pre-existing reds surfaced by their FIRST-ever run (TASK-20260818-ledger-starter). QUARANTINED 2026-08-20** (TASK-20260820-local-ci-gate, AC9) so the e2e suite could be adopted as a merge gate — `test.fail()`, NOT `test.skip()`: the rows still run and report, and the suite FAILS if one starts passing, so this quarantine must shrink deliberately rather than rot. Four rows: the github/spotify/weather content pins + the read-only row. The underlying defect is unchanged and still owed: each app keys its pre-connect surface on a connected-fetch probe that the READ-ONLY starter route never answers (netHandler is deliberately absent there), so the pinned copy may never render on that route. Needs per-app cold-boot investigation (fix the pin, or the app's probe-less boot state); the ledger row and both hue tile rows pass.
- **2026-08-18 — Owner manual test to ADD (once TASK-20260818-ledger-starter merges): Ledger end-to-end with a real SimpleFIN token.** Install Ledger from the shelf → sample banner + planted money-leaks render → wizard: create/log into SimpleFIN Bridge, paste a REAL setup token → claim + verify → first sync replaces every sample row → real accounts/charts render → one ask-lane answer cites real rows → mark a real subscription cancelled and watch the verified flip after the next cycle (long-tail check).

- **2026-08-19 — Owner walk owed: sample mode on the four connected starters (TASK-20260819-starter-sample-data, PR #76).** The four sample surfaces are desktop-only/env-gated, so NO test covers how they actually look — and the Gate-5 review already found four state-dependent defects of exactly the kind a walk surfaces (a sample surface appearing in a state its author never pictured). Walk each unconnected: **Moodboard** (five named lit rooms + "Movie Night" mood, and confirm a paired-bridge FAILURE still shows the skeleton+overlay, not the sample home), **Rewind** (sample portrait with the banner; a real journaled portrait must still outrank it), **Trade Copilot** (sample portfolio + the completed-TWAP centrepiece; confirm a previously-connected install during an outage does NOT fall back to sample — that is the `connected_once` flag's whole job), **Telepath** (sample chats ONLY when never-linked/helper-down; with real chats present a helper blip must never swap a real open thread for the sample one). Then re-run the 3 pending Telepath linking-sync walk items on a build containing this change. Note the installed-starter caveat, SOFTENED 2026-08-21 (TASK-20260820-starter-updates, ADR-0045): installed copies can now take the sample-mode rebuild through the header's update button (bump the affected starters' `starter.json` when shipping it), and the vouch follows the newest factory pin so an updated copy keeps guided setup.

- **2026-08-20 — DECIDED: the desktop shell ships macOS-only through alpha, beta and 1.0; Windows desktop is reconsidered post-1.0 (ADR-0021 D8 addendum).** Windows desktop is structurally broken — wry's WebView2 backend discards `for_main_frame_only`, so `__TAURI_INTERNALS__` (carrying the invoke key as a plaintext literal) is present inside `sandbox="allow-scripts"` app iframes; no off-switch exists at the wry/tauri/WebView2 layer. macOS unaffected (40/40); no Windows build ever distributed; the CI gate must STAY RED — do not soften `keyReachable`. Root cause: `docs/solutions/2026-08-13-webview2-subframe-ipc-injection.md`; threat-model R-5 is a known defect, and the platform statement is now public in `docs/threat-model.md` + SECURITY.md. **Build enforcement DONE 2026-08-20** (TASK-20260820-desktop-bundle-targets-macos): `bundle.targets` is `["app","dmg"]`, `icons/icon.ico` is deleted, and `bundleTargets.test.ts` pins config + tree + icon generator. Note the exact strength — the shipped config REQUESTS macOS targets only; an explicit `--bundles nsis` still overrides it, so this is not a build-level refusal (threat-model R-5b carries the precise wording). **Still OPEN, and the reason this line survives the decision:** the only detector for an actual R-5 regression is the CI Windows leg staying red for the RIGHT reason, which the billing block above has made unreadable.

- **2026-08-13 — Owner hardware verifications (no test covers these; RE-POINTED at the TASK-20260815 re-curated shelf):** Coinbase re-credential + portfolio still owed — procedure per ADR-0030: open the wizard on the Coinbase connection (drift migration stages the Ed25519 shape), create an **Ed25519** key in the CDP portal (the default — NOT ECDSA), paste key name + base64 secret (or PEM), run "test this connection" (expect 2xx), then confirm the owner's installed Trade Copilot portfolio renders live — and additionally install the NEW `trade-copilot` starter and confirm its baseline + Smart-order (TWAP) surfaces against the same connection (each slice must raise the confirm dialog). Hue: install the NEW `hue` starter (Moodboard) on desktop — expect real rooms, first apply prompts once naming the bridge IP, lights change (supersedes the old hue-lights-party retest; pairing itself hardware-CONFIRMED 2026-08-14, ADR-0025). Spotify: install the NEW `spotify` starter (Rewind) — sign-in was hardware-VERIFIED 2026-08-15; confirm the portrait renders from the 7-scope pin and playback control raises the confirm. NEW: `weather` (OpenWeather free key end-to-end) and `github` (PAT or OAuth app) starters each need one live connect+render pass — folds the 2026-08-08 real-provider gap below for those two providers. ~~**NEW 2026-08-17 — `whatsapp` (WhatsApp Twin)**~~ — **DONE 2026-08-17, owner-verified on hardware**: macOS shell gate GREEN (incl. `ipc-sidecar-fetch-refused` in a real WKWebView), QR pairing end to end, thread analysed, manual reply delivered. **SUPERSEDED and RE-OWED 2026-08-17 by TASK-20260817-telepath** (the Twin was rebuilt as **Telepath**): a fresh walk is owed on the new surface — open Telepath (helper auto-starts from the mount read) → chat list renders with avatars, previews and badges → open a thread (text + inline photos; tap one for full size) → receive a live message with the app open (it appears, the list re-sorts, the badge moves) → tap ✨ for a draft in your voice → send it (the host confirm fires) → tap 🧠 for the first analysis (full history, real names in the output) → send one more message → tap 🧠 again (delta-only re-analysis, headline leads with what changed) → Charts tab draws → the app's wiki shows vision/requirements/plan/lessons + build-prompt (ADR-0035). Eight integration defects were found and fixed in that pass (`docs/solutions/2026-08-17-eight-seam-defects-in-one-feature.md`). Standing caveats for anyone repeating it: linking an automation tool to a personal WhatsApp account violates WhatsApp's terms and accounts have been banned; `node` 20+ must be on the GUI PATH (not just the shell's); and the helper must be installed with `pnpm --filter whatsapp-sidecar install:helper` — packaging is deliberately out of scope.
- **2026-08-15 — Wizard/runtime error-surfacing queue (from the TASK-20260815-coinbase-ed25519 plan review; deliberately out of that task's scope):** (1) RunView's app-runtime banner misdescribes a template-RENDER fault as a missing connection — `NET_AUTH_FAILED` thrown before any network call reaches the generic connect-CTA copy (`RunView.tsx:598-631`), so the honest fix-naming message from `cdp_jwt`/`ed25519-key.ts` is only ever seen probe-time; classify render faults distinctly. (2) The wizard-open drift heal swallows migration throws entirely (`ConnectionWizardSheet.tsx` `migrateConnectionRegistryDrift(...).catch(() => undefined)`) — a failed heal renders the row as stored with no probe button and no notice; surface it as a non-blocking wizard notice. (3) A passing probe is component-local state only — reopening the wizard on a verified connection shows "credentials saved" again (Gate-5 review, PLAUSIBLE): owner decides between persisting a `probeVerifiedAt` connection-state fact (ADR-0025 `lanVerifiedAt` symmetry, cleared on re-credential) or auto-running the probe on done-screen open. (4) `onReapprove={() => void reapproveFromDiff()}` discards `{ok:false,message}` — with ADR-0030 §5's pre-promotion deletion a failed promotion is silent while the staged diff waits; render the failure on the diff screen. (5) Refactor candidate: move dropped-field secret deletion INTO `db.reapproveConnection` so every future promotion path inherits the invariant instead of remembering it (today's two call sites are covered; the wizard owns the only field-set-changing one).
- **2026-08-18 — The connection requirement inferrer has no app id (from TASK-20260817-per-app-model-selector, ADR-0036 residual).** `liveInferenceAdapter` now takes an optional `appId` and the two call sites that have one pass it (the intent classifier, runtime-contract synthesis). `RunConnectionRequirementInferenceInput` carries `providerName`/`slot` but no app id — that inference runs at build time before an id is settled — so it resolves the Settings default, exactly as before. Not a defect; thread it if a per-app model is ever wanted for that turn, which means changing that input contract and its callers.

- **2026-08-18 — Telepath linking/sync follow-ups (from TASK-20260818-telepath-linking-sync's Gate-5 review; none blocking, all deliberately deferred).** (1) **A `sync` seat on the `/events` page** (spec-sync'd contract field) so the host header reads progress from the long-poll it already runs instead of pulling the full `/chats` body every 4 s for two numbers. (2) **One file-persistence primitive**: `thread-cache.ts` has atomic temp+rename + quarantine; `store.ts`'s token write has neither — extract the stronger one and have both use it. (3) **One epoch-loop scaffold**: `createSidecarLivePump` and `createSidecarSyncPoll` carry two copies of the epoch/supersession state machine. (4) `start_helper` holds the sidecar mutex through its 600 ms survival wait (pre-existing; autostart adds a launch-time window) — insert-then-verify without the lock held. (5) The shell's `should_autostart` borrows the helper's `whatsapp-session/creds.json` layout — replace with an explicit shell↔helper marker contract when the store format changes (SQLite successor, ADR-0037). (6) **Second-app token mint**: `/pair/status` releases the token once per link, so a second app linking to WhatsApp lands on "linked but no key" — decide the multi-app credential story. (7) A saved (contact-tier) name can never be demoted after the user deletes the address-book entry — decide whether a tombstone/expiry is wanted. (8) The v1 thread-cache snapshot is a synchronous whole-store JSON write per debounce during heavy sync — the SQLite successor retires this. (9) **Helper staleness bit on hardware (2026-08-18)**: the installed `~/Snug/helpers/whatsapp-sidecar` is a second deploy target — rebuilding the repo does not update it, and the owner's restart test ran a pre-task helper (empty store, "syncing 0%" forever). Stamp a version into the helper at install and have the shell's preflight (which already checks node) compare it against the expected version, naming the `install:helper` fix when they diverge.
- **2026-08-18 — Telepath hardware walk: mostly VERIFIED across six live rounds (owner + Claude, same day); two items remain.** Confirmed on hardware: first-click QR, fresh pairing with explicit-complete history sync (674 chats / 16.6k messages cached durably), helper auto-start + boot resume with cache intact across desktop restarts, two-phase header capsule (Syncing % → Names n/m) converging honestly, roster sweep with throttle cooldown + write-off accounting (162 loaded, 71 dead groups named as such in-app). REMAINING: (1) post-`6af7364` confirmation that recent DMs pick up names via the cross-spelling copy; (2) one 🧠 analysis spot-check that the pseudonymized payload uses a `~`-free label map; (3) **the ADR-0034 push seam has never been hardware-confirmed on Telepath** — new messages required manual refresh, and the new polling safety net (thread 10 s / list 25 s) now GUARANTEES the UX while also masking the doorbell; verify hint delivery (host pump → `snug:host-event` → app listener) before citing the push path as proven, e.g. by temporarily lengthening the poll beats and sending a live message.

- **2026-08-14 — Desktop icon has never been seen on a real dock/taskbar** (pixel-verified only; no bundle was built). Run `pnpm --filter desktop bundle` on real hardware and eyeball it before the icon is treated as done. **Narrowed 2026-08-19 by TASK-20260819-desktop-icon-squircle (PR #75)**: the three defects that pixel-sampling could see are FIXED (white corners, white niche, no squircle margin) and pinned per-platform (16 assertions, macOS gate GREEN) — what remains is exactly the part no test can reach: how it reads at dock size beside neighbour apps. A dark+light preview render was owner-approved; the real-bundle check is still owed.

- **2026-08-12 — App-reply remainder (AC1/AC3 + the delivery-loss conviction MERGED, PR #40; task closed 2026-08-15):** owner decides the parse-failure retry policy (leading candidate: one non-streaming `stream:false` fallback retry — no delta framing to lose) and the AC4 first-row-wins bare-array hazard (wrap / refuse / KB-teach); optionally a HAR/curl capture to name the losing hop. **Ops note:** subscription servers built before PR #40 omit `stopReason` on the SSE `done` event — truncations charge parse-failure strikes through them until redeployed.


- **2026-08-14 — Playground vitest flake — RETURNED and now FIXED 2026-08-22 (TASK-20260822-playground-flake).** It came back at **2 of 6** full runs on clean `main` (`d5baf0e`), blocking the merge gate's workspace leg. Per this entry's own instruction the failing run was captured FIRST, and the failure TEXT (not the count) named the cause — which was **not** file parallelism. Two tests synchronized on a fixed wall-clock wait instead of on the condition they assert: `sidecarLive.test.ts`'s backoff test slept 20 ms then asserted four recorded sleeps, and `starterInstall.test.tsx`'s `settleUntil` budgeted by attempt count (100 × 5 ms) whose interleaved `act()` calls shrank the real budget precisely under load. Both now wait for their own condition behind a wall-clock deadline that still fails a stuck subject. **The 2026-08-19 remedy was tried and measured ineffective** — `testTimeout` + `maxThreads` moved the rate not at all (2/6 → 2/8) — so both knobs were reverted rather than left looking like a fix; the vitest config carries that measurement as a comment. Result: **0 failures in 10 consecutive full runs.** Load only ever set the odds; the races were in the tests. The "deliberately no config change" instinct recorded below was right for the right reason. Historical context follows. ~~NOT REPRODUCIBLE as of 2026-08-20; downgraded from blocker to watch (TASK-20260820-local-ci-gate).~~ The 2026-08-19 classification (file-parallelism; a different 14–31 tests timing out per run at 5 s) did not reproduce on clean `main` at `a8bad77`: **8 consecutive full-parallel runs green at 1303/1303** (5 idle + 3 under 8 deliberately-spinning cores to force contention), plus one full `turbo run test --force` at 23/23. The suite is not the one that was measured — count moved 1254 → 1303 and four playground PRs landed in between (#76, #78, #80, #86). Three readings remain unseparated: one of those PRs incidentally fixed it; it is machine-state-dependent beyond an 8-run sample; or the original timeouts were themselves an artifact of a loaded machine. **Deliberately NO config change** (`testTimeout` / `maxThreads` untouched): an edit against an unreproducible symptom cannot be observed to work, and would permanently blunt the very signal that distinguishes a genuine red from contention. `--no-file-parallelism` stays the documented confound-remover. If it returns, capture the failing run FIRST.

- **2026-08-15 — Queued from TASK-20260815-spotify-scopes-wizard-links Gate-5 review (altitude findings, deliberately not expanded into the High-tier diff):** (1) export the ADR-0029 pinned-console-URL rule from `packages/auth` (the sheet hand-enumerates entry+option registrations; the next URL-bearing seat or second consuming surface forces a second spelling); (2) replace the drift migration's per-seat predicates (fieldSet/gainsSeats/scopes) with ONE structural compare of substituted-vs-stored plus a seat classification table (silently-promote / stage / re-consent) so a NEW pinned seat defaults to "staged and visible" instead of "undetected" — ADR-0029's consoleUrl-edit residue is the first known casualty; (3) move token invalidation into an auth-layer "invalidate minted consent artifacts for this slot" op (the wizard re-spells kind names + secret keys the auth package owns, and skips provider-side revoke); (4) a shared `ExternalLink`/platform opener so the RFC 8252 "webview never navigates to a provider" posture holds for the NEXT anchor by construction, not per-JSX vigilance; (5) the shared `memoryQuartet` test helper (7th copy added — fold into the queued shared-auth-test-harness item below).

- **2026-08-14 — Queued from TASK-20260814-hue-starter-real-connection (ADR-0026) Gate-5 review:** (1) requirement-derived flow classification (`credentials | pair | oauth`) consulted by the step machine and the sheet, replacing the five hand-placed `isLanRequirement` guards; (2) shared auth test harness — extract `packages/auth/src/__tests__/helpers.ts` (quartet/row scaffolding copied across ~10 files); (3) scope the failed-pairing revision bump (slot-scoped announcement instead of a full re-migration per redisplay); (4) delete the dead `LanPairScreen onPaired` prop before someone wires logic into it.
- **2026-08-14 — Queued from TASK-20260814-hue-pairing-e2e (ADR-0025) Gate-5 review:** (1) widen `withSession` to accept an async callback and collapse the five hand-rolled copies (two already lack try/catch; the missed post-await recheck class); (2) a shared revision-gated row hook (`useConnectionRow`) — the sheet gates on `loadedRevision === revision`, `ConnectionSlotsCard`/`AuthChoiceCard` subscribe with NO gate; (3) Settings pill verified-awareness for LAN rows (an owe-pairing row reads "connected"); (4) consider moving the verify probe + verified-marker semantics into `packages/auth` (not urgent while the playground is the only wizard host).

- **2026-08-13 — Small, from PR #42's CI round:** audit the remaining hand-typed gate-driver expectation lists (`EXPECTED_HARNESS_IDS_STATIC`, `EXPECTED_PERSIST_IDS`, `EXPECTED_JOURNEY_STEPS` in `apps/desktop/gate/run-gate.mjs`) — the same stale-twin shape that bit `IPC_CHECK_IDS`; only the IPC list was converted to derive.

- **2026-08-13 — Queued from TASK-20260812-desktop-auth-awareness close (P6):** (1) Windows gate leg for `lan_fetch` + `cdp_jwt` (rides the D8-blocked Windows story above; `cdp_jwt` needs native WebCrypto ECDSA); (2) Signify-CA pinning for Hue bridges (threat-delta R-3 pairing-window MITM residual; CA portal is login-gated, old bridges self-signed); (3) untrusted-import pin/secret overwrite — its own task (threat-delta R-4; generalizes to OAuth tokens/API secrets — one decision, not per-credential-type); (4) owner to confirm the web Hue tile staying fully locked (product judgment, not a security rule); (5) sweep env-gated e2e specs — `SNUG_E2E_HAS_APP` gates 8 spec files and an env-gated spec is a spec nobody runs (see the CI item below); (6) Apple Music's registry endpoints are INVENTED URLs that silently widen the derived ceiling — verify against Apple's docs or drop them (its pinned `oauth2_auth_code` kind also still owes the truthfulness pass from PR #37); (7) google / googledrive / slack registry entries are wizard-incomplete (no fields, no registration walkthrough) — their journeys dead-end (**gmail CLOSED 2026-08-19**, TASK-20260819-gmail-starter/ADR-0039: scopes, both credential fields and the console walkthrough pinned; the other three are unchanged and the same fix applies to each; **2026-08-22, ADR-0049:** the pickup should also decide each entry's WEB story — gmail now models the web seats (`webRedirectPosture`/`webRegistration`), and web REFUSAL semantics were deliberately deferred to this pickup: an absent web seat means "entry walkthrough serves web too", so an entry whose client type cannot register a web origin needs either web seats of its own or the refuse member designed then); (8) lint or refuse BOTH templates on one requirement (a `pathAndQuery`-signing header template beside a `queryTemplate` signs pre-injection and sends post-injection — fails closed but mystifying; no shipped entry does it yet).
- **2026-08-13 — Queued from the same task's P4:** the `html_mismatch` surface has no renderer — a user who edits an installed declaring starter loses guided connection setup SILENTLY (`starterDeclaration.ts` console.warn is the only signal). UI-only change; keep the "no manifest at all" case distinct (pinned by test).

- **2026-08-12 — Desktop follow-ups still open (from the scaffold):** (1) DNS pin-resolution for native fetch — resolve-then-connect-by-IP so a public hostname can't rebind private mid-flight; unlocks removing the `isForbiddenNetHost` browser-edition caveat; (2) device-flow transport (RFC 8628) — GitHub's oauth_app is postured `device-flow` and refuses today; (3) https-bridge page (CF Pages static; Slack-family + Coinbase/Apple-Music postures point at it) — re-check Coinbase's dashboard for 127.0.0.1 literal acceptance first (loopback would be cheaper); (4) export `STATE_TTL_MS` from oauth-service and drop `DESKTOP_FLOW_TTL_MS`'s pinned mirror; (5) installer/signing/notarization + download links — **LARGELY DONE 2026-08-21** (TASK-20260821-hardening-polish P5, ADR-0047): GitHub Releases hosting, `tauri-plugin-updater` with minisign artifact signing, the `/download` page, the offered-not-auto update UX, and `scripts/release-desktop.mjs`. **Still owed:** the Apple Developer ID itself (the signing path is wired and env-gated; until it lands builds are unsigned and the download page says so — threat-model R-29), and the FIRST actual `gh release create`, which needs its own explicit ask in that session (PROCESS.md release rules).

- **2026-08-11 — Lean-runtime/data-chat remainder:** (a) **subscription-mode server twins** — the gap family now spans `schema_apply`/`app_doc_write` (2026-08-03), observability parity (2026-08-04), `runtime_contract_write` + the data tools (2026-08-11), and `provider_request` + the provider lane (2026-08-15, TASK-20260815-provider-chat-lane); design ONE wire contract (no tool payloads, no credentials) rather than per-tool patches; (b) C1 scanner: `KNOWN_KEY_PREFIX` is `^`-anchored (`protocol/src/security.ts:32`) so a key embedded in prose is missed on EVERY envelope path — scanner-level change with its own false-positive budget; (c) widen `DbDriverResult` so the audit can record a true post-execution count; (d) drift detection compares counts, not rows — a count-preserving concurrent change passes; (e) optional pre-write confirm on classifier-routed `app_change` turns if the threat-model asymmetry proves uncomfortable in use.

- **2026-08-12 — Auth follow-ups (from PRs #37/#38):** admission kind-substitution decision (D6/AC10 split-brain — own task + ADR); borrower-declared OAuth `endpoints` still survive substitution when the matched option has none (flag-vs-replace at the next threat-model pass); consider a bounded, linted seat for model-proposed alternative labels; author auth-option variants for more providers as owners ask; owner-assist: read the real rows in the owner's browser to confirm the historical no-row diagnosis.

- **2026-08-08 — Real-provider verification gap (restated 2026-08-10 as still standing):** the auth-spectrum starters shipped verified against stubs through the real wizard/injection/scrub path — the path is real, the providers are not. Confirm live: OpenWeather + CoinGecko query-string auth, GitHub PAT scope behaviour. (Spotify's real PKCE round trip: hardware-VERIFIED 2026-08-15, TASK-20260815.) One free key per provider, run each connect flow by hand, record results in a task file.
- ~~**2026-08-05 — Alpha-umbrella exit AC3 never run:** cold `git clone` → working app in < 10 min, verified by a timed scripted run (no evidence it was ever executed).~~ **EXECUTED 2026-08-23 (TASK-20260823-launch-readiness) — and it caught a real cold-clone breaker:** `pnpm dev` built only the bare `server` package, whose tsc (and playground's vite) need every workspace dep's `dist/` — green for months only on trees with stale build output. Fixed in `scripts/dev.mjs` (`--filter server... --filter playground^...`, test-pinned). Post-fix timing, cold pnpm store, GitHub clone: **clone 98 s + install 41.5 s + boot-to-HTTP-200 17 s ≈ 2 m 37 s** — under the 10-min claim with 7 min to spare. Caveat: one warm-network machine, curl-level "up" — the flip-week re-run on a genuinely foreign machine (runbook stage-7 README box) still applies.
- ~~**2026-08-08 — E2e in CI**~~ — **CLOSED 2026-08-20** (TASK-20260820-local-ci-gate): `gate:local` carries an `e2e` leg, so Playwright is now part of the merge gate for the first time. Two notes it settled: `SNUG_E2E_HAS_APP` is NOT a manual env var — `playwright.config.ts` derives it from `appIsPresent()`, and all three files it checks exist, so the 10 "env-gated" specs DO run (the gate is a pre-integration vestige, not a live suppression). The gate still asserts that precondition, because a rename would silently convert 10 assertions into skips. The broader env-gate sweep item stands.
- **2026-08-14 — Extend browser-layout coverage** beyond `e2e/inspector-layout.spec.ts` to the other width-sensitive surfaces (run header action cluster, connection banners at narrow widths, builder `<details>` inspector) — jsdom's 0×0 rects made CSS assertions prove nothing about geometry.

- **2026-08-06/07 — Turbo cache correctness:** the `examples` KB≡SDK byte-sync suite can replay a stale green when `packages/sdk/embedded/**` changes — `turbo.json` still declares no `inputs` on any task, so the remedy needs authoring (the one-line fix described earlier does not exist). Force-run the examples suite on embedded-SDK edits until it lands.

- **2026-08-15 — Internal-doc ratifications (owner, 5 min):** the private roadmap still says v3-PROPOSED though S7/S8 merged 2026-08-12 (PR #35, ADR-0018/0019) — edit to plain v3. ~~LAUNCH_OPS registration boxes behind reality~~ **boxes corrected 2026-08-23** (GitHub org/repo rows checked; npm org + Cloudflare email verification remain genuinely pending). ~~The LAUNCH_OPS item-0 remote-object purge stays STAGED~~ **item-0 purge is now the #1 flip blocker with a live 2026-08-23 probe — see the launch-readiness entry at top.**

## Later / parked

- **AL-10 security-hardening + AL-11 threat model — HELD (owner's manual tests, 2026-08-08).** Queued candidates: tripwire URL-borne secret patterns (Slack webhook paths, `sig=`, `X-Amz-Signature` — pattern additions only); the app-authored credential-shaped query-param decision (nothing inspects app-authored query strings; some providers legitimately auth by query — state the boundary in the threat model either way); structured `NET_AUTH_FAILED` sub-code; fuzz corpus (poisoned-docs fixtures, directive schema, userdb import incl. the 2026-08-06 id-collision revival probe); bounds remainder (`kindHint` length cap, `z.url()` slots); whether strong doc evidence may override a given kindHint; keyed-subscription inference server twin (honest copy shipped as the interim); secrets-path review — the Settings BYOK field still echoes the saved key as a `value` attribute (prefer write-only "key saved — replace?"); re-vouch install-act declarations on every version write so a legitimate edit keeps guided setup (its tombstone dependency is now satisfied in v4); SDK announce-face drift (typed hooks vs embedded, asserted only via `toMatchObject`). AL-11 also owes the threat-model **v1** doc — only per-task deltas exist in `docs/security/`.
- **AL-12 spec staging — HELD (2026-08-05 gating decision):** v0.3 auth/net publishes as an explicitly-marked draft at 1.0 or 1.1, gated on a Beta-exit churn checkpoint; the local staged draft exists (connection surface only — net-frame/allowlist semantics not yet staged); nothing pushes to `snugprotocol/spec` without an explicit ask.
- **AL-15 landing page — HELD:** `apps/website` static landing + playground at `/app`. **Its stated dependency is now satisfied** (TASK-20260821, ADR-0047: the download links exist and the hub carries the `/download` page), so this is no longer blocked — it is simply not started. The hub remains the download surface until it is.
- **2026-08-12 — BYOK browser CORS advisory (recorded, not commissioned):** web-mode Coinbase-class providers can't be called from a browser; recommended ladder: local sidecar relay behind the `fetchImpl` seam → desktop native fetch (SHIPPED) → subscription server twin LAST (custody moves — own task + ADR). Refused: public CORS proxies, disabling browser security. **2026-08-17 update:** the first real sidecar precedent now exists (ADR-0032, `apps/whatsapp-sidecar` + `sidecar_fetch`), and it establishes the shape a relay should copy — a locally-spawned helper is reached by a PURPOSE-BUILT Rust command over a unix socket, never by widening the host ceiling (which is host-granular and cannot express a port, so `127.0.0.1` would grant every loopback service on the machine).

- **2026-08-18 — PR #68 carried another task's work under the wrong commit message (recorded, not fixable).** A hotfix branch cut off a `main` holding the owner's uncommitted `TASK-20260817-per-app-model-selector` work, plus a blanket `git add -A`, squashed 31 files and five commits' worth of that feature into a commit titled for a WhatsApp bug. **Verified intact**: `git diff origin/feat/TASK-20260817-per-app-model-selector origin/main` is empty for those paths, ADR-0036 + its architecture/glossary/done-INDEX entries are on `main`, and its 36 tests pass. Only the ATTRIBUTION is wrong, and correcting it would require force-pushing shared history — not worth it. Left here so the next reader of `git log` is not confused by a WhatsApp commit that adds `ModelSelect.tsx`. The branch `origin/feat/TASK-20260817-per-app-model-selector` can be deleted once the owner confirms.
- **2026-08-18 — `linkVerifiedAt` is written and never read.** `connectionWizard.ts:1146` stamps it into the connection KV and `credential-store.ts:75` types it, but nothing consumes it — so the ADR-0025 verify-before-claim marker for the linked-device family is currently a dead seat that LOOKS load-bearing. Either gate something on it (a `connected` row with no marker means nothing proved that link, which is the self-repair story ADR-0032's changelog entry describes) or delete it; leaving it is the shape that makes a future reader assume a guard exists. Found while diagnosing TASK-20260818-sidecar-shutdown.
- **2026-08-19 — Adopt the verified-connection refresh signal in the other sample-mode starters (from TASK-20260819-inbox-copilot-fixes).** The HOST half is app-agnostic: the wizard's Done screen offers "load my real data" for any app with a live frame and a verified connection, and `notifyAppRefresh` rings it. Only `gmail` listens today. trade-copilot, spotify, hue, whatsapp and ledger flip out of sample mode automatically on first successful fetch (trade-copilot's persisted `connected_once` is the strongest form), so they gain less — but a user who RE-connects or changes an account hits exactly the staleness this closes. Two lines each, and it should ride the `useHostEvents` hook above rather than adding five more hand-rolled copies.
- **2026-08-17 — An SDK `useHostEvents` hook (from TASK-20260817-telepath, ADR-0034). NOW DUE — the trigger fired 2026-08-19.** `connection-event` has a THIRD consumer as of TASK-20260819-inbox-copilot-fixes: Inbox Copilot hand-rolls the same listener Telepath does (shape-gated on `{v:1, type, event}`, slot-filtered, hint-driven so a stale frame costs one redundant governed refetch). This entry's own condition — "a third consumer should get a hook" — is met, and the copy-paste was accepted deliberately rather than by oversight: promoting it is a KB≡SDK sync change plus a byte-block bump across all 13 starters, which is a task of its own and not a rider on a defect fix. Do it before a fourth consumer appears; two hand-rolled copies are a pattern, three would be a convention nobody chose.
- **2026-08-17 — Sidecar hardening follow-ups (from the first end-to-end run; none blocking).** ~~(1) The C2 gate's `ipc-sidecar-fetch-refused` passed while the command was UNREGISTERED — every negative IPC check wants a positive twin.~~ **DONE 2026-08-17 (TASK-20260817-telepath):** `ipc-sidecar-fetch-dispatchable` ships and is GREEN in the macOS gate — it proves the main window dispatches `sidecar_fetch` and the command BODY answers (a body-level refusal such as "helper not running" passes; only the unregistered/uncapable shapes fail). The generalized rule is now in `lessons.md`. (2) The helper is spawned via the system `node`, so its runtime is whatever the GUI PATH finds; the preflight names a too-old one, but bundling a known-good runtime is the real fix and stays out of scope. (3) `install:helper` is a dev/owner step — packaging the helper into the app bundle is the same deferred question.

- **2026-08-17 — Arming surface for standing write approvals (deferred by owner from TASK-20260816-whatsapp-twin; ADR-0033 accepted with the gate built and this half open).** `createStandingApprovalGate` is implemented, tested and wired into the executor, but nothing can ARM it: an app lives in a sandboxed iframe and `FRAME_TYPES` has no seat for a standing approval. Options, with the tradeoff already scoped: (a) **a host-rendered arm control** on the connections/settings card — no protocol change, and a standing write approval granted on a surface the app cannot forge is a stronger guarantee than one the app renders itself (ADR-0033 §2 already wants armed state disclosed there); (b) **a new `snug:standing-request` frame** — C3 spec-sync, schema + runner routing + size class + review surface, but keeps the arm switch in the app where the user is already working. Also owed whichever lands: **persist the grant with the connection** (v1 is in-memory, so a reload disarms — safe direction, but not what §2 specifies). Until then WhatsApp Twin ships manual-Reply-only and says so on its own surface.
- **2026-08-06 — WebLLM 1.2-GA queue:** model picker (a DIFFERENT picker from ADR-0036's — web-llm prebuilt ids are their own namespace, and the per-app selector deliberately renders nothing under the webllm brain) + cold-start/download UX + cache management · artifact-style card instead of raw fenced HTML in the reply bubble · context-fit truncation rule before webllm chats attach to big apps · `context_window_size` vs KV-memory tradeoff · promote the adapter into `packages/adapters` at GA · engine-load progress in the builder status line · real-model e2e stays skip-by-default.
- **2026-08-03 — Offline runtime (portable-hub F3):** vendored/inline-runtime app template variant (srcdoc iframes can't be SW-cached) + offline Playwright gate (`context.setOffline(true)`).
- **2026-08-03 — Vision later phases:** OneDrive/Drive/S3 SyncProviders · CRDT/changeset multi-device merge · marketplace/spotlight curation.
- **2026-08-03 — Living-apps deferreds:** per-table dirty tracking in the materializer (write-amplification lever) · doc-history/versioned wiki if pages start churning.
- **Small UI/copy queue:** 2026-08-05 — OpenAI "0% cached" renders where Anthropic renders nothing (decide if a reported zero is worth showing) · 2026-08-05 — a byte-budget-elided payload should say WHY it is missing · 2026-08-06 — builder chat renders raw markdown (dead `/artifacts/…` route in serverless byok) · 2026-08-06 — signed-out "this hub" sync says "hub rejected pull (401)" instead of "sign in first" · 2026-08-06 — wizard primary actions sit below the fold (sticky `Sheet` footer; the chat connect card has the same class) · 2026-08-06 — the directive's ```json fence renders as literal text below the connect card · 2026-08-06 — owner call: should starters ship a pre-seeded "how this app was made" bootstrap thread? · 2026-08-06 — expose kv hydration state from the SDK hook (pre-hydration writes still lose) + KB note steering early-write state to `useAppDB` · 2026-08-06 — add the explicit no-form rule to the KB authoring template (suite-enforced but untaught) · ~~2026-08-06 — dev-only: StrictMode double-mount kills the `?idea=` hub→build handoff on `pnpm dev`~~ **FIXED 2026-09-03 (TASK-20260903-build-thread-continuity, ADR-0062: the send lives in the thread session, and there is no unmount abort for StrictMode to fire).**
- **2026-08-06 — Contract drift, small:** `HostCapabilities` in `packages/sdk/src/types.ts` still lacks the `net` flag `hostReadySchema` carries; the knowledge `renderedStore()` test helper still omits `schema-apply`/`app-doc-write`.
- **2026-08-10 — Dynamic-auth v2 loose ends:** flaky Playwright DOM-detach race in `openWizardFromCard` (~1-in-5 full-suite; diagnose before it trains anyone to re-run reds) · align ADR-0017's residual-risk #2 wording to the threat-model delta's per-case table · ASCII-lookalike registry borrows stay accepted-with-disclosure — revisit only if an untrusted declaration channel opens.
- **2026-08-13 — Registry notes (revisit-if):** CoinGecko cannot get a useful `testRequest` (keyless public API answers 200 to anything; the Pro probe fails correct demo keys) — revisit if the demo host key-gates · prefer query-form credentials for CORS-reflecting providers (CoinGecko reflects any requested header).
- **2026-08-07 — `http request` doesn't retrieve the connected-API teaching** — left unpinned deliberately; revisit if a live sweep shows a builder actually stranded.
- **2026-07-31 — Bootstrap remnants (unverified):** npm org registration · Cloudflare email verification click. Launch sequencing lives in the private roadmap + the private launch-ops notes (flip stages owner-gated). Standing rule until flip: fixing a listed good-first-issue closes the tracker issue AND updates `docs/good-first-issues.md` in the same branch.
- **2026-07-31 — Eval harness for prompt changes** (phase 2 — prompts are eval-addressable by path per ADR-0004).
