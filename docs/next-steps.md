# Snug — Next Steps

Dated, ordered backlog. Append with a date; ✅ when shipped. Task files are the workspace; this is the queue. Full 6-week plan: `internal/01-extraction-launch-plan.md`.

---

## Now / next

- **2026-07-31 — Bootstrap** (this scaffold; ADR-0001..0003). ✅ scaffold committed; pending: GitHub org + repos + push, npm org registration, Cloudflare email verification click.
- ✅ 2026-07-31 — **Week 1–4 delivered in one umbrella (TASK-20260731-build-hub):** protocol v0.1 + runner (C2-gated) + knowledge store (ADR-0004) + sdk/db + server/adapters + playground/examples. Remaining from the original week-1 line: spec-repo push (needs explicit ask). Original items follow for traceability:
- **2026-07-31 — Week 1 (plan §5):** extract envelope protocol → `packages/protocol` · runner skeleton with C2 sandbox locked by tests · knowledge base port → `packages/knowledge` · draft SPEC.md v0.1 skeleton sync (first spec push — needs explicit ask).
- **Week 2:** `apps/server` (/invoke + artifact store) · `packages/adapters` (anthropic, openai, mock).
- **Week 3:** `packages/db` — sql.js + OPFS, `.sqlite` export/import · `useAppDB`.
- **Week 4:** Playground + examples (chess, flying-pig, habit-tracker).
- **Week 5:** polish, docs, demo videos, private beta (10–15 people).
- **Week 6:** launch (see `internal/LAUNCH_OPS.md` — landing page T-3, flip public T-2, Show HN).
- **v1.1 (+4 weeks):** `packages/auth` credential broker per `internal/03-audit-auth.md` build order.
- **2026-07-31 — Post-hub next:** eval harness for prompt changes (phase 2 — prompts are eval-addressable by path per ADR-0004) · demo videos + quickstart timing run (week 5) · spec v0.1 push to `snugprotocol/spec` (needs explicit ask) · auth broker v1.1 per `internal/03` build order.
- **2026-08-03 — Portable user-owned hub umbrella (TASK-20260803-portable-hub, in progress):** single per-user SQLite (ADR-0007) · serverless run + BYOK/local LLM (ADR-0008) · SyncProvider origins + Dropbox (ADR-0009) · Google OIDC on sample hub · spec v0.2 draft.
- **2026-08-03 — Queued from portable-hub review (F3):** true network-offline app runtime — vendored/inline-runtime app template variant (srcdoc iframes can't be SW-cached; CDN allowlist is the current runtime source) + offline Playwright gate (`context.setOffline(true)`).
- **2026-08-03 — Queued (later phases from vision):** desktop local hub example · OneDrive/Drive/S3 SyncProviders · CRDT/changeset multi-device merge · marketplace/spotlight curation.
- ✅ 2026-08-03 — **Living apps (TASK-20260803-living-apps, branch `feat/TASK-20260803-living-apps`, awaiting PR/merge):** per-app data as LLM-designed NATIVE tables + verbatim-DDL schema registry (ADR-0010, schema v2) · app-attached chat (context assembler, durable thread pin, pinned bootstrap turn) · factory-pinned versions + reset · per-app knowledge wiki + `schema_apply`/`app_doc_write` tools · marketplace install dedup (`install_source`) · SSO retrofit (identity chip, post-login return, logout→sync rebuild).
- **2026-08-03 — Queued from living-apps:** subscription-mode server twins for `schema_apply`/`app_doc_write` (today the new tools run in byok/local direct mode; subscription keeps artifact parity only) · per-table dirty tracking in the materializer (write-amplification lever) · doc-history/versioned wiki if pages start churning.
- ✅ 2026-08-03 — **Hub ops (TASK-20260803-hub-ops):** the real long-build fix (iteration ceiling 6 → 48 — there was never a timeout) + partial-text preservation on dropped streams · 128K max output on both adapters, `max_completion_tokens` added to OpenAI, 8K cap on local · server 30-min lifetimes + `step` SSE progress events · build step timeline (replacing the last-write-wins activity pill) · **LLM round-trip inspector** (in-memory, ring-buffered, redacted — a sibling of the structural frame inspector, never an extension of it) · `deleteApp` cascade with tombstone (delete is terminal; pinned rows included) · LLM-optional app doctrine (ADR-0011) + pig game ported to the contract.
- **2026-08-04 — Queued from hub-ops review:** `importUserDb` clears `lastSavedHash` but **not** `namespaceByFile` — same cache-coherence family as the F1 resurrection bug, pre-existing and out of that task's scope. Worth closing before the next materializer change.
- ✅ 2026-08-04 — **Hub polish (TASK-20260804-hub-polish):** sign-out moved into a header identity menu + Google avatar (`picture` claim through the 5-place chain) · ember-niche logo, favicon and 2× wordmark · the `llm` and `inspector` rail tabs merged into one **think** surface with icon tabs (text kept as the accessible name) · build view finally renders its round trips · **the app-frame transport's round trips too** (there are TWO `runAgentTurn` call sites; only the builder's was wired, so a Chess move was invisible) · starters open read-only with an explicit Install in the run view · a built app's run view opens on the build conversation · a revision-less 412 reaches the divergence resolver instead of throwing.
- **2026-08-04 — Queued from hub-polish:** the `applyRemote()` empty-origin dead end is fixed, but the sync **resolver copy** still reads as a generic two-button choice — worth a pass to make each button state its consequence. Also: `docs/code-map.md` per-package test counts drift every task; a script that regenerates that column from a real run would stop the manual re-baselining.
- **2026-08-04 — Queued from hub-polish (TASK-20260804-hub-polish) Q1 decision:** subscription-mode parity for observability — server-side `app_doc_write`/`schema_apply` (so a subscription build can write its wiki at all) and a round-trip wire event that carries **no** tool payload and **no** credential. Deferred deliberately: `invoke.ts` today forwards tool NAME + phase only, and that boundary is not worth breaking without a designed contract. Until then the UI states plainly why those panels are empty in subscription mode.
