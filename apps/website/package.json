{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "dev:local": "PUBLIC_SITE_MODE=local astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "tsc -p tsconfig.test.json --noEmit && vitest run",
    "sync-docs": "node scripts/sync-spec.mjs",
    "stage-local-desktop": "node scripts/stage-local-desktop.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "@astrojs/starlight": "^0.35.0",
    "astro": "^7.1.1",
    "rehype-external-links": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
