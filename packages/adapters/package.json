{
  "name": "@snugprotocol/adapters",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "@snugprotocol/protocol": "workspace:*"
  },
  "devDependencies": {
    "@snugprotocol/runner": "workspace:*"
  }
}
