{
  "name": "@snugprotocol/knowledge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "gen:content": "node scripts/gen-content.mjs",
    "build": "node scripts/gen-content.mjs && tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "@snugprotocol/protocol": "workspace:*"
  }
}
