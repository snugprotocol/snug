{
  "name": "@snugprotocol/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && vitest run",
    "gen:schemas": "pnpm build && node scripts/export-schemas.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "zod": "^4"
  }
}
