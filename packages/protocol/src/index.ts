// @snugprotocol/protocol — the Snug envelope protocol: zod schemas, typed helpers,
// and the single home of every wire constant. Source of truth for snugprotocol/spec
// (see docs/engineering/SPEC_SYNC.md). Browser-safe; runtime dependency: zod only.

export {
  CDN_ALLOWLIST,
  ERROR_CODES,
  FRAME_TYPES,
  LIMITS,
  NET_ERROR_CODES,
  NET_METHODS,
  NET_MUTATING_METHODS,
  NET_RESPONSE_HEADER_WHITELIST,
  PROTOCOL_VERSION,
  SNUG_APP_REQUEST_TAG,
  STRIP_HEADERS,
  classifyErrorCode,
  isKnownErrorCode,
  isWhitelistedNetResponseHeader,
  type FrameType,
  type KnownErrorCode,
  type NetErrorCode,
  type NetMethod,
} from './constants.js';

export {
  APP_KV_TABLE,
  APP_OBJECT_NAME_RULE,
  APP_RESERVED_PREFIXES,
  APP_REST_PREFIX,
  APP_REST_SEPARATOR,
  STANDARD_APP_DOC_SLUGS,
  USERDB_DDL,
  USERDB_FILE,
  USERDB_INDEX_DDL,
  USERDB_LIMITS,
  USERDB_OPFS_DIR,
  USERDB_CONNECTIONS_TABLE,
  USERDB_SCHEMA_VERSION,
  USERDB_TABLES,
  appDataToken,
  appRestTableName,
  isValidAppObjectName,
  type AppSchemaJson,
  type AppSchemaObject,
  type UserDbTable,
} from './userdb-schema.js';

export {
  AUTH_KINDS,
  AUTH_SECRET_PREFIX,
  AUTH_SPEC_STATUS,
  AUTH_SPEC_STATUSES,
  apiKeySchema,
  authFieldSchema,
  authProviderInfoSchema,
  authRegistrationSchema,
  authRequestSchema,
  authSpecSchema,
  basicAuthSchema,
  bearerTokenSchema,
  deriveAuthAllowedHosts,
  getAuthUserLayer,
  hostSetEquals,
  isAuthSpecUnknownKeysOnlyFailure,
  isOrgEligibleAuthSpec,
  normalizeAuthHost,
  authSpecHintsSchema,
  oauth2AuthCodeSchema,
  oauth2ClientCredsSchema,
  resolveAuthCodeLayer,
  type ApiKeySpec,
  type AuthField,
  type AuthFieldType,
  type AuthKind,
  type AuthProviderInfo,
  type AuthRegistration,
  type AuthRequest,
  type AuthSpec,
  type AuthSpecHints,
  type AuthSpecStatus,
  type BasicAuthSpec,
  type BearerTokenSpec,
  type Oauth2AuthCodeSpec,
  type Oauth2ClientCredsSpec,
  type OrgEligibleAuthSpec,
} from './auth-schema.js';

export {
  appAnnounceSchema,
  appCancelSchema,
  appEventSchema,
  appMessageSchema,
  appResponseSchema,
  createResponder,
  dbRequestSchema,
  dbResponseSchema,
  frameWithinLimits,
  hostEventSchema,
  hostReadySchema,
  netRequestSchema,
  netResponseSchema,
  openUrlRequestSchema,
  openUrlResultSchema,
  parseFrame,
  respondTo,
  responseErrorSchema,
  type AppAnnounceFrame,
  type AppCancelFrame,
  type AppEventFrame,
  type AppMessageFrame,
  type AppResponseFrame,
  type DbRequestFrame,
  type DbResponseFrame,
  type Frame,
  type FrameParseResult,
  type HostEventFrame,
  type HostReadyFrame,
  type NetRequestFrame,
  type NetResponseFrame,
  type OpenUrlRequestFrame,
  type OpenUrlResultFrame,
  type Responder,
  type ResponseError,
} from './frames.js';

export {
  appRequestEnvelopeSchema,
  buildAppRequest,
  isAppRequest,
  parseAppRequest,
  type AppRequestEnvelope,
  type AppRequestParseResult,
  type EnvelopeInput,
} from './envelope.js';

export { parseAgentReply, type AgentReplyResult } from './reply.js';

export {
  AUTH_EVIDENCE_MAX_CHARS,
  AUTH_EVIDENCE_MAX_ITEMS,
  AUTH_PROVENANCES,
  AUTH_WIZARD_DIRECTIVE_KIND,
  CONNECTION_REQUIREMENT_DIRECTIVE_KIND,
  authWizardDirectiveSchema,
  connectionRequirementDirectiveSchema,
  inferrerProposalSchema,
  renderDirectiveSchema,
  type AuthProvenance,
  type AuthWizardDirective,
  type ConnectionRequirementDirective,
  type InferrerProposal,
  type RenderDirective,
} from './render-directive.js';

// Dynamic Auth v2 (TASK-20260810 P0) — the requirement contract. ADDITIVE: the v3
// `auth-schema.ts` surface above keeps shipping until P3/P4 rewire its last consumers.
export {
  AUTH_MAX_SLOTS_PER_APP,
  CONNECTION_FIELD_DESCRIPTION_MAX_CHARS,
  CONNECTION_FIELD_KEY_RULE,
  CONNECTION_FIELD_LABEL_MAX_CHARS,
  CONNECTION_FIELD_PLACEHOLDER_MAX_CHARS,
  CONNECTION_HEADER_NAME_RULE,
  CONNECTION_HOST_RULE,
  CONNECTION_KINDS,
  CONNECTION_LAN_HOST_CLASSES,
  CONNECTION_PROVENANCES,
  CONNECTION_QUERY_NAME_RULE,
  CONNECTION_REQUIREMENT_HEADER_VALUE_MAX_CHARS,
  CONNECTION_REQUIREMENT_INSTRUCTION_MAX_CHARS,
  CONNECTION_REQUIREMENT_MAX_FIELDS,
  CONNECTION_REQUIREMENT_MAX_HEADER_ENTRIES,
  CONNECTION_REQUIREMENT_MAX_INSTRUCTIONS,
  CONNECTION_REQUIREMENT_MAX_QUERY_ENTRIES,
  CONNECTION_REQUIREMENT_URL_MAX_CHARS,
  CONNECTION_SLOT_RULE,
  CONNECTION_STATUS,
  CONNECTION_STATUSES,
  CONNECTION_TEST_REQUEST_PATH_MAX_CHARS,
  connectionFieldSchema,
  connectionLanHostSchema,
  connectionProviderSchema,
  connectionRegistrationSchema,
  connectionRequestSchema,
  connectionRequirementSchema,
  connectionTestRequestSchema,
  canonicalRequirementHash,
  deriveConnectionAllowedHosts,
  isRfc1918Ipv4Literal,
  type ConnectionField,
  type ConnectionKind,
  type ConnectionLanHost,
  type ConnectionLanHostClass,
  type ConnectionProvenance,
  type ConnectionProvider,
  type ConnectionRegistration,
  type ConnectionRequest,
  type ConnectionRequirement,
  type ConnectionStatus,
  type ConnectionTestRequest,
} from './connection-requirement.js';

// Lean runtime turns (TASK-20260811 P0, ADR-0018) — the compact artifact a runtime app
// turn assembles FROM. Internal draft: host-assigned at both call sites, never app-claimed,
// and OUT of the json-schemas SOURCES until a spec push is authorized.
export {
  RUNTIME_CONTRACT_MAX_BYTES,
  RUNTIME_CONTRACT_MAX_SETTINGS,
  RUNTIME_CONTRACT_OVERVIEW_MAX_CHARS,
  RUNTIME_CONTRACT_PERSONA_MAX_CHARS,
  RUNTIME_CONTRACT_RESPONSE_GUIDANCE_MAX_CHARS,
  RUNTIME_CONTRACT_SETTING_KEY_RULE,
  RUNTIME_CONTRACT_SETTING_VALUE_MAX_CHARS,
  RUNTIME_CONTRACT_STATE_GUIDANCE_MAX_CHARS,
  RUNTIME_MAX_OUTPUT_TOKENS_CEILING,
  RUNTIME_MAX_OUTPUT_TOKENS_FLOOR,
  canonicalRuntimeContract,
  parseRuntimeContract,
  runtimeContractSchema,
  type RuntimeContract,
} from './runtime-contract.js';

// Intent-routed app chat (TASK-20260811 P0, ADR-0019; provider lane + exhaustive lane
// map TASK-20260815, ADR-0031) — the router's input contract.
export {
  CHAT_INTENTS,
  CHAT_INTENT_CLARIFICATION_MAX_CHARS,
  CHAT_INTENT_DATA_LANE,
  CHAT_INTENT_FEATURE_LANE,
  CHAT_INTENT_PROVIDER_LANE,
  CHAT_LANES,
  LANE_FOR_INTENT,
  chatIntentSchema,
  isDataIntent,
  isFeatureIntent,
  isProviderIntent,
  laneForIntent,
  parseChatIntent,
  type ChatIntent,
  type ChatIntentClassification,
  type ChatLane,
} from './chat-intent.js';

export {
  scanForCredentialValues,
  stripCredentialHeaders,
  type CredentialFinding,
  type CredentialScan,
} from './security.js';

export { buildJsonSchemas } from './json-schemas.js';
export { CONNECTION_URL_SCHEME, parseConnectionUrl, type ConnectionUrlParse } from './connection-url.js';

// The sidecar HTTP contract (ADR-0032, internal draft — not a published schema). One home
// for literals shared by the Node sidecar, the Rust admission, the wizard and the app.
export {
  APP_REACHABLE_SIDECAR_ROUTES,
  SIDECAR_AUTH_HEADER,
  SIDECAR_ROUTES,
  SIDECAR_SOCKET_BASENAME,
  SIDECAR_SYMBOLIC_HOST,
  isAppReachableSidecarRoute,
  type SidecarMethod,
  type SidecarRoute,
} from './sidecar-contract.js';
